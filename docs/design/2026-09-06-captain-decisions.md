# 캡틴 결정 10건 — 2026-09-06 (§1 8건) · 2026-09-07 (§5 2건)

> **재론하지 않는다.** 이 문서는 결정과 그 근거, 그리고 **결정이 부과하는 제약**을 소유한다.
> 질문의 출처는 `docs/design/2026-09-06-gap-investigation.md` §4이고, 태스크 상태의 정본은
> **Backlog.md**다 — 여기서 재서술하지 않는다.
>
> ⚠️ **내가 유도한 것과 캡틴이 말한 것을 갈라 적었다.** 캡틴이 답하지 않은 자리에서 내가 답을
> 유도한 곳은 **「내 유도」**로 표시했다 — 틀렸으면 그 줄만 뒤집으면 된다.

---

## §1. 결정 표

| # | 질문 | **캡틴 결정** |
|--:|---|---|
| 1 | 드릴 4턴을 지시문 문구로 강제할까, 코드로 강제할까 | **코드로 강제한다.** 드릴 횟수는 **설정값(config·env)에 따로 두고 읽는다** |
| 2 | 계획이 만든 질문 3~5개를 대화 상대에게 전달할까 | **전달한다** |
| 3 | 시나리오 내용을 대화에 전달할까 · 초점과 충돌하면 무엇이 이기나 | **대화에 전달하고, 대화를 기반으로 시나리오를 생성한다** |
| 4 | 주간 리포트의 주 경계 · 적재 방식 · 마이그레이션 번호 | **주 시작은 월요일 · 표로 적재한다** (번호는 「내 유도」) |
| 5 | 업무 시나리오 6종을 주제 9종에 더할까, 별도 계열로 둘까 | **주제 9종에 더한다** |
| 6 | 쉐도잉 오디오 저장 예외 · 재생 속도 · 반복 횟수 | **녹음 비교용 오디오를 저장한다. 비교된 오디오는 당일이 지나면 삭제한다.** 재생 속도 **0.5~2배**를 지정할 수 있게 **설정값으로 따로 관리**한다. 반복 횟수 **1~10회**를 지정할 수 있게 **설정값으로 저장**한다 |
| 7 | 007을 개발 DB에 적용할까 | ⏸ **아직 결정되지 않았다** — "007이 무엇인지 몰라". 더 쉬운 설명을 §4에 다시 썼다 |
| 8 | PUBLIC 저장소에 그대로 푸시할까 | **그대로 푸시한다** |

---

## §2. 결정이 부과하는 제약 — 착수 전에 읽는다

### 설정값은 새로 만들지 않는다 (결정 1·6)

세 값이 설정값으로 결정됐다: **드릴 턴 수 · 재생 속도 · 반복 횟수.**
⚠️ **새 설정 체계를 만들지 마라** — `app/backend/app/config.py`의 `Settings`가 이미
`BaseSettings`(env 파일 연동)이고 모델 이름·워커 스위치·음성 어댑터 같은 값을 담고 있다.
**같은 클래스에 필드를 더한다.** 기본값은 그 클래스에 두므로 예시 env 파일을 만들 필요가 없다
(`.env.example`은 지금도 없다).

**값역 검증을 코드로 강제한다** — 캡틴이 범위를 명시했으므로 그 범위를 지키는 것이 계약이다:
재생 속도 **0.5~2.0** · 반복 횟수 **1~10**. 범위 밖 값은 기동 시점에 거부한다(런타임에 조용히
잘리지 않게).

### "코드로 강제한다"의 뜻을 좁힌다 (결정 1)

⚠️ **자유 발화의 턴 수는 사후 관측만 가능하다** — 대화 모델이 몇 턴 말할지 코드가 미리 정할 수
없다. 그래서 "코드로 강제"를 이렇게 읽는다:
- **턴 수 자체를 코드가 센다**(지시문 문구에만 의존하지 않는다). 셀 자리는 이미 있다 —
  `audio_gateway/session.py`가 "agent가 말을 시작했다 = 사용자 턴이 닫혔다"로 턴 경계를 감지한다
  (지금은 분석 작업 배치용이다).
- **미달을 관측하면 드러낸다.** 조용히 통과시키지 않는다.
- ⚠️ **미달일 때 무엇을 할지**(세션 종료를 막을지, 기록만 남길지)는 상세설계에서 정한다 —
  종료를 막는 것은 학습자를 붙잡는 동작이라 가볍지 않다.

### 결정 2·3은 같은 자리를 건드린다 — 함께 설계한다

둘 다 **지시문 자료구조(`models/plan:SessionInstruction`)와 그 조립 경로**를 바꾼다. 따로
구현하면 두 번 고친다. **같은 태스크 묶음에서 설계한다**(TASK-6·TASK-25가 그 자리다).

### 결정 6의 "당일이 지나면 삭제" — 달력 날짜 규약이 걸린다

⚠️ **"당일"을 `current_date`로 구하지 않는다.** 이 프로젝트의 DB·서버 기본 시간대는 UTC이고,
UTC 자정~오전 9시(한국 시각) 구간에는 `current_date`가 한국 날짜보다 **하루 이르다**(실측된 함정).
삭제 경계는 **`users.timezone`을 기준으로 변환해** 구한다. 삭제가 하루 일찍 돌면 학습자가 아직
비교하지 못한 녹음이 사라진다 — 되돌릴 수 없다.

**그리고 이 결정은 R10-7의 예외다.** R10-7은 *"오디오 원본은 **기본** 저장하지 않는다"*이고
"기본"이므로 절대 금지가 아니었다 → 요구사항을 뒤집는 것이 아니라 **예외를 연 것**이다.
남는 것: 저장 위치 · 삭제를 실행하는 주체(워커인지 조회 시점인지) · 삭제 실패 시 재시도.

---

## §3. 내가 유도한 것 2건 — 틀렸으면 이 줄만 뒤집으면 된다

### 유도 1 — 주간 리포트 마이그레이션 번호는 **008**이다 (결정 4의 미답 부분)

캡틴이 번호를 말하지 않았다. **002를 재사용할 수 없다**는 것은 판단이 아니라 기술적 사실이다:
마이그레이션은 **파일명 정렬 순서로 적용**되고(`scripts/migrate.py`가 `sorted(glob("*.sql"))`을 돈다),
개발 DB에는 이미 `001·003·004·005·006`이 적용돼 있다. 지금 `002`를 만들면 **이미 적용된 003보다
앞으로 정렬**되어 새 DB와 기존 DB의 적용 순서가 갈라진다. → **다음 빈 번호인 008을 쓴다.**
⚠️ `001` 머리말의 "002로 미룬다"는 서술은 **낡았다** — 002는 만들어진 적이 없고 번호는 003으로
건너뛰었다. 그 서술을 근거로 002를 되살리지 마라.

### 유도 2 — 초점과 시나리오가 충돌하면 **초점이 이긴다** (결정 3의 미답 부분)

캡틴이 "대화를 기반으로 시나리오를 생성한다"고 답했다. **시나리오가 대화에서 나온다면 시나리오는
산출물이고 초점은 입력이다** — 입력이 산출물에 이긴다. 그래서 충돌 자체가 구조적으로 줄어들고,
남는 충돌(이미 시드된 정적 시나리오 대 오늘의 초점)에서는 **초점이 이긴다.**
근거: 초점은 학습자의 **실제 오류 이력**에서 나오고 시나리오는 그 연습을 담는 **배경**이다.
⚠️ **이것은 내 유도다.** 반대라면(시나리오가 이긴다) 오늘의 약점과 무관한 연습이 나올 수 있다.

---

## §4. 007이 무엇인가 — 더 쉽게 다시 (결정 7이 아직 열려 있다)

**한 문장**: 데이터베이스에 **표 두 개를 새로 만들고**, 기존 표 하나의 **허용 값 목록에 항목을
하나 더하는** 작업이다.

**왜 필요한가**: 지금 개발용 데이터베이스에는 **"다음 세션에 무엇을 연습할지"를 적어 둘 자리가
없다.** 그래서 실물 모델로 계획을 만들어도 **저장할 곳이 없어서 버려진다.** 캡틴이 결정한
"실물 모델로 계획 만들기"를 하려면 이 자리가 먼저 있어야 한다.

**무엇이 만들어지나** (셋)
1. **세션 계획 표** — 오늘의 추천 이유, 연습할 오류 패턴, 질문 3~5개, 목표 수준을 담는다.
2. **관찰 노트 표** — 모델이 남기는 학습자 관찰 기록.
3. **기존 작업 목록 표의 허용 값에 "계획 만들기"를 추가** — 지금은 "분석하기"만 허용한다.

**위험은 무엇인가**: 1·2번(표 신설)은 위험이 없다 — 없던 것이 생길 뿐이다.
**3번이 유일한 위험**이다. 이미 데이터가 들어 있는 표의 규칙을 바꾸는 것이고,
**되돌리는 스크립트가 준비돼 있지 않다.**

**그래서 이렇게 하려 한다** — 캡틴이 "그렇게 해"라고만 해 주면 된다:
1. 적용 **전에** 개발 데이터베이스를 **파일로 백업**한다(되돌릴 수단을 손에 쥔다).
2. 적용한다.
3. 적용 **후에** 표 2개가 실제로 생겼는지, 기존 데이터 행 수가 **하나도 줄지 않았는지** 직접
   조회해 확인한다.
4. 어긋나면 백업으로 되돌린다.

⚠️ **실물 데이터가 사라질 위험**: 현재 개발 데이터베이스에는 세션 7건·발화 92건 등 그동안 쌓인
기록이 있다. 백업을 먼저 뜨는 이유가 그것이다.

---

## §5. 추가 결정 3건 — 2026-09-07 (`TASK-6`·`TASK-25` 설계 착수 중)

> **재론하지 않는다.** 셋 다 **§1의 결정이 답하지 않고 남긴 자리**다 — 결정 1은 "센다"까지만
> 정하고 미달 처리를 상세설계로 이연했고, 결정 3은 "전달한다"만 답하고 초점 충돌을 열어 뒀다.
> 그 구멍들만 물었고, 이미 결정된 것은 다시 묻지 않았다.

| # | 질문 | **캡틴 결정** |
|--:|---|---|
| 9 | 시나리오와 계획의 초점이 충돌할 때 무엇이 이기나 (결정 3이 남긴 자리 · `TASK-25` AC#2) | **역할을 갈라 충돌 자체를 줄인다: 시나리오 = 무대(상황·역할) · 계획 = 목표(초점 패턴과 질문 3~5개).** 진짜로 공존할 수 없을 때만 — 시나리오 문구가 **패턴을 지정하는 경우** — **계획이 이긴다** |
| 10 | 드릴 4턴 미달을 코드가 관측했을 때 무엇을 하나 (결정 1이 이연한 자리 · `TASK-6`) | **기록하고 드러낸다. 종료를 막지 않는다.** 턴 수를 세어 세션에 남기고 결과 화면·로그에 보이게 하되 학습자를 붙잡지 않는다 |
| 11 | `learning_sessions.mode` 리터럴 고정을 이 묶음에서 함께 처리하나 (`TASK-25` AC#4) | **다루지 않는다. `TASK-27`이 소유한다.** 이번 설계서는 지시문 조립 경로만 다루고, `mode` 파라미터화는 첫 소비자가 생길 때 그 태스크가 한다 |

### 결정 11이 부과하는 제약

- **이번 설계서는 `mode`를 읽지도 쓰지도 않는다.** `_CREATE_SESSION_SQL`의 `'speaking'`
  리터럴(`app/backend/app/services/sessions.py:65`)을 **건드리지 않는다.**
- 근거는 **동작 변화가 0이라는 실측**이다: `mode`를 **읽는 코드가 0곳**이다(`select` 절 히트 0건,
  이 세션에서 직접 확인). 지금 인자로 열면 소비자 없는 인자가 먼저 생기고 쓰는 자리만 늘어난다.
- **새 태스크를 만들지 않는다** — `TASK-27` **AC#7**이 이미 *"값역은 이미 열려 있음을
  전제한다(`task_type`·`category`·`mode` 3종) — 스키마 값역을 새로 넓히지 않는다"*로 이 문제를
  자기 전제로 흡수했다. DB CHECK가 `speaking`·`shadowing`·`review` 3종을 이미 열어 두었으므로
  (직접 조회) **마이그레이션도 필요 없다.**
- ⚠️ 그래서 `TASK-25` AC#4의 답은 **"함께 처리하지 않는다 + 소유자는 `TASK-27`"**이고,
  이것으로 AC#4가 닫힌다. 판단을 요구한 AC였고 판단이 내려졌다.

### 결정 9가 부과하는 제약

- **`build_system_prompt`의 축 분석에 시나리오 축을 하나 더 얹는다** — 그 함수 docstring이 이미
  축마다 「계획이 대체한다/안 한다」를 근거와 함께 적어 두었고(힌트 시점·목표 수준은 대체 · 오늘의
  상황은 대체하지 않는다), **시나리오는 「오늘의 상황」과 같은 축이다.** 그래서 시나리오도
  **후보를 주는 쪽**이고 규칙 3의 순서를 덮지 않는다.
- **버리는 근거가 비대칭이라는 같은 논리다**: 계획은 학습자의 **실제 오류와 밀린 복습**에서 오고
  시나리오는 **수준으로만** 골린다. 시나리오가 초점을 덮으면 오늘 고쳐야 할 오류를 거른다 —
  반대 방향의 비용은 「화제가 밀린다」뿐이다. 되돌릴 수 없는 쪽이 한쪽에만 있다.
- ⚠️ **`scenario_id`가 null이면 지금 동작을 그대로 유지한다**(`TASK-25` AC#3). 시나리오 블록만
  빠지고 나머지는 같다.
- ⚠️ **지금 `learning_scenarios`는 3행·전부 A2·`prompt_template`이 31~35자다**(직접 조회).
  설계는 그 빈약함을 전제로 해야 한다 — 무대 문구가 짧아도 성립하는 형태여야 한다.

### 결정 10이 부과하는 제약

- **미달의 주어는 학습자가 아니라 대화 모델이다.** 4턴 시퀀스(질문 → 답변 → 후속 질문 → 다른
  표현으로 다시 답하기)를 만드는 것이 코치의 일이므로, 미달은 **모델이 지시를 안 지킨 것**이다.
  학습자를 막으면 엉뚱한 사람을 벌한다.
- **집계된 미달은 「지시문을 고치는 입력」으로 쓴다** — 그것이 이 관측의 목적이다.
- ⚠️ **화면 톤 계약과 충돌하지 않게 적는다**: 결과 화면은 「채점하지 않습니다. 시범합니다」가
  계약이다(`docs/storyboard.html`). 미달 표시가 **점수·정답률처럼 보이면 안 된다.**
- ⚠️ **런타임 개입(`agent_reprompt`)에 의지하지 않는다** — 그 경로는 미구현이고 사각의 크기를
  재는 것이 `TASK-24`다. 이 설계는 **관측과 기록만**으로 성립해야 한다.

---

_작성: 2026-09-06. 인용한 코드 심볼과 문서 절은 이 세션에서 직접 열어 확인했다._
_2026-09-07 §5 추가: 결정 9·10. `TASK-6`·`TASK-25` 설계 착수 중 §1이 남긴 두 구멍을 물어 닫았다._
_2026-09-07 결정 11 추가: `TASK-25` AC#4(`mode` 리터럴)를 `TASK-27` 소유로 넘겼다 — `mode`를 읽는_
_코드가 0곳이라 지금 여는 것은 동작 변화 0이고, `TASK-27` AC#7이 이미 값역 문제를 전제로 흡수했다._
