# HANDOFF — OhMyEnglish

> 최종 갱신 **2026-09-19 05시대 KST** · 세션 `ohmyenglish-46` · 브랜치 `design/first-vertical-slice`
> ⛔ **이전 판은 `handoff/backup/2026-09-19/` 에 있음**(`HANDOFF-0519.md` · `-prev-0918-1812.md`).
> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 이고, 근거 정본은 **태스크 노트 ·
> `docs/ops/pitfalls.md`(`H-CA`~`H-CH`) · 설계서 §6-1** 임.

## ⛔ 먼저 챙길 것 — 첫 코드블록보다 앞에 둠

1. ⛔ **이 세션은 개발 세션임.** 살아 있는 지시: 묻지 않고 권고대로 감 · 개발 뒤 테스트 필수 ·
   짧게 핵심만 보고 · **기능은 심플하게** · 주요 개발 뒤 `/simplify`. ⛔ **En-Coach 는 고치지 않음.**
   ⚠️ **사용자가 2026-09-19 에 「승인이 필요한 작업은 사전 승인」을 줬음** — 실물 Bedrock 회차를
   그 승인으로 돌렸음. 새 세션에서 그 승인이 이어지는지는 확인이 필요함.
2. ⛔ **마감 트리거는 「쓴 컨텍스트 70% 초과」와 「compact 경고」 둘뿐임** — 원장이 닫힌 것은 마감
   조건이 아님(정본 `rules/session-handoff.md` §5·8항). ⚠️ 사용자가 직접 마감을 지시하면 그것이 우선.
3. ⛔ **`skill`·`rules/**` 본문은 영어, 사람이 읽거나 리포에 남는 것은 한글**(대화·커밋 메시지·
   handoff·원장 노트·설계서·코드 주석). 경계는 `rules/korean-writing-standard.md` 머리말이 소유함.
4. ⛔ **Bedrock: 이 리포는 SigV4 가 필요함.** 셸에는 bearer 만 있고 SigV4 두 키는
   `app/backend/.env` 에만 있음(2026-09-19 재확인). 앱 경로는 `prepare_bedrock_credentials` 가
   덮지만 **직접 스크립트에는 `env -u AWS_BEARER_TOKEN_BEDROCK` 을 앞에 붙임.**
5. ⛔ **브라우저 검증은 `localhost:3000`**(`H-CA`) · **세션 화면을 `?mode=…` 로 열지 않음**(`H-CC`) ·
   ⛔ **낭독 판정 화면은 스텁 둘로 관측 불가**(`H-CG`).
6. ⛔ **워커를 「관측용으로 잠깐」 켜지 않음**(`H-CD`) · **세션을 만든 회차 뒤에는
   `teardown_session.py --session-id` 를 돌림**(`app/backend` 에서 돌려야 `.env` 가 읽힘).
   ⚠️ 전사만 하는 회차는 세션을 만들지 않아 걷을 것이 없음(2026-09-19 실측).
7. ⛔ **결정 124 는 반증됐음**(따옴표 인용을 프롬프트에 다시 넣지 말 것) · ⛔ **자막을 얻으려 하지
   말 것**(결정 126).
8. ⛔ **DB 배치**: 우리 표는 스키마 **`ohmyenglish`** 에 있음. `pg_dump` 는 **`-n ohmyenglish`**(`H-BX`).
   `psql` 은 `/opt/homebrew/opt/postgresql@17/bin/psql` 임.
   ⚠️ **`public` 에 En-Coach 호환 뷰 셋이 있음** — introspection 에는 `current_schema()` 를 **반드시
   걸어야 함**(`H-BX`. 2026-09-19 에 그 규율이 한 자리에서 빠져 있던 것을 고쳤음 · `TASK-218`).
9. ⚠️ **백엔드가 떠 있음** — `:8002` · `WORKER_ENABLED=false VOICE_ADAPTER=stub` · `/health` 200 ·
   로그는 `/tmp/omy-backend.log`. ⛔ **`--reload` 가 없어 소스를 고쳤으면 재기동함.**
   ⛔ **프런트는 떠 있지 않음** — 화면을 볼 일이 있으면 `:3000` 에 직접 띄움.
10. ⛔ **`git` 이 exit 69 로 죽으면 Xcode 라이선스임**(`H-CH`). 2026-09-19 에 실제로 막혔고
    **사용자가 해결했음** — 지금은 우회 없이 돌아감. 다시 나면 `H-CH` 가 우회와 항구 해결을 가짐.
11. ⚠️ **낭독 전사는 「전사 전용 모드」가 기본임**(`TASK-217`) — 입력 2,143 → 897 이고 전사문은
    글자까지 같았음. 실측 정본은 설계서 §6-1 임.

## 인계 지표 4개 (이 턴에 직접 돌린 출력)

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`5946a8b`** · 작업트리 clean · `origin` 과 **0/0**. ⛔ 이 handoff 의 마감 커밋이 그 뒤에 오므로 **차이가 handoff·원장뿐이면 정상**임 |
| 2 | 다음 한 걸음 | **`TASK-223`** — 3갈래가 수렴한 결함(고아 스윕이 정지 중 세션의 `.part` 를 지움). 그 뒤 `226`(적용 묶음) → `227`·`228` 순서를 권함(§⑥) |
| 3 | 게이트 | **여덟 다 통과** — `pytest` **수집 1400 · 통과 1400**(skip 0) · `ruff` 0 · `ruff format` **305 files** · `ty` 0 · `tsc` 0 · `eslint` 0(경고 1건은 기준선) · `next build` 0(`/` 가 `○` Static 유지) |
| 4 | 착수 전 필수 | 전체 **289** · 열린 것 **여섯**(`TASK-223`~`228` · 정리 회차가 갈라낸 것) · `In Progress` **0건** · 의존 **0건** |

```bash
cd ~/MyProject/OhMyEnglish
git rev-parse --short HEAD && git rev-list --left-right --count HEAD...origin/design/first-vertical-slice
backlog task list --plain
cd app/backend && ./.venv/bin/pytest -q
cd app/backend && ./.venv/bin/ruff check . ../../tests ../../scripts \
  && ./.venv/bin/ruff format --check . ../../tests ../../scripts && ~/.local/bin/ty check
cd app/frontend && npx tsc --noEmit && npx eslint . && npx next build
```

## ① 이 세션이 한 것 — 태스크 여덟을 닫았음

`TASK-214`(좁은 `Transcriber` 포트) · `215`(사용량 기록을 펌프 `finally` 로) · `216`(teardown 이 앱의
삭제 규칙 재사용 · **동작 바뀜 — 삭제 범위 좁아짐**) · `217`(전사 전용 프롬프트 · 실물 A/B 로 입력
−58.1% 출력 −78.0% 전사문 동일) · `218`(스키마 무필터 단정) · `219`(git 고장을 gitignore 로 오보) ·
`220`(품질 리뷰 적용) · `221`(녹음 규격 상수를 `app/models` 로).
⛔ **커밋별 상세는 `git log --oneline 8559e07..HEAD` 와 각 태스크 노트가 가짐** — 여기 복제하지 않음.

## ② 품질 리뷰 — 받았고 적용했음 (`TASK-220` · 닫힘)

**CRITICAL 0 · HIGH 0 · MEDIUM 3 · LOW 2** ⇒ 승인. 넘긴 하나 빼고 전부 적용했음. **상세는
`TASK-220` 노트가 표로 가짐.** 넘긴 하나는 `_user_finals` 를 `asyncio.timeout().reschedule()` 로
바꾸는 것 — 리포에 그 관용구가 0 곳이고 실물 계측으로 정한 타이밍 코드라 거래가 나쁨.

⛔ **되돌린 판단**: 20분 동안 결과가 오지 않는 것을 보고 **「미수신으로 끝났다」고 단정했는데
틀렸음** — 다섯 전부 그 뒤에 결과를 냈음. `idle` 은 완료도 미완료도 뜻하지 않음.
⚠️ **다음에 같은 모양을 보면 먼저 기다릴 것.**

## ③ 이 세션이 배운 것 — 다음 세션이 반복하지 않을 것

1. ⛔ **게이트 수치가 handoff 와 다르면 그 차이가 먼저 정보임.** 기록은 `1389 passed` 였고 첫 실행이
   `2 failed, 1387 passed` 였음 — **둘 다 실물 결함**이었음(환경 `H-CH` 하나 · 8일 묵은 무필터 단정
   하나). 「환경 탓」으로 접지 않은 것이 둘을 갈라낸 이유임.
2. ⛔ **같은 커밋에서 통과와 실패가 갈리면 단정이 재려는 성질이 아닌 것에 붙어 있음** — 전체 실행은
   통과하고 고립 실행은 8/8 실패했고, 그 비대칭이 곧 카탈로그 행 순서 의존의 증거였음.
3. ⛔ **판별력은 「고치는 대상과 같은 모양의 입력」으로 재야 함** — `H-CH` 는 라이선스가 풀리기
   «전에» 재서 `exit 69` 문면을 직접 봤고, 풀린 뒤에는 그 입력을 만들 수 없었음.

## ④ 착수 전 필수

1. ⛔ **`pytest`·`ruff` 는 `app/backend` 에서 돌림**(`H-BN`). 부분 실행은 `-c pyproject.toml`(`H-AJ`).
2. ⛔ **뮤테이션마다 `__pycache__` 를 지움**(`H-CF`) · **마이그레이션 전 `pg_dump -n ohmyenglish`**.
3. ⛔ **`git add <디렉터리>` 금지** · 커밋 뒤 `git show --stat` 으로 담긴 것 전체를 봄.
4. ⚠️ **나머지 함정의 정본은 `docs/ops/pitfalls.md` 임**(이 세션이 `H-CH` 를 더함). 그 문서의
   「최종 갱신」 줄은 8일 낡았음 — 범위 밖이라 두었고 손대는 세션이 함께 고침.
5. ⚠️ **한글 훅이 정상 낱말을 막을 수 있음**(명사형 어미 둘). 표현을 바꾸는 쪽이 값쌌고, 훅이 지목한
   음절을 **그대로 인용하면 다시 막힘**.

## ⑤ 범위 밖으로 남긴 것

`end_input()` 으로 침묵·조용함 상수를 없애는 축과 `audioOutputConfiguration` 제거는 **재지 않았음**
(`TASK-217` 노트가 근거) — 각각 별 A/B 가 필요하고, 뒤쪽은 출력이 이미 −78% 라 남은 이득이 작음.

## ⑥ 정리 회차 — `TASK-222` 닫힘. 지적은 태스크 여섯으로 갈라 놓았음

리뷰 여섯 갈래(재사용·단순화·효율·고도·도구낡음·codex 회귀위험)를 읽기 전용으로 돌려 **전부
받았음.** ⛔ **지적 전문·계측값·회귀 위험 목록의 정본은 `TASK-222` 노트임** — 여기 복제하지 않음.
이 회차에서 «적용한» 것은 위반 0건 규칙군 다섯뿐임(`232fda1`). **회귀 0**(수집 1400 유지).

**갈라낸 여섯** — 신뢰도 순: `TASK-223`(결함 · **3갈래 수렴** · 고아 스윕이 정지 중 세션의 `.part` 를
지움 · 동작변경=예) · `224`(lease 를 잃은 뒤 커밋 · 예) · `225`(워커 라우팅 표 + 누락 분기 테스트) ·
`226`(동작변경 없는 지적 전부 적용) · `227`(`analysis_jobs` 인덱스 둘) · `228`(거짓 신호 검사 여섯).

⛔ **`226` 착수 전에 `TASK-222` 노트의 회귀 위험 8건을 먼저 읽음** — 「정리하다 조용히 깨질 자리」이고
**넷은 테스트가 못 잡음**. ⛔ **문면 정본 검사가 많음**(`SYSTEM_PROMPT` 부분 문자열 12건 등) — 걸리면
검사를 고치지 말고 내 변경을 고침. ⛔ **묶은 자리마다 변이를 한 번 걸어** 그 자리를 덮는 테스트가
있는지 잼.

⚠️ **위임 전달의 실측 둘**: 리뷰 결과가 `idle` 이 된 «뒤» 20분·46분에 왔고 두 번 다 **직접 제출
요청 뒤**였음. ⛔ `idle` 을 완료로도 미완료로도 읽지 않음 — 이 세션이 그 오독을 두 번 했음.
