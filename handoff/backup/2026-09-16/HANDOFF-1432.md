# HANDOFF — OhMyEnglish

> 최종 갱신 **2026-09-16 14:35 KST** · 세션 `ohmyenglish-42` · 브랜치 `design/first-vertical-slice`
> ⛔ **이전 판은 `handoff/backup/2026-09-16/HANDOFF-1435.md` 에 있음**(그 앞 판들도 같은 폴더).
> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. 상태 정본은 원장(`backlog/`), 근거
> 정본은 **태스크 노트 · `tests/harness/runs/**` · `docs/ops/captain-instruction-register.md`** 임.
>
> ⚠️ **역할**: 통합 테스트로 출발했고 13시 KST 에 사용자가 **구현 권한을 연장**했음(결정 113 마지막
> 절). 그 뒤 기능 여섯을 TDD 로 구현하고 실물로 검증했음.
> ⛔ **새 세션은 기본값(통합 테스트)에서 출발하고 열려 있는지 확인함.**
> ⚠️ **살아 있는 사용자 지시 둘**: ⑴ *"테스트 진행은 니가 권고안 대로 나에게 뭍지 말고 진행해, 내가
> 따로 말하기 전까지"* ⑵ *"짧게 핵심만 보고해 … 각 항목당 두줄 요약"*.
> ⛔ **설계·제품 판단은 그대로 물음** — 그 구분이 ⑴의 내용임(실제로 고치는 자리·호출어 값역·「계속」의
> 뜻은 물었고, 다음 테스트 대상은 묻지 않았음).

## 인계 지표 4개 (이 턴에 직접 돌린 출력)

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | 게이트를 돌린 시점은 **`0cf036c`** 이고 `origin` 과 **0/0** · 미커밋 **0건** 이었음. ⚠️ 새 세션 `HEAD` 는 그보다 뒤임 — 그 뒤 커밋은 **이 handoff 와 원장뿐이고 코드가 아니므로** 게이트 수치가 같으면 실질 일치임. ⛔ 자기 커밋 해시를 여기 적지 않는 이유는 **반드시 낡기 때문임** |
| 2 | 다음 한 걸음 | **없음 — 잔여 5건이 전부 파킹이거나 사용자 판단 대기임.** 아래 ④ |
| 3 | 게이트 | **여섯 다 초록** — `pytest` **1263 passed**(15.10s) · `ruff check` · `ruff format --check` **49 files** · `ty check` · `npx tsc --noEmit` **exit 0** · `npx eslint .` **exit 0** |
| 4 | 착수 전 필수 | 아래 ③. 잔여 **5건**(전부 `To Do` · In Progress **0** · 전체 **193** · 완료 **188**) |

```bash
cd ~/MyProject/OhMyEnglish
git rev-parse --short HEAD && git rev-list --left-right --count HEAD...origin/design/first-vertical-slice
backlog task list --plain
cd app/backend && ./.venv/bin/pytest -q && ./.venv/bin/ruff check . && ~/.local/bin/ty check
cd ../frontend && npx tsc --noEmit && npx eslint .
```

## ① 이번 세션이 한 것 — 갈림을 재고, 보이게 만들고, 정지·재개를 새로 만들었음

**회차 일곱**(Nova 합 **44세션** · Claude **0회**): `runs/2026-09-16-task61-13-requested-stall` ·
`task61-14-prompt-change-regression` · `task61-15-accepted-without-execution`(회차 2개) ·
`task61-16-divergence-surface` · `task61-9-pause-resume` · `task61-15-rejected-result`.

**Done 아홉**: `TASK-61.9` · `61.11` · `61.13` · `61.14` · **`61.15`** · `61.16` · `61.17` ·
`61.18`(+ `TASK-39` 는 판단만 받고 파킹).
**결정 등재 일곱**: **112**(고치는 자리는 앱) · **113**(표면은 화면 · 대상) · **114**(표지 단순화) ·
**115**(dev DB 마이그레이션 승인) · **116**(「Hello」 제거) · **117**(「계속」은 같은 세션을 이음) ·
**118**(버린 명령에 거절을 돌려줌).
**마이그레이션 025** 를 발급하고 **공유 dev DB 에 적용**했음 — `schema_migrations` **22 → 23**.
**픽스처 열넷 신설**: `vc27`~`vc40`.

## ② 지금 상태 — 음성 명령 여섯 · 표면 셋 · 표지 셋

- **명령 여섯**: `end` · `show_report` · `next_question` · `start_additional` · **`pause`** ·
  **`resume`**. 값역 정본은 `models/voice_command.ControlCommand` 임. ⛔ 지시문은 **개수를 말하지
  않음**(세면 낡음 — 단정이 그것을 지킴).
- **표지 셋**: `hey` · `헤이` · `해이` + 앱 이름 셋. 「Hello」는 결정 116 이 뺐음.
  ⚠️ **남은 대가**: 「Hey, how are you?」 는 여전히 표지를 얻어 분석에서 빠짐(단정이 지킴).
- **화면 표면 셋**: 버려진 명령 알림(명령 다섯) · 확인 대기(`end`·`start_additional`) ·
  **정지 중 상태**(계속 남는 줄).
- **정지의 계약 하나**: 정지 중에는 학습 발화를 저장하지 않음(실물 3/3). 소켓은 살아 있음.
- ✅ **`accepted` 를 실행 판정 뒤로 미뤘음**(결정 118) — 버린 턴에는 `rejected` 와 이유가 나가고
  코치가 「안 됐다」고 말했음(실물 2/2 · 앞 회차의 「됐다」 4/4 와 대조). 게이트웨이가 `finally` 로
  보고하고 어댑터가 미보고를 경고로 셈.
- ⚠️ **코치가 문면을 안 지키는 것이 네 번 관측됐음** — 지시문 낭독 셋(`61.14`·`61.16`) · 정지 중
  계속 말하기 3/3(`61.9`). 전부 **앱의 계약이 손실을 막았음.**

## ③ 착수 전 필수 — 앞 판에서 유효한 것 + 이번 세션 실측

1. ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/` 이고 `ty` 만 `~/.local/bin` 임.
   ⚠️ **툴 호출 사이에 cwd 가 남으므로** 절대 경로를 씀.
2. ⛔ **`pytest` 는 `app/backend` 에서 인자 없이 돌림.** 경로를 주면 `asyncio_mode` 가 안 잡혀 async
   테스트가 전부 실패하고, 리포 루트에서 돌리면 경로 의존 테스트 10건이 깨짐. 부분 실행은
   `-o asyncio_mode=auto` 를 붙임 — 둘 다 이번에 밟았음.
3. ⛔ **`git add <디렉터리>` 금지** · 커밋 전 `git diff --cached --name-only` 로 건수를 셈.
4. ⛔ **검증 전용 스택 규약**: DB 소유자 `ohmy` · `WORKER_ENABLED=false` · 끝나면 죽이고 drop.
   불변 지표: **새 DB 의 `schema_migrations` 23**(025 적용 뒤 바뀌었음) · 공유 dev DB 는
   `learning_sessions` **17** · `schema_migrations` **23** · `session_plans` **6** ·
   `status='paused'` **0건**.
5. **브라우저 레그 조합**(이번 세션에 여섯 번 통했음): 전용 Chrome `:9333` + 프론트 사본 `/tmp/fe-*`
   + 래퍼 백엔드 `:8012`. ⛔ `node_modules` 는 하드링크 복사(`cp -Rl`) — 심볼릭 링크는 Next 16 이
   거부함(`H-BU`). ⛔ **코드를 고친 뒤에는 회차 백엔드를 재기동함**(`--reload` 없음 · 이번에 두 번).
6. ⛔ **새 로그를 `info` 로 두지 않음**(함정 `H-Z`) — 이번에 정지 중 미저장 로그가 `info` 라 **버렸는데
   0건으로 보였음.** 크기를 세야 하는 로그는 `warning` 임.
7. ⛔ **`recv.voice_command` 로 판정하지 않음** — 프레임 수임. 어댑터 계측(래퍼가 `translate` 를
   감쌈)이 정본이고 줄마다 ISO 시각을 넣어 팔 귀속을 시각으로 가름.
8. ⛔ **앞 회차와 대조하려면 그 회차가 «심은 데이터»를 먼저 확인함** — 기준을 결과에 맞춰 고치지 않고
   심고 다시 돌림.
9. ⚠️ **`grep -c` 가 0을 내면 `&&` 사슬이 끊김** — 증거 수집을 한 사슬에 이으면 뒤가 안 돌음.

## ④ 열린 태스크 5건 — 전부 사용자 판단이나 파킹

| 태스크 | 왜 열려 있나 |
|---|---|
| `TASK-78.1` | AC#1(문턱 수치)을 사용자가 정해야 함. 이득은 단순화뿐이라 급하지 않음 |
| `TASK-116.5` | 문장 인용 구멍 2/59 — 파킹. 되살릴 조건은 그 비율이 커지는 것임 |
| `TASK-122`(LOW) | 네 세션이 「지금 고치지 않는다」로 판정했음 — 다시 열지 않음 |
| `TASK-39` | AC#2 를 **결정 115 가 「운영 관찰 시점」으로 확정**했음 — 지금 하지 않음 |
| `TASK-41` | AC#2 가 캡틴 결정 대기. 결정 31 이 「모든 구현이 끝난 뒤」로 미룸 |

## ⑤ 착수 전 반드시 읽을 것

- **결정 112~118** — 표면의 대상 · 표지 값역 · 「Hello」 제거의 남은 대가 · 정지의 계약 · 거절 보고.
- **`TASK-61.15` 노트** — 결정 118 의 경위와 **지시문 낭독 표본 셋**을 그 태스크가 가짐. 낭독은 새
  태스크로 쪼개지 않았음(계약이 손실을 막고 있음).
- ⚠️ **문면으로 거동을 보장하려 하지 않음**(결정 112) — 이번 세션에 코치가 문면을 어긴 것이 네 번
  관측됐고 매번 **앱의 계약**이 손실을 막았음. 새 요구가 오면 「앱이 무엇을 보장하는가」를 먼저 정함.
- ⚠️ **라운드를 더 돌지 않는 규율이 살아 있음** — 회차가 새 findings 를 만들면 라운드가 아니라
  게이트를 바꿈. 이번 세션에 사슬을 표면으로 끊었음.
