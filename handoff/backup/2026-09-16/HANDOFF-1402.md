# HANDOFF — OhMyEnglish

> 최종 갱신 **2026-09-16 13:35 KST** · 세션 `ohmyenglish-42` · 브랜치 `design/first-vertical-slice`
> ⛔ **이전 판은 `handoff/backup/2026-09-16/HANDOFF-1335.md` 에 있음**(그 앞 판들도 같은 폴더).
> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. 상태 정본은 원장(`backlog/`), 근거
> 정본은 **태스크 노트 · `tests/harness/runs/**` · `docs/ops/captain-instruction-register.md`** 임.
>
> ⚠️ **역할**: 이 세션은 통합 테스트로 출발했고 **13시 KST 에 사용자가 구현 권한을 연장했음**(결정
> 113 의 마지막 절). 그 뒤 기능 넷을 TDD 로 구현하고 실물로 검증했음.
> ⛔ **새 세션은 기본값(통합 테스트)에서 출발하고 열려 있는지 확인함.**
> ⚠️ **살아 있는 사용자 지시 둘**: ⑴ *"테스트 진행은 니가 권고안 대로 나에게 뭍지 말고 진행해, 내가
> 따로 말하기 전까지"* ⑵ *"짧게 핵심만 보고해 … 각 항목당 두줄 요약"*. ⛔ **설계·제품 판단은 그대로
> 물음** — 그 구분이 ⑴의 내용임(실제로 「고치는 자리」는 물었고 「다음 테스트 대상」은 묻지 않았음).

## 인계 지표 4개 (이 턴에 직접 돌린 출력)

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | 게이트를 돌린 시점은 **`7c287ff`** 이고 `origin` 과 **0/0** · 미커밋 **0건** 이었음. ⚠️ 새 세션 `HEAD` 는 그보다 뒤임 — 그 뒤 커밋은 **이 handoff 와 원장 상태뿐이고 코드가 아니므로** 게이트 수치가 같으면 실질 일치임 |
| 2 | 다음 한 걸음 | **`TASK-61.9`(일시 정지)** — 결정 115 로 마이그레이션 적용 승인을 받아 착수 조건이 풀렸음. ⛔ 착수 전에 아래 ⑤의 **읽기 확정**을 먼저 함 |
| 3 | 게이트 | **여섯 다 초록** — `pytest` **1250 passed**(14.84s) · `ruff check` · `ruff format --check` **49 files** · `ty check` · `npx tsc --noEmit` **exit 0** · `npx eslint .` **exit 0** |
| 4 | 착수 전 필수 | 아래 ③. 잔여 **8건**(전부 `To Do` · In Progress **0** · 전체 **193** · 완료 **185**) |

```bash
cd ~/MyProject/OhMyEnglish
git rev-parse --short HEAD && git rev-list --left-right --count HEAD...origin/design/first-vertical-slice
backlog task list --plain
cd app/backend && ./.venv/bin/pytest -q && ./.venv/bin/ruff check . && ~/.local/bin/ty check
cd ../frontend && npx tsc --noEmit && npx eslint .
```

## ① 이번 세션이 한 것 — 갈림을 재고, 보이게 만들고, 표지를 갈았음

**회차 다섯**(Nova 합 **38세션** · Claude **0회**):
`runs/2026-09-16-task61-13-requested-stall` · `task61-14-prompt-change-regression` ·
`task61-15-accepted-without-execution`(회차 2개) · `task61-16-divergence-surface`.

**Done 여섯**: `TASK-61.13` · `61.14` · `61.16` · `61.17` · `61.18`(+ `TASK-39` 는 판단만 받고 파킹).
**새로 등재 다섯**: `61.14` · `61.15` · `61.16` · `61.17` · `61.18`.
**결정 등재 다섯**: **112**(고치는 자리는 앱) · **113**(표면은 화면 · 대상 셋) · **114**(표지 단순화) ·
**115**(dev DB 마이그레이션 승인) · **116**(「Hello」 제거).
**픽스처 열 신설**: `vc27`~`vc36`(표지 없는 명령 여섯 · 새 표지 셋 · 오탐 측정용 하나).

## ② 지금 상태 — 갈림은 그대로 있고 학습자가 그것을 «볼 수» 있음

- **표면 둘이 들어갔음**(`TASK-61.16`): 표지가 없어 버린 명령은 `voice_command_ignored` 프레임으로
  화면이 말하고, 확인 대기(`end`·`start_additional` 의 `requested`)도 같은 자리에서 말함.
  ⛔ **`next_question` 은 두 표면에 없음** — 버려져도 코치가 다음 질문을 해서 화면이 어긋나지 않음.
- **표지는 `hey`·`헤이`·`해이` + 앱 이름 셋임**(`_WAKE_FORMS` 가 정본). 「Hello」는 결정 116 이 뺌.
  ⚠️ **남은 대가**: 「Hey, how are you?」 는 여전히 표지를 얻어 분석에서 빠짐(단정이 지킴).
- ⛔ **`accepted` 를 먼저 돌려주는 구조는 안 바뀌었음**(`TASK-61.15` AC#3) — 어댑터가 tool 결과를
  앱의 실행 판정보다 **먼저** 보냄. 이번에 만든 것은 알림이고 그 순서는 그대로임.
- ⚠️ **코치가 지시문·내부 추론을 소리로 읽는 것이 세 번 관측됐음**(`61.14` §3-② · `61.16` §3-③).
  `TASK-61.15` 가 그 표본을 가짐 — 별도 태스크로 쪼개지 않았음.

## ③ 착수 전 필수 — 앞 판에서 유효한 것 + 이번 세션 실측

1. ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/` 이고 `ty` 만 `~/.local/bin` 임.
   프론트는 `app/frontend` 에서 돌림. ⚠️ **툴 호출 사이에 cwd 가 남으므로** 절대 경로를 씀.
2. ⛔ **`pytest` 는 `app/backend` 에서 인자 없이 돌림.** 경로를 주면 rootdir 이 바뀌어
   `asyncio_mode` 가 안 잡히고(**async 테스트가 전부 실패**) 리포 루트에서 돌리면 경로 의존 테스트
   10건이 깨짐. 부분 실행이 필요하면 `-o asyncio_mode=auto` 를 붙임 — 둘 다 이번에 밟았음.
3. ⛔ **`git add <디렉터리>` 금지** · 커밋 전 `git diff --cached --name-only` 로 건수를 셈.
4. ⛔ **검증 전용 스택 규약**: DB 소유자 `ohmy` · `WORKER_ENABLED=false` · 끝나면 죽이고 drop.
   불변 지표: 새 DB 의 `schema_migrations` **22** · 공유 dev DB 는 `learning_sessions` **17** ·
   `schema_migrations` **22** · `session_plans` **6** · `weekly_reports` **0**.
5. **브라우저 레그 조합**(이번 세션에 다섯 번 통했음): 전용 Chrome `:9333` + 프론트 사본
   `/tmp/fe-*`(`.env.local` · 픽스처를 **그 사본의** `public/harness/` 에) + 래퍼 백엔드 `:8012`.
   ⛔ `node_modules` 는 하드링크 복사(`cp -Rl`) — 심볼릭 링크는 Next 16 이 거부함(`H-BU`).
   ⛔ **코드를 고친 뒤에는 회차 백엔드를 재기동함**(`--reload` 없음) — 이번에 회차 중간에 한 번 했음.
6. ⛔ **`recv.voice_command` 로 판정하지 않음** — 프레임 수임. 어댑터 계측이 정본이고 줄마다 ISO
   시각을 넣어 팔 귀속을 시각으로 가름.
7. ⛔ **앞 회차와 대조하려면 그 회차가 «심은 데이터»를 먼저 확인함** — 심지 않으면 패널·계획 질문을
   대조할 수 없음. 기준을 결과에 맞춰 고치지 않고 **심고 다시 돌리는** 쪽을 골랐음.
8. ⚠️ **`grep -c` 가 0을 내면 `&&` 사슬이 끊김** — 증거 수집을 한 사슬에 이으면 뒤가 안 돌음.

## ④ 열린 태스크 8건

| 태스크 | 상태 |
|---|---|
| **`TASK-61.9`** | **다음 걸음.** 결정 115 로 마이그레이션 적용 승인됨 — AC#2(재개 계약)는 ⑤의 읽기에 걸림 |
| `TASK-61.11` | 「학습 계속」 — ⑤의 읽기가 확정되면 `61.9` 와 한 기능일 수 있음 |
| `TASK-61.15` | AC#3(`accepted` 를 뒤로 미룰지)만 남음 — 설계 판단이라 물어야 함 |
| `TASK-78.1` | AC#1(문턱 수치)을 사용자가 정해야 함. 이득은 단순화뿐이라 급하지 않음 |
| `TASK-116.5` | 문장 인용 구멍 2/59 — 파킹. 되살릴 조건은 그 비율이 커지는 것임 |
| `TASK-122`(LOW) | 네 세션이 「지금 고치지 않는다」로 판정했음 — 다시 열지 않음 |
| `TASK-39` | AC#2 를 **결정 115 가 「운영 관찰 시점」으로 확정**했음 — 지금 하지 않음 |
| `TASK-41` | AC#2 가 캡틴 결정 대기. 결정 31 이 「모든 구현이 끝난 뒤」로 미룸 |

## ⑤ 착수 전 반드시 읽을 것

- ⛔ **`TASK-61.11` 의 읽기를 확정하지 않았음.** 사용자 답이 *"지금 세션에서 그대로 「학습계속」
  해야하면 진행해"* 였고, 이것을 **「정지된 세션을 그대로 잇는다」**(값역 ②)로 읽으면 `61.9` 와 한
  기능이 됨. ⛔ **추정으로 설계하지 않고 그 태스크를 열 때 다시 물음**(결정 115 가 그렇게 적었음).
- **결정 113·114·116** — 표면의 대상 셋 · 표지 값역 · 「Hello」 제거의 근거와 남은 대가.
- **`TASK-61.15` 노트** — AC#3 에 들어갈 재료 셋. 그 태스크가 지시문 낭독 표본도 가짐.
- ⚠️ **라운드를 더 돌지 않는 규율이 살아 있음** — 회차가 새 findings 를 만들면 라운드가 아니라
  게이트를 바꿈. 이번 세션에 사슬이 `61.13`→`61.15`→`61.16` 으로 이어졌고 **표면을 만들어 끊었음.**
