# Handoff — 발음 축 · 세션 `ohmyenglish-d9`

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 판정 근거를 옮기지 않고
> **태스크 조회와 설계서·회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-pronunciation-2026-09-13-0019.md`(그 앞은 `…-2336` · `…-2232`).
> ⛔ **다른 갈래를 건드리지 않았음** — `HANDOFF-plan-prompt.md`(`ohmyenglish-f4`) 는 그쪽 소유임.

최종 갱신 **2026-09-13 00:19 KST**(`date` 로 확인함) · 브랜치 `design/first-vertical-slice`

---

## ① 이 세션이 한 것

**닫음(넷)**: `TASK-128.1`·`.2`(결정 72 구현) · `TASK-128.3`(검증 회차) · `TASK-128.5`(결정 요청).
**세움**: `TASK-128.4`(화면 문구 · **AC#1 만 열림**) · `TASK-116.1`(결정 82 구현 · **다음 걸음**).
**받은 결정 둘**: **82**(다음 수단은 **기록 경로** · 프롬프트를 더 고치지 않음) · **83**(진입 문구는
「후보가 아직 없음」을 참으로 말함). 둘 다 `AskUserQuestion` 으로 직접 받아 결정 대장에 적었음.

**회차 정본** `runs/2026-09-12-task128-sound-as-candidate.md` — Nova **7세션** · 죽은 회차 0.
회귀 팔 **4/4** ⇒ 문면 유지 · 바이트 게이트를 `prompt_dedicated_v4.txt` 로 갱신.
⛔ 판별 팔 **3/3 으로 다시 후보의 키를 기록** ⇒ **세 방향(더하기·덜기·재료)이 모두 반증됐음.**
한 세션은 코치가 **없는 /f/ 를 발명**했음(`early` 에 /f/ 가 없음).

**설계 정본** `docs/design/2026-09-13-decision82-record-path-verification.md` — 결정 82 이행 설계.
**제품에 들어간 것**: 단수 지목 제거 · 전용 프롬프트의 후보 블록 · `build_pronunciation_prompt(known_sounds)`
· `create_voice_adapter(pronunciation_mode)` · `_pronunciation_candidates` · **폴백 제거** · 화면 문구.
**영구 지식 하나**: `H-BQ`(`backlog task edit --notes` 가 기존 노트를 덮음).

## ② 다음 한 걸음 — **`TASK-116.1`**(결정 82 구현)

설계서가 **판단을 이미 끝냈음** — 다시 정하지 않고 읽고 착수함. 요지 넷:

1. **형태 검사를 하지 않음** — `TASK-97` AC#3 이 기각한 방향임. 이 태스크는 **발화 대조**이고
   재료가 둘(키 + 같은 턴의 발화)이라 어긋남이 정의됨. ⇒ 기각을 뒤집는 것이 아님.
2. **기제를 새로 만들지 않음** — `review.py:188` 의 `signal_source = 'nova_tool'` 필터가 「자격 없는
   기록을 단계 전진에서 배제」하는 선례임(결정 59 · `TASK-74`). 그 자리에 조건 한 줄.
3. ⛔ **대조는 세션 종료 패스에 얹음**(`resolve_dangling` · 설계서 §4-1). 기록 시점에 두면 tool 이
   코칭 발화와 동시에 와서 **경합이 조용히 통과**함. ⇒ 조건의 방향은 **「어긋남으로 표시된 것을
   제외」**이고 「검증된 것만 포함」이 **아님.**
4. ⛔ **후보 대조를 하지 않음** — 「키가 우리가 준 후보 안인지」는 가장 그럴듯해 보이는데 결함이
   정확히 「후보를 되풀이하는 것」이라 **판별력 0**임(설계서 §3 C).

**측정된 판별력 2/3** · 맹점은 **발화와 기록이 «함께» 틀린 경우**임(설계서 §5).
⛔ **회차가 필요 없음** — 프롬프트를 만지지 않으므로 실물 왕복이 판정을 바꾸지 않음.

**나란히 가능한 갈래**: `TASK-128.4` AC#1(브라우저 레그 · 아래 ③ 8번) · `TASK-78` · `TASK-81`.

### ⛔ 사용자 결정을 기다리는 것 — 하나

`TASK-116.1` AC#5: **검증에 걸린 기록을 화면에 어떻게 보이는가.** ⛔ 복습 큐에서 빼는 것은 결정 82 가
이미 정했으므로 **묻지 않음.** ⚠️ 그 AC 는 구현의 **마지막** 칸이라 앞 넷을 먼저 할 수 있음.

## ③ 착수 전 필수

1. ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/`(`ty` 만 `~/.local/bin`).
2. ⛔ **회차 상한과 해석 규칙을 돌리기 전에 적음.** 표에 없는 모양이 나오면 **새 행으로** 적음.
3. ⛔ **팔마다 검증 DB 를 따로 둠** — 한 DB 에 두 소리를 넣으면 판별 장치가 무력해짐.
4. ⛔ **픽스처의 오류와 후보가 맞는지 «먼저» 확인함**(`scenarios-PQ-qwen-phoneme.md`).
   `pq05` 는 판별용으로 못 씀 · `pq13`(/r/→/l/)+후보 `f_as_p` 가 지금의 판별 장치임.
5. ⛔ **`pytest` 에 경로를 주지 않음**(`H-AJ`) · 게이트는 `app/backend` cwd(`H-A`·`H-BN`) · 파이프 뒤
   `$?` 금지(`H-AZ`) · ⚠️ **zsh 는 변수를 단어 분할하지 않음** — 경로 목록을 `$FILES` 로 넘기면 한
   단어로 붙어 `E902` 가 남(밟았음). 경로를 그대로 열거함.
6. ⛔ **`git add <디렉터리>/` 금지**(`H-BE`) · 커밋 **뒤** `git show --stat`(`H-BA`) · push **전**
   `git log --oneline origin/<브랜치>..HEAD` 로 열거(`H-BO`).
7. ⚠️ **검증 스택은 공유 자원을 건드리지 않음** — 전용 DB + `:8012`·`:8013`. 끝나고 다섯 표의 행 수
   무변경을 대조하고 ⛔ **정리를 «주장하지 않고» 다시 읽음**(`H-BK`). PID 를 직접 `kill` 함.
   ⛔ **`createdb` 에 `-O ohmy` 를 빼지 않음** — 소유자가 `redstar` 면 마이그레이션이
   `permission denied for schema public` 로 죽음(동료 세션이 직접 밟았음).
   ⚠️ **마이그레이션을 더할 때 동료 세션이 `pytest` 를 도는지 먼저 확인함** — `conftest` 가 매 실행
   테스트 DB 를 마이그레이션에서 재생성하므로 도는 중에 파일을 더하면 그쪽에 유령 실패가 남.
8. ⚠️ **브라우저 레그 자산이 전부 정리돼 있음**(직접 확인) — 절차는
   `runs/2026-09-12-task10-2-entry-browser-leg.md` §0. ⛔ **안내 줄의 창이 짧아 폴링으로는 놓침** —
   클릭 «전»에 `MutationObserver` 를 심음.
9. ⛔ **`backlog task edit --notes` 를 쓰지 않음**(`H-BQ`) — 덮음. 태스크 md 를 직접 고침.
   ⚠️ 한글 파일명을 `git status` 출력에서 복사하면 `git show` 가 죽음 — zsh 글롭으로 경로를 얻음.
10. ⛔ **공유 파일을 `add` 한 뒤 `git diff --cached -- <그 파일>` 을 봄**(`H-BR`) — 결정 대장 ·
    `pitfalls.md` · 원장 노트 · handoff. 경로를 열거해도 **파일 «안»의 남의 편집**은 못 막고 충돌도
    나지 않음. ⚠️ **고친 턴에 바로 커밋함.**
11. ⛔ **`create_voice_adapter` 시그니처를 또 바꿀 때 `test_ws.py` 의 팩토리 대역이 «둘»임** — 모듈
    수준 `_capture_factory_args` 와 `test_ws_passes_assembled_instructions_to_the_adapter` 안의 지역
    대역. 지역 대역을 빠뜨리면 **부분 실행은 통과하고 전체 실행에서만** red 가 됨. ⇒ 시그니처 변경은
    **전체 실행**으로 확인함.

## ④ 인계 지표 — 이 마감 시점에 **직접 돌려** 얻음

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`ba09e15` 이상** · ⚠️ 동료 세션이 계속 커밋하므로 **등호를 요구하지 않음.** push 사전 승인됨 |
| 2 | 다음 걸음 | **`TASK-116.1`**(`To Do`). 내 `In Progress` 는 `116`(high) · `78` · `81` · `97` · `128` · `128.4` |
| 3 | 게이트 | `b7ada24` 트리에서: `pytest` **1088 passed**(12.98s · exit 0) · `ruff check .` 0 · `format --check .` **42 files** · 게이트 밖 `check` 0 · `format` **162 files** · `ty` 0 · 프론트 `tsc` 0 · `eslint` 0 |
| 4 | 착수 전 필수 | **11개**(위 ③) |

⛔ **그 1088 을 동료 세션이 «독립으로» 같은 트리에서 세서 같은 값을 얻었음** — 두 세션이 따로 센 것이
이 리포에서 가장 값싼 검출기임. ⚠️ 그 뒤 커밋 둘(설계서·handoff)은 **문서만**이라 수치를 바꾸지 않음.
⛔ **내 축 가드를 무력화해서 쟀고 4/4 가 물었음** — 파일을 고치지 않고 프로세스 안에서
`_known_sounds_block`·`_SOUND_INSTRUCTION` 을 대체했음(`H-AW` 를 만들지 않음). 되돌린 뒤 원래 성질
셋이 다시 참인 것까지 확인했음.
⚠️ **오늘 쓴 Nova**: 이 세션 **7세션** · 누적 **157**.
⛔ **공유 dev DB 에 내가 남긴 변경 0건** — 검증 DB 둘을 지웠고 다섯 표를 전후로 대조했음:
`learning_sessions=17`·`pronunciation_attempts=7`·`error_patterns=9`·`session_plans=6`·`learner_notes=7`.

## ⑤ 이 세션이 얻은 규율

⛔ **세 번 같은 방향이 나오면 그것이 결론임.** 표본 각 3 이라 비율은 못 말하지만 **방향**은 말할 수
있고, 그것을 올려 결정 82 를 받았음.
⛔ **「발화와 기록의 일치」처럼 그럴듯한 지표가 «둘이 함께 틀린» 경우를 못 가림** — 일치를 세기 전에
그 일치가 **무엇을 배제하는지** 물음.
⛔ **내가 기록을 파괴했고 `git show --stat` 의 삭제 줄 수가 그것을 드러냈음**(`H-BQ`) — 더하기만 한
변경에 삭제가 있으면 덮은 것임.
⛔ **낡은 `git status` 를 「지금 깨끗하다」의 근거로 썼고 남의 편집을 담아 갔음**(`H-BR`). ⚠️ **신호는
눈앞에 있었음** — 그 파일을 읽을 때 남의 결정이 이미 보였는데 「이미 커밋된 것」으로 설명해 버렸음.
⇒ **예상과 다른 것을 봤을 때 설명을 만들지 말고 그 자리에서 다시 잼.**
⚠️ **편집 뒤에 다시 재지 않으면 초록이 낡음** — 마지막 lint 뒤에 넣은 주석이 `E501` 을 만들었고
동료 세션이 찾았음.
⚠️ **파일명·수치에 계산값을 쓰지 않음 — 이 세션에서 두 번 밟았음**(백업 파일명의 시각을 두 번 다
`date` 실측과 다르게 적고 고쳤음). ⇒ **이름도 기록임.**
⛔ **화면 문구를 고쳤지만 렌더된 화면을 읽지 못했음 — 「확인했다」로 적지 않고 AC 를 열어 뒀음.**
