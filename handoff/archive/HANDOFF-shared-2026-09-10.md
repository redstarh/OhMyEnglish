# Handoff — OhMyEnglish

> ## 이 파일의 작성 원칙 (2026-09-10 사용자 지시 — 이후 계속 따른다)
>
> **두 가지만 담는다: ① 이번 세션에서 마무리한 것 요약 ② 다음 세션이 이어가는 데 필요한 핵심 정보.**
> ⛔ **누적하지 않는다.** 새 세션이 이 파일을 **새로 쓴다** — 지난 세션의 서술을 덧붙이지 않는다.
> ⛔ **새로 쓰기 전에 이전 판을 `handoff/backup/<날짜>/` 로 백업한다.** 새 판에는 **백업 경로만**
> 적고 그 내용을 옮기지 않는다 — **필요할 때만 그 백업을 참고한다.**
> 그 밖의 것은 정본을 가리킨다: 결정은 `docs/ops/captain-instruction-register.md` · 태스크 상태는
> 원장(`backlog task view <ID>`) · 관측은 `tests/harness/runs/**` · 함정은 `docs/ops/pitfalls.md`.
> ⛔ **줄 수·개수를 적지 않는다** — 적는 순간 낡는다(`H-O`).
>
> **이전 판 백업**: `handoff/backup/2026-09-10/HANDOFF-0815.md` (같은 날 앞선 판은 그 폴더의
> `HANDOFF.md`. 그 앞은 `2026-09-06` · `2026-08-30`). ⚠️ **같은 날 두 번 마감하면 이름이 겹친다** —
> 시각 접미사로 갈랐다.

최종 갱신 **2026-09-10** · 브랜치 `design/first-vertical-slice`

---

## ① 이번 세션(구현 갈래)에서 마무리한 것

**`TASK-80` 을 닫았다 (Done)** — SigV4 자격증명 쌍이 두 출처에서 섞이는 결함이다. **2라운드였다**:
첫 고침 뒤 codex 재리뷰가 **HIGH 1건**을 냈고 그것이 반대 방향의 같은 결함이었다(환경에 세션
토큰만 잔류 + `.env` 에 영구 키 쌍). ⛔ **그 결함은 내가 알고 남긴 축소였다** — 첫 판 docstring 이
*"환경에 남아 있는 잔여 토큰은 건드리지 않는다"*고 스스로 적었고 그 문장이 가리키는 상태가 정확히
그것이다. 재현하고 고쳤다.

**`TASK-73` 을 닫았다 (Done)** — 결정 48 이 인정한 등가 증거(뮤테이션 KILL 확인)를 **AC 항목 열넷
전부**에 붙였다. AC 문서 「선언 자리」 표의 항목 4 가 ✅ 로 올라갔다.

**`TASK-70` 을 닫았다 (Done)** — codex 재확인이 **approve** 했고 항목 6 이 ✅ 로 올라갔다.
⛔ **그래서 6항목이 전건 충족됐다. 그러나 선언 문장은 적지 않았다** — 6항목은 선언의 *조건*이고
선언 자체는 제품 이정표라 사용자 확인을 Slack 으로 요청했다. **확인이 오기 전까지 「Phase 1 이
선언됐다」로 인용하지 않는다.** 그 구별은 AC 문서의 「선언 자리」 절이 소유한다.

**`TASK-81` 은 AC#4 만 남기고 닫았다** — 계획 프롬프트와 세션 지시문 두 층을 고쳤다. ⛔ **그런데
착수 전 가정이 반증됐다**(아래 ②).

**`TASK-79` 는 AC#2 만 남기고 닫았다** — 프론트 상수를 없애고 응답 필드(`awaiting_analysis`)로
계약을 만들었다. codex 가 그 항목은 **지적 없이 approve** 했다.

⛔ **이 세션이 틀렸다가 고친 것 넷** — ⑴ 픽스처에 `an` 이 없어 발음 코칭이 불가능하다고 판단했다
(코치는 자기가 들은 `th_as_s`·`sh_sound` 를 지목한다) ⑵ 두 조건을 한 모집단으로 묶어 확률을
계산했고 **검정 방향까지 반대**였다 ⑶ 전사문이 영어로 복원되면 발음 오류가 사라진다고 봤다
(Nova 는 오디오를 직접 받는다) ⑷ 위 `TASK-80` 의 축소. **⑵·⑶ 은 테스트 갈래가 잡아 줬고 ⑷ 는
codex 가 잡았다** — 내 검증만으로는 걸리지 않았다.

## ①-2 테스트 갈래 절 — **그쪽 소유다. 내가 쓰지 않았다**

이전 판의 그 절은 백업(`HANDOFF-0815.md`)에 있다. 그쪽 이번 세션 산출(`TASK-82` P계층 이월분 ·
`TASK-84` 판정 대기 · `H-AX`)은 **그쪽 마감이 여기에 새로 쓴다.**

---

## ② 다음 세션이 이어가는 데 필요한 것

### 다음 한 걸음 — **사용자 확인이 오면 AC 문서에 선언 문장을 적는다**

6항목이 전건 충족됐고 그 근거는 AC 문서 「선언 자리」 표가 갖는다. **남은 것은 선언 문장 하나이고
그것은 사용자 확인 사안이다.** 확인이 오면 그 절에 적는다 — 원장에 상태를 두 벌 쓰지 않는다.

⚠️ **확인이 아직 없으면 이것을 기다리지 말고 아래를 진행한다.** 선언은 코드 작업을 막지 않는다.

⛔ **선언과 함께 알아야 할 것**: Phase 1 의 AC 는 충족되지만 **발음 복습 주기는 아직 돌지 않는다**.
발음 코칭이 어떤 프롬프트 조건에서도 일어나지 않기 때문이다(아래 「착수 전 필수」 2번). 그 둘은
다른 축이므로 「Phase 1 충족」을 「발음 기능 동작」으로 읽지 않는다.

### 그 밖에 열려 있는 것 — 전부 **다른 것을 기다린다**

| 태스크 | 남은 AC | 기다리는 것 |
|---|---|---|
| `TASK-81` | AC#4(앱 경로 코칭 확인) | **`TASK-75`** 의존을 걸었다. 규칙 9·11 이 풀리기 전에는 관측이 성립하지 않는다 |
| `TASK-79` | AC#2(지연 시나리오 계측) | **:8002 재기동**과 브라우저 레그. 프론트에 단위 테스트 도구가 없다(`package.json` 에 `test` 없음) |
| `TASK-78` | AC#3·#4 | 다른 세션 소유 |
| `TASK-84` | 판정 | **사용자 결정 대기** — 다른 세션이 등록했다 |

### 착수 전 필수

1. ⛔ **실행 중 백엔드는 낡은 코드다.** 이 리포의 백엔드는 `--reload` 가 없다 — 내가 커밋한
   `awaiting_analysis` 가 **응답에 아직 없다**(직접 조회해 확인). 재기동해야 반영된다. 재기동
   시점에 보존 세션 여섯의 응답 키가 하나씩 늘어난다.
2. ⛔ **`TASK-81` 의 결론을 다시 조사하지 않는다.** 발음 코칭이 나지 않는 원인은 계획 블록이
   **아니다** — 같은 시점 교차 대조에서 기준 팔도 0 이었다. 남은 층은 `SYSTEM_PROMPT` 규칙 9·11
   이고 **그 둘이 겹치지 않는 두 경로로 막는다**(9 는 문법 교정이 없는 턴에서도, 11 은 있는 턴에서
   상호배제로). 하나만 풀면 다른 하나가 남는다. 정본은
   `tests/harness/runs/2026-09-10-task81-pronunciation-focus.md` 다.
3. ⛔ **규칙 9·11 을 사용자 결정 없이 바꾸지 않는다** — 캡틴 결정 B-2 의 구현이다. 안 셋(계획이
   발음 초점을 지정한 세션만 완화 / 규칙 유지 / 발음 전용 모드)을 Slack 으로 올려 뒀다.
4. ⛔ **결정 50 의 순서 ③(우회로 제거)을 지금 실행하지 않는다.** 측정 결과가 tool 신호 0 이므로
   걷어내면 발음 기록이 통째로 사라진다.
5. ⚠️ **다른 세션이 같은 리포에서 일한다.** 매번 `ListAgents` 로 확인하고 `pytest` 전에 알린다
   (`H-X`). ⛔ **커밋은 경로를 열거해 스테이징한다**(`H-AM`).
6. ⚠️ **정적 검사는 편집 중 파일을 읽으면 유령 실패를 낸다** — `ty`·Pyright 모두. 내 변경과 무관한
   자리에서 실패가 나면 한 번 더 돌린다(다른 세션 실측, `cwd` 는 원인이 아니다).
7. ⚠️ **원장 날짜는 UTC 다.** `created_date`·`updated_date` 를 KST 로 읽으면 하루 어긋난다.

### 게이트 — 이 cwd 에서만 판정한다 (`H-A`)

```bash
cd app/backend
.venv/bin/pytest -q                        # ⛔ 동시 실행 금지 (H-X) · 경로만 주면 async 가 죽는다 (H-AJ)
.venv/bin/ruff check . && .venv/bin/ruff format --check .
/Users/redstar/.local/bin/ty check         # ⚠️ `.venv/bin/ty` 는 없다 — pipx 전역
.venv/bin/ruff check ../../tests ../../scripts    # 게이트 «밖» — 기준선 0
.venv/bin/ruff format --check ../../tests         # 게이트 «밖» — unformatted 0
cd ../frontend && npx tsc --noEmit && npx eslint app lib
```

⚠️ 게이트 전에 `brew services list | grep postgresql@17` — 안 떠 있으면 대량 errors 가 나는데
**회귀가 아니라 연결 거부다**(`H-T`).

### 인계 지표 — **직접 돌려** 얻고 대조한다

| # | 지표 | 2026-09-10 마감 시점에 직접 돌려 얻은 값 |
|--:|---|---|
| 1 | `git rev-parse --short HEAD` · `git status` | **`daa1070` 이상**(등호를 요구하지 않는다 — `H-P`) · `origin` 과 동기 · 내 미커밋 0건 |
| 2 | `grep -h "^status:" backlog/tasks/*.md \| sort \| uniq -c` | Done **64** · To Do **19** · In Progress **3**(`TASK-79`·`TASK-81` 내 것 · **`TASK-78` 다른 세션**) · Awaiting Decision **1**(`TASK-84` 다른 세션) |
| 3 | 게이트 | **897 passed** · `ruff check` exit 0 · format **34 files already formatted** · `ty check` 통과 · 게이트 **밖** `ruff` **0건** · format **100 files** · 프론트 `tsc`·`eslint` exit 0 |
| 4 | DB (읽기만) | `schema_migrations` **9** · `learning_sessions` **13** · `utterances` **120** · `error_occurrences` **24** · `session_plans` **2** · `pronunciation_attempts` **4** · `analysis_jobs` **49** — 여기까지 기준선 그대로. ⛔ **둘이 바뀌었다**: `error_patterns` **10**(기준선 9) · `review_tasks` **17**(기준선 15). **회귀가 아니라 다른 갈래의 P6 오탐 산출물이고 `TASK-84` 가 그 판정을 갖는다** |
| 5 | 결과 API 상태 6개 | `210233be` analyzing · `6225ddaf` final · `b2f0d169` partial_failure · `76d9ef31` connection_failed · `d127dece` no_utterances · `e0c5e580` final. ⛔ **여섯을 지우지 않는다.** ⚠️ **`awaiting_analysis` 는 아직 응답에 없다** — 위 「착수 전 필수」 1번 |

⚠️ **3번과 5번이 핵심이다** — 읽기는 전달을 증명하지 못하고 **직접 돌린 출력**만 데이터다.

### 이 리포의 지배 실패 모드

**"본문을 고치고 그것을 설명하는 문장을 안 고친다."** 개수를 말하는 문장은 **아예 세지 않는 서술로
바꾸는 것**이 실제로 통한 유일한 구조적 해법이다 — 이 세션에서 그것이 값어치를 했다(due 목록 개수를
적지 않아 다른 갈래가 그 표를 바꿨어도 내 기록이 낡지 않았다).

⛔ **이 세션이 얻은 파생 규칙 둘**:

- **내가 방금 적은 「하지 않는다」가 곧 미구현 선언일 수 있다.** `H-AL` 은 남이 적은 부정문을
  경계하는데, `TASK-80` 의 HIGH 는 **내가 그 커밋에서 적은** 부정문이 결함의 서술이었다.
  「건드리지 않는다」를 쓸 때 그것이 판단인지 공백인지 자문한다.
- **한 항목을 두 방향으로 무력화해야 방어가 다 드러난다.** `TASK-73` 의 W5 가 실증이다 — 상수를
  바꾸면 상수 단정만 FAIL 하고 거동 테스트는 **상수를 따라가 통과**한다. 비교 연산자를 바꾸면 그
  반대다. 하나만 재면 절반이 무보호다. W7·R1 도 같은 구조였다.

### 진입 절차

1. **원장을 조회해 태스크 ID 를 말한 뒤 착수한다.** 태스크가 없는 일이면 **먼저 등록한다.**
2. 게이트를 `app/backend` cwd 에서 돌려 지표 3 과 대조한다. **다르면 그 차이를 먼저 설명한다.**
3. 태스크 상태는 원장을, 다음 걸음이 바뀌면 이 파일을 갱신한다. 커밋 뒤 **푸시한다.**
4. ⛔ **태스크를 `Done` 으로 올린 같은 턴에 다음 태스크를 조회해 착수한다 — 보고로 턴을 끝내지
   않는다**(결정 43). **멈추는 조건은 셋뿐이다**: 승인 밖 항목 · 인계 트리거(≲35%·compact 경고 —
   이때는 멈추는 것이 아니라 **마감**) · 사용자가 멈추라고 한다.
