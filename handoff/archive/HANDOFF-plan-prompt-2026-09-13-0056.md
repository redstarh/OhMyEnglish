# Handoff — 계획 프롬프트·배치·시나리오 생성 갈래 · 세션 `ohmyenglish-f4` (마감)

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. 짧게 씀 — 판정 근거를 옮기지 않고
> 태스크 조회와 회차를 가리킴(`backlog task view <ID> --plain`).
>
> 이전 판은 `handoff/archive/HANDOFF-plan-prompt-2026-09-13-0024.md` 임(그 앞은 `…-0007.md`).
> ⛔ 다른 갈래의 handoff 를 건드리지 않았음 — `HANDOFF-pronunciation.md` 는 발음 축 소유임.

최종 갱신 2026-09-13 · 브랜치 `design/first-vertical-slice`

---

## ① 이 세션이 한 것

**닫은 태스크 셋**: `TASK-130`(설계 결함 판정) · `TASK-130.1`(그 결정의 구현) · `TASK-5`(계획 여섯 완료).
**받은 사용자 결정 하나**: **81** — 노출 순서를 `created_at` 에서 떼어내 `display_order` 로 옮김.
정본은 `docs/ops/captain-instruction-register.md` 임.

**제품에 들어간 것**: 마이그레이션 **019**(`display_order`) · 시드가 배열 자리를 1부터 매기고 upsert 가
그것을 갱신함 · 읽는 자리 넷을 옮김(후보 SQL · `Candidate`→`_staleness` ⑶ · `scenario_progress` 정렬) ·
`nova._SCENARIO_INTAKE_INSTRUCTION`(질문 다섯) · `build_system_prompt` 의 필수 인자 `scenario_intake` ·
`ws.py` 의 무대 정하기 진입과 세션 행 `mode` 기록 · 프런트 「질문 답변 5개」에 `mode` 추가.

**그 뒤 `TASK-102.1` AC#1·#3 도 닫았음** — 무대 정하기 파이프라인을 **앱 경로에서 종단으로** 쟀고
검증 전용 DB(`ohmyenglish_v102` · `:8022` · 스텁 · 워커 끔)를 쓰고 지웠음. 실물 Claude 로 **거부와
성공을 같은 회차에서** 쟀고 무대 한 행이 생겼음. `p5_worker_leg.py` 에 `generate_scenario` 분기를
더했음(마지막 갈래가 `else` 라 그 job 이 `process_analysis` 로 가서 영원히 `failed` 가 될 자리였음).

**회차 둘**: `runs/2026-09-12-task4-rotation-app-path.md` **§3-3·§3-4** ·
`runs/2026-09-13-task102-intake-pipeline/`(새로 씀 · 실행체 `intake_stub_leg.py` 포함).
**등록한 것**: 함정 `H-BR`(아래 ⑤) · `TASK-133`(생성 제목의 언어가 시드와 갈림).

## ② 다음 한 걸음 — ⛔ **사람의 결정 셋이 앞을 막고 있음**

**코드로 나아갈 자리가 없음. 셋 다 사용자 판단이고 그중 둘은 이 세션이 새로 올린 것임.**

1. **`TASK-102.1` AC#2** — 실물 Nova 1회로 「다섯을 **하나씩** 묻는가」를 잼. **마이크가 필요해
   승인 사안**임(결정 38·39 의 선례). ⛔ 그것 없이 그 문장을 참으로 적지 않음 — 배선과
   파이프라인은 참이고 **문면의 유도력만** 미검증임.
2. **`TASK-133` AC#1** — 무대 제목의 언어. 생성된 것은 **한국어**이고 시드 30행은 **영어**임.
   ⚠️ 한국어가 정본이면 **시드 30행이 틀린 쪽**이 되므로 범위가 달라짐. 학습자 노출 문구를
   승인 사안으로 다룬 선례가 있음(결정 83).
3. **`TASK-132` AC#1** — 파서 두 판을 하나로 합칠지(`Awaiting Decision` · 이전 세션이 올림).

**결정 없이 갈 수 있는 것**: `TASK-122`(빈 이름 키 · `low` · 되살릴 조건이 그 노트에 있음).
⚠️ 그 태스크는 「지금 고치지 않는다」로 이미 판정됐으므로 **되살릴 조건이 관측되기 전에는 손대지
않음** — 즉 실질적으로 잔여 작업이 결정 대기에 걸려 있음.

## ③ 착수 전 필수 — 7개

1. ⛔ 태스크를 새로 열기 전에 **결정 대장과 원장을 함께** `grep` 함(같은 요구가 두 태스크에 있었음).
2. ⛔ **부분 실행에 `-c pyproject.toml`** 을 붙임(`H-AJ`). 빼면 `asyncio` 모드가 안 걸려 거짓 빨강임.
3. ⛔ **게이트는 cwd `app/backend`**(`H-A`·`H-BN`). 리포 루트에서 부르면 `line-length` 를 88 로 잼.
4. ⛔ **`ty` 는 절대경로 `/Users/redstar/.local/bin/ty`** — 이름만으로는 **exit 127** 임.
5. ⛔ `git add <디렉터리>` 금지(`H-BE`) · 커밋 **전** `git diff --cached --name-only` · push **전**
   `git log --oneline origin/<브랜치>..HEAD`(`H-BO`). **그리고 공유 파일은 고친 턴에 커밋함**(`H-BR`).
6. ⚠️ 마이그레이션 번호는 적용 직전 `schema_migrations` 조회로 발급함(`H-AL`) — **이 마감 시점의
   최대는 `019`** 임(dev DB 에 적용됨).
7. ⛔ **`db_pool` 픽스처는 아무것도 정리하지 않음** — 손으로 지움. **사용자를 먼저** 지워야
   세션·발화·job 이 cascade 되고 그 뒤에 무대를 지울 수 있음(반대 순서는 FK 위반).

## ④ 인계 지표 — 이 마감 시점에 직접 돌려 얻음

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **이 handoff 를 담은 커밋 이상** — 그 앞이 **`224e54b`** 임. ⚠️ **등호를 요구하지 않음**: 이 표를 담은 커밋이 뒤에 붙고, 같은 브랜치에 발음 축이 커밋하면 더 앞섬. 다르면 `git log --oneline 224e54b..HEAD` 로 그 사이를 봄. **미커밋 0건** |
| 2 | 다음 걸음 | ⛔ **사람의 결정 셋**(②) — `TASK-102.1` AC#2 · `TASK-133` AC#1 · `TASK-132` AC#1. 코드로 나아갈 자리가 없음 |
| 3 | 게이트 | **전부 초록 · 종료코드까지 확인함** — `pytest` **1088 passed**(exit 0 · **동료 세션이 독립으로 센 값과 일치**) · `ruff check .` 0 · 게이트 밖 `ruff check` 0 · `ruff format --check` 0(206 files) · `ty` 0 · 하네스 둘 `--help` exit 0. ⚠️ 프런트(`tsc`·`eslint` exit 0)는 **이 회차에서 다시 재지 않았음** — 프런트 파일을 만지지 않았음 |
| 4 | 착수 전 필수 | 7개(③) · 원장 집계 To Do 12 · In Progress 8 · Awaiting Decision 1 · Done 125 |

⚠️ **`In Progress` 8건 가운데 여섯은 발음 축 것임**(`TASK-116`·`78`·`81`·`97`·`128`·`128.4`). 내 것은
`TASK-102`·`TASK-102.1` 둘임 — 그 축의 상태를 내가 고치지 않음.
⚠️ **`dev DB` 는 이 회차에서 오염되지 않았음** — teardown 뒤 조회로 `seed 30 · generated 0` ·
`scenario_intake` 세션 **0건**을 확인했음. 마이그레이션 최대는 여전히 **019** 임.

## ⑤ 이 세션이 얻은 규율

- ⛔ **전건 초록이 이행을 뜻하지 않음 — 축을 옮길 때 «옛 축을 재는 단정»을 남기지 않음.** 019 를
  넣고 앱 넷을 옮긴 직후 `pytest` 가 1080 passed 였는데 기존 단정들은 여전히 `created_at` 을 재고
  있었음. 통과해도 아무것도 지키지 않는 단정임.
- ⛔ **필터가 무엇을 배제하는지 먼저 봄.** 무력화 검증을 `-k "scenario"` 로 돌려 **9 passed** 를 보고
  「판별력 없음」으로 판정할 뻔했음 — 새 테스트 이름에 그 낱말이 없어 **안 돌았던 것**임. 통과
  개수가 그대로면 「안 깨졌다」가 아니라 「안 돌았다」일 수 있음.
- ⛔ **같은 파일을 두 세션이 고치면 먼저 커밋하는 쪽이 남의 편집을 담아 감**(`H-BR`). `git add` 에
  경로를 열거해도 그 파일 «안»의 남의 편집은 못 막고, 병합 충돌이 나지 않아 도구가 알려 주지 않음.
  드러난 신호는 **내가 고친 파일이 `git status` 에 없었던 것** 하나였음.
- ⚠️ **과거 감사 기록의 문면은 고치지 않고 «이후 변경»을 덧붙임.** 현재 코드에 맞추려 고치면 그
  회차의 증거가 훼손됨(`scenarios-E-agent-learning.md:169`).
- ⚠️ **설계서가 정하지 않은 자리를 만나면 규약이 가리키는 층에 둠.** 무대 정하기 세션에서 드릴
  질문·무대를 걷는 판단이 그것이고, 조립 규약 ⑵(소켓이 데이터로 정함)에 따라 소켓에 뒀음.
- ⛔ **관측이 두 기전을 함께 만족하면 그 관측은 근거를 못 가름.** `pick=new · source=generated` 는
  「생성이 먼저」와 「자리가 앞」이 같은 답을 내는 자리였음 — 한쪽을 **무력화(자리를 맨 뒤로)** 해서야
  이기는 것이 `is_generated` 라고 말할 수 있었음. 조건을 어긋나게 만들지 않은 관측은 단정이 아님.
- ⛔ **마지막 갈래를 `else` 로 두면 새 종류가 조용히 그리로 감.** 워커 실행체가 그랬고 새 job 종류가
  **5회 재시도 뒤 영원히 `failed`** 가 될 자리였음. 종류마다 **지목**하고 모르는 것은 거부함.
- ⚠️ **검증용 DB 는 소유자를 `ohmy` 로 만듦** — 다른 사용자로 만들면 `permission denied for schema
  public` 로 마이그레이션이 죽음(직접 밟았음).
