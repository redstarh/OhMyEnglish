# Handoff — 발음 축 · 세션 `ohmyenglish-7f`

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 판정 근거를 옮기지 않고
> **태스크 조회와 회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-pronunciation-2026-09-11-0857.md`(그 앞은 `…-0320` · `…-0250`).
> ⛔ **다른 갈래의 handoff 를 건드리지 않았음** — `HANDOFF-test-harness.md`(세션 `ohmyenglish-40`) ·
> `HANDOFF-implementation.md` · `HANDOFF-audit.md` 는 그쪽 소유임.

최종 갱신 **2026-09-11 09:0x KST** · 브랜치 `design/first-vertical-slice`

---

## ① 이번 세션이 한 것

사용자 지시 둘 — 「handoff 를 읽고 4지표를 대조한 뒤 `TASK-1` 의 접근을 골라 이어가라」 →
「결정이 필요한 것만 Slack 으로 보내고 계속 진행하라(자리 비움)」.

**닫은 태스크 셋**: `TASK-1`(접근 B — 일일 오류 요약 표 012 + 학습자 화면) ·
`TASK-100`(계획 거부 사유가 원인을 말하게 함 · 정체 원인 가설은 반증됨) ·
`TASK-98`(AC#4 판정 — 구별되지 않음).
**등록한 태스크 둘**: `TASK-104`(정체의 실제 원인 — 워커가 꺼진 채 운영됨) ·
`TASK-105`(소리 줄이 실린 프롬프트로 21회).
**받은 결정 하나**: 사용자가 `TASK-1` 을 **접근 B** 로 확정(2026-09-11).

⛔ **판정 넷만 옮김.**

1. **`session_plans` 정체의 원인은 JSON 거부 결함이 아님.** pending 15건이 전부 `attempts=0`·
   `last_error` null 이고 떠 있는 백엔드(:8002 · PID 25563)의 환경이 `WORKER_ENABLED=false` 임
   (`ps -Eww` 로 직접 확인). `TASK-104` 가 소유함.
2. **그 결함은 간헐적이고 원인 미확정임** — 같은 세션·같은 프롬프트로 2회 재시도해 둘 다 저장까지
   갔음(실패 1 · 성공 2). 예산 절단은 배제했음(`extract_text` 가 `stop_reason` 을 먼저 잡음).
3. **`TASK-81` 기전이 세션 지시문까지 닿음** — 사본 DB 에서 제품 경로로 계획을 다시 생성하니
   초점 첫 자리가 `pronunciation_an_as_a` 이고 제품 지시문 5,181자에 소리 줄이 **1건** 붙었음.
4. **「계획이 tool 도착을 바꾸는가」는 구별되지 않음**(`TASK-98` §9) — 표본이 작고, 더 근본적으로
   두 팔이 같은 분기에 있어 그 축이 경로에 닿지 않음. 표본을 늘려도 안 갈림.

**관측 정본**: `runs/2026-09-11-task98-production-prompt.md` §9(판정) · 태스크 노트
`TASK-1`·`TASK-100`. **새 함정 넷**: `H-BB`·`H-BC`·`H-BD`·`H-BE`(`docs/ops/pitfalls.md`).
**실물 호출**: Claude **5회**(계획 프로브 1 · 계획 저장 1 · 분석 3). Nova 왕복 **0회**.

## ② 다음 한 걸음 — **`TASK-105`**(21회 회차 · 지금 `To Do`)

`backlog task view TASK-105 --plain` 이 근거·선행·인수기준을 전부 가짐. 결정 53 이 승인한 범위임.

**착수 순서**: ⑴ 공유 dev DB 에서 계획을 다시 생성함(`p5_worker_leg.py` 새 판 · `--expect-session`
과 `--out` 둘 다 필수) ⑵ 저장된 초점과 제품 지시문의 소리 줄을 직접 조회로 확인함 ⑶ 21회를
기준선 팔과 교대로 돌림 ⑷ 판정은 `TASK-86` 에 넘김.

⚠️ **동료 세션(`ohmyenglish-40`)에 착수를 알렸음**(계획 재생성이 공유 학습 데이터를 건드림).
답이 오면 그 시점을 따름. 답이 없으면 보존 세션 여섯과 baseline 을 먼저 스냅샷으로 뜨고 진행함.

## ③ 착수 전 필수

1. ⛔ **계획을 다시 생성하는 순간 `TASK-98` 이 측정한 제품 프롬프트 4,505자가 낡음** — 모든 세션
   시작이 새 계획을 읽음. 그 사실을 회차 기록에 적음.
2. ⛔ **`pq*` 세트의 쓰임은 셋뿐임** — 대조군 · 0건이 유지돼야 하는 회귀 기준선 · ASR 복원력.
3. ⛔ **결정 52 를 먼저 읽음.** 「코칭이 드문 것」은 약한 발음 구간에서만 설계값임.
4. ⚠️ **게이트는 `app/backend` cwd 에서만 판정함**(`H-A`) — 리포 루트에는 ruff 설정이 없어 `E501`
   이 아예 켜지지 않음. `tests`·`scripts` 는 그 cwd 에서 경로를 지정해 따로 돌림(`H-L`).
5. ⚠️ **파이프 뒤 `$?` 는 `tail` 의 코드임**(`H-AZ`) — 종료 코드로 판정할 때 파이프를 걸지 않음.
6. ⚠️ **`git add <디렉터리>/` 를 쓰지 않음**(`H-BE` · 이 세션이 밟았음) — 파일을 열거함.
7. ⚠️ **CDP 에서 마이크가 열리지 않아 브라우저로 세션을 시작할 수 없었음**(`H-BD`) — 화면만 볼
   때는 서비스 함수로 전사문을 남기고 결과 화면을 봄.
8. ⛔ **`backlog task create` 의 반환 ID 를 읽고 씀** — 짐작하지 않음.

그 밖의 실측 함정은 `docs/ops/pitfalls.md` 가 소유함 — 여기에 복제하지 않음.

## ④ 인계 지표 — **직접 돌려** 얻고 대조함

| # | 지표 | 이 마감 시점 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`09bec77` 이상**(등호를 요구하지 않음 — 다른 세션이 커밋함). ⚠️ **`origin` 보다 4 앞섬** — 이 세션이 푸시하지 않았음 |
| 2 | 다음 걸음 | **`TASK-105`**(`To Do`). `backlog task view TASK-105 --plain` 을 먼저 돌림 |
| 3 | 게이트 | `pytest` **917 passed**(16.28s) · `ruff check` 안·밖 **All checks passed** · `format --check` **unformatted 0** · `ty` **All checks passed** · 프론트 `tsc`·`eslint` **exit 0**(파이프 없이) |
| 4 | 착수 전 필수 | **8개**(위 ③). `TASK-105` 의 `dependencies` **0건** · 미충족 AC **5건**(전부 미체크) |

⚠️ **format 의 「N files」는 `.md` 를 세므로 지표로 적지 않음**(`H-AR`).
⚠️ **DB**(공유 dev · 이 세션 마감 시점): `error_patterns` **9** · `review_tasks` **15** ·
`session_plans` **2**(2026-09-08 이 최신) · `analysis_jobs` **57**(pending 15) ·
`learning_sessions` **17** · `utterances` **128** · `daily_error_summary` **0**.
⛔ **표는 18 → 19 로 늘었음**(012 적용). 이 세션의 검증은 전부 **별 DB·별 포트**에서 했고 공유
DB 행 수 무변경을 대조했음. ⚠️ **미커밋에 내 것은 0건**임 — `runs/2026-09-10-*` 셋과
`handoff/archive/…-0857.md` 만 남음(앞 셋은 세션 `ohmyenglish-40` 것임).

## ⑤ 이 세션이 얻은 규율 — **「그 관측이 무엇을 배제하는가」를 사유 문구에도 적용함**

`TASK-100` 의 사유가 `raw[:200]` 만 담아 **두 원인을 구별할 수 없었음** — 즉 「내가 직접 돌렸다」가
아니라 **「이 사유가 무엇을 가릴 수 있는가」**가 문제였음. 고친 것은 결함이 아니라 그 눈먼 자리임.
같은 형태가 `models/analysis.py:223` 에 남아 있음(범위 밖이라 언급만 함).
⚠️ **`attempts=0` 을 「한 번도 시도되지 않았음」으로 단정하지 않았음** — 하네스 복원으로도 그 값이
만들어짐. 그래서 `WORKER_ENABLED=false` 를 `ps` 로 직접 확인해 두 번째 근거를 세웠음.
