# Handoff — 발음 축 · 세션 `ohmyenglish-19` (마감)

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 판정 근거를 옮기지 않고
> **태스크 조회와 회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-pronunciation-2026-09-12-1258.md`(그 앞은 `…-1013` · `…-0807`).
> ⛔ **다른 갈래를 건드리지 않았음** — `HANDOFF-test-harness.md`(`ohmyenglish-40`) ·
> `HANDOFF-plan-prompt.md`(`ohmyenglish-f4`)는 그쪽 소유임. 구현·감사 갈래는 앞 세션이
> `handoff/backup/2026-09-12/` 로 내렸음(`TASK-117`).

최종 갱신 **2026-09-12 12:39 KST**(`date` 로 확인함) · 브랜치 `design/first-vertical-slice`

---

## ① 이 세션이 한 것 — 태스크 다섯을 닫았고 «지시문으로는 못 막는다»를 확정했음

**닫음**: `TASK-111`(전용 지시문의 규칙 번호 참조) · `TASK-120`(판별 조건) · `TASK-103`(판정 호출의
옳은 문장이 저장되게 함) · **`TASK-118`**(결정 71) · **`TASK-123`**(결정 70).
**올림**: `TASK-116` 을 결함 «확정» 으로 바꾸고 `priority high` 로 뒀음 — 복습 일정이 오염됨.
**새로 세움**: `TASK-118` · `TASK-123` · `TASK-125` · **`TASK-128`**.
**받은 결정 셋**: **70**(키 강제를 뺀다) · **71**(전용 모드에서만 `Grammar first` 구절을 버린다) ·
**72**(계획이 오늘의 소리를 **이름으로 주지 않고 «후보»로만** 내려받는다 — 내가 물어 받았고 내가 대장에
기록했음). ⛔ 70·71 은 전달받은 것이라 **대장에서 직접 읽어** 확인한 뒤 착수했음.
**70·71 은 이행 완료 · 72 는 설계와 서브태스크까지** (`docs/design/2026-09-12-decision72-…md`).

**회차 기록 넷이 정본임**(§0 에 상한·해석 규칙을 **돌리기 전에** 적었음):

1. `runs/2026-09-12-task111-116-selfcontained-key.md` — REG **4/4** · KEY 팔은 조건부 키 규칙을
   **시험하지 못했음**(`pq05` 가 판별 조건을 못 만듦) · Nova 7세션.
2. `runs/2026-09-12-task120-absent-planted-sound.md` — **판별 조건을 세웠음**(오디오에 없는 소리를
   심음) ⇒ 조건을 **더한** 판이 **3/3 실패** · Nova 3세션.
3. `runs/2026-09-12-task103-target-form-storage.md` — **Nova 0세션.** TDD 로 저장을 고치고 **화면을
   직접 열었음**(`screen_fixed.png`) · 그 관측의 **반증 대조**까지(`screen_residual.png`).
4. `runs/2026-09-12-task123-118-subtract-key-forcing.md` — 결정 70·71 이행 · Nova 7세션. ARM-A
   **4/4**(회귀 없음 · 1,927→**1,660자**). ⛔ **ARM-B 는 강제를 «아예 뺐는데도» 3/3 으로 계획 키를
   기록했음** ⇒ **「지시문으로는 못 막는다」 확정**(두 방향 반증) · 계획의 소리가 **코칭 내용까지** 끎.

**제품에 들어간 것**: 규칙 11 자기완결 · 번호 참조 전면 제거 · **`target_sound` 강제 제거**(결정 70) ·
**규칙 9 구절의 모드별 칸**(결정 71) · `record_attempt` 판정 UPDATE 가 `target_form` 을 덮음.
**함정 셋을 영구 지식에 넣었음**: `H-BK`(`lsof -ti` 가 클라이언트까지 죽인다) · `H-BL`(`/tmp` 프론트
사본의 `.env.local`) · `H-BM`(무조건 대입이 CHECK 로 트랜잭션을 죽인다).

## ② 다음 한 걸음 — **`TASK-128.1`+`TASK-128.2` 를 한 묶음으로**(결정 72 이행)

⛔ **쪼개서 커밋하지 않음.** `build_pronunciation_prompt` 의 시그니처가 바뀌면 팩토리의 모드 판정
(`pronunciation_sound is not None`)도 **같은 커밋에서** 바뀌어야 함. `.3`(회차)까지 **같은 세션에서**
끝내는 것을 권고함 — 그 사이 바이트 게이트가 빨감.
⚠️ **범위 실측: 8파일 39곳**(`app` 4 · `tests` 4). 바꿀 일곱 자리를 `TASK-128.1` 노트가 **file:line
으로** 갖음 — 착수 전에 그것을 읽음.

**설계 정본**: `docs/design/2026-09-12-decision72-sound-as-candidate.md` — 무엇을 왜 바꾸는지(그리고
「후보 기제가 이미 있다」는 핵심)를 그 문서가 갖음. ⛔ 여기서 재서술하지 않음.
⛔ **`.3` 판별 팔의 성공 조건이 이전과 «반대»임** — 계획 키가 실리지 **않는** 것이 성공임.

**나란히 가능한 갈래**: `TASK-125`(둘 다 pending 봉투 — 눈 증거가 `screen_residual.png` 에 있고 사용자
결정 없이 감) · `TASK-129`(둘째 경로 관측) · `TASK-97` · `TASK-78` · `TASK-81`.

### ⛔ 사용자 결정을 기다리는 것 셋 — 이것을 먼저 올릴 것

| 태스크 | 무엇을 정해야 하나 |
|---|---|
| `TASK-97` AC#4 | 결정 49 를 유지할지 뒤집을지. ⛔ 받기 전에 `toolChoice` 강제를 제품 경로에 넣지 않음 |

⚠️ **셋이 답을 받아 이 표에서 내려갔음** — `TASK-123`·`TASK-118`(결정 70·71) · `TASK-128` AC#3(결정 72).
⛔ **`TASK-97` 하나만 남았음.**

**나란히 가능한 갈래**: `TASK-97`(선행 `TASK-103` 이 풀렸음 · `In Progress`) · `TASK-78` · `TASK-81` ·
`TASK-4`(주제 9종 + 70:30) · `TASK-128` AC#2(기록 경로를 코드로 보는 것은 결정 없이 가능함).

## ③ 착수 전 필수

1. ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/` 을 씀(`ty` 만 `~/.local/bin`).
   이 세션이 첫 호출에서 홈브루 3.14 로 잡혀 `No module named pytest` 를 봤음.
2. ⛔ **회차 상한과 «해석 규칙»을 돌리기 전에 회차 기록에 적음.** 이 세션은 셋 다 그렇게 했고
   `TASK-120` 에서 **그 덕에 Nova 2세션을 아꼈음**(ARM-C 를 미리 조건부로 예약해 뒀음).
3. ⛔ **미리 적은 해석 표에 결과를 «억지로 끼워 넣지 않음»** — `TASK-116` 의 KEY 팔이 넷째 모양이었고
   그것을 그대로 적었음. 끼워 넣으면 §0 을 먼저 적은 이유가 사라짐.
4. ⛔ **픽스처의 오류와 「오늘의 소리」가 맞는지 «먼저» 확인함.** 정본은
   `scenarios-PQ-qwen-phoneme.md`. ⚠️ `pq05` 는 판별용으로 **쓸 수 없음**(`TASK-120` 노트).
5. ⛔ **`pytest` 에 경로를 주지 않음**(`H-AJ`) · 게이트는 `app/backend` cwd(`H-A`) · `tests`·`scripts`
   는 경로 지정(`H-L`) · 파이프 뒤 `$?` 금지(`H-AZ`) · `ls -t` 는 셰임(`H-BG` — 또 밟았음).
6. ⛔ **`git add <디렉터리>/` 금지**(`H-BE`) · index 는 공유 자원이라 커밋 **뒤** `git show --stat`
   확인(`H-BA`). 이 세션은 커밋 셋 다 `git diff --cached --stat` 을 **먼저** 봤고 남의 파일 0건이었음.
7. ⚠️ **검증 스택은 공유 자원을 건드리지 않음** — 검증 전용 DB + `:8012` + 필요하면 `/tmp` 프론트
   사본. 끝나고 **행 수 무변경을 대조함**(이 세션 회차 셋 다 다섯 표 무변경).
   ⛔ **정리를 «주장하지 않고» 다시 읽음**(`H-BK` · `H-BL`).
8. ⚠️ **회차 디렉터리의 실행체는 `.py.txt` 로 보관함** — `.py` 로 두면 `ty.toml` 의 `include`(`tests`)와
   게이트 밖 `ruff` 가 집어 부채가 됨(`H-AV`).
9. ⛔ **`tests`·`scripts` 도 «`app/backend` cwd 에서» 부름**(`H-BN` — 기전은 플래그가 아니라 **cwd**
   임. 루트에 `pyproject.toml` 이 없어 88 로 떨어짐) → `check`·`format --check` **둘 다 baseline 0.**
   ⛔ 루트 cwd 의 수십 건을 baseline 이라 부르지 않음 — 그렇게 읽어 **내 E501 3건을 놓쳤음.**
   ⚠️ **ruff 에는 `-c` 가 없음**(exit 2) — `-c` 는 `pytest` 것임(`H-AJ`).

## ④ 인계 지표 — 이 마감 시점에 **직접 돌려** 얻음

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`984042f` 이상** · `origin` 과 **동기**(`0 0` · 사용자 승인으로 푸시했음) · ⚠️ 동료 세션이 계속 커밋하므로 **등호를 요구하지 않음** |
| 2 | 다음 걸음 | **`TASK-128.1`+`TASK-128.2` 묶음**(둘 다 `To Do`) — 위 ②. `In Progress` 는 `TASK-116`(high · 선행 `TASK-128`) · `78` · `81` · `97` · `128` |
| 3 | 게이트 | 전부 `app/backend` cwd · 파이프 없이 · **여섯 다 `exit 0`**: `pytest` **1006 passed**(11.85s) · `ruff check .` **0** · `format --check .` **38 files** · `ruff check ../../tests ../../scripts` **0** · `format --check ../../tests ../../scripts` **152 files** · `ty` **0**. 프론트는 이 세션에서 한 줄도 바꾸지 않았고 앞 실측이 `tsc` **0줄** · `eslint` **0줄** |
| 4 | 착수 전 필수 | **9개**(위 ③) |

⚠️ **오늘 쓴 Nova**: 이 세션 **17세션** · 누적 **150**(`TASK-103`·`TASK-128` 설계는 0).
⛔ **공유 dev DB 에 내가 남긴 변경 0건** — 검증 전용 DB 여섯을 지웠고 회차마다 다섯 표를 **전후로
대조**했음: `learning_sessions=17`·`pronunciation_attempts=7`·`error_patterns=9`·`session_plans=6`·`learner_notes=7`.
⚠️ **`pytest` 984 → 1006 과 마이그레이션 `013`·`014` 는 동료 세션 것임**(후자는 사용자 승인 ·
00:36 UTC). ⛔ **둘 다 drift 로 읽지 않음.**

## ⑤ 이 세션이 얻은 규율

⛔ **판별력 없는 단정을 세 번 잡았음.** ⑴ `pq05` 가 판별 조건을 못 만들어 시험이 성립하지 않았음 —
새 픽스처 대신 **조건을 바꿨음**. ⑵ 빈 값 가드 테스트가 고침 전에도 통과했음 — **순진한 판을 일부러
넣어 빨간 것을 본 뒤** 초록을 믿었음. ⑶ 화면 검사에 **반증 대조**를 붙였음.
⛔ **한 축을 「두 방향 다 반증」으로 닫았음.** 앞 회차에서 절반만 반증됐을 때 단정을 **일부러
보류했고** 그 보류가 이번에 값을 냈음.
⚠️ **결정이 「목적 미달성」으로 끝나도 그 결정을 무효로 만들지 않음** — 되돌리지 않고 「그 근거로
인용하지 말라」를 게이트 독스트링에 뒀음.
⛔ **적은 수치 하나가 «직접 돌린 값이 아니었음»**(`985` 로 적고 실측 `984`) — 정정을 덧붙이고 **지우지
않았음.** ⚠️ 전달받은 결정·판정도 **직접 읽고 직접 돌려** 확인했음(결정 대장 · `H-BN` 판별 실험).
⛔ **잘못된 자로 잰 값을 baseline 이라 불러 내 위반 5건을 두 번 놓쳤고 둘 다 동료 세션이 찾았음.**
