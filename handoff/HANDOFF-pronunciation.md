# Handoff — 발음 축 · 세션 `ohmyenglish-7f` 후속 (마감)

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 판정 근거를 옮기지 않고
> **태스크 조회와 회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-pronunciation-2026-09-12-0807.md`(그 앞은 `…-0215` · `…-0043`).
> ⛔ **다른 갈래의 handoff 를 건드리지 않았음** — `HANDOFF-test-harness.md`(세션 `ohmyenglish-40`) 는
> 그쪽 소유임. ⚠️ **구현·감사 갈래는 미사용으로 판정해 `handoff/backup/2026-09-12/` 로 내렸음**
> (사용자 지시 2026-09-12 · 근거는 그 폴더 `README.md` · `TASK-117`).

최종 갱신 **2026-09-12 08:07 KST**(`date` 로 확인함) · 브랜치 `design/first-vertical-slice`

---

## ① 이 세션이 한 것 — 발음 축이 «막힌 결함»에서 «작동하는 기능»으로 넘어갔음

**받은 결정**: `57`·`58`·`60`·`61`·`63`·**`64`**(전용 모드를 제품 코드로) · **`65`**(계획 출력
계약 유지 — 사용자가 위임한 결정). 사용자 지시 둘도 이행했음 — **Qwen3 로 테스트 시나리오** ·
**추가 결정은 내 권고안으로 진행**. 정본은 `docs/ops/captain-instruction-register.md` 임.

**회차 기록 여덟이 정본**(원인 규명 → 구현 → 검증이 한 줄로 이어졌음):

1. `runs/2026-09-11-task86-sound-shaped-questions.md` — 질문 모양을 고쳐도 tool 0/7.
2. `…-task86-prompt-composition.md` — 규칙 문면·위치를 고쳐도 0/6·0/6.
3. `…-task86-dedicated-session.md` — **짧은 판에서 tool 6/8**.
4. `…-task86-length-boundary.md` — **자수가 아니라 대화 규칙 1~7 이 덮음**(③ 판정을 정정).
5. `runs/2026-09-12-task86-app-leg.md` — **앱 경로 2/2** · 복습 시계가 섰음 ⇒ `TASK-86` 닫음.
6. `runs/2026-09-12-task10-2-entry-browser-leg.md` — 진입 화면을 **브라우저로** 확인.
7. `runs/2026-09-12-task114-mild-band-dedicated.md` — **중간 지대 4/4 코칭** · 대조군은 어휘로 오라우팅.
8. `runs/2026-09-12-task115-pq13-replacement.md` — `pq04` 실패는 **소리가 아니라 문장** 때문이었음.

**닫은 태스크**: `TASK-86` · `TASK-10` · `TASK-10.1` · `TASK-10.2` · `TASK-114` · `TASK-115` ·
`TASK-109`(결정 65) · `TASK-26` AC#1.
**제품에 들어간 것**: `?mode=pronunciation`(전용 지시문 1,702자) · `?source=additional` ·
추가 학습 메뉴 여섯 · `session_started.pronunciation_focus`(**키의 부재가 폴백 신호**) ·
`learning_source` 기록.
**자산**: `pq02a`~`pq08a` 정답판 여섯 + `pq13`/`pq13a` · `ws_session.py --mode/--wav/--url`.

## ② 다음 한 걸음 — `TASK-111` + `TASK-116` 을 **한 묶음으로**

⛔ **둘을 따로 고치면 회차를 두 번 쓴다.** 전용 지시문은 `test_nova.py` 의 **바이트 대조 게이트**로
실측 판(4/4)에 묶여 있어 문면을 고치면 그 게이트가 깨지고 회차를 다시 돌려야 함.

- **`TASK-111`** — 규칙 11 이 이 프롬프트에 **없는 규칙 4** 를 가리킴(측정된 문면이라 그대로 뒀음).
- **`TASK-116`** — 기전은 이미 코드로 확정했음: 소리 줄이 *"put `{sound}` in target_sound both
  times"* 로 **계획의 키를 강제**함(`nova.py:235`). 그래서 코칭한 소리와 기록된 소리가 어긋남.
- ⛔ **그 회차의 해석 규칙에 「고친 판이 4/4 보다 나빠지면 되돌린다」를 «먼저» 적음** — 표본 4 로는
  열등을 배제하지 못함.

**나란히 가능한 갈래**: `TASK-97`(`In Progress` · `target_form` 에 무너진 전사 — 같은 지시문을
건드리므로 위 묶음과 함께 설계하면 회차를 한 번 더 아낌) · `TASK-4`(주제 9종 + 70:30) ·
`TASK-108`(값싼 경로를 노트에 적어 뒀음 — `last_error` 문면을 세면 과거 발생률이 나옴) · `TASK-112`.

## ③ 착수 전 필수

1. ⛔ **회차 상한과 «해석 규칙»을 돌리기 전에 회차 기록에 적음.** 이 세션은 여덟 회차 가운데 일곱을
   그렇게 했고 그 규칙이 판정을 갈랐음. ⚠️ 여덟째(`TASK-115`)는 상한을 **명령줄에만** 적고 돌렸음 —
   그 어긋남을 그 회차 §0 에 적었음.
2. ⛔ **표본 1~8 의 0 을 「안 된다」로 읽지 않음** — `P(0 in 8 | 0.13)=0.33`. 낮은 비율이 배제된 것은
   **누적 76** 에서였음(`P(0 in 76 | 0.05)=0.020`).
3. ⛔ **픽스처의 오류와 「오늘의 소리」가 맞는지 «먼저» 확인함** — 정본은
   `scenarios-P-pronunciation.md:75~76`(p계층) · `scenarios-PQ-qwen-phoneme.md`(pq계층 · 정답판 규약
   §4.5 · `pq04` 대체 §4.6). 이 확인을 빠뜨려 앞 세션이 21회를 낭비했음.
4. ⛔ **`pytest` 에 경로를 주지 않음**(`H-AJ`) · 게이트는 `app/backend` cwd(`H-A`) · `tests`·`scripts`
   는 경로 지정(`H-L`) · 파이프 뒤 `$?` 금지(`H-AZ`).
5. ⛔ **`git add <디렉터리>/` 금지**(`H-BE`) · index 는 공유 자원이라 커밋 **뒤** `git show --stat`
   확인(`H-BA` — 이 세션이 한 번 밟았음: `e650edf`).
6. ⚠️ **검증 스택은 공유 자원을 건드리지 않음** — 검증 전용 DB(`createdb`+`migrate.py`) + `:8012` +
   필요하면 `/tmp` 프론트 사본(`cp -Rc`)·CORS 래퍼(`H-BC`). 끝나고 **행 수 무변경을 대조함.**
7. ⚠️ **zsh 함정 둘** — 무인용 변수는 단어 분할되지 않음(`${pair%%:*}` 꼴을 씀) · 노트에 백틱을 쓸
   때는 따옴표 붙인 heredoc 을 명령 치환으로 감쌈(`H-BI` · 그 형태를 직접 시험해 통과 확인했음).

**이 세션이 더한 함정 다섯**: `H-BF`(Nova 55초 타임아웃이 **증거 없이 표본을 줄임**) ·
`H-BG`(`ls -t` 셰임) · `H-BH`(`error_patterns.category` 는 `pronunciation_intonation`) ·
`H-BI`(셸 인용이 노트를 삼키거나 남김) · `H-BJ`(**마지막 단계가 끝난 뒤에 다시 읽어야 함** — 시점이
이르든 늦든 틀림). 그리고 ⛔ **`H-BD` 를 좁혔음 — 마이크는 열 수 있음**(전용 Chrome 을
`--use-fake-device-for-media-stream` 으로 띄움).

## ④ 인계 지표 — 이 마감 시점에 **직접 돌려** 얻음

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`caa981e` 이상** · `origin` 과 **동기**(`0 0`) · 미커밋 **0건**. ⚠️ 이 handoff 를 담는 커밋이 뒤에 붙고 동료 세션도 계속 커밋하므로 **등호를 요구하지 않음** |
| 2 | 다음 걸음 | **`TASK-111`+`TASK-116` 묶음**(`둘 다 To Do`) — 위 ②. `In Progress` 는 `TASK-97`·`78`·`81`(전부 내 갈래) |
| 3 | 게이트 | `pytest` **964 passed**(17.13s) · `ruff` **0** · `format --check` **0** · `ty` **0** · 프론트 `tsc` **0** · `eslint` **0** — 전부 파이프 없이 |
| 4 | 착수 전 필수 | **7개**(위 ③) |

⚠️ **원장**(직접 세었음): 전체 **121** · 완료 **102** · `In Progress` **3** · `To Do` **16**.
⚠️ **오늘 쓴 Nova**: 이 세션 **63세션**(누적 **133**) · Claude 계획 생성 2회 · Qwen3-TTS 생성 8건
(**로컬 모델 · API 비용 0**).
⛔ **공유 dev DB 에 내가 남긴 변경 0건** — 검증 전용 DB 다섯을 만들어 쓴 뒤 전부 지웠음.
⚠️ **다만 공유 DB 는 그 뒤 «동료 세션이» 바꿨음**(`TASK-104` 계획 job 소화 · 사용자 승인) —
`session_plans` 3→**6** · `learner_notes` 4→**7** · pending **11**(계획 4 · 분석 7) · 최신 계획
`2026-09-11 22:41`. ⛔ **그 변화를 drift 로 읽지 않음.**

## ⑤ 이 세션이 얻은 규율

⛔ **판정을 네 번 정정했고 넷 다 남겼음.** ⑴ 「원인은 프롬프트 규모」 → 「대화 규칙 1~7 이 덮음」
⑵ 회차 기록의 `target_sound` 열이 내 추출기 정규식 오류였음 ⑶ 「연결 0 이니 복습이 안 돈다」 →
`review.py` 는 `target_sound` 로 매칭하므로 틀린 읽기였음 ⑷ `H-BI` 에 적은 대응 한 구절이 같은 항목
안에서 모순이었음(동료 세션이 고치고 **내가 직접 시험해 확인**했음).
⛔ **부분 제거가 새 고장을 만듦** — 규칙 4 만 뺐더니 모델이 자기 절차를 낭독했음.
⚠️ **변이 검사를 구현마다 돌렸음**(3회) — 초록을 믿기 전에 일부러 깨뜨려 그 테스트가 잡는지 봤음.
⚠️ **창이 짧은 것을 「부재」로 오독할 뻔했음** — 「없다」를 적기 전에 **관측 창이 그것을 볼 수 있었는지**
먼저 물음.
⚠️ **번호를 인용만 하고 쓰지 않았음** — 결정 64 를 태스크 노트에서 인용했는데 대장에 없었고, 새 결정을
64 로 적으려다 충돌을 발견해 65 로 밀고 그 자리를 채웠음.
