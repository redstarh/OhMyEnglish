# Handoff — 발음 축 · 세션 `ohmyenglish-7f` 후속

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 판정 근거를 옮기지 않고
> **태스크 조회와 회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-pronunciation-2026-09-12-0215.md`(그 앞은 `…-0043` ·
> `…-09-11-1837`).
> ⛔ **다른 갈래의 handoff 를 건드리지 않았음** — `HANDOFF-test-harness.md`(세션 `ohmyenglish-40`) ·
> `HANDOFF-implementation.md` · `HANDOFF-audit.md` 는 그쪽 소유임.

최종 갱신 **2026-09-12 02:15 KST**(`date` 로 확인함) · 브랜치 `design/first-vertical-slice`

---

## ① 이번 세션이 한 것 — 발음 축이 «막힌 결함»에서 «작동하는 기능»으로 넘어갔음

**받은 결정 일곱**: `57`(지렛대는 계획 생성) · `58`(주 경계 월요일) · `60`(프롬프트 구성 실험) ·
`61`(전용 세션을 먼저 재기) · `63`(「짧다」의 경계) · `64`(전용 모드를 제품 코드로) · 그리고 사용자
지시 둘(**Qwen3 로 테스트 시나리오** · **추가 결정은 내 권고안으로 진행**).
정본은 `docs/ops/captain-instruction-register.md` 임.

**원인 규명 → 구현 → 검증이 한 줄로 이어졌음** (회차 기록 일곱이 정본):

1. `runs/2026-09-11-task86-sound-shaped-questions.md` — 질문 모양을 고쳐도 tool 0/7.
2. `…-task86-prompt-composition.md` — 규칙 문면·위치를 고쳐도 0/6·0/6.
3. `…-task86-dedicated-session.md` — **짧은 판에서 tool 6/8**.
4. `…-task86-length-boundary.md` — **자수가 아니라 대화 규칙 1~7 이 덮음**(③ 판정을 정정).
5. `runs/2026-09-12-task86-app-leg.md` — **앱 경로 2/2** · 복습 시계가 섰음 ⇒ `TASK-86` 닫음.
6. `runs/2026-09-12-task10-2-entry-browser-leg.md` — 진입 화면을 **브라우저로** 확인.
7. `runs/2026-09-12-task114-mild-band-dedicated.md` — **중간 지대 4/4 코칭** · 대조군은 어휘로 오라우팅.

**닫은 태스크 여섯**: `TASK-86` · `TASK-10` · `TASK-10.1` · `TASK-10.2` · `TASK-114` · `TASK-26` AC#1.
**등록한 태스크 일곱**: `TASK-108`·`109`(계획 검증 결함) · `111`(규칙 11 참조 정리는 재측정 선행) ·
`112`(`mode` 값역) · `114`(닫음) · **`115`**(pq04 문장이 모델을 코딩 조수로 끌어당김) ·
**`116`**(발화로 코칭한 소리와 `target_sound` 가 어긋남).

**제품에 들어간 것**: `?mode=pronunciation`(전용 지시문 1,702자) · `?source=additional` ·
추가 학습 메뉴 여섯 · `session_started.pronunciation_focus`(**키의 부재가 폴백 신호**) ·
`learning_source` 기록. **자산**: `pq02a`~`pq08a` 정답판 여섯 · `ws_session.py --mode/--wav/--url`.

## ② 다음 한 걸음 — `TASK-115` → `TASK-116` → `TASK-97`

`In Progress` 셋이 전부 내 갈래임: **`TASK-97`**(`target_form` 에 무너진 전사) · `TASK-78` · `TASK-81`.

**권고 순서**:
1. **`TASK-115`** — `pq04` 문장이 모델을 코딩 조수 역할로 끌어당겨 **/r/ 축이 아직 미지**임. 문장을
   바꾼 새 픽스처를 만들면(Qwen3-TTS · 같은 화자 · `lang_code=en`) 1회로 닫힘. ⛔ `pq04` 를 덮지 않음.
2. **`TASK-116`** — 발화로 코칭한 소리와 기록된 `target_sound` 가 어긋남. `TASK-97` 과 같은 축의
   새 형태이므로 둘을 함께 보는 것이 값어치 있음.
3. **`TASK-97`** — 남은 결함은 `target_form` 에 무너진 전사가 실리는 것 하나로 좁혀졌음.

**나란히 가능한 갈래**: `TASK-4`(주제 9종 + 70:30) · `TASK-108`·`109`(계획 생성이 두 번 연속 거부됨) ·
`TASK-112`(`mode` 값역 — 지금 전용 세션이 `speaking` 으로 집계됨).

## ③ 착수 전 필수

1. ⛔ **회차 상한과 «해석 규칙»을 돌리기 전에 회차 기록에 적음.** 이 세션은 일곱 회차 전부 그렇게
   했고 그 규칙이 실제로 판정을 갈랐음(0 을 「무효」로 읽지 않게 막았음).
2. ⛔ **표본 1~8 의 0 을 「안 된다」로 읽지 않음** — `P(0 in 8 | 0.13)=0.33` 임. 낮은 비율이 배제된
   것은 **누적 76** 에서였음(`P(0 in 76 | 0.05)=0.020`).
3. ⛔ **픽스처의 오류와 「오늘의 소리」가 맞는지 «먼저» 확인함** — 정본은
   `scenarios-P-pronunciation.md:75~76`(p계층)과 `scenarios-PQ-qwen-phoneme.md`(pq계층 · 정답판
   규약은 그 §4.5)임. 이 확인을 빠뜨려 앞 세션이 21회를 낭비했음.
4. ⛔ **`pytest` 에 경로를 주지 않음**(`H-AJ`) — 주면 async 테스트가 전부 죽고 「13 failed」로 보임.
5. ⛔ **게이트는 `app/backend` cwd**(`H-A`) · `tests`·`scripts` 는 경로 지정(`H-L`) · 파이프 뒤 `$?`
   금지(`H-AZ`) · `git add <디렉터리>/` 금지(`H-BE`) · index 는 공유 자원이라 커밋 **뒤**에
   `git show --stat` 으로 확인(`H-BA` — 이 세션이 한 번 밟았음: `e650edf`).
6. ⚠️ **검증 스택은 공유 자원을 건드리지 않음** — 검증 전용 DB(`createdb`+`migrate.py`) + `:8012` +
   필요하면 `/tmp` 프론트 사본(`cp -Rc`)·CORS 래퍼(`H-BC`). 끝나고 **행 수 무변경을 대조함.**
7. ⚠️ **zsh 는 무인용 변수를 단어 분할하지 않음** — `set -- $pair` 로 인자를 쪼개려다 세션 4건이
   빈손으로 죽었음. `${pair%%:*}` 꼴을 씀.

**새 함정 넷을 이 세션이 더했음**: `H-BF`(Nova 는 오디오 간격 55초를 넘기면 죽고 **증거 파일도 남지
않아 표본이 조용히 줄어듦**) · `H-BG`(`ls -t` 셰임) · `H-BH`(`error_patterns.category` 의 발음 값은
`pronunciation_intonation` 임) · 그리고 ⛔ **`H-BD` 를 좁혔음 — 마이크는 열 수 있음**(전용 Chrome 을
`--use-fake-device-for-media-stream` 으로 띄움. 원 서술은 지우지 않았음).

## ④ 인계 지표 — 이 마감 시점에 **직접 돌려** 얻음

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`4f891d4` 이상** · `origin` 과 **동기**(`0 0`) · 미커밋 **0건**. ⚠️ 이 handoff 를 담는 커밋이 뒤에 붙으므로 **등호를 요구하지 않음** |
| 2 | 다음 걸음 | **`TASK-115`**(`To Do` · AC 4건) — 권고 순서는 위 ②. `In Progress` 는 `TASK-97`·`78`·`81` |
| 3 | 게이트 | `pytest` **964 passed**(16.72s) · `ruff` **0** · `format --check` **0** · `ty` **0** · 프론트 `tsc` **0** · `eslint` **0** — 전부 파이프 없이 |
| 4 | 착수 전 필수 | **7개**(위 ③) |

⚠️ **원장**(직접 세었음): 전체 **121** · 완료 **99** · `In Progress` **3**(전부 내 갈래) · `To Do` **19**.
⚠️ **오늘 쓴 Nova**: 이 세션 **61세션**(앞 세션 70 포함 누적 **131**) · Claude 계획 생성 2회 ·
Qwen3-TTS 생성 6건(**로컬 모델 · API 비용 0**).
⛔ **공유 dev DB 에 남긴 변경 0건** — 검증 전용 DB 넷(`pronmode`·`uicheck`·`pqmild`·`pqsounds`)을
만들어 쓴 뒤 전부 지웠고 회차 앞뒤 행 수(`17`·`7`·`9`)를 대조했음.

## ⑤ 이 세션이 얻은 규율

⛔ **판정을 세 번 정정했고 셋 다 남겼음.** ⑴ 「원인은 프롬프트 규모」 → 「대화 규칙 1~7 이 덮음」
⑵ 회차 기록의 `target_sound` 열이 내 추출기 정규식 오류였음 ⑶ 「연결 0 이니 복습이 안 돈다」 →
`review.py` 는 `target_sound` 로 매칭하므로 틀린 읽기였음.
⛔ **부분 제거가 새 고장을 만듦** — 규칙 4 만 뺐더니 모델이 자기 절차를 낭독했음. 그래서 전용 모드는
규칙 1~7 을 **통째로** 대체함.
⚠️ **변이 검사를 구현마다 돌렸음** — 초록을 믿기 전에 일부러 깨뜨려 그 테스트가 잡는지 봤음(3회).
⚠️ **창이 짧은 것을 「부재」로 오독할 뻔했음** — 진입 안내를 폴링으로 놓쳤고 `MutationObserver` 로
잡았음. 「없다」를 적기 전에 **관측 창이 그것을 볼 수 있었는지** 먼저 물음.
