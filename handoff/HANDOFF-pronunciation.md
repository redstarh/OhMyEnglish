# Handoff — 발음 축 · 세션 `ohmyenglish-19` (마감)

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 판정 근거를 옮기지 않고
> **태스크 조회와 회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-pronunciation-2026-09-12-1013.md`(그 앞은 `…-0807` · `…-0215`).
> ⛔ **다른 갈래를 건드리지 않았음** — `HANDOFF-test-harness.md`(세션 `ohmyenglish-40`) ·
> `HANDOFF-plan-prompt.md`(세션 `ohmyenglish-f4` · 2026-09-12 신설)는 그쪽 소유임.
> ⚠️ 구현·감사 갈래는 앞 세션이 `handoff/backup/2026-09-12/` 로 내렸음(`TASK-117`).

최종 갱신 **2026-09-12 12:39 KST**(`date` 로 확인함) · 브랜치 `design/first-vertical-slice`

---

## ① 이 세션이 한 것 — 태스크 다섯을 닫았고 «지시문으로는 못 막는다»를 확정했음

**닫음**: `TASK-111`(전용 지시문의 규칙 번호 참조) · `TASK-120`(판별 조건) · `TASK-103`(판정 호출의
옳은 문장이 저장되게 함) · **`TASK-118`**(결정 71) · **`TASK-123`**(결정 70).
**올림**: `TASK-116` 을 결함 «확정» 으로 바꾸고 `priority high` 로 뒀음 — 복습 일정이 오염됨.
**새로 세움**: `TASK-118` · `TASK-123` · `TASK-125` · **`TASK-128`**.
**받은 결정 둘**: **70**(키 강제를 뺀다) · **71**(전용 모드에서만 `Grammar first` 구절을 버린다).
⛔ 전달을 그대로 받지 않고 **대장에서 직접 읽어** 확인한 뒤 착수했음
(`docs/ops/captain-instruction-register.md:221`·`:236`).

**회차 기록 넷이 정본임**(§0 에 상한·해석 규칙을 **돌리기 전에** 적었음):

1. `runs/2026-09-12-task111-116-selfcontained-key.md` — REG **4/4**(기준선과 같음) · Nova 7세션.
   KEY 팔은 조건부 키 규칙을 **시험하지 못했음**(`pq05` 가 판별 조건을 못 만듦).
2. `runs/2026-09-12-task120-absent-planted-sound.md` — **판별 조건을 세웠음**: 오디오에 /f/ 가 없는
   문장(`pq13`)에 `f_as_p` 를 심음 ⇒ 코치는 `early` 의 `er` 을 코칭했고 기록은 **`f_as_p` ×3**.
   ⛔ **조건부 키 규칙이 막지 못함이 확정됐음.** ARM-C 는 §0 의 조건대로 **돌리지 않았음**(Nova 3세션).
3. `runs/2026-09-12-task103-target-form-storage.md` — **Nova 0세션.** TDD 로 저장을 고치고 **화면을
   직접 열었음**(`screen_fixed.png`). 그 관측이 **반증할 수 있는지**도 확인했음(`screen_residual.png`).
4. `runs/2026-09-12-task123-118-subtract-key-forcing.md` — 결정 70·71 이행 · Nova 7세션.
   ARM-A **4/4**(회귀 없음 · 프롬프트 1,927→**1,660자** · 번호 참조 **0**). ⛔ **ARM-B 는 강제를 «아예
   뺐는데도» 계획의 키 `f_as_p` 를 3/3 기록했음** ⇒ **「지시문으로는 못 막는다」가 확정됐음**
   (더하기·덜기 두 방향이 모두 3/3 반증). ⛔ 그리고 계획의 소리가 **코칭 내용까지** 끌어당겼음.

**제품에 들어간 것**: 규칙 11 자기완결 · 번호 참조 전면 제거 · **`target_sound` 강제 제거**(결정 70) ·
**규칙 9 구절의 모드별 칸**(결정 71 · `_GRAMMAR_FIRST_CLAUSE`) · `record_attempt` 판정 UPDATE 가
`target_form` 을 덮음.
**함정 셋을 영구 지식에 넣었음**: `H-BK`(`lsof -ti` 가 클라이언트까지 죽인다) · `H-BL`(`/tmp` 프론트
사본의 `.env.local`) · `H-BM`(무조건 대입이 CHECK 로 트랜잭션을 죽인다).

## ② 다음 한 걸음 — `TASK-125`

**`TASK-125`** — 두 tool 호출이 «모두 pending» 인 봉투에서 행이 둘 열리고 무너진 전사가 화면에 남음.
`TASK-103` 이 닫지 못한 나머지이고 **눈 증거가 이미 있음**(`screen_residual.png` — 카드 두 장 ·
한쪽 「시범 문장」이 오발음). ⛔ **사용자 결정 없이 갈 수 있음** — AC#2 가 「정하고 근거를 적는다」임.
⛔ **표시로 덮지 않음**(그 태스크 AC#2) · 씨앗은 `seed_residual.py.txt` 를 **그대로 씀**.

### ⛔ 사용자 결정을 기다리는 것 셋 — 이것을 먼저 올릴 것

| 태스크 | 무엇을 정해야 하나 |
|---|---|
| `TASK-128` AC#3 | 계획이 준 소리를 「오늘 다룰 소리」가 아니라 **「후보」로 내려받을지.** 그러면 코치가 실제 오류를 고르고 기록도 그것을 따름 — ⛔ **제품 요구사항 변경이라 팀리드가 정하지 않음** |
| `TASK-97` AC#4 | 결정 49 를 유지할지 뒤집을지. ⛔ 받기 전에 `toolChoice` 강제를 제품 경로에 넣지 않음 |

⚠️ **`TASK-123`·`TASK-118` 의 결정은 받았고 이행했음**(결정 70·71) — 이 표에서 내렸음.

**나란히 가능한 갈래**: `TASK-97`(선행 `TASK-103` 이 풀렸음 · `In Progress`) · `TASK-78` · `TASK-81` ·
`TASK-4`(주제 9종 + 70:30) · `TASK-128` AC#2(기록 경로를 코드로 보는 것은 결정 없이 가능함).

## ③ 착수 전 필수

1. ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/` 을 씀(`ty` 만 `~/.local/bin`).
   이 세션이 첫 호출에서 홈브루 3.14 로 잡혀 `No module named pytest` 를 봤음.
2. ⛔ **회차 상한과 «해석 규칙»을 돌리기 전에 회차 기록에 적음.** 이 세션은 셋 다 그렇게 했고
   `TASK-120` 에서 **그 덕에 Nova 2세션을 아꼈음**(ARM-C 를 미리 조건부로 예약해 뒀음).
3. ⛔ **미리 적은 해석 표에 결과를 «억지로 끼워 넣지 않음»** — `TASK-116` 의 KEY 팔이 넷째 모양이었고
   그것을 그대로 적었음. 끼워 넣으면 §0 을 먼저 적은 이유가 사라짐.
4. ⛔ **픽스처의 오류와 「오늘의 소리」가 맞는지 «먼저» 확인함.** 정본은
   `scenarios-PQ-qwen-phoneme.md`. ⚠️ `pq05` 는 판별용으로 **쓸 수 없음**(`TASK-120` 노트).
5. ⛔ **`pytest` 에 경로를 주지 않음**(`H-AJ`) · 게이트는 `app/backend` cwd(`H-A`) · `tests`·`scripts`
   는 경로 지정(`H-L`) · 파이프 뒤 `$?` 금지(`H-AZ`) · `ls -t` 는 셰임이라 빈 출력(`H-BG` — 이 세션이
   또 밟았음).
6. ⛔ **`git add <디렉터리>/` 금지**(`H-BE`) · index 는 공유 자원이라 커밋 **뒤** `git show --stat`
   확인(`H-BA`). 이 세션은 커밋 셋 다 `git diff --cached --stat` 을 **먼저** 봤고 남의 파일 0건이었음.
7. ⚠️ **검증 스택은 공유 자원을 건드리지 않음** — 검증 전용 DB + `:8012` + 필요하면 `/tmp` 프론트
   사본. 끝나고 **행 수 무변경을 대조함**(이 세션 회차 셋 다 다섯 표 무변경).
   ⛔ **정리를 «주장하지 않고» 다시 읽음**(`H-BK` · `H-BL`).
8. ⚠️ **회차 디렉터리의 실행체는 `.py.txt` 로 보관함** — `.py` 로 두면 `ty.toml` 의 `include`(`tests`)와
   게이트 밖 `ruff` 가 집어 부채가 됨(`H-AV`).

## ④ 인계 지표 — 이 마감 시점에 **직접 돌려** 얻음

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`cc8c017` 이상** · `origin` 과 `1 0`(내가 1 앞섬 · **미푸시**) · ⚠️ 이 handoff 를 담는 커밋이 뒤에 붙으므로 **등호를 요구하지 않음** |
| 2 | 다음 걸음 | **`TASK-125`**(`To Do`) — 위 ②. `In Progress` 는 `TASK-116`(high · 선행 `TASK-128`) · `78` · `81` · `97` |
| 3 | 게이트 | `pytest` **1006 passed**(12.52s) · `ruff check` **0** · `format --check` **38 files** · `ty` **0** — **넷 다 `exit 0`** · 파이프 없이. 프론트는 이 세션에서 한 줄도 바꾸지 않았고 앞 실측이 `tsc` **0줄** · `eslint` **0줄** |
| 4 | 착수 전 필수 | **8개**(위 ③) |

⚠️ **오늘 쓴 Nova**: 이 세션 **17세션**(REG 4 · KEY 3 · ARM-T 3 · ARM-A 4 · ARM-B 3) · 누적 **150**.
`TASK-103` 은 **0**.
⛔ **공유 dev DB 에 내가 남긴 변경 0건** — 검증 전용 DB 여섯(`_t111`·`_t111k`·`_t120`·`_t103`·
`_t123a`·`_t123b`)을 만들어 쓴 뒤 전부 지웠고, 회차마다 다섯 표를 **전후로 대조**했음
(`learning_sessions=17` · `pronunciation_attempts=7` · `error_patterns=9` · `session_plans=6` ·
`learner_notes=7`).
⚠️ **`pytest` 가 984 → 1006 으로 늘어난 것은 동료 세션이 테스트를 더한 것임** — 내 변경만의 수치가
아님. ⛔ drift 로 읽지 않음.
⚠️ **동료 세션(`ohmyenglish-f4`)이 마이그레이션 `013`·`014` 를 발급하고 공유 DB 에 적용했음**(사용자
승인 · 00:36 UTC). ⛔ **그 변화를 drift 로 읽지 않음.**

## ⑤ 이 세션이 얻은 규율

⛔ **판별력 없는 단정을 세 번 잡았음.** ⑴ `pq05` 가 판별 조건을 못 만들어 조건부 키 규칙을 시험할 수
없었음 — 새 픽스처 대신 **조건을 바꿨음**(오디오에 없는 소리를 심음). ⑵ 빈 값 가드 테스트가 고침
전에도 통과했으므로 무력했음 — **순진한 판을 일부러 넣어 빨간 것을 본 뒤** 초록을 믿었음. ⑶ 화면
검사에 **반증 대조**를 붙였음(무너진 전사를 담은 세션을 같은 화면에 띄워 드러나는 것을 봤음).
⛔ **한 축을 「두 방향 다 반증」으로 닫았음** — 조건을 더해도 3/3, 강제를 빼도 3/3. 앞 회차에서는
절반만 반증됐으므로 단정을 **일부러 보류했고** 그 보류가 이번에 값을 냈음.
⚠️ **결정이 「목적 미달성」으로 끝나도 그 결정을 무효로 만들지 않음** — 결정 70 은 사용자가 골랐고
회귀가 없으므로 되돌리지 않았음. 대신 「기록이 코칭과 맞는다」의 근거로 **인용하지 말라**를 게이트
독스트링에 뒀음.
⛔ **적은 수치 하나가 «직접 돌린 값이 아니었음»** — `985 passed` 로 적고 실측은 `984` 였음. 같은 턴에
정정을 덧붙이고 **지우지 않았음**(`TASK-103` 노트 끝).
⚠️ **전달받은 결정을 그대로 쓰지 않았음** — 대장에서 직접 읽어 문면과 출처를 확인한 뒤 착수했음.
⚠️ **게이트 «안»이 내 주석 때문에 빨갰음**(`E501` 2건 + 포맷 1건) — 주석도 게이트 대상임.
