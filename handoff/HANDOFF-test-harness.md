# Handoff — 테스트 하네스 축 (주인 세션 없음 · 최종판)

> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. **짧게 씀** — 근본 원인·실측 수치·판정
> 근거를 여기 옮기지 않고 **태스크 조회와 회차 기록을 가리킴**(`backlog task view <ID> --plain`).
>
> **이전 판**: `handoff/archive/HANDOFF-test-harness-2026-09-14-2253.md`(그 앞은 `…-2026-09-12-0746`
> · `…-09-11-2228`(110줄) · `…-2100`(122줄) · `…-1837` · `…-0740` ·
> `HANDOFF-shared-2026-09-10.md` · `handoff/backup/2026-09-10/`).
> ⛔ **이전 판을 지우지 않았음** — 뒤집힌 결정의 경위가 그 안에만 있을 수 있음.
>
> ⛔ **이 판은 이 축의 주인 세션이 아니라 `realtime-meeting-b2` 가 썼음** (사용자 지시 2026-09-14).
> 이유는 **주인 세션 `ohmyenglish-40` 이 종료됐고 그 뒤 이 파일이 이틀 낡았기 때문임** — tmux 창
> 일곱 개 가운데 OhMyEnglish 창이 0개이고 에이전트 목록에도 그 세션이 없음(직접 확인).
> ⚠️ **판정을 새로 하지 않았음.** 이 판이 하는 것은 «지금 상태를 측정해 적는 것» 뿐임.

최종 갱신 **2026-09-14 22:53 KST** · 브랜치 `design/first-vertical-slice`

---

## ① 인계 지표 4개 — 이 턴에 직접 돌린 출력

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`19568ab`** · `origin/design/first-vertical-slice` 와 **0/0** · 미커밋은 미추적 `paseo.json` 하나뿐 |
| 2 | 다음 한 걸음 | **`TASK-138`** 「테스트 코드에 필요 없어진 것과 겹치는 것이 있는지 감사한다」 · `In Progress` · **AC 넷 전부 미충족** |
| 3 | 게이트 | `app/backend` cwd · 파이프 없이 · **넷 다 exit 0**: `pytest` **1191 passed**(13.36s) · `ruff check .` · `ruff format --check .` **48 files** · `ty check` |
| 4 | 착수 전 필수 | **AC #3 이 사용자 승인을 요구함**(아래 ③). ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/` 이고 `ty` 만 `~/.local/bin` 임 |

```bash
cd ~/MyProject/OhMyEnglish
git rev-parse --short HEAD
backlog task view TASK-138 --plain
cd app/backend && ./.venv/bin/pytest -q && ./.venv/bin/ruff check . && ~/.local/bin/ty check
```

⚠️ **zsh 는 변수를 단어로 쪼개지 않음** — `for c in "check ."; do ruff $c; done` 이 exit 2 로 죽음(이
턴에 실측). 게이트는 명령을 그대로 나눠 쓴다. ⛔ `pytest` 에 경로를 주지 않음(이전 판 `H-AJ`).

## ② ⛔ 이 파일이 낡았던 사이에 일이 계속 갔음

이전 판의 최종 갱신은 **2026-09-12 08:10** 이고 그 뒤 **커밋 164건**이 쌓였음. 그 가운데 `tests/` 를
만진 것이 다수임 — 가장 최근 둘이 `TASK-138` 자체의 작업임.

    2f7b904  test(TASK-138): 낡은 개수 서술 넷을 고쳤음 — 시드가 3에서 30으로 늘며 두 번 낡았음
    c82b68e  test(TASK-138): 발음 기록 출처 값역 검사에 024 의 새 값을 더했음 — 내가 만든 구멍이었음

⛔ 즉 **이 축은 죽지 않았고 다른 세션이 이어받아 진행했음.** 이전 판 ①의 「이번 세션이 마무리한
것」(닫은 태스크 여덟)은 그 시점 기록으로만 읽어야 함. ⛔ 이 판은 그 164건을 다시 판정하지 않음 —
상태 정본은 원장이고 근거 정본은 태스크 노트와 `runs/` 임.

⚠️ **「시드가 3에서 30으로 늘며 개수 서술이 두 번 낡았다」가 이 축의 반복 함정임** — 테스트에 개수를
적을 때 그 수가 낡을지 먼저 생각한다. 낡을 것이면 세지 않는 서술로 바꾼다.

## ③ 다음 한 걸음의 인수기준 — 원장에서 그대로 옮김

    #1 필요 없어진 테스트 후보를 근거와 함께 열거한다 — 삭제된 대상·뒤집힌 결정·낡은 서술 셋으로 가른다
    #2 겹치는 테스트 후보를 열거한다 — 같은 성질을 두 번 재는 것과 파라미터화로 묶을 수 있는 것을 구별한다
    #3 지울 것과 남길 것을 사용자 승인으로 정한다 — 승인 없이 지우지 않는다
    #4 지운 뒤 게이트를 돌려 개수 변화를 기록한다 — 줄어든 수와 남은 수를 직접 돌린 출력으로 적는다

⛔ **AC #3 이 사람의 승인을 요구함** — 열거까지는 혼자 하고 삭제 앞에서 멈춘다. 지금 기준선은
`pytest` **1191 passed** 이므로 AC #4 는 이 수와 대조한다.

## ④ 이 축 밖의 열린 것 — 헷갈리지 않게 적어 둠

원장의 잔여 14건 가운데 이 축은 `TASK-138` 하나뿐임. 나머지는 **발음 축 9건**
(`HANDOFF-pronunciation.md` 가 표로 가짐)과 별 축 넷(`TASK-61` 음성 명령 · `TASK-39` botocore 재시도 ·
`TASK-41` 전용 스키마 이관 · `TASK-129` 관측)임.

## ⑤ 다른 갈래를 건드리지 않았음

이 리포의 갈래 파일 넷 가운데 **이 파일만** 고쳤음. `HANDOFF-integration-test.md`(오늘 21:55 마감) ·
`HANDOFF-plan-prompt.md`(오늘 10:03 마감)는 최신이라 손대지 않았고, `HANDOFF-pronunciation.md` 는
같은 지시로 **따로** 최종판을 만들었음.
