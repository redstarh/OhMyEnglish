# Handoff — OhMyEnglish

> **두 가지만 담는다: 다음 한 걸음 / 착수 전 필수.** 그 밖은 각 정본이 소유하고 여기서는 **가리키기만** 함.
> 최종 갱신 **2026-09-08** · 브랜치 `design/first-vertical-slice`
> ⛔ **줄 수를 적지 않는다** — 적는 순간 낡는다(`H-O`).

---

## 다음 한 걸음 — **`TASK-37`(하네스 5차수) `In Progress`. 회차 정본은 회차 파일임**

> 정본은 **`tests/harness/runs/2026-09-09-run-5.md`** 이고 브라우저 다리는
> **`…-run-5-browser.md`** 임. **여기서 진행 상태를 재서술하지 않음** — 그 두 파일과 원장이 가짐.
>
> ✅ **캡틴 결정 38 (2026-09-09) — 실물 Nova·마이크 세션 1회 승인.** 결정 24 가 걸었던 「구현 3건
> 뒤」 조건이 `TASK-43`·`44`·`45` 의 `Done` 으로 충족됐음. 제약 4개는 등록부 결정 38 이 소유함.
> ⛔ **워커 금지가 여기서만 풀림** — 그 한 세션의 구간 B 안에서만 켜고 즉시 되돌림. 그 밖에서는
> 여전히 금지임.
> ⛔ **`TASK-13`·`24`·`36` 은 이제 `To Do` 임**(결정 38 로 되돌렸음). 선행 `TASK-37` 은 유지됨 —
> 그 관측을 5차수 세션 안에서 치름. **다시 `Awaiting Decision` 으로 되돌리지 않음.**
>
> ⚠️ **착수 전에 `H-AS` 를 읽음** — 문서 기동 명령이 워커를 켠다. 지금 도는 백엔드가 올바른
> 플래그로 떠 있는지 **프로세스에서** 확인함(`/health` 는 그것을 노출하지 않음).
>
> ⛔ **막는 것은 그대로임**: `TASK-23`·`42`·`38` 착수 금지(결정 28·29·30) ·
> critic 재검증 루프 재개 금지.
>
> **`TASK-45` 는 `Done`(2026-09-09)** — 남은 UI 하나는 `TASK-10` 이 소유하고 그 노트에 선점
> 목록이 있음. `TASK-40` 도 선행이 풀렸음.

> ⚠️ **한국어 문체가 바뀌었음** (2026-09-08 사용자 결정). 정본은
> `~/.claude/rules/korean-writing-standard.md` §6 이고 **명사형 `~함` · 주체 명시 · 이모지 0건**임.
> 반말 규칙은 **폐기됐음.** 대화 응답에도 적용됨. ⛔ 이 파일도 그 문체로 씀.
> 긴 글을 커밋·발송하기 전에는 `korean-proofread` 스킬로 훑음.


## 게이트 명령 — 이 cwd 에서만 판정한다 (`H-A`)

```bash
cd app/backend
.venv/bin/pytest -q                        # ⛔ 동시 실행 금지 (H-X) · 경로만 주면 async 가 죽는다 (H-AJ)
.venv/bin/ruff check . && .venv/bin/ruff format --check .
/Users/redstar/.local/bin/ty check         # ⚠️ `.venv/bin/ty` 는 **없다** — pipx 전역이다
.venv/bin/ruff check ../../tests ../../scripts    # 게이트 «밖» — 기준선 0 (지표 3)
.venv/bin/ruff format --check ../../tests         # 게이트 «밖» — 기준선 0
```

⚠️ **`ty` 경로에서 세 세션이 각각 헛짚었다.** `.venv/bin/ty` 는 `no such file` 이 나는데
**회귀가 아니라 경로다.** `ruff`·`pytest` 는 venv 안이라 `.venv/bin/` 을 붙인다.
⛔ **`tests/`·`scripts/` 를 건드렸으면 게이트 «밖»도 반드시 다시 잰다.** 2026-09-08 세션에서
이 형태가 **반복해서** 났다 — 안쪽만 보고 밖이 늘어난 것을 놓쳤고 매번 되돌렸다(개수를 적지
않는다: 세는 순간 낡는다).
⚠️ **기준선이 2026-09-09 에 0 으로 바뀌었음** — `TASK-37` AC#6 이 `tests/harness/` 넷
(`inject_errors.py`·`measure_contrast.py`·`spike_nova_protocol.py`·`ws_session.py`)을 정리했음.
이전 판의 「유지값 6건·4건을 지키라」는 서술은 **낡았음.** 지금은 **늘어나는 것만 회귀**이고
「줄이는 것도 회귀」였던 이유(그 6건의 소유자가 미착수 태스크였음)는 사라졌음.
⛔ **그래도 `ruff check --fix` 를 디렉터리에 걸지 않음 — 내가 건드린 파일에만 검.** 그 규약은
살아 있음: `--fix ../../tests/` 를 돌려 범위 밖 파일이 고쳐진 사고가 2026-09-08 에 있었음.
⚠️ **그리고 게이트 밖 수치가 0 이 된 것이 스크립트가 도는 것을 증명하지 않음** — import 재정렬을
했으면 네 스크립트에 `--help` 를 돌려 exit 0 을 직접 확인함(2026-09-09 에 그렇게 확인했음).

## 끝난 일의 기록 — 다시 하지 마라

**`TASK-45`(쉐도잉 과제 — `Done` 2026-09-09)** · `TASK-44`(발음 복습 주기) · `TASK-43`(복습 과제
히스토리 · 010) · `TASK-35`(기본값 테스트 환경 격리) · `TASK-33`(`nova.py` 낡은 개수).
커밋: `9df2369`→`57f67f8`→`99d41f1`(44) · `d0dca09`→`2b90c40`(43) · `ca72a4a`(35) · `5a3b660`(33) ·
`e456195`→`9b773e2`→`74a0097`→`6421a30`→`237b64e`→`7f31dd5`→`c004aec`→`0e18ce7`→`730154d`→
`5709695`→`2f57fbc`(45).

⚠️ **`TASK-45` 에서 얻은 것 중 다음 태스크에 그대로 쓰이는 것 하나** — **판별력 확인을 절차로
돌리면 실제 결함이 나옴.** 이 태스크에서 「무력화해도 통과하는 단정」을 **세 번** 만났고(세션 경계
SQL · 삭제의 `active` 예외 · uuid 순서에 달린 flaky · 낭독 턴 정리 미검증 4건) 전부 실제 구멍이었음.
방법: 고친 곳을 하나씩 되돌려 **대응 단정 1건만** red 가 되는지 잼. ⛔ `PYTHONDONTWRITEBYTECODE=1`
로 돌림(`H-AP`) — 크기가 같은 변형은 stale pyc 로 조용히 틀린 결과를 냄.

리뷰 보고서는 `.superpowers/sdd/2026-09-07-scenario-and-drill-turns-design/` 에 있음
(`review-task4{3,4}-report.md` + 각 `-rereview-report.md`). ⚠️ **그 디렉터리는 git 추적 밖임 —
지우지 않음.** 💡 다음 리뷰를 위임하기 전에 **`H-AO`** 를 읽음 — 그 프롬프트 형식과, 위임 리뷰가
분석은 다 하고 산출물만 잃는 실패를 막는 규약이 거기 있음.

⚠️ **`review_tasks` 는 이제 이력임.** 패턴당 (사이클 × 도달 단계)행이고 재계산이 지우지 않고
자연키로 upsert 함. 「패턴당 0행 또는 1행」을 전제한 서술을 만나면 그것이 낡은 것임. 값역은
`pending`·`done`·`abandoned`·`superseded` 이고 **`skipped` 는 제거됐음**(학습자 행동은 이 표
밖에 두고 `TASK-3` 이 만듦).

⚠️ **기본값을 재는 테스트는 격리돼 있음**(`TASK-35`). `Settings` 를 만드는 테스트에
`_env_file=None` 이 붙어 있고, 앱의 `get_settings()` 를 지나는 통합 테스트는 `tests/conftest.py` 의
**`pin_settings_env`** 로 **선언된 기본값에 못 박음**(지우기만 하면 `.env` 파일이 그 자리를 채움 —
실측). ⛔ 그것을 지우면 개발자의 `app/backend/.env` 가 기본값 단정을 조용히 무의미하게 만듦.

## 착수 전 필수

| # | 무엇 | 정본 |
|--:|---|---|
| 1 | **받은 캡틴 결정을 다시 묻지 않음.** 개수를 세지 않음 — register 가 열거함. 특히 **22**(설계·문서는 리뷰 없이 `Done` · **코드는 아니다**) · **26**(행 삭제 승인 + 팀리드 제약) · **27**(008·002 **영구 결번** · **번호를 미리 예약하지 않는다**) · **28·29**(DB 비밀번호·AWS 키 **실행 금지**) · **30**(PUBLIC 유지 ≠ push 승인) · **31**(스키마 이관은 전부 끝난 뒤) · **33**(010 개발 DB 적용은 **리뷰 통과 후** · 절차는 26) | `docs/ops/captain-instruction-register.md` |
| 2 | ⛔ **critic 재검증 루프를 다시 돌리지 않음.** 4회차까지 갔고 결정 22 가 설계·문서의 리뷰 게이트를 없앴음. 4회차 findings 는 줄 번호를 **제거**해 부류째 닫았음(`2ea2b7f`) | `.superpowers/.../critic-reverify-4-report.md` + 그 끝의 **팀리드 정정** |
| 3 | **설계는 끝났음 — 다시 하지 않음.** `TASK-6`·`9`·`25`·`27` 전부 `Done`. 구현 소유자 중 `TASK-44`·`TASK-43` 도 **`Done`** 이고 남은 것은 **`TASK-45`**(막혔다 — 위 상자) | 원장 |
| 4 | ⛔ **`git push`·원격 연결을 하지 않음.** ⚠️ **이 줄이 2026-09-09 에 바뀌었음** — `TASK-42`·`TASK-38` 은 **`Done`** 임(결정 41 로 **회전 면제** + DB 비밀번호를 `.env` 로 옮기고 추적 문서에서 제거. 추적 파일 평문 **0건** 확인). **그런데 push 차단은 그대로임**: 회전하지 않았으므로 값이 **git 이력에 남아 있고**(`7a5bfce`) 저장소 설정이 **PUBLIC** 임(결정 30). `TASK-23` 을 **`Awaiting Decision`** 으로 옮겨 브리핑이 「착수 가능」으로 올리지 못하게 했음 — 남은 선택지 셋(회전 · 이력 재작성 · 스쿼시)은 **캡틴 몫**임. ⛔ **「문서가 깨끗해졌으니 push 해도 된다」로 읽지 않음** | 결정 41 · 30 |
| 5 | ⚠️ **`backlog task list --ready` 는 상태를 걸러내지 않는다** — `Awaiting Decision` 과 `In Progress` 태스크까지 "착수 가능"으로 뜬다(두 세션이 각각 실측했다). **SessionStart 브리핑의 분류를 믿고, `--ready` 는 의존만 본다고 읽음** | 이 파일 |
| 6 | **워커를 켜지 않음**(비용 + `browser_leg.md` §9 보존 세션이 깨짐). 실물 호출은 결정 24 로 구현 뒤로 미뤘음 | 결정 20·24 |
| 7 | **함정을 안다.** `H-AO`(⛔ **위임 리뷰가 산출물만 잃고 죽음** — 나눠 쓰게 지시하고, 실패하면 `SendMessage` 로 이어 씀) · `H-AM`(⛔ 병렬 dispatch — 같은 워킹트리면 커밋이 서로의 스테이징을 집는다. 대응은 `git commit -- <path>`. **리뷰어가 읽는 중인 파일은 건드리지 않고 기다림** — 더 나은 대응은 **먼저 커밋해서 리뷰어에게 커밋 범위를 주는 것**임) · `H-AJ`(pytest 에 경로만 주면 async 가 죽음 → `-c pyproject.toml`) · `H-AL`(완료 태스크의 낡은 노트) · `H-AN`(게이트 밖 6건은 **`ruff` 수치다** — `ty` 로 재현하면 0) · `H-X`(⛔ **동시 `pytest` 금지**) · **`H-AP`**(⛔ **판별력 확인이 stale `__pycache__` 를 남김** — 파일에는 새 값이 보이는데 테스트는 낡은 값으로 실패함. 크기가 같은 변형은 같은 초에 복원하면 pyc 검증을 통과함. **판정은 파일 내용이 아니라 `import` 결과로** 하고 `PYTHONDONTWRITEBYTECODE=1` 로 돌림) | `docs/ops/pitfalls.md` |
| 8 | **DB 는 공유 인스턴스임**(`:5432`, StockAgent·En-Coach 와). 재시작·`ALTER SYSTEM`·`ALTER DATABASE … SET TimeZone` 금지 | `docs/ops/shared-database-guide.md` |
| 9 | ⚠️ **`.superpowers/**` 는 git 추적 밖임** — SDD 원장과 critic·리뷰 보고서 **전부**가 거기 있음. **지우지 않음** | `.superpowers/sdd/2026-09-07-scenario-and-drill-turns-design/` |

---

## 이 세션이 남긴 미결 — 원장이 상태의 정본이다

**`In Progress`**: **없음.** `TASK-45` 가 `Done` 이 됐음(2026-09-09).
⚠️ **미커밋 변경이 있는데 `In Progress` 가 0건이면 Stop 훅 G1 이 턴을 막음** — 착수할 태스크를
먼저 `In Progress` 로 올림.
**`Awaiting Decision`** — 개수를 적지 않음. 열거가 정본임: `TASK-13`·`24`·`36`(실물 호출 ·
결정 24 로 구현 뒤) · `TASK-38`(AWS 키 · 결정 29) · `TASK-42`(DB 비밀번호 · 결정 28).

⚠️ **관측 파이프라인이 한 줄이다:** `TASK-37`(하네스 5차수 · **선행이 풀렸다**) →
{`TASK-13`·`24`·`30`·`36`}. 네 건의 관측을 **전부 `TASK-37` 이 이미 AC 로 갖고 있다** — 각각
실물 세션을 켜면 비용을 네 번 낸다.

⚠️ **마이그레이션**: **002·008 은 결정 27 로 영구 결번**이고 앞으로 번호를 **미리 예약하지
않는다**(쓰는 턴에 `ls db/migrations/` 로 최대값을 다시 확인한다). 실측 최대는 **011**이다.
**010·011 둘 다 개발 DB 에 적용됐음**(`schema_migrations` 에 `001·003~007·009·010·011`).
`review_tasks` **10행** · `error_patterns.next_review_at` **8건 전부** · `shadowing_items` **1행**
(시드) · `utterances` **114행 유지**.
⚠️ **011 적용은 2026-09-09 · 캡틴 결정 36 이었음.** 이전 판의 「011 은 파일만 있고 적용되지
않았음 — 의도한 상태임」은 **낡았음.** 적용 전후 대조는 `TASK-45` 원장 노트가 소유함.
⛔ **이제 `utterance_type` 값역 축소가 되돌리기에서 실패할 수 있음** — `shadowing_recording` 행이
생기는 순간부터임(설계서 §4.6).

⚠️ **`TASKS.md` 는 절 제목 + 포인터만 남았다**(결정 32 · `da66f3e`). 내용 정본은
`docs/design/2026-09-08-tasks-md-archive.md`. **파일을 지우지 않음** — 인용이 살아 있음.

---

## 인계 지표 — **직접 돌려** 얻고 대조한다

| # | 지표 | 기준값 (마감 시점에 직접 돌려 얻었다) |
|--:|---|---|
| 1 | `git rev-parse --short HEAD` | **`2f57fbc` 이상**(등호를 요구하지 않음 — `H-P`) · 추적된 미커밋 **0건** |
| 2 | 원장 집계 — `grep -h "^status:" backlog/tasks/*.md \| sort \| uniq -c` | 전체 **51** · Done **30** · To Do **16** · **In Progress 0** · Awaiting Decision **5**. ⛔ `backlog task list --plain \| grep -c "^  TASK-"` 로 세지 마라 — 우선순위 라벨이 붙으면 `  [HIGH] TASK-N` 으로 출력돼 **5건이 빠진다**(실측 46 대 51) |
| 3 | 게이트 (`app/backend` cwd) | **843 passed** · `ruff check` exit 0 · `ruff format --check` **unformatted 0** · `ty check` exit 0 · 게이트 **밖** `ruff check ../../tests ../../scripts` **0 errors** · `ruff format --check ../../tests`·`../../scripts` **unformatted 0** · 프론트 `npx tsc --noEmit`·`npx eslint app lib` 둘 다 exit 0. ⛔ **format 의 「N files」는 적지 않음** — `.md` 를 함께 세므로 회차 기록을 추가하면 늘어남(`H-AR`) |
| 4 | DB 상태 (읽기만) | `schema_migrations` **9건** = `001·003·004·005·006·007·009·010·011` · `review_tasks` **10행**(`done` 2 · `pending` 8) · `next_review_at` 있는 패턴 **8건** · `shadowing_items` **1행**(시드) · `utterances` **114행** · public 표 **17개**. ⚠️ **10행이 7행이 아닌 것이 정상임** — 010 이 접힌 단계와 발음 과제를 보존한 결과임. 줄어 있으면 그것이 회귀임. ⚠️ **`shadowing_items` 가 이제 존재함** — 이전 판의 「표 없음이 정상」은 낡았음(결정 36) |
| 5 | 착수 전 필수 | 위 표 전체. ⛔ 막는 것 셋: **`TASK-23`·`42`·`38` 금지** · **워커 금지** · **critic 루프 재개 금지** |

⚠️ **3번이 핵심이다** — 읽기는 전달을 증명하지 못하고 **직접 돌린 출력**만 데이터다.
⚠️ **게이트 밖 기준선은 이제 0 이다** — `TASK-37` AC#6 이 2026-09-09 에 정리했음(`ruff check` ·
`ruff format --check` 둘 다). **늘어나면 그것이 회귀임.** 이전 판의 「6건·4건을 유지하라」는
서술은 낡았고, 그 6건을 「줄이는 것도 회귀」였던 근거(소유자가 미착수 태스크였음)도 사라졌음.

✅ **인계 확인: 4/4 일치** (2026-09-08 · 새 세션 `ohmyenglish-63` · tmux `claude_air1-1-1`).
그 세션이 네 지표를 **직접 돌려** 얻은 값이 기준값과 전건 일치했음 — HEAD `f9226ff`(기준 `33537ba`
이상) · 미커밋 0건 · 원장 51/29/16/1/5 · **775 passed** 12.05s · `ruff`·`ty` exit 0 · 게이트 밖
`ruff` 6 errors · format tests 4 files / scripts 0 · 프론트 exit 0 · `schema_migrations` 010 까지 ·
`review_tasks` 10행(done 2 · pending 8) · `next_review_at` 8건 · **`shadowing_items` 표 없음**.
그 세션이 `shadowing_items` 부재와 `review_tasks` 10행을 **정상으로 읽었음** — 지표 4 에 그 사실을
넣어 둔 것이 실제로 오독을 막았음.

⚠️ **그 대조가 원장 결함 2건을 잡았음.** `TASK-45` 노트 앞부분이 본문보다 낡아 있었음:
「AC#1 은 착수하지 않았다」(실제로는 `[x]` · 커밋 `9b773e2`)와 「`--dep TASK-10` 을 걸었다」(실제
`dependencies` 는 `TASK-27` 하나). **둘 다 마감 세션이 직접 확인해 참임을 검증하고**, 덮지 않고
정정을 append 했음(`H-AL`). 뒤쪽 노트에는 정정이 있었으나 **앞부분에 표시가 없어** 순서대로 읽으면
「`TASK-10` 이 막고 있다」로 오독할 자리였음. **인계 확인의 값어치가 이것임** — 「읽었다」로는
안 나오고 **직접 돌려 대조해야** 나옴.

⛔ **끊긴 세션을 이어받을 때의 교훈 셋** — 사례 전문은 `H-AO` 와 각 태스크 노트가 소유함.
① **위임 산출물을 파일로 남기게 지시함.** 세션이 죽어도 파일은 삶 — 그것이 `TASK-44` 재리뷰
복구를 가능하게 했음.
② **끊긴 세션의 미커밋 작업을 「완료」로도 「미완」으로도 가정하지 않음. 게이트를 돌려 잼.**
그렇게 해서 게이트 안의 회귀 2건과 밖의 4건을 찾았음.
③ **다른 세션이 「돌렸다」고 적은 수치는 내 증거가 아님.** `TASK-43` AC#4 를 red 재관측 전까지
체크하지 않은 이유가 그것이고, 재관측해 보니 수치가 실제로 달랐음(테스트가 늘어서였고 회귀는
아니었음).

---

## 이 리포의 지배 실패 모드

**"본문을 고치고 그것을 설명하는 문장을 안 고친다."** 판별법: **본문을 고쳤으면 그것을 설명하는
문장·개수·방향·시그니처를 같은 커밋에서 함께 고친다.** 개수를 말하는 문장은 **아예 세지 않는
서술로 바꾸는 것**이 실제로 통한 유일한 구조적 해법이다.

**사례를 여기서 세지 않는다** — 세는 문장이 곧 이 실패 모드의 먹이다. 실측 사례는 그것을 잡은
커밋과 리뷰 보고서가 소유한다(`H-AL`·`H-AN`·`H-AO` 가 각 변종을 갖는다). ⚠️ **가장 비싼 변종은
「설명 문장」이 코드가 아니라 실행되는 단정·살아 있는 설계 정본일 때다** — `TASK-43` 리뷰가 낸
HIGH 2건이 정확히 그것이었다(스모크 `Check` 하나 · `Done` 인 자매 설계서의 사후조건).

⛔ **파생 규칙 3개** (전부 실측):
- **설계서·원장의 「~하지 않는다/없다」를 근거로 쓸 때 그것이 지금도 참인지 코드로 확인한다**(`H-AL`).
- **subagent 결과는 최소 1건을 직접 검증한다** — 위임 보고에서 매번 오류가 나왔고 매번 직접
  검증이 이겼다. ⚠️ **다른 세션이 「돌렸다」고 적은 수치도 내 증거가 아니다.**
- **수치에 도구 이름을 붙인다** — "게이트 밖 6 errors"가 아니라 "게이트 밖 `ruff` 6건"(`H-AN`).

---

## 진입 절차

1. **원장을 조회해 태스크 ID 를 말한 뒤 착수함.** 태스크가 없는 일이면 **먼저 등록함.**
2. 게이트를 `app/backend` cwd 에서 돌려 위 지표 3 과 대조함. **다르면 그 차이를 먼저 설명함.**
   ⚠️ 게이트 전에 `brew services list | grep postgresql@17` — 안 떠 있으면 대량 errors 가 나는데
   **회귀가 아니라 연결 거부다**(`H-T`).
3. **태스크 상태는 원장을, 다음 걸음이 바뀌면 이 파일을** 갱신함.
