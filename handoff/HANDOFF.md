# HANDOFF — OhMyEnglish

> 최종 갱신 **2026-09-16 08:36 KST** · 세션 `ohmyenglish-42` · 브랜치 `design/first-vertical-slice`
> ⛔ **이전 판은 `handoff/backup/2026-09-16/HANDOFF-0836.md` 에 있음**(그 앞 판들도 같은 폴더).
> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` 임. 상태 정본은 원장(`backlog/`), 근거
> 정본은 **태스크 노트 · `tests/harness/runs/**` · `docs/ops/captain-instruction-register.md`** 임.
>
> ⚠️ **역할**: 이 세션은 **통합 테스트**로 출발해 그대로 유지했음 — 앱 코드·테스트 코드·설계서를
> 쓰지 않았고 코드 커밋도 없음(회차 기록·픽스처·원장·결정 등재만 커밋했음).
> ⚠️ **살아 있는 사용자 지시 하나**: *"테스트 진행은 니가 권고안 대로 나에게 뭍지 말고 진행해, 내가
> 따로 말하기 전까지"*(2026-09-16 08시경). ⇒ **어느 태스크를 어느 순서로 테스트할지는 묻지 않고
> 고름.** ⛔ **설계·제품 판단(고치는 자리·값역·요구사항)은 그대로 물음** — 그 구분이 그 지시의 내용임.

## 인계 지표 4개 (이 턴에 직접 돌린 출력)

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`d5cafba`** · `origin` 과 **0/0** · 미커밋 **0건**. ⚠️ 이 판을 담은 커밋이 그 뒤에 오므로 새 세션 `HEAD` 는 더 뒤일 수 있음(그 뒤는 handoff 파일뿐임) |
| 2 | 다음 한 걸음 | **없음 — 잔여 9건이 전부 파킹이거나 개발 세션·사용자 판단 대기임.** 아래 ④ |
| 3 | 게이트 | **여섯 다 초록** — `pytest` **1239 passed**(14.25s) · `ruff check` · `ruff format --check` **49 files** · `ty check` · 프론트 `npx tsc --noEmit` **exit 0** · `npx eslint .` **exit 0** |
| 4 | 착수 전 필수 | 아래 ③. 잔여 **9건**(전부 `To Do` · In Progress **0** · 전체 **190** · 완료 **181**) |

```bash
cd ~/MyProject/OhMyEnglish
git rev-parse --short HEAD && git rev-list --left-right --count HEAD...origin/design/first-vertical-slice
backlog task list --plain
cd app/backend && ./.venv/bin/pytest -q && ./.venv/bin/ruff check . && ~/.local/bin/ty check
cd ../frontend && npx tsc --noEmit && npx eslint .
```

## ① 이번 세션이 한 것 — 회차 넷을 돌려 「코치의 말과 앱 상태가 갈린다」의 원인 둘을 재고 갈랐음

**회차 4개**(Nova 합 **29세션** · Claude **0회** · 코드 변경 **0건**):
`runs/2026-09-16-task61-13-requested-stall` · `runs/2026-09-16-task61-14-prompt-change-regression` ·
`runs/2026-09-16-task61-15-accepted-without-execution`(그 안에 둘째 회차 §4 포함).

**닫은 것**: `TASK-61.14` **Done**(무회귀) · `TASK-61.13` AC#1·#2 · `TASK-61.15` AC#1·#2.
**새로 등재**: `TASK-61.14` · **`TASK-61.15`**. **결정 등재 1건**: **112**.
**픽스처 여섯 신설**: `vc27`~`vc32`(전부 **표지가 없는** 명령 — 조건을 ASR 우연에 기대지 않게 함).

## ② 지금 상태 — 갈림의 원인이 둘이고 둘 다 앱 층에서 만남

- **원인 A(`TASK-61.13`)**: 종류를 답한 뒤 `confirmed` 까지 가는 비율 **4/8**. 정지한 팔은 코치가
  종류를 **스스로 골라 예고한** 팔 4/4 이고, **되물은 팔은 4/4 로 성립**했음(언어와 엉켜 있음).
  ⛔ 학습자의 명시적 「예」도 정지를 풀지 못했음(1/1).
- **원인 B(`TASK-61.15`)**: 표지가 없어 앱이 버린 명령에 코치가 「됐다」고 말하는 비율 **4/4**(+앞
  회차 1건 = 5/5). 기전을 코드로 확인했음 — `accepted` 를 돌려주는 자리(`nova.py`
  `_flush_tool_results`)와 실행을 판정하는 자리(`session.py` 표지 게이트)가 **다른 층**임.
- **네 명령이 같은 층에서 버려지는데 학습자가 겪는 크기가 다름** — `end`(되돌릴 수 없는 것을 됐다고
  들음) · `start_additional`(기록이 엉뚱한 세션에) · `show_report`(**패널이 없음**) ·
  `next_question`(화면 갈림 없음). ⇒ 「명령별로 나누는 안」이 `61.15` AC#3 의 재료임.
- ✅ **문면 변경(`9048522`)은 회귀를 만들지 않았음** — 세 명령이 앞 회차와 같이 성립했음(`61.14`).
- ⚠️ **영어 표지 ASR 은 그대로 불안정함** — 같은 픽스처가 2회 중 1회 `all my english` 로 전사됐음.

## ③ 착수 전 필수 — 앞 판에서 유효한 것 + 이번 세션 실측

1. ⛔ **게이트 도구는 `python3` 에 없음** — `app/backend/.venv/bin/` 이고 `ty` 만 `~/.local/bin` 임.
   프론트는 `app/frontend` 에서 `npx tsc --noEmit` · `npx eslint .`. ⚠️ **툴 호출 사이에 cwd 가
   남으므로** 긴 절차에서는 절대 경로를 씀(이번에 한 번 밟았음).
2. ⛔ **`git add <디렉터리>` 금지** · 커밋 전 `git diff --cached --name-only` 로 건수를 셈.
3. ⛔ **검증 전용 스택 규약**: DB 소유자 `ohmy` · `WORKER_ENABLED=false` · 끝나면 죽이고 drop.
   불변 지표: 새 DB 의 `schema_migrations` **22** · 공유 dev DB 는 `learning_sessions` **17** ·
   `schema_migrations` **22** · `session_plans` **6** · `weekly_reports` **0**.
4. **브라우저 레그 조합**(이번 세션에 네 번 통했음): 전용 Chrome `:9333` + 프론트 사본 `/tmp/fe-*`
   (`.env.local` 로 백엔드 지정 · 픽스처를 **그 사본의** `public/harness/` 에 복사) + 래퍼 백엔드
   `:8012`(`/tmp/*_app.py` 가 `FRONTEND_ORIGIN` 을 갈고 `translate` 를 감쌈). ⛔ `:9222` 는 남의 것.
   ⛔ `node_modules` 는 **하드링크 복사**(`cp -Rl` · 6.2초) — 심볼릭 링크는 Next 16 이 거부함(`H-BU`).
5. ⛔ **`recv.voice_command` 로 「무엇을 불렀는가」를 판정하지 않음** — 프레임 수임. 어댑터 계측이
   정본이고 **줄마다 ISO 시각을 넣어** 팔 귀속을 시각으로 가름(이번에 더한 것 · 앞 판은 순서로 추정).
6. ⛔ **앞 회차와 대조하려면 그 회차가 «심은 데이터»를 먼저 확인함** — 이번에 심지 않고 돌려
   패널·계획 질문을 대조할 수 없었고, 기준을 결과에 맞춰 고치지 않고 **심고 세 팔을 다시 돌렸음**.
   계획은 공유 dev DB 의 최신 `session_plans` 를 **읽어서** 옮기고 `learning_sessions` 더미 1행에 맴.
   주간 리포트의 `week_start` 는 `date_trunc('week', (now() at time zone 'Asia/Seoul'))::date` 로 구함.
7. ⚠️ **`grep -c` 가 0을 내면 `&&` 사슬이 끊김** — 증거 수집을 한 사슬에 이으면 뒤가 안 돌음(밟았음).

## ④ 열린 태스크 9건 — 전부 «지금 열 자리가 아닌» 이유가 적혀 있음

| 태스크 | 왜 열려 있나 |
|---|---|
| `TASK-61.13` | AC#3(고침 뒤 관측)만 남음 — 고침은 **개발 세션** 몫임(결정 112) |
| `TASK-61.15` | AC#3(고치는 자리)·#4(고침 뒤 관측) — AC#3 은 설계 판단이라 물어야 함 |
| `TASK-61.9` | 일시 정지. AC#1 이 마이그레이션의 dev DB 적용 여부를 **사용자 판단**으로 요구함 |
| `TASK-61.11` | 학습 계속. 「계속」의 뜻부터 사용자 판단임 |
| `TASK-78.1` | 결정 101 로 파킹. AC#2 는 닫혔고 AC#1(문턱)을 지금 정하지 않음 |
| `TASK-116.5` | 문장 인용 구멍 2/59 — 측정하고 파킹함. **다시 올릴 조건은 그 비율이 커지는 것**이고 이번 회차들은 그 모집단이 아님(음성 명령 축임) |
| `TASK-122`(LOW) | 세 세션이 「지금 고치지 않는다」로 판정했고 AC#3 이 조건부임 — 다시 열지 않음 |
| `TASK-39` | AC#2 를 결정 96 이 보류(Phase 2 운영 관찰 시점) |
| `TASK-41` | 결정 31 이 「모든 구현이 끝난 뒤 다시 올린다」로 미룸 |

## ⑤ 착수 전 반드시 읽을 것

- **결정 112** — 정본은 `docs/ops/captain-instruction-register.md` 머리 근처임. 「고치는 자리는 문면이
  아니라 앱이고 수행은 개발 세션」이며 **표면의 모양과 소유 층은 정해지지 않았음.**
- **`TASK-61.15` 노트와 그 회차 §3·§4** — AC#3 에 들어갈 재료 셋(층이 갈림 · 명령별로 크기가 다름 ·
  어댑터 주석이 같은 위험을 번역기 경로에만 막아 뒀음).
- ⛔ **개발 세션에 넘길 요청은 `TASK-61.13` 노트에 이미 적혀 있음**(재현 절차·증거·게이트 실측).
  살아 있는 개발 세션이 없어 전달하지 못했음 — 뜨면 그 노트를 가리킴.
- ⚠️ **라운드를 더 돌지 않는 규율이 살아 있음** — 회차가 새 findings 를 만들면 라운드가 아니라
  게이트를 바꿈. 이번에 사슬이 `61.13` → `61.15` 로 한 번 더 이어졌고 **거기서 멈췄음.**
