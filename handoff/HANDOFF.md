# HANDOFF — OhMyEnglish

> 최종 갱신 **2026-09-20 01시대 KST** · 세션 `ohmyenglish-6a` · 브랜치 `design/first-vertical-slice`
> ⛔ **이전 판은 `handoff/backup/2026-09-20/HANDOFF-0128.md`** (그 앞 판들은 같은 폴더의 날짜별).
> 인계 원칙의 정본은 `~/.claude/rules/session-handoff.md` · 근거 정본은 **태스크 노트 ·
> `docs/ops/pitfalls.md` · 회차 노트**임.

## ⛔ 먼저 챙길 것 — 첫 코드블록보다 앞에 둠

1. ⛔ **원장 잔여 0건임**(`To Do`·`In Progress`·`Awaiting Decision` 전부 0 · `Done` 325 — 아래
   지표표와 같은 값이어야 함).
   사용자 지시(2026-09-19)에 따라 **방향 후보를 고르지도, 어느 쪽으로 갈지 묻지도 않고 멈춤.**
   상태만 짧게 적고 턴을 끝냄.
2. ⛔ **비용 제약이 풀렸음**(사용자 지시 2026-09-20 · 「유료 호출 상관 하지 말고 진행해」).
   그래도 `w_worker_round.py` 의 `CALL_CAP` 은 남겨 둠 — 뜻이 예산에서 **폭주 방지**로 바뀐 것이고,
   job 재시도가 다섯 종류 × 최대 5회라 파서가 계속 거부하면 호출이 조용히 수십 건으로 늘음.
3. ⛔ **이 세션은 개발 세션임.** 살아 있는 지시: 묻지 않고 권고대로 감 · 개발 뒤 테스트 필수 ·
   짧게 핵심만 보고 · 기능은 심플하게. ⛔ **En-Coach 는 고치지 않음.**
4. ⚠️ **스택이 내려가 있음** — `:8002`·`:3000` 둘 다 리스너가 없음(직접 확인). 이전 세션이 닫히며
   함께 죽었음. 필요할 때 아래 명령으로 띄움. ⛔ 리스너 확인은
   `lsof -nP -iTCP:8002 -sTCP:LISTEN -t` 로 함 — `lsof -ti:8002` 는 클라이언트까지 잡아 오독됨(`H-CJ`).
5. ⛔ **유료 회차는 격리 DB `ohmyenglish_worker` 에서 돌림 — dev DB 가 아님.** 픽스처 문장을
   사용자의 실제 오류 패턴·복습 일정에 섞지 않기 위한 것이고(`TASK-191` 의 결정) 비용과 무관한
   근거임. dev DB 의 행은 이 세션에서 한 건도 바뀌지 않았음.
6. ⛔ **Bedrock 은 SigV4 가 필요함.** 셸에는 bearer 만 있고 두 키는 `app/backend/.env` 에만 있음 —
   직접 스크립트에는 `env -u AWS_BEARER_TOKEN_BEDROCK` 을 앞에 붙임.
7. ⛔ **회차 출력을 `head` 로 자르지 않음** — 파이프가 먼저 닫히면 드라이버가 `SIGPIPE` 로 죽어
   회차 중간이 사라질 수 있음(2026-09-20 에 한 번 그 위험을 밟고 운으로 살아남았음). `> 파일` 로
   받고 뒤에서 `grep` 함.
8. ⛔ **DB 배치**: 우리 표는 스키마 **`ohmyenglish`** 임. `pg_dump` 는 `-n ohmyenglish`(`H-BX`) ·
   `psql` 은 `/opt/homebrew/opt/postgresql@17/bin/psql` · `public` 에 En-Coach 호환 뷰가 있어
   introspection 에는 `current_schema()` 를 **반드시** 걸음.
9. ⚠️ **PRD 가 v1.6 임** — §16 발음 보조 신호 요구 철회 · §17 즉시 드릴 진입을 화면으로 한정.
   그 두 절을 읽지 않고 발음·드릴 영역을 손대지 않음.
10. ⛔ **낡은 전제 하나**: 완료 선언문의 「발음 코칭이 일어나지 않는다」는 반증됐음(`TASK-244`).
    그 문서를 인용할 때 확인함.

## 인계 지표 4개 (이 턴에 직접 돌린 출력)

| # | 지표 | 값 |
|--:|---|---|
| 1 | 기준 커밋 | **`4c96aba`** · `origin` 과 **0/0** · 작업트리 clean |
| 2 | 다음 한 걸음 | **없음 — 원장 잔여 0건**(`Done` **325** · 마지막 둘은 `TASK-260`·`261`) |
| 3 | 게이트 | 백엔드 넷 **exit 0** — `pytest` **1428 passed** · `ruff` 0 · `ruff format` 328 files · `ty` 0. ⚠️ 프런트 셋은 프런트 파일을 고치지 않아 돌리지 않았음 |
| 4 | 착수 전 필수 | dev DB `analysis_jobs` pending **0** · 미결 승인 0 · 스택 내려가 있음 |

```bash
cd ~/MyProject/OhMyEnglish
git rev-parse --short HEAD && git rev-list --left-right --count HEAD...origin/design/first-vertical-slice
backlog task list --plain | tail -5
cd app/backend && ./.venv/bin/pytest -q
cd app/backend && ./.venv/bin/uvicorn app.api.main:app --port 8002   # 필요할 때만
cd app/frontend && npx next dev                                      # 필요할 때만
```

## ① 이 세션이 한 것 — 워커를 켜는 회차 셋

1. **`TASK-255`** — 실물 Claude 로 `run_worker` 를 켜 경로 셋을 닫았음(`daily_error_summary` 쓰기 ·
   `summarize_week` 처리 · 파서 계약). 단정 7건 통과 · 호출 11건.
   ⛔ 착수 승인의 전제(「미등록 발화 10건」)가 사라진 것을 착수 직전에 실측해 **격리 DB 로 방향을
   바꿨음**(위 4번·5번). 함정 둘 등록: `H-CK`(직접 만든 `BedrockClaudeClient` 는 `usage_sink` 가
   없어 유료 호출이 `llm_calls` 에 0행) · `H-I` 확장(`recreate_database` 는 시드를 넣지 않음).
2. **`TASK-256`** — 비용 제약이 풀려 고친 드라이버로 **한 번에 관통**했음. `llm_calls` 가 1행에서
   11행으로 늘어 드라이버 계수기와 같아졌고(`H-CK` 를 기계 검사로 세움) 무대 job 이 첫 시도에
   `done` 이 됐음. 단정 9건 통과.
3. **`TASK-257`** — 그 회차가 잡은 결함을 고쳤음. 분석 프롬프트에 「`findings` 의 모든 항목이 키
   이름을 글자 그대로 쓴다」를 더하고 단위 테스트를 세웠음. 수정 뒤 회차는 호출 10건 · 재시도 0건.
4. **`TASK-258`** — 계약 표본기(`tests/harness/contract_sampler.py`)로 네 갈래 160호출 · 항목 590개를
   재서 **위반 0건**. ⛔ 그 결과가 `TASK-257` 에서 내가 관측 1건으로 추정한 빈도(항목의 약 4%)를
   **반증했음** — 실제는 항목 628개에 1건(0.16%)이고, 그 프롬프트 줄의 근거는 「모호함을 없앰」
   하나임. 정정을 `analysis.py` 주석·회차 노트·태스크 노트에 적었음.
5. **`TASK-259`** — 프롬프트가 「무대가 없으면 `category` 를 null 로 내라」고 지시하는데 파서가
   그 답을 거부하고 큐가 5회 물었음(유료 호출 5건). 실물 모델 8회 전건이 `null` 인 것을 재서
   도달 가능성을 확인한 뒤 `ScenarioNoStage` + `jobs.fail_terminally` 로 **첫 시도 종결**로 고쳤음
   (호출 5건 → 1건). 설계서 §5 에 정제 절을 더했음.
6. **`TASK-260`** — 같은 낭비를 찾아 큐 실패 19자리를 전수 분류했고 **추가 변경 없음**으로 판정했음.
   근거는 `jobs.fail_terminally` 의 docstring 이 가짐.
7. **`TASK-261`** — 오늘 고친 셋을 실물 파이프라인으로 다시 관통시켜 단정 9/9 · 재시도 0건 확인.

⛔ **회차별 상세는 `tests/harness/runs/2026-09-20-task25*.md` 셋과 태스크 노트가 가짐** — 복제하지
않음. 드라이버는 `tests/harness/w_worker_round.py`(`setup`·`run`·`verify`).

⚠️ **비용 제약이 풀린 뒤 도구 둘이 생겼음** — `contract_sampler.py`(실물 응답의 계약 준수율 ·
`--rescan` 으로 호출 0건 재계수) · `scenario_null_probe.py`(무대 없음 갈래 탐침).

## ② 이 세션이 배운 것 — 다음 세션이 반복하지 않을 것

1. ⛔ **승인은 「무엇을 한다」가 아니라 「어떤 전제 아래 한다」로 받은 것임.** 전제가 사라지면 같은
   명령을 그대로 돌리는 것이 오히려 승인 밖의 행위가 됨 — 증거가 같고 되돌릴 수 없는 효과가 작은
   변형을 고르고 그 근거를 산출물에 적음.
2. ⛔ **드라이버가 실물 배선을 베끼지 않으면 회차가 자기 수치를 못 셈.** `usage_sink` 를 빠뜨려
   유료 호출 9건이 `llm_calls` 에 0행으로 남았고 그것을 **우연히** 발견했음 ⇒ 이제 드라이버 계수기와
   DB 를 대조하는 단정이 회차마다 돎.
3. ⛔ **단정 낱말의 판별력을 재고 고름.** 「글자 그대로」는 다른 절에 이미 있어 그 줄이 없어도
   통과함 — 그 줄만 지워 테스트가 실제로 죽는지 본 뒤에 낱말을 확정했음.
4. ⛔ **개수를 눈으로 세지 않음.** 단정 수를 「10건」이라 적었는데 실제는 9건이었음(세는 명령을
   돌린 값만 적음). ⚠️ **표본이 작으면 「좋아졌다」를 주장하지 않음** — 관측 1건으로 추정한 빈도가
   뒤에 한 자리 수 배로 틀린 것이 드러났음(`TASK-258`).
5. ⛔ **고치기 전에 도달 가능성을 잼.** `TASK-259` 는 대가(호출 5건)를 먼저 세고 실물 8회로 그
   경로가 결정론적임을 확인한 뒤에 고쳤음 — 도달 불가였다면 고치지 않을 자리였음.

## ③ 남은 것 — 원장 밖에 적어 두는 자리

1. **계획 부재 시의 퇴화 동작** — 「계획이 없으면 패턴 키만 세션 행에 남음」. 앱 WS 경로가 고정
   사용자로 열려 격리 사용자로 우회가 안 됨. 계획 생성을 끄는 스위치가 생기면 그때 볼 자리임.
2. **출처를 잃은 문장 1건**(`408d6cc5-da54-4f82-b3a7-69b32404ecd4`) — 사용자가 처리하지 않기로
   정했음(설계서 `2026-09-18-video-learning-design.md` §10 항목 4). 결함으로 쫓지 않음.
3. **`complete()` 가 `last_error` 를 지우지 않음** — 재시도 뒤 성공한 job 이 낡은 실패 문면을 들고
   `done` 이 됨. 화면 판정은 job 상태로만 하므로 사용자에게 새지 않음. 진단할 때 알아 둘 자리임.
