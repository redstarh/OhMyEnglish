# Handoff — OhMyEnglish (단일 연속성 문서)

> **이 파일에는 두 가지만 담는다: 지금 어디까지 왔는가 / 다음 한 걸음이 무엇인가.**
> 그 밖의 것은 각 정본이 소유하고 여기서는 **가리키기만** 한다 — 복사하면 한쪽이 낡는다.
>
> | 찾는 것 | 어디 |
> |---|---|
> | 전체 작업·진행 상태·미결정 | **`TASKS.md`** (상태의 정본) |
> | 요구사항 | `docs/PRD.md` v1.1 |
> | 결정과 근거 | `docs/design/**` |
> | 실행 방법·포트·게이트 | **`docs/ops/local-run.md`** |
> | 실측된 함정 | `docs/ops/pitfalls.md` (H-A~H-Z, **26건**) |
> | 하네스 차수 | `tests/harness/runs/ROUNDS.md` · `tests/harness/README.md` |
> | **실물 마이크 절차·결과** | **`tests/harness/runs/2026-09-01-mic-1.md`** ← 마이크를 쓸 거면 이것부터 |
> | I-1 완료 보고(공유용) | `docs/i1-status-2026-09-03.html` — **스냅샷이다.** 값이 다르면 정본이 맞다 |
> | 5개 층 일관성 점검 | `docs/consistency-audit-2026-09-01.html` (스냅샷) |
>
> 최종 갱신 **2026-09-03** · 브랜치 `design/first-vertical-slice`
> 기준 커밋 **HEAD ≥ `2756f5e`** (마지막 **코드** 커밋 = I-4 리퍼 로그 레벨. 그 뒤는 docs 전용이라 코드는 그대로다)
> **이 파일이 유일한 handoff다.** 이전 판은 `handoff/backup/2026-08-30/`.

---

## 지금 어디까지 왔나

**첫 수직 슬라이스가 실물 음성으로 관통했고, 거기서 나온 오탐 결함(I-1)을 고쳐 리뷰까지 닫았다.**
**그 리뷰가 남긴 I-4(`active` 고아 세션)도 2026-09-03에 닫았다** — 캡틴 결정("1분 이상 답이 없으면
`failed`") + live 가드. 구현·게이트·**실물 확인**(`kill -9` → 재기동)까지 끝났고, 남은 것은
**독립 코드 리뷰 하나**다(위임 2회가 완주하지 못했다 — `TASKS.md` I-4).
**§10은 9태스크 중 8.5개 완료**(남은 것은 Task 9 = 하네스 P9~P12). 캡틴 결정 후속 8건 중
**G-1·G-3·G-4 완료**. **§11(추천 학습 루틴)은 설계만 끝났고 코드가 0줄이다.**

| 되는 것 | 안 되는 것 |
|---|---|
| 실시간 음성 대화·전사문 영속 저장 | **§11 추천 루틴 전체** (C절) |
| **조각 발화를 턴 경계까지 모아서 분석** (I-1) | **복습 큐·주기 갱신** — 컬럼은 있고 앱 참조 0곳 |
| **잃어버린 묶음을 워커가 회복** — 끝난 세션 스윕 + **`active` 고아 리퍼**(I-4) | 리퍼가 닫은 세션의 **결과 화면엔 교정이 안 뜬다** — 규칙 1이 `connection_failed`로 가린다 |
| 세션 화면 발음 배지 + 결과 화면 발음 카드 | 음성 명령 제어 — **3단계로 이연**(설계서가 명시) |
| 문법 오류 분석 → 패턴 병합 → 상위 교정 | `pending_learning_utterances`는 **앱 호출처 0곳**(G-7 삭제 후보) |

**폐기된 서술 3건 — 되살리지 마라.** ① "배지가 없는 것이 정상"(이제 뜬다. 안 뜨면 조사할 일이다)
② "실물 Nova 왕복 0회"(1회 완료) ③ **"조각 발화 오탐은 완화만 했고 미해소"**(구조적으로 해소했다).
③의 근거: 보존해 둔 마이크 1회 데이터에 새 SQL을 읽기 전용으로 걸어 `"i'm going to"` +
`"have a meeting"` 병합과 job **18건 → 14건**을 확인했다. 상세는 `TASKS.md` **I-1**.

---

## 다음 한 걸음

### 실물 마이크 2회 — I-1이 정말 오탐을 없앴는지 대조한다

**착수 전 필수 0건.** I-1은 구현·게이트·코드 리뷰(Approve)까지 끝났지만 **실제 음성으로 확인한
것은 아니다** — 스텁·단위 테스트와 보존 데이터 재현까지가 지금의 증거다.

절차·판별은 `tests/harness/runs/2026-09-01-mic-1.md` §1이 소유한다. **2회에서 볼 것**:

1. 조각은 **여전히 생겨도 정상**이다(`LOW` 완화는 조각을 0으로 못 만든다). 볼 것은
   **조각이 생겨도 오탐 패턴이 안 생기는가**다.
2. 새 `error_patterns`에 `verb_form_missing_subject` 계열이 **없어야** 한다.
3. job 수가 사용자 learning 발화 수보다 **적어야** 한다(1회 데이터에서는 18발화 → 14 job).
4. 덤 1: barge-in으로 사용자 final이 agent final **뒤에** 도착하면 한 문장이 두 묶음으로
   갈릴 수 있다 — 리뷰어가 남긴 **미실측 추측**이다. 로그로 확인해 볼 가치가 있다.
*(I-4 리퍼 실물 확인은 마이크와 무관하게 2026-09-03에 끝냈다 — `TASKS.md` I-4 「실물 확인」.)*

⚠️ Nova를 건드리면 `spike_nova_protocol.py --wav p1a.wav`로 자격증명 생존을 먼저 확인한다(**과금된다**).

### 그다음 순서 권고

1. **I-4 독립 코드 리뷰** — 구현·게이트·실물 확인은 끝났다. opus 리뷰어 위임을 **2회 시도했고 둘 다
   완주하지 못했다**(절전·출력 소실). 자기 리뷰 결과와 확인해야 할 가설은 `TASKS.md` I-4가 소유한다.
2. **§11 착수 전 문서 정정 2건**(D-5·D-6) — 코드 변경 0 · 30분. 설계서가 **없는 컬럼** 2개를 전제한다.
3. **G-2**(문서 한 줄) · **G-7**(죽은 함수 삭제) · **G-8**(필터 한 줄) — 짧고 선행 없음.
4. **§11 슬라이스 1** — 선행은 2번. 착수 시 함정 **H-S**(달력 날짜를 `current_date`로 구하지 않는다).
5. **Task 9 · 하네스 5차수** — "I-1 수정 전에 열지 말 것" 제약은 **해제됐다.** 단 I-1 **이후**
   코드로 돌리고, `inject_errors.py`가 문장마다 턴을 닫도록 바뀐 것을 먼저 읽어라(모듈 docstring ⚠️).

---

## 실측값 (2026-09-03, 이 절을 쓴 턴에 직접 실행)

```bash
cd app/backend                                    # 게이트는 이 cwd에서만 판정한다 (함정 H-A)
.venv/bin/pytest -q                               # 378 passed   (부분 실행은 -c pyproject.toml — H-W)
.venv/bin/ruff check . && .venv/bin/ruff format --check .   # passed / 27 files
ty check                                          # All checks passed!
.venv/bin/ruff check ../../tests ../../scripts    # Found 6 errors  (베이스라인, 게이트 밖)
.venv/bin/ruff format --check ../../tests         # 4 files         (베이스라인, 함정 H-L)
cd ../frontend && npx tsc --noEmit && npx eslint app lib   # 둘 다 exit 0
```

⚠️ **게이트를 `| tail`로 파이프하지 마라** — exit code가 `tail`의 것이 되어 실패가 `&&`를
통과한다(이 세션에서 실제로 잘못된 커밋 주장을 만들었다). ⚠️ **검증을 subagent에 위임한 동안
게이트를 겹쳐 돌리지 마라** — 테스트 DB는 실행마다 DROP/CREATE되는 공유 자원이다(**H-X**).

| 항목 | 값 |
|---|---|
| 테스트 | **378 passed**, skip/xfail 0 (241 → 312 → 327 → 333 → 347 → 348 → 366 → 377 → **378**) |
| DB | `:5432` homebrew **17.9** · 역할 `ohmy` **`TimeZone=UTC`**. 인스턴스 기본은 `Asia/Seoul`이라 풀리면 함정 **H-S**가 안 보인다 |
| dev DB 행 | 세션 **3** · 발화 **51** · 패턴 **4** · occurrence **8** · job **21** · 발음시도 **3** · **`active` 세션 0** |
| 보존 이유 | 오탐 패턴 2개만 삭제하고 전사문·세션·job은 남겼다 — **마이크 2회에서 같은 좌표로 재대조**하기 위해서다 |
| 서버 | `:8002`·`:3000` 둘 다 **내려가 있음**(`lsof` 확인). `:8000`은 StockAgent(남의 것) |
| 미커밋 | `.claude/` · `.mcp.json` · `backlog/`(untracked) — **커밋하지도 지우지도 마라** |

⚠️ `pronunciation_an_as_a`는 freq 1 · occurrence 0인데 **정상**이다 — 문법 패턴의 `frequency`는
`error_occurrences` 수지만 **발음 패턴은 `pronunciation_attempts`의 시도 수**를 센다. 버그로
오인하지 마라. 근거는 `docs/database-schema.md`의 `frequency` 파생 설명과
`pronunciation_intonation` 카테고리 행이다 — **줄 번호로 인용하지 않는다**(그 문서는
줄 단위 인용이 실제로 깨진 전례가 있다: 함정 **H-H**).

---

## 다음 세션 진입 절차

1. 이 파일 → **`tests/harness/runs/2026-09-01-mic-1.md` §1**(다음 걸음이 마이크 2회다).
   I-1이 무엇을 바꿨는지 알아야 할 때만 **`TASKS.md` I-1**. `TASKS.md` 전체를 읽지 않는다.
2. 게이트를 `app/backend` cwd에서 돌려 위 실측값과 대조한다. **다르면 그 차이를 먼저 설명한다.**
   ⚠️ **게이트 전에 DB부터 본다** — `brew services list | grep postgresql@17`. 안 떠 있으면
   `pytest`가 대량 errors로 끝나는데 **회귀가 아니라 연결 거부다**(**H-T**가 판별법을 소유한다).
   그 인스턴스는 **StockAgent와 공유**하니 인스턴스 재시작·`ALTER SYSTEM` 금지, 어느 서버인지는
   `show server_version`으로만 판별한다(**H-V**).
3. 계획서를 열면 **머리말의 "구현 후 정정" 표를 먼저 읽는다** (**H-N**).
4. **태스크 상태가 바뀌면 `TASKS.md`를, 다음 한 걸음이 바뀌면 이 파일을** 갱신한다 —
   별도 지시를 기다리지 않는다.

---

## 인계 기록

- **2026-08-28**·**2026-08-30** 2회 모두 ✅ 4/4 일치. 후자는 새 세션이 기준 해시 오류를 잡아냈다 —
  인계 확인이 실제로 작동한 사례다(**H-R**).

### 다음 세션이 대조할 기준값 (이 절을 쓴 턴에 직접 실행해 얻었다)

| # | 지표 | 값 |
|--:|---|---|
| 1 | HEAD | **`HEAD ≥ 2756f5e`** — 반드시 `≥`로 읽는다(**H-P**). 이 문서를 담은 커밋 자신이 HEAD를 옮긴다. 작성 시점 실제 HEAD는 `2756f5e`(코드). I-4는 `f83080d`+`2756f5e` 두 커밋이다 |
| 2 | 다음 한 걸음 | **실물 마이크 2회 — I-1 오탐 해소 대조**. I-1·I-4는 구현·실물 확인까지 완료 |
| 3 | 게이트 | **378 passed** · ruff·format(27파일)·ty 전부 **exit 0** · `tests/**` 베이스라인 **6 errors·4 files** · 프론트 `tsc`·`eslint` exit 0 |
| 4 | 착수 전 필수 | **0건.** 단 §11 슬라이스 1로 가려면 D-5·D-6이 선행이다 |

인계 확인은 새 세션이 **직접 돌려 얻은** 4개가 위 값과 일치하는 것이다 — "읽었다"는 지표가 아니다.
절차는 `~/.claude/rules/session-handover.md`, 함정은 `pitfalls.md` **H-P**·**H-Q**.

---

## 발음 쪽을 다시 건드릴 때 (§10)

계약의 정본은 **설계서 §3.1a·§3.2·§4.3**이다 — **계획서 본문 서술은 낡았다.** 규칙은
`app/backend/app/services/pronunciation.py`가 소유하고 세션은 배선만 한다(§3.1a): 새 감지기를
`session.py`에 박지 말고 `note_transcript` 안에 넣는다. 미결(`target_sound` 값역이 계획과
다르다 — 실측 키에 음소 치환이 0)은 **`TASKS.md` I-2**가 소유한다.
