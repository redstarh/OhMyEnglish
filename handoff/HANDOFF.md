# Handoff — OhMyEnglish (단일 연속성 문서)

> **이 파일에는 두 가지만 담는다: 지금 어디까지 왔는가 / 다음 한 걸음이 무엇인가.**
> 그 밖의 것은 각 정본이 소유하고 여기서는 **가리키기만** 한다 — 복사하면 한쪽이 낡는다.
>
> | 찾는 것 | 어디 |
> |---|---|
> | 전체 작업·진행 상태·미결정 | **`TASKS.md`** (상태의 정본) |
> | 요구사항 | `docs/PRD.md` v1.1 |
> | 결정과 근거 | `docs/design/**` |
> | 실행 방법·포트·게이트 | **`docs/ops/local-run.md`** |
> | 실측된 함정 | `docs/ops/pitfalls.md` (H-A~H-Z + **H-AA**, **27건**) |
> | 하네스 차수 | `tests/harness/runs/ROUNDS.md` · `tests/harness/README.md` |
> | **실물 마이크 절차·결과** | 절차는 **`runs/2026-09-01-mic-1.md` §1** · 2회 결과는 **`runs/2026-09-03-mic-2.md`** ← 마이크를 쓸 거면 이 둘 |
> | **슬라이스 1 실물 검증** | `runs/2026-09-04-slice1-live.md` (L1 006 적용 · L2 실물 Claude가 새 필드를 낸다) |
> | I-1 완료 보고(공유용) | `docs/backup/2026-09-05-superseded/i1-status-2026-09-03.html` — **스냅샷이다.** 값이 다르면 정본이 맞다 |
> | 5개 층 일관성 점검 | `docs/backup/2026-09-05-superseded/consistency-audit-2026-09-01.html` (스냅샷) |
>
> 최종 갱신 **2026-09-05** · 브랜치 `design/first-vertical-slice` · **§11 슬라이스 2 실행 중**
> 기준 커밋 **HEAD ≥ `d316054`** (슬라이스 2 S2-6 완료 — 고침 4라운드. 슬라이스 1의 마지막 코드 커밋은 `aae6f91`이었다)
> **이 파일이 유일한 handoff다.** 이전 판은 `handoff/backup/2026-08-30/`.

---

## 지금 어디까지 왔나

**첫 수직 슬라이스가 실물 음성으로 관통했고, 거기서 나온 결함(I-1·I-4~I-8)을 전부 닫았다.**
I-1은 실물 마이크 2회로 확증됐다(`runs/2026-09-03-mic-2.md`, 판정 3/3 — 조각은 생기지만 오탐 0건).
**I절 미결 0건이라 그 상세는 다시 읽을 필요가 없다** — 소유자는 `TASKS.md` I절이다.
**§10은 9태스크 중 8.5개 완료**(남은 것은 Task 9 = 하네스 P9~P12). 캡틴 결정 후속 8건 중
**G-1·G-3·G-4 완료**.

**§11 슬라이스 1이 구현됐다 — `next_review_at`에 값을 넣는 코드가 처음 생겼다.**
계획서 `docs/design/2026-09-03-learning-coach-slice1-plan.md`(7태스크)를 Task 1~7까지 실행했고
**실물 검증 L1·L2도 PASS**다(캡틴 승인 2026-09-04, 기록은 `runs/2026-09-04-slice1-live.md`).
실물 Claude가 `suggested_contexts` 3개와 재시도 판정 `incorrect`를 실제로 냈다(두 회차 독립 확인) —
컬럼만 만들고 값은 영원히 null일 위험이 닫혔다. **코드 리뷰도 반영해 닫았다**(CRITICAL 0 ·
HIGH 2건 수정 · 지적 1건 반박 · 이연 4건). 처리 표는 `TASKS.md` C절이 소유한다.

| 되는 것 | 안 되는 것 |
|---|---|
| 실시간 음성 대화·전사문 영속 저장 | **§11 슬라이스 2** — 추천 학습 루틴의 *판단*부(C절). 슬라이스 1(기록·계산)은 됐다 |
| **조각 발화를 턴 경계까지 모아서 분석** (I-1) | 세션 시작이 여전히 **고정 시나리오 1개**다 — 계획을 읽는 경로가 슬라이스 2 |
| **잃어버린 묶음을 워커가 회복** — 끝난 세션 스윕 + **`active` 고아 리퍼**(I-4) | 리퍼가 닫은 세션의 **결과 화면엔 교정이 안 뜬다** — 규칙 1이 `connection_failed`로 가린다 |
| **복습 큐·주기 갱신** — 분석 1턴이 `next_review_at`·`review_tasks`·`mastery_score`를 만든다(006, `services/review.py`). 목록 조회 `load_due_reviews`도 있다 | 그 목록을 **앱 경로에서 쓰는 곳이 0곳**(백필 스크립트가 보고용으로만 부른다) — 화면도 계획 생성도 슬라이스 2다 |
| **연습 상황 3개 저장**(`suggested_contexts`) · **재시도 정답 여부**(`pattern_attempts`) · **만성 지표 쿼리**(`services/chronic.py`) | `chronic.py`를 **호출하는 코드가 0곳** — 소비자는 슬라이스 2의 계획 생성이다 |
| 세션 화면 발음 배지 + 결과 화면 발음 카드 | 음성 명령 제어 — **3단계로 이연**(설계서가 명시) |
| 문법 오류 분석 → 패턴 병합 → 상위 교정 (**발음 키는 문법 프롬프트에서 제외** — G-8) | **§11 슬라이스 2 전체** — 계획 생성 job · `session_plans` · `learner_notes` · 지시문 전달 · 수준 갱신. 007 마이그레이션이 필요하다 |

**폐기된 서술 5건 — 되살리지 마라.** ① "배지가 없는 것이 정상"(이제 뜬다) ② "실물 Nova 왕복
0회"(1회 완료) ③ "조각 발화 오탐은 완화만 했다"(구조적 해소, 근거는 `TASKS.md` **I-1**)
④ "`suggested_contexts`·`next_review_at`은 앱 참조 0곳"(006 이후 거짓) ⑤ "`impact_score`는
3단계로 연기"(**영구 제외**다 — 설계서 §11 미결 3).

---

## 다음 한 걸음

### §11 슬라이스 2 — 판단과 적용 · **실행 중** (2026-09-04~05)

**계획서**: `docs/design/2026-09-04-learning-coach-slice2-plan.md` (12태스크).
**상태의 정본은 `TASKS.md` C절의 「슬라이스 2 태스크 원장」**이다 — 여기서 재서술하지 않는다.
2026-09-05 기준 **S2-1~S2-6 완료(S2-6 은 Approve 받았다) · S2-7~S2-12 대기.**

**실행 방식**: 태스크별 작업자 + 독립 리뷰(`superpowers:subagent-driven-development`).
브리프 12개가 `.superpowers/sdd/2026-09-04-learning-coach-slice2-plan/`에 **전부 생성돼 있다**.

⚠️ **내가 캡틴 대신 내린 판단 40건은 `docs/design/2026-09-05-slice2-execution-log.md`가 소유한다
(추적됨 — §1~3이 31건, §4가 S2-6 고침 라운드의 9건).** 각 항목에 근거와 "틀렸을 때 비용"이 있다 — **동의하지 않는 것만 골라 뒤집으면 된다.**
그 파일의 §3(내가 놓쳤다가 잡힌 패턴 4개)을 **착수 전에 읽어라.** 진행 경위와 실행 출력은
`.superpowers/.../progress.md`에 더 상세하지만 **git 무시 대상이라 사라질 수 있다.**

## 다음 한 걸음 — S2-7 (계획·노트·수준을 한 트랜잭션에 저장)

**S2-6은 닫혔다 — 최종 판정 `APPROVE`** (Critical 0 · Important 0 · 사양 준수 ✅). 고침 4라운드
(`fdfd83f`·`1aff1ff`·`dc05ad3`·`d316054`)가 **내용·근거·red 관측 35건을 소유한다** — 여기서
재서술하지 않는다. 22건 중 20 ADDRESSED · 2 이연. 4라운드의 계단과 교훈은 **`TASKS.md` C절**이 소유.

**S2-7 브리프에 내가 넘긴 것 4건이 실려 있다** — `complete`의 반환값 계약(lease 상실 시 롤백) ·
지어낸 id 거부 가드 · 콜드스타트 호출 생략 · `level.action` 정합 처분. 그 뒤 S2-8~S2-12.
**S2-6이 넘긴 이연 2건**(Minor, 리뷰어 합의): `"The lists below are **facts**"` · 최근 창 날짜 무테스트.

### ⚠️ S2-7 착수 전에 이 둘만 읽어라 (S2-6에서 값비싸게 얻었다)

1. **허용 id 집합을 프롬프트와 다르게 정하면 정당한 계획이 거부된다.** S2-6의 Critical이 여기
   있었다 — 프롬프트가 "lists above"라고 말했지만 **네 목록 중 둘만 `pattern_id`를 싣는다**
   (최근 교정·발음). 계획서 **Task 7 ①**이 집합을 `due_reviews ∪ chronic`으로 못 박았고 프롬프트가
   이제 그 두 절 제목(`"Due for review today"`·`"Chronic metrics"`)을 **그대로 인용**한다.
   **가드의 집합을 그것과 같게 만들어라.**
2. **테스트를 쓰기 전에 `tests/unit/test_plan.py` 머리말을 읽어라.** 이 슬라이스의 지배 실패
   모드가 "통과하지만 이유가 틀린 테스트"이고 **누적 22건**이다. 네 라운드 연속으로 **내 고침
   자체에서** 다시 났고 매번 한 단계 더 좁은 형태였다 — 그 계단과 헬퍼 3개(`_bullet`·
   `_bullet_lead`·`_section_header`)가 판별법을 담는다. 한 줄 요약: **창을 좁히는 것으로는
   부족하고 문구를 주어에 묶어야 하며, 경계를 재는 값이 재려는 대상에서 파생되면 안 되고,
   헬퍼가 조용히 퇴화할 수 있으면 시끄럽게 죽여야 한다.**

⚠️ **착수 전 반드시 볼 것 2건**:
1. **계획서 「구현 전 정정」표를 먼저 읽는다**(함정 H-N). 실행 중 **11건이 더 추가됐다** —
   내 계획서가 참조한 이름·계약·관례가 실제와 달랐던 자리들이다. 그 표를 안 읽으면 같은 자리를 다시 밟는다.
2. **캡틴 승인이 필요한 것 2건이 남아 있다**: **007을 실물 dev DB에 적용**(되돌리기 어렵다.
   그래서 아직 안 했고, 덕분에 오염된 입력으로 만든 계획이 존재하지 않는다) · **실물 Claude 계획 생성 1회**(비용).

**이연 4건은 `TASKS.md` C절이 소유한다.** 그중 둘은 슬라이스 2가 먼저 봐야 한다:

1. **발음 패턴은 복습 스케줄에 들어오지 않는다** — 알고 남긴 축소다. 근거는 설계서 §4.1
   「구현 공백 4」. ⚠️ **버그로 다시 조사하지 마라.**
2. **`review_tasks` 행의 `id`·`status`가 재계산마다 초기화된다** — UI가 과제 id로 "완료 표시"를
   하려면 그 전에 경계를 정한다. **슬라이스 2는 `review_tasks`를 읽지 않으므로 이 슬라이스에서는 무해하다.**

⚠️ **실행 중에 정본 설계서가 한 번 고쳐졌다** — §4.2의 "최근 창"이 `utterance_type`만 적어
**코치 발화가 학습자 발화로 섞이는 결함의 원천**이었다(dev DB 실측: 14일 창 92행 중 코치 54행 = 59%).
`speaker`와 쌍으로 걸어야 한다는 것과 그 근거가 이제 §4.2에 있다.

### 그다음 순서 권고

1. **Task 9 · 하네스 5차수** — "I-1 수정 전에 열지 말 것" 제약은 **해제됐다.** 단 I-1 **이후**
   코드로 돌리고 `inject_errors.py` **모듈 docstring의 ⚠️ 2건**을 먼저 읽어라: 문장마다 턴을 닫는
   이유, 그리고 **대기 중 리퍼가 그 세션을 `failed`로 닫는다**는 것(I-4 — 회귀가 아니다).
   ⚠️ **하네스도 게이트 밖 자산이다** — 함정 **H-AA**(스모크가 이틀간 죽어 있었다)와 같은 부류다.
   돌리기 전에 `git log -1 -- tests/harness/`와 최근 앱 변경 날짜를 대조한다.

---

## 실측값 (2026-09-05 갱신, 이 절을 쓴 턴에 직접 실행)

```bash
cd app/backend                                    # 게이트는 이 cwd에서만 판정한다 (함정 H-A)
.venv/bin/pytest -q                               # 534 passed (2026-09-05 실측 — 슬라이스 2 진행에 따라 늘어난다. 부분 실행은 -c pyproject.toml — H-W)
.venv/bin/ruff check . && .venv/bin/ruff format --check .   # passed / 32 files
ty check                                          # All checks passed!
.venv/bin/ruff check ../../tests ../../scripts    # Found 6 errors  (베이스라인, 게이트 밖)
.venv/bin/ruff format --check ../../tests         # 4 files         (베이스라인, 함정 H-L)
cd ../frontend && npx tsc --noEmit && npx eslint app lib   # 둘 다 exit 0 (2026-09-05 직접 확인)
```

⚠️ **게이트를 `| tail`로 파이프하지 마라** — exit code가 `tail`의 것이 되어 실패가 `&&`를
통과한다(이 세션에서 실제로 잘못된 커밋 주장을 만들었다). ⚠️ **검증을 subagent에 위임한 동안
게이트를 겹쳐 돌리지 마라** — 테스트 DB는 실행마다 DROP/CREATE되는 공유 자원이다(**H-X**).

| 항목 | 값 |
|---|---|
| 테스트 | **534 passed**, skip/xfail 0 — 2026-09-05 실측. 슬라이스 2 실행 중이라 **태스크마다 늘어난다**: 454(슬라이스 1 마감) → 460(S2-1) → 465(S2-2) → 468(S2-3) → 484(S2-4) → 512(S2-5) → 520(S2-6) → 529(라운드 1) → **534**(라운드 2~4). 라운드 3·4는 수가 늘지 않았다 — 단정을 강화하고 새 케이스를 기존 테스트에 얹었다. 정확한 현재 값은 **직접 돌려 얻어라** |
| DB | `:5432` homebrew **17.9** · 역할 `ohmy` **`TimeZone=UTC`**. 인스턴스 기본은 `Asia/Seoul`이라 풀리면 함정 **H-S**가 안 보인다 |
| dev DB 행 | 세션 **7** · 발화 **92** · 패턴 **7** · occurrence **17** · job **33** · 발음시도 **4** · `active` **0** · `pattern_attempts` **0**(dev DB로 세션을 돌린 적이 없다) · **`review_tasks` 6행**(백필이 만들었다) |
| 복습 목록 | **오늘 복습할 목록 1건** — 처음으로 0행이 아니다. 문법 패턴 6개가 예정일을 받았고 발음 1개는 null(구조적 제외) |
| 마이그레이션 | dev DB `schema_migrations` **5행** — `001`·`003`·`004`·`005`·**`006`**(L1, 2026-09-04 적용). 슬라이스 2는 **007**을 쓴다 |
| 보존 이유 | ✅ **소진됐다** — 보존 목적(마이크 2회 재대조)은 2026-09-03에 끝났고 1·2회차 좌표는 `runs/2026-09-01-mic-1.md`·`runs/2026-09-03-mic-2.md`가 소유한다. 이제 dev DB를 비워도 잃는 증거가 없다(비울 이유가 생기면 비워도 된다) |
| 서버 | `:8002`·`:3000` 둘 다 **내려가 있음**(`lsof` 확인). `:8000`은 StockAgent(남의 것) |
| 미커밋 | `.claude/` · `.mcp.json` · `backlog/`(untracked) — **커밋하지도 지우지도 마라** |
| 스모크 DB | `ohmyenglish_smoke`에 L2 증거가 남아 있다(지우지 않는다 — 다음 스모크가 drop/create한다). 실물 모델의 `suggested_contexts`·`attempts` 원본을 보려면 그 DB를 조회한다 |

⚠️ **발음 패턴이 freq ≥ 1 · occurrence 0인 것은 정상이다** — 근거는 `docs/database-schema.md`의
`pronunciation_attempts` 절이 소유한다. **그 문서를 줄 번호로 인용하지 않는다**(함정 **H-H**).

---

## 다음 세션 진입 절차

1. 이 파일 → **`TASKS.md` C절**(코드 리뷰 결과 표 · **이연 4건** · 슬라이스 2 확인 4건). `TASKS.md` 전체를 읽지
   않는다 — I절은 **전부 닫혔다**. 마이크를 쓸 일이 생기면 `runs/2026-09-01-mic-1.md` §1.
2. 게이트를 `app/backend` cwd에서 돌려 위 실측값과 대조한다. **다르면 그 차이를 먼저 설명한다.**
   ⚠️ **게이트 전에 DB부터 본다** — `brew services list | grep postgresql@17`. 안 떠 있으면
   `pytest`가 대량 errors로 끝나는데 **회귀가 아니라 연결 거부다**(**H-T**). 인스턴스는
   **StockAgent와 공유**하니 재시작·`ALTER SYSTEM` 금지, 판별은 `show server_version`만(**H-V**).
3. 계획서를 열면 **머리말의 「구현 전/후 정정」 표를 먼저 읽는다** (**H-N**).
4. **태스크 상태가 바뀌면 `TASKS.md`를, 다음 한 걸음이 바뀌면 이 파일을** 갱신한다.

---

## 인계 기록

- **2026-08-28**·**2026-08-30** 2회 모두 ✅ 4/4 일치. 후자는 새 세션이 기준 해시 오류를 잡아냈다(**H-R**).
- **2026-09-03 마감** — I절 전건 종결 · D-5·D-6·G-2·G-7·G-8 소진 · 마이크 2회로 I-1 확증.
- **2026-09-04 마감** — 새 세션이 **4/4 일치**로 인계를 확인한 뒤 §11 슬라이스 1을 **계획부터
  코드 리뷰까지** 관통해 닫았다: 계획서(7태스크, critic CHALLENGE 7건 반영) → Task 1~7 →
  실물 L1·L2 → 코드 리뷰 반영. 커밋 12건(`f8b3669`…`aae6f91`). **미결 0건**(이연 4건은
  `TASKS.md` C절).
  **내 결함·과장 3건을 잡아 고쳤다** — ① 완주 판정에 간격 조건이 없어 같은 세션에서 세 번
  맞히면 1·3·7일을 경과하지 않고 완주했다(critic) ② 부가 필드 규격 위반이 그 발화의 교정을
  통째로 태웠다(리뷰 HIGH-1) ③ 마이그레이션만으로는 복습 목록이 0행이라 "0행을 벗어났다"는
  내 완료 보고가 과장이었다(리뷰 MEDIUM-6 → 백필 신설).
  ⚠️ **이 리포에는 git remote가 없다** — `git remote -v`가 비어 있어 푸시할 대상이 없다.
  리포 생성·연결은 캡틴 몫이고, 붙이면 `remoteOperations`도 되돌린다.

- **2026-09-04 인계 확인 (새 세션 `claude_air_1-1`)** — ✅ **4/4 일치.** 직접 돌린 값: HEAD `df368e4`
  (`aae6f91..HEAD` 4건 전부 **docs-only**, 변경 파일은 `handoff/HANDOFF.md` 하나 — H-P의 `≥`로 읽어
  일치 판정) · 다음 한 걸음 **§11 슬라이스 2**(원장 C절 "슬라이스 2 — 계획 생성·반영 ⏭") ·
  게이트 **454 passed** · ruff·format(**29 files**)·ty 전부 exit 0 · 베이스라인 **6 errors·4 files** ·
  프론트 `tsc`·`eslint` exit 0 · 착수 전 필수 **0건**(이연 4건 확인) ·
  DB `postgresql@17` **started**. ⚠️ **W-live 스모크 11/11은 돌리지 않았다** — 실물 Claude 비용이
  들어 대조에서 제외했다(다른 6개 수치가 일치하므로 인계 판정에는 영향 없음).
  이전 세션은 tmux 밖에 있었으므로 `kill-session` 없음 — `tmux ls`에 `claude_air_1-1` 하나뿐이다.

- **2026-09-05 마감** — 같은 세션(`claude_air_1-1`)이 §11 **슬라이스 2를 계획부터 S2-5까지 관통**했다:
  일관성 점검(모순 9건) → 계획서 12태스크 → **S2-1~S2-5 완료**(각각 독립 리뷰 + 고침 라운드) →
  S2-6 구현·리뷰(고침 미반영). 테스트 **454 → 520**. 게이트 밖 베이스라인은 **여섯 태스크 연속 유지**.
  커밋: `e6f4fef`…`49ce1e1`(코드) + 문서·원장 다수.
  **리뷰가 잡은 것 중 가장 큰 것 셋**: ① 코치 발화가 학습자 발화로 섞여 계획 입력의 **59%가 오염**
  (정본 설계서 §4.2가 원천 — 고쳤다) ② 지시문 수준이 무제약이라 **화면은 A2·대화 상대는 C2**인 계획이
  통과 ③ 프롬프트가 **모델이 만들 수 없는 값**(`pattern_id`)을 요구 — 지어낸 UUID가 FK 없이 조용히 저장된다.
  **내 계획서 결함이 12건 사전에, 여러 건이 리뷰에서 잡혔다** — 목록과 근거는
  `docs/design/2026-09-05-slice2-execution-log.md`가 소유한다.
  ⚠️ **이 세션은 tmux `claude_air_1-1` 안이었고 다음 세션도 그 그룹을 이어 쓸 수 있다**(서버가 살아 있다).
  `session-handover.md` §3의 `PREFIX` 유도가 정상 작동한다.
  ⚠️ **미완을 정직하게 남겼다**: S2-6 고침 라운드 1의 부분 작업을 **커밋하지 않고 되돌렸고** 패치로 보존했다
  (`.superpowers/.../task6-fix-round1-partial.patch`). 반쯤 고친 코드를 넘기지 않기 위해서다.

### 다음 세션이 **하지 말아야 할 것** 3건 (이 세션에서 실제로 겪은 사고)

1. **게이트 안/밖을 섞어 보고하지 마라.** 작업자가 게이트 밖 베이스라인(`ruff check ../../tests …`)을
   게이트 안 검사(`ruff check .`)로 착각해 **깨진 게이트를 "기준선과 동일"로 보고**했다. 넷을 각각 돌려라.
2. **작업자가 활동 중일 때 게이트를 돌리지 마라.** 내가 그것을 어겨 **중간 상태를 재고** 하마터면 없는
   회귀를 쫓게 할 뻔했다. 더 나쁜 것은 `pytest`가 테스트 DB를 **DROP/CREATE**하므로 **남의 실행을
   깨뜨릴 수 있다**(H-X). 순서: ① DONE 보고 → ② `git status --short`로 트리가 깨끗한지 확인 → ③ 그 다음 측정.
3. **원장 두 곳 중 한쪽만 갱신하지 마라.** `TASKS.md`만 고치고 handoff를 **5개 태스크째 낡게** 두어
   새 세션이 대조할 기준값 4개가 전부 틀린 상태였다. 태스크 완료마다 **둘을 함께** 고친다.

### 다음 세션이 대조할 기준값 (이 절을 쓴 턴에 직접 실행해 얻었다)

| # | 지표 | 값 |
|--:|---|---|
| 1 | HEAD | **`HEAD ≥ d316054`** — 반드시 `≥`로 읽는다(**H-P**). `d316054`가 마지막 **코드** 커밋(S2-6 고침 라운드 4)이고 그 뒤는 전부 문서·원장 커밋이다. 불일치를 보면 `git log --oneline d316054..HEAD`가 docs-only인지 먼저 본다 |
| 2 | 다음 한 걸음 | **S2-7 — 계획·노트·수준을 한 트랜잭션에 저장.** S2-6은 Approve 받아 ✅다. 상태의 정본은 `TASKS.md` C절 태스크 원장이므로 **거기서 직접 읽어라** |
| 3 | 게이트 | **534 passed** · `ruff check .`·`ruff format --check .`·`ty check` 전부 **exit 0** · `tests/**`+`scripts/**` 베이스라인 **6 errors·4 files**(일곱 태스크 + 고침 4라운드 연속 유지) · 프론트 `tsc`·`eslint` 둘 다 **exit 0**(라운드 4 마감에 직접 돌렸다). ⚠️ **게이트 안/밖을 구분해 각각 돌려라** — 이 세션에서 작업자가 둘을 혼동해 **깨진 게이트를 "기준선과 동일"로 보고**한 사고가 있었다. ⚠️ 테스트 수는 태스크마다 늘어난다 — 불일치가 회귀가 아니라 진행일 수 있다. W-live 스모크 11/11은 **실물 Claude라 비용**이 들어 대조에서 제외한다 |
| 4 | 착수 전 필수 | **읽을 것 3건**: ① `docs/design/2026-09-05-slice2-execution-log.md` **§3**(내가 놓쳤다가 잡힌 패턴 4개 — 같은 실수를 반복하지 않으려면 이것이 가장 값어치 있다) ② 계획서 「구현 전 정정」표(실행 중 **12건 추가**) ③ `TASKS.md` C절의 **이연 4건**. **캡틴 승인 대기 2건**: **007을 실물 dev DB에 적용**(되돌리기 어렵다. 아직 안 해서 `session_plans` 표가 dev DB에 **없고**, 그 덕에 오염된 입력으로 만든 계획이 **존재하지 않는다**) · **실물 Claude 계획 생성 1회**(비용). 둘 다 S2-12 이후다 |

인계 확인은 새 세션이 **직접 돌려 얻은** 4개가 위 값과 일치하는 것이다 — "읽었다"는 지표가 아니다.
절차는 `~/.claude/rules/session-handover.md`, 함정은 `pitfalls.md` **H-P**·**H-Q**.

### ⚠️ 이번 인계의 tmux 상태 — 한쪽만 tmux다

2026-09-04 실측: 마감하는 세션은 **tmux 밖**이었다(`TMUX` 비어 있음 · `tmux ls`가
`error connecting to /private/tmp/tmux-501/default` — 서버가 아예 없었다. 순수 iTerm2).
캡틴 지시로 **새 세션만 tmux 안에서 시작한다**:

| | 값 |
|---|---|
| 새 세션 이름 | **`claude_air_1-1`** (tmux 3.6a · iTerm2 새 창 전면 · `attached`) |
| 그 세션의 cwd | `/Users/redstar/MyProject/OhMyEnglish` (확인함) |
| 번호가 `1-1`인 이유 | 서버가 없어 **상속할 그룹이 없었다.** 다음 인계부터는 `session-handover.md` §3의 `PREFIX` 유도가 정상 작동한다 |

**그래서 새 세션이 하지 말아야 할 것 2개** (`session-handover.md` §5·§6이 전제하는 상대가 없다):

- **`tmux kill-session`으로 이전 세션을 정리하지 마라 — 죽일 세션이 없다.** 이전 세션은 tmux
  밖에서 `/exit`으로 끝난다.
- **`send-keys` 양방향 보고를 기다리지 마라.** 대조 결과를 받아 적을 이전 세션이 없으므로,
  4개 지표를 직접 돌려 **자기가 이 파일의 「인계 기록」에 결과를 적는다**(일치했으면 한 줄,
  다르면 차이를 먼저 설명한다).

다음 세션부터는 서버가 살아 있으니 §3·§5·§6을 그대로 쓸 수 있다.

---

## 발음 쪽을 다시 건드릴 때 (§10)

계약의 정본은 **설계서 §3.1a·§3.2·§4.3**이다 — **계획서 본문 서술은 낡았다.** 새 감지기를
`session.py`에 박지 말고 `note_transcript` 안에 넣는다. 남은 미결(`target_sound` 값역)은
**`TASKS.md` I-2**가 소유한다. 착수 안내 전문은 `TASKS.md` A절이다.
