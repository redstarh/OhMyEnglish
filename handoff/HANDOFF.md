# Handoff — OhMyEnglish

> ## 이 파일의 작성 원칙 (2026-09-10 사용자 지시 — 이후 계속 따른다)
>
> **두 가지만 담는다: ① 이번 세션에서 마무리한 것 요약 ② 다음 세션이 이어가는 데 필요한 핵심 정보.**
> ⛔ **누적하지 않는다.** 새 세션이 이 파일을 **새로 쓴다** — 지난 세션의 서술을 덧붙이지 않는다.
> ⛔ **새로 쓰기 전에 이전 판을 `handoff/backup/<날짜>/HANDOFF.md` 로 백업한다.** 새 판에는
> **백업 경로만** 적고 그 내용을 옮기지 않는다 — **필요할 때만 그 백업을 참고한다.**
> 그 밖의 것은 정본을 가리킨다: 결정은 `docs/ops/captain-instruction-register.md` · 태스크 상태는
> 원장(`backlog task view <ID>`) · 관측은 `tests/harness/runs/**` · 함정은 `docs/ops/pitfalls.md`.
> ⛔ **줄 수·개수를 적지 않는다** — 적는 순간 낡는다(`H-O`).
>
> **이전 판 백업**: `handoff/backup/2026-09-10/HANDOFF.md` (그 앞은 `2026-09-06` · `2026-08-30`).

최종 갱신 **2026-09-10** · 브랜치 `design/first-vertical-slice`

---

## ① 이번 세션(2026-09-09~10)에서 마무리한 것

**결과 화면 갈래를 닫았다** — `TASK-55`(HTTP 상태 코드 노출) · `56`(4xx 영구 재시도) ·
`57`(문체 혼용) · `77`(「분석 대상 없음」을 첫 판독으로 확정). 상태 코드는 이제 **분류용**이고
학습자 문구는 화면이 소유한다.

**게이트를 둘 늘렸다** — `TASK-34`(결과 화면은 수를 그리지 않는다 = `A3-3`) ·
`TASK-64`(A3-1 판정 단위를 세션에서 status 로). 둘 다 무력화에서 FAIL 을 관측했다.

**Phase 1 을 6항목으로 대조했고 선언하지 않았다**(`TASK-47`) — 4항목 충족 · 2항목 미충족.
캡틴 결정 B-7 이 지목했던 셋(W-live·E2E-S·실물 마이크)은 전부 닫혔고, 항목 4·6 이 이 대조에서
처음 드러났다.

**리뷰를 두 번 받았다**(`TASK-70`, 아직 열림) — 1차 HIGH 3건 중 하나를 고치고(어댑터 오류를
`failed` 로 기록) 하나는 공허한 테스트를 지우고 AC 문면을 고쳤으며(`TASK-76`) 하나는 폴링을
고쳤다(`TASK-77`). **재리뷰가 그 셋을 해소로 인정**했고 새 지적 둘(`TASK-79`·`TASK-80`)이 남았다.

**캡틴 결정 셋을 받아 등록했다** — **48**(red→green 의 등가 증거는 뮤테이션 KILL) ·
**49**(보조 신호로 축소 — 뒤에 전제가 반증됨) · **50**(측정 뒤 단순화).

⛔ **이 세션이 틀린 것 둘을 남긴다** — ⑴ 발음 tool 을 **단일 발화로 재고 다중 턴을 결론**해 결정
49 를 잘못된 전제 위에 만들었다 ⑵ **`--app-prompt` 를 「앱 프롬프트」로 취급**했는데 그것은 기반
프롬프트였고 그 사실이 코드 주석에 있었다. 둘 다 대장·회차 기록·태스크 노트에 정정을 남겼다.

---

## ①-2 같은 기간에 **테스트 갈래**가 마무리한 것 (`ohmyenglish-15`)

정본은 `tests/harness/runs/2026-09-09-task59-qwen-tts.md` · `-task65-session-context.md` ·
`2026-09-10-task78-{coaching-implies-tool,app-path}.md` 다. **여기서 수치를 다시 세지 않는다.**

- **`TASK-59`** — Qwen3-TTS 로 픽스처를 바꾸려다 **반증했다. 이 목적에는 `say -v Yuna` 가 낫다**
  (Qwen 음원은 언어코드를 어느 쪽으로 줘도 영어로 전사돼 오류가 재현되지 않는다).
  ⛔ **참고 프로젝트의 「Qwen 이 낫다」를 다시 가져오지 않는다** — 그쪽은 전사가 **맞는 것**을,
  P계층은 전사가 **무너지는 것**을 좋다고 센다. 부호가 반대다.
- **`TASK-63`** → **결정 47**(쓴다 · 미리 만들어 저장). 요구사항에 **「원어민」 조건이 없다**.
  ⛔ 그 태스크의 「스키마를 바꾸지 않는다」는 반증됐다 — 담을 자리가 없다(`TASK-66`).
- **`TASK-65`** — **세션의 첫 발화가 이후 턴의 ASR 언어를 고정한다.** 「경로가 오디오를 바꾼다」는
  반증됐고 **파일이 아니라 순서가 정한다.** ⛔ **P4·P12 의 관측 경로가 이것 때문에 바뀌었다**(아래).
- **`TASK-78`**(열림) — 「문장 되말하기 ⟺ tool」이 예외 없이 성립하고 `target_sound` 도 tool
  이벤트 전건에 실린다. **그러나 앱이 보내는 조립 프롬프트에서는 코칭이 0회다** → `TASK-81`.
- **`TASK-68`·`TASK-69`** — 하네스 원자료가 회차마다 덮이던 것을 고쳤고(이미 한 번 일어났다)
  P계층 판정 서술 셋을 원자료에 맞췄다. 스파이크에 **`--wav a,b`(다중 턴)** 팔이 생겼다.

⛔ **이 갈래가 틀린 것 둘** — ⑴ **표본 넷으로 결론**했다가 여덟로 늘리자 무너졌다 ⑵ **하중을 지는
수치를 검산 없이 적었다**(회차 수와 이벤트 수를 섞었다). **둘 다 다른 세션이 잡아 줬다 — 내 검증만
으로는 걸리지 않았다.** 세는 **단위를 적고 기계로 재검산한다.**

---

## ② 다음 세션이 이어가는 데 필요한 것

### 테스트 갈래의 다음 한 걸음 — **`TASK-37` P계층 이월분**. 지금 막혀 있지 않다

⚠️ **`TASK-78` AC#3·AC#4 는 `TASK-81` 뒤다** — 그것을 기다리지 말고 아래를 먼저 한다.

| # | 무엇 | 알고 착수할 것 |
|---|---|---|
| **P4**(`p2k`) | 강한 억양의 앱 경로 관측 | ⛔ **그 픽스처를 세션의 «첫» 발화로 준다.** `TASK-65` 가 순서가 정한다는 것을 확정했으므로 다른 발화 뒤에 두면 **구조적으로 관측 불가**다(`scenarios-P-pronunciation.md` §3.2) |
| **P1**(`p2a`) | 정확 발음 기준선 | 4차수가 `p1` 쌍만 돌렸다. `p2a`·`p2k` 는 **앱 경로를 지나간 적이 없다**(원자료 헤더로 확인) |
| **P5** | 한글 전사문의 분석 처리 | **4차수 최우선 위험이었고 `p2` 쌍이 미실행이다.** ⛔ **워커가 필요하다 — `H-AT` 를 먼저 읽고 구간을 좁혀 켜고 즉시 되돌린다.** 보존 세션 여섯을 파괴한 그 경로다 |
| **P6** | 발음 오류의 오탐 | 같은 세션에서 얻는다. `error_patterns` 에 패턴이 생기면 오탐이고 `frequency` 를 오염시킨다 |

**절차 실측본은 `runs/2026-09-10-task78-app-path.md` 가 갖는다** — 재기동 명령 · 주입 스크립트 ·
teardown 형식(§1·§7). 요지 넷만: 백엔드 `VOICE_ADAPTER=nova WORKER_ENABLED=false`(⛔ 뒤쪽을 빼지
않는다 — `H-AS`) · 클릭은 **CDP 실제 입력**(`eval` 의 `element.click()` 은 user activation 을 만들지
않는다) · 픽스처는 `public/harness/` 에 이미 있다 · **teardown 은 시각창으로 잡고 7개 표를 기준선과
대조해야 끝난 것**이다(실패한 실행도 세션을 만든다).

**N계층도 열려 있다** — `N6`·`N11`·`N12`·`N13`. ⚠️ `N13`(endpointing)은 **앱 경로로 못 잰다**
(`nova.py` 가 `inputAudioDetectionOffsetMs` 를 중계하지 않는다) — 스파이크로만 대조된다.

### 구현 갈래의 다음 한 걸음

**`TASK-81` → `TASK-79` → `TASK-80` → 좁힌 재리뷰 → `TASK-73`.**
`79`·`80`·재리뷰·`73` 이 닫히면 **Phase 1 완료 선언**이 되고, `81` 은 발음 경로를 살리는 선행이다.

| | 무엇을 알고 착수하는가 |
|---|---|
| `TASK-81` | 결정 50 의 순서 ①이다. 계획 블록(`Focus on:`·무대·힌트)이 전부 문법을 가리켜 **발음 코칭이 밀려난다.** ⛔ `TASK-75`(규칙 9 게이트)와 **다른 층**이다 — 묶으면 한쪽을 고치고 둘 다 고쳤다고 착각한다. `TASK-78`(다른 세션)이 이것을 기다린다 |
| `TASK-79` | ⛔ **상수를 키워서 닫지 않는다.** 스윕 시점을 보장하는 **계약이 없어** 어떤 상수도 맞을 수 없고, 지금 계측도 지연 시나리오를 못 잡는다 |
| `TASK-80` | 결정 41(자격증명 회전 면제)에 **걸리지 않는다** — 회전이 아니라 선택 로직의 결함이다 |
| 재리뷰 | ⚠️ **범위를 좁혀 돌린다**(사용자 지시). 전체 Phase 1 모듈이 아니라 **`79`·`80` 이 건드린 자리와 이웃만** — 1·2차 판정이 나머지를 덮었다 |
| `TASK-73` | ⛔ **codex 리뷰가 도는 동안 뮤테이션을 걸지 않는다** — 리뷰어가 변이된 코드를 읽어 판정이 무효가 된다. 대상 단정은 그 노트가 줄 번호로 갖고 **W1 은 완료**다 |

**Phase 1 선언 자리는 AC 문서**(`2026-08-25-first-slice-acceptance-criteria.md` 「선언 자리」 절)이고
근거는 `docs/design/2026-09-09-phase1-completion-audit.md` 다. ⛔ **원장에 상태를 두 벌 쓰지 않는다.**

### 착수 전 필수

1. ⛔ **받은 캡틴 결정을 다시 묻지 않는다.** 정본은 register 하나다. 지금 판단을 바꾸는 것 넷:
   **43**(태스크 경계에서 멈추지 않는다) · **40**(크리티컬 아닌 것은 사전 승인 — 실물 호출·워커·
   재기동 포함) · **48**(등가 증거는 뮤테이션 KILL) · **50**(계획 수정 → 재측정 → 그때 우회로 제거.
   **순서를 건너뛰지 않는다**).
2. ⛔ **발음 tool 의 현재 사실**: 배선은 문제가 **아니고**(문장 되말하기 ⟺ tool) **지금 앱 설정에서
   코칭 자체가 안 일어난다.** 지금 우회로를 걷어내면 **둘 다 0** 이 된다.
   ⚠️ **`--app-prompt` 는 기반 프롬프트다**(2,339자). 앱은 `build_system_prompt`(인자 6개)로
   계획·무대를 붙여 보낸다 — **이 구별을 놓치면 통제 대조가 무효다.** 재결정 전에는 `toolChoice`
   강제를 제품 경로에 넣지 않는다(강제하면 발화가 사라진다).
3. ⚠️ **다른 세션이 같은 리포에서 일한다.** 매번 `ListAgents` 로 확인하고 **`pytest` 를 돌리기 전에
   알린다**(`H-X` — codex 도 스스로 돌린다). ⛔ **커밋은 경로를 열거해 스테이징한다**(`H-AM`) —
   2026-09-10 에 디렉터리로 `add` 해 **다른 세션의 태스크 파일을 내 커밋에 삼켰다.** 남의 태스크
   md·handoff 를 건드리지 않는다.
4. ⚠️ **백엔드 기준선**: pid **58241** · `VOICE_ADAPTER=stub_unresponsive` · `WORKER_ENABLED=false`.
   ⛔ **워커를 켜기 전에 `H-AT` 를 읽는다** — `flush_ended_sessions` 가 job 을 새로 만들어 보존
   세션을 파괴한다(두 번 일어났다). DB 는 **공유 인스턴스**다(`:5432`) — 재시작·`ALTER SYSTEM` 금지.
5. ⚠️ **`backlog task list --ready` 는 상태를 걸러내지 않는다** — SessionStart 브리핑을 믿는다.
6. **최근에 실제로 물린 함정 넷**(전체는 `pitfalls.md`): `H-AV`(`tests/` 아래 새 `.py` 는 `ty` 대상인데
   `pytest` 가 수집하지 않는다 — 하네스를 건드렸으면 게이트를 모두 다시 잰다) · `H-AJ`(`pytest` 에
   **경로만** 주면 async 가 죽는다 → `-k` 만) · `H-AU`(전수에는 `command grep -r`) ·
   `H-AR`(format 의 「N files」는 `.md` 를 세므로 지표로 적지 않는다).

### 게이트 — 이 cwd 에서만 판정한다 (`H-A`)

```bash
cd app/backend
.venv/bin/pytest -q                        # ⛔ 동시 실행 금지 (H-X) · 경로만 주면 async 가 죽는다 (H-AJ)
.venv/bin/ruff check . && .venv/bin/ruff format --check .
/Users/redstar/.local/bin/ty check         # ⚠️ `.venv/bin/ty` 는 없다 — pipx 전역
.venv/bin/ruff check ../../tests ../../scripts    # 게이트 «밖» — 기준선 0
.venv/bin/ruff format --check ../../tests         # 게이트 «밖» — unformatted 0
cd ../frontend && npx tsc --noEmit && npx eslint app lib
```

⚠️ 게이트 전에 `brew services list | grep postgresql@17` — 안 떠 있으면 대량 errors 가 나는데
**회귀가 아니라 연결 거부다**(`H-T`).

### 인계 지표 — **직접 돌려** 얻고 대조한다

| # | 지표 | 2026-09-10 마감 시점에 직접 돌려 얻은 값 |
|--:|---|---|
| 1 | `git rev-parse --short HEAD` · `git status` | **`c12c176` 이상**(등호를 요구하지 않는다 — `H-P`) · `origin` 과 동기 |
| 2 | `grep -h "^status:" backlog/tasks/*.md \| sort \| uniq -c` | Done **60** · To Do **21** · In Progress **3**(`TASK-70`·`TASK-73` 내 것 · **`TASK-78` 다른 세션**) · Awaiting Decision **0** |
| 3 | 게이트 | **884 passed** · `ruff check` exit 0 · format **unformatted 0** · `ty check` 통과 · 게이트 **밖** `ruff` **0건** · 프론트 `tsc`·`eslint` exit 0 |
| 4 | DB (읽기만) | `schema_migrations` **9** · `learning_sessions` **13** · `utterances` **120** · `error_patterns` **9** · `error_occurrences` **24** · `review_tasks` **15** · `session_plans` **2** · `pronunciation_attempts` **4** · `analysis_jobs` **49** · **보존 세션 6개 전건 생존**. ⚠️ **수치에 표 이름을 붙여 적는다** |
| 5 | 결과 API 상태 6개 | `210233be` `analyzing` · `6225ddaf` `final` · `b2f0d169` `partial_failure` · `76d9ef31` `connection_failed` · `d127dece` `no_utterances` · `e0c5e580` `final`(교정 2 + 드릴 미달 — **A4-2·A3-3 표본 겸용**). ⛔ **여섯을 지우지 않는다** |

⚠️ **3번과 5번이 핵심이다** — 읽기는 전달을 증명하지 못하고 **직접 돌린 출력**만 데이터다.
⚠️ **인용한 남의 수치는 그쪽이 고칠 때 함께 낡는다** — 최소 1건을 직접 검증하고, 정정되면 내 문서도
같은 턴에 고친다(2026-09-10 에 두 번 일어났다).

### 이 리포의 지배 실패 모드

**"본문을 고치고 그것을 설명하는 문장을 안 고친다."** 본문을 고쳤으면 그것을 설명하는 문장·개수·
방향·시그니처를 **같은 커밋에서** 고친다. 개수를 말하는 문장은 **아예 세지 않는 서술로 바꾸는 것**이
실제로 통한 유일한 구조적 해법이다.

⛔ 파생 규칙 셋(전부 실측): **설계서·원장의 「~하지 않는다/없다」를 근거로 쓸 때 지금도 참인지 코드로
확인한다**(`H-AL`) · **재는 단위를 앱이 도는 단위와 맞춘다**(단일 발화로 재고 다중 턴을 결론해 결정
하나를 잘못 만들었다) · **한 명령에 여러 단계를 묶지 않는다**(앞 단계 실패를 확인하지 않고 다음을
실행해 보존 세션을 파괴했다 — `H-AT`).

### 진입 절차

1. **원장을 조회해 태스크 ID 를 말한 뒤 착수한다.** 태스크가 없는 일이면 **먼저 등록한다.**
2. 게이트를 `app/backend` cwd 에서 돌려 지표 3 과 대조한다. **다르면 그 차이를 먼저 설명한다.**
3. 태스크 상태는 원장을, 다음 걸음이 바뀌면 이 파일을 갱신한다. 커밋 뒤 **푸시한다.**
4. ⛔ **태스크를 `Done` 으로 올린 같은 턴에 다음 태스크를 조회해 착수한다 — 보고로 턴을 끝내지
   않는다**(결정 43). **멈추는 조건은 셋뿐이다**: 승인 밖 항목 · 인계 트리거(≲35%·compact 경고 —
   이때는 멈추는 것이 아니라 **마감**) · 사용자가 멈추라고 한다.
