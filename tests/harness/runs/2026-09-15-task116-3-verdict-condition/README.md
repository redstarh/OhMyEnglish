# 회차 — `TASK-116.3` AC#1·#2: 결정 82 의 검사가 **발화하지 못하는 조건**을 특정한다

세션 `ohmyenglish-65` · 2026-09-15 KST · 태스크 `TASK-116.3`(결정 97 이 「다음 수단」으로 지목함) ·
관측의 출처는 `runs/2026-09-14-task129-plan-review-key` 와 `runs/2026-09-15-task81-app-leg` 임

> ⛔ **Nova 를 쓰지 않았음.** 재는 것이 **기록 경로**이고 입력(코치 발화 · 실린 키)이 이미 실측돼
> 있음. 실물 왕복은 같은 입력을 다시 만들 뿐이라 판정을 바꾸지 않음.
> ⚠️ 그래서 이 회차의 비용은 **0** 임(Nova 0 · Claude 0).

---

## 0. 두 팔 — **판별력을 재는 팔을 함께 둠**

⛔ 재현만 하면 「비어 있음」이 **코드 때문인지 입력 때문인지** 가르지 못함. 그래서 같은 경로에
입력 한 조각만 바꾼 팔을 붙였음.

| 팔 | 코치 발화 | 미리 적은 기대 |
|---|---|---|
| **A(재현)** | 브라우저 레그 ARM-3 의 **실측 발화 원문** — `"early"` · `"er-lee"` · 문장 전체를 인용함 | `sound_check` 가 **비고** 복습 예정일이 그대로 남음 |
| **B(판별력)** | 같은 발화에 `The "er" sound was the one that slipped.` 한 조각만 더함 | `mismatched` 로 표시되고 복습 예정일이 **물러남** |

⛔ **B 가 뒤집히지 않으면 「1~3자 인용이 문턱이다」라는 판정이 틀린 것**이고 그때는 다른 원인을 찾음.

**스택**: 검증 전용 DB `ohmyenglish_t1163`(마이그레이션 024) · 제품 writer 만 씀
(`create_session` · `save_final_transcript` · `record_attempt` · `check_recorded_sounds`) ·
`WORKER_ENABLED=false` · 공유 dev DB 와 `:8002`·`:3000` 을 건드리지 않음.
스크립트는 `reproduce.py.txt` 이고 출력 원본은 `results.txt` 임.

## 1. 결과 — 기대대로 갈렸음 (`results.txt` 의 값)

| | A(재현) | B(판별력) |
|---|---|---|
| 인용 토큰(1~3자) | **`[]`** | **`['er']`** |
| `sound_check_verdict` | **`None`** | **`mismatched`** |
| 표시된 행 수 | **0** | **1** |
| 행의 `sound_check` | **`None`** | **`'mismatched'`** |
| 복습 예정일 | 검사 전후가 **같음**(전진한 채 남음) | 검사 뒤 **물러남**(그 시도가 이력에서 빠짐) |

B 에서 제품 로그가 그대로 났음 — *"코치가 말하지 않은 소리로 기록된 발음 시도 1건을 복습에서
제외했다"*. ⇒ **기제 자체는 작동함. 무력화한 것은 입력의 «모양» 임.**

⚠️ `frequency` 값은 두 팔이 같은 패턴에 쌓여 올라감(1·2 → 재실행에서 3·4). **판정에 쓰지 않는 값**이고
`results.txt` 는 재실행 판의 출력임.

## 2. AC#2 의 답 — 조건은 **따옴표로 감싼 1~3자 영문 토큰**임

정본은 `app/backend/app/services/pronunciation.py` 의 인용 정규식임 — ⚠️ **이 절은 고치기 «전»의
상태를 적은 것이고 그때 이름이 `_QUOTED_SOUND_RE` 였음.** AC#4 에서 `_QUOTED_TOKEN_RE` 로 바뀌었음(§6):

```
['"‘’“”]([A-Za-z]{1,3})['"‘’“”]
```

그 상한 3 의 근거가 주석에 있음 — `TASK-128.3` 회차에서 **인용된 소리는 `th`·`er`·`f` 였고 낱말은
`early`·`think`·`fine` 처럼 4자 이상**이었음. 즉 「소리와 낱말을 길이로 가른다」가 설계 의도임.

⛔ **일반 세션의 코치는 그 모양으로 말하지 않음.** 실측 인용 셋 —

| 코치가 인용한 것 | 왜 문턱을 넘지 못하나 |
|---|---|
| `"early"` | 5자라 상한 3 을 넘음 — **낱말로 분류됨** |
| `"er-lee"` | 하이픈이 들어가 `[A-Za-z]{1,3}` 에 걸리지 않음 |
| `"my brother will early vealy tomorrow morning."` | 문장 전체 인용 |

⇒ **토큰이 0개가 되어 함수가 `None` 을 돌려주고, 호출부는 그것을 배제 사유로 쓰지 않음**(설계가
정한 안전한 방향임 — 「어긋남만 증명한다」). **결과가 오염 통과임.**

⚠️ **전용 모드에서는 같은 함수가 작동했음** — 그 모드의 코치는 `the "th" sound` 처럼 **소리를 이름으로**
인용함. 즉 결정 82 의 이행은 **모드에 따라 유효성이 갈림**이고 그것이 이 태스크의 결함 서술임.

## 3. AC#1 의 답 — 재현됨

A 팔이 그것임: 일반 세션의 실측 발화로 `sound_check` 가 비고 **복습 예정일이 전진한 채 남음**.
⛔ 앞선 두 회차의 관측(일반 세션 3/3 · 브라우저 1/1)과 **같은 결과가 기록 경로만으로 재현됨** —
즉 원인이 Nova 의 변덕이 아니라 **코드의 문턱**임이 확정됨.

## 4. ⛔ 다음은 사용자 판단임 (AC#3)

「인용이 없을 때도 배제할지」는 **결정 82 의 전제(잘못된 배제가 더 비싸다)를 건드리는 것**이라
팀리드가 정하지 않음. 후보를 셋으로 정리함 — 대가를 함께 적음.

| 후보 | 무엇 | 대가 |
|---|---|---|
| ① 문턱을 낱말까지 넓힘 | 인용 상한을 늘려 `"early"` 같은 낱말도 대조 재료로 씀 | 키에 우연히 겹치는 낱말이 늘어 **정상 기록을 배제**할 수 있음 |
| ② 낱말과 키를 «소리»로 잇는 겹을 새로 만듦 | `early` → /r/ 계열처럼 낱말에서 소리를 얻어 키와 대조함 | 사전·규칙을 새로 발명해야 하고 결정 54·59 가 경계한 「추정값」에 가까움 |
| ③ 그대로 두고 다른 겹에서 막음 | 검사는 손대지 않고 기록 시점이나 계획 경로에서 다른 방어를 세움 | 이 태스크가 열린 채 남고 오염이 계속됨 |

## 5. 정리 — 직접 돌려 얻은 값

| 무엇 | 확인 |
|---|---|
| 검증 전용 DB | `dropdb ohmyenglish_t1163` **exit 0** · 남은 DB 는 `ohmyenglish`·`_test`·`_smoke` 뿐 |
| 실물 사용 | **Nova 0 · Claude 0** |
| 공유 dev DB | 회차 전후가 **같음** — `learning_sessions=17` · `pronunciation_attempts=7` · `error_patterns=9` · `session_plans=6` · `utterances=128` · `review_tasks=15` |

---

## 6. AC#4 — 결정 98 을 이행했음 (TDD)

사용자가 이 세션이 코드를 쓰는 것을 승인했음(2026-09-15). 절차는 이 리포의 TDD 규약을 따랐음.

### 고친 것 둘

| # | 자리 | 무엇 |
|--:|---|---|
| ① | `_QUOTED_TOKEN_RE` | 하이픈 · **닫는 따옴표 앞의 문장부호** · **낱말 안의 아포스트로피**를 받음. 앞 둘은 실측 발화 `is "er-lee."` 를 통째로 놓쳤기 때문이고, 셋째는 codex 리뷰가 찾은 소유격 오추출을 막기 위함임(아래 리뷰 절) |
| ② | `sound_check_verdict` | 갈래를 둘로 나눔 — 소리 인용이 있으면 이전 거동 그대로, **낱말만 인용됐으면** 키의 소리 조각이 그 낱말에 한 자리도 없을 때만 `mismatched` |

`_key_sound_segments` 는 키를 `[^a-z]+` 로 쪼개고 연결어(`as`·`to`·`for`·`vs`·`instead`)를 뺌.
⛔ **조각이 둘 미만이면 판정을 포기함** — 키 모양을 계약으로 쓰지 않기 위한 안전판임.

### RED → GREEN 경위 — 실패를 두 번 봤음

1. **RED ①** 낱말 단정 둘을 먼저 썼고 `assert None == 'mismatched'` 로 실패했음(기능 부재).
2. **GREEN ①** 갈래 둘을 구현했고 9건 통과. 그때 **기존 테스트 하나가 깨졌음** —
   `test_speech_without_a_quoted_sound_gives_no_verdict` 가 이전 거동을 못박고 있었음.
   ⛔ 지우지 않고 **뒤집힌 사실과 근거를 그 테스트 docstring 에 적었음**(이름도
   `test_speech_that_quotes_nothing_gives_no_verdict` 로 좁혔음).
3. **RED ②** 종단 재현 출력이 인용 토큰을 `['early']` 하나만 냈고, 그래서 **하이픈 표기는 잡히지도
   않았으며 내 첫 테스트는 낱말 갈래로 통과한 것**이었음. ⚠️ 이름이 주장하는 것과 실제로 도는 갈래가
   달랐음 — 출력을 읽어서 발견했음. 그 자리에 하이픈 전용 단정을 새로 썼고 실패를 봤음.
4. **GREEN ②** 정규식에 문장부호를 더해 `er-lee` 가 잡히게 했음.

### ⛔ 설계를 한 번 뒤집었음 — 하이픈을 소리로 쪼개지 않음

첫 구현은 `"er-lee"` 를 `er`·`lee` 로 쪼개 **소리 토큰**으로 썼음. 그것을 버린 이유가 실측 논리임 —
키가 `r_as_l` 인 **정상 기록**에서 `er` 이 그 키 «안에» 없어(`er` ⊄ `r_as_l`) `mismatched` 가 되고
**복습 일정이 지워짐.** 하이픈 토큰을 낱말로 두면 키의 조각 `r` 이 `er-lee` 안에 있어 배제하지 않음.
⇒ 단위 테스트가 그 두 방향(`f_as_p` → `mismatched` · `r_as_l` → `None`)을 한 자리에서 못박음.

### 판별력 — 무력화로 확인했음

보호 갈래(`any(segment in word …) → None`)를 지우고 돌렸더니
`test_a_quoted_word_does_not_exclude_a_record_that_names_that_word_s_own_sound` 가
`'mismatched' is not None` 으로 **실패했음.** 되돌린 뒤 9건 통과 · `MUTANT` 흔적 0건을 grep 으로 확인했음.

### 종단 재현 — 고친 코드로 다시 돌렸음 (`results-after-fix.txt`)

| | 고치기 전(`results.txt`) | 고친 뒤 |
|---|---|---|
| 인용 토큰 | `[]` | **`['early', 'er-lee']`** |
| 순수 함수 판정 | `None` | **`mismatched`** |
| 표시된 행 수 | 0 | **1** |
| `sound_check` | `None` | **`'mismatched'`** |
| 복습 예정일 | 전진한 채 남음 | **`None`**(그 시도가 이력에서 빠져 예정이 사라짐) |

제품 로그도 함께 났음 — *"코치가 말하지 않은 소리로 기록된 발음 시도 1건을 복습에서 제외했다"*.

### 게이트 (직접 돌린 출력)

`pytest` **1194 passed**(13.12s · 이전 1191 + 새 단정 3) · `ruff check .` · `ruff format --check .`
**48 files** · `ty check` — 넷 다 exit 0.

⚠️ **작성과 검증을 같은 컨텍스트에서 끝내지 않음** — 이 변경은 codex 리뷰에 걸었고 그 결과와 처리를
아래에 적음.

### codex 리뷰 결과와 처리 — 발견 5건(HIGH 3 · MEDIUM 2 · CRITICAL 0)

⛔ **받은 대로 구현하지 않고 각 항목을 코드로 확인한 뒤 갈랐다** — 셋은 **결정 98 이전부터 있던 것**이고
하나만 이 변경이 만든 것이다.

| 등급 | 발견 | 내 확인 | 처리 |
|---|---|---|---|
| HIGH | 소유격 `"Sri's"` 의 `'` 를 닫는 따옴표로 오인해 `Sri` 를 소리로 뽑음 ⇒ 키 `s_as_z` 정상 기록 배제 | **재현됨**(테스트가 `'mismatched' is None` 으로 실패). ⚠️ 앞 판 정규식도 1~3자였으므로 **이전부터 있던 오추출** | **고쳤음** — 낱말 안의 아포스트로피를 낱말의 일부로 받음(뒤에 글자가 올 때만). 실패 테스트를 먼저 보고 고쳤음 |
| HIGH | 철자가 소리를 감추는 낱말(`laugh` 의 /f/ = `gh`) ⇒ 정상 기록 배제 | **맞음. 이 변경이 만든 것임** — 갈래 ②가 철자로 대조한다 | ⛔ **고치지 않음.** 고치려면 낱말→소리 사전이 필요하고 그것은 **결정 98 이 기각한 후보 ②** 임 ⇒ 테스트에 **이름을 붙여** 대가를 드러냈고 결정 대장의 대가 서술을 정정했음 |
| HIGH | 세션 전체 발화를 창으로 써서 앞 시도의 짧은 소리가 뒤 시도의 판정을 선점 | **맞음. 다만 이전부터임** — 창을 넓게 둔 것이 설계 §4-1 의 의도이고 앞 판도 같은 창을 썼음. ⚠️ 갈래 ②는 반대로 안전함(낱말 하나라도 키 조각을 담으면 `None`) | **`TASK-116.4` 로 등록했음** — 창 정책은 이 태스크 밖이고 좁히면 `TASK-128.3` 실측(판정 턴 인용)이 다시 걸림 |
| MEDIUM | 문장 전체 인용은 여전히 안 잡혀 오염이 통과함 | **맞음** | 테스트로 **이름을 붙였음**(`test_a_whole_sentence_quote_is_still_not_judged`) · 주석의 낡은 서술도 고쳤음 |
| MEDIUM | `"Sri's"` 단정이 같은 발화의 `"th"` 때문에 오추출을 가림 | **맞음** | 소유격 **단독** 반대 방향 단정을 새로 썼음(위 첫 행) |

**리뷰 뒤 게이트**: `pytest` **1197 passed**(13.42s · 단정 3건 더함) · `ruff check .` ·
`ruff format --check .` **48 files** · `ty check` — 넷 다 exit 0.
