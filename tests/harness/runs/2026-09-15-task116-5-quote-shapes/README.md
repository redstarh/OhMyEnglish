# 회차 — `TASK-116.5` AC#1·#2: 문장 전체 인용이 얼마나 오는가

세션 `ohmyenglish-65` · 2026-09-15 KST · ⛔ **새 회차를 돌리지 않았다**(AC#1 이 그것을 요구했다) —
재료는 이미 있는 프레임 증거 `.harness/evidence/*frames*.json` 이다. 실물 모델 사용 **0**.

분류는 **제품 코드의 문턱을 그대로 쓴다** — `count_quote_shapes.py.txt` 가
`pronunciation._QUOTED_TOKEN_RE` 와 `_SOUND_MAX_LEN` 을 import 한다. ⛔ 정규식을 회차 쪽에 다시
쓰지 않았다(두 곳에 두면 한쪽이 낡는다).

## 1. 실측 (`results.txt` 의 출력)

**발화 단위** — 프레임 파일 66개 · 코치 발화 **120건**:

| 모양 | 수 | 검사가 하는 일 |
|---|--:|---|
| `sound`(1~3자 토큰) | **44/120** | 갈래 ①로 판정한다 |
| `word`(낱말·하이픈) | **10/120** | 갈래 ②로 판정한다(결정 98) |
| `sentence_only`(따옴표는 있고 토큰 0) | **5/120** | ⛔ **판정하지 못한다 — 이 태스크의 구멍** |
| `none`(인용 없음) | **61/120** | 판정하지 못한다(설계가 받아들인 자리) |

⚠️ **그런데 판정은 «세션 단위»로 돈다** — `check_recorded_sounds` 가 그 세션의 agent 발화를 이어
붙인다. 그래서 결정에 걸리는 수치는 세션 단위다:

| 세션(프레임 파일) 59개 | 수 |
|---|--:|
| 판정 가능(소리 또는 낱말 인용이 하나라도 있음) | **37/59** |
| ⛔ **문장 인용만 있어 막힌 세션** | **2/59** (`PQS-pq04` · `T123B1`) |
| 인용이 전혀 없는 세션 | **20/59** |

⇒ **이 태스크가 겨냥한 구멍은 2/59 다.** ⚠️ 더 큰 미판정 덩어리는 「인용 없음」 20/59 이지만 그것은
**설계가 받아들인 자리**다(증명할 수 없으면 배제하지 않는다) — 이 태스크의 결함이 아니다.

## 2. AC#2 — ⛔ **지금 닫지 않는다**(위임받아 정함)

| 후보 | 왜 채택하지 않았나 |
|---|---|
| 검사 쪽: 문장 인용을 낱말 자루로 쪼갠다 | ⛔ **이득이 없다.** 한 문장에는 낱말이 여럿이라 갈래 ②의 `word_supports_key` 가 거의 항상 참이 되고 결과는 지금과 같은 `None` 이다 — 판정력이 늘지 않는다 |
| 프롬프트 쪽: 코치가 소리를 인용하게 만든다 | **결정 82 가 「프롬프트를 더 고치지 않는다」로 막았다** — 뒤집으려면 사용자 판단이 필요하고, 2/59 로 그 판단을 요청할 근거가 약하다 |
| 그대로 둔다 | **채택.** 수치를 남기고 태스크를 `To Do` 로 파킹한다 |

⇒ 태스크를 **닫지 않고 `To Do` 로 둔다.** ⛔ AC#3(구현)을 하지 않았으므로 체크하지 않는다 —
결과에 맞춰 AC 문면을 고치지 않는다. 다시 올릴 조건은 **이 비율이 커지는 것**이고, 그때 같은
스크립트로 다시 세면 된다(`count_quote_shapes.py.txt` 가 그 도구다).
