# 5차수 — `TASK-37`

> **루프 5/10.** 원장은 [ROUNDS.md](ROUNDS.md) 이고 이 파일이 5차수의 관측 정본임.
> 선언 범위는 4차수가 남긴 것 전부임: N6~N13 · B1~B4 + 라이트/다크 5상태 ·
> 회귀(A1·A2·E4·E5·E6·D1·D2 + 프론트 `tsc --noEmit`) · P8 · P계층 `p2` 쌍 · P7 재캡처 ·
> `tests/harness/**` ruff 정리 · A-3 tool 스키마 · A-4 · B-2 · O-1 · P9~P12 신설.
>
> ⛔ **캡틴 결정 38 (2026-09-09)** — 실물 Nova·마이크 세션 **1회**를 승인받았음. 제약 4개는
> `docs/ops/captain-instruction-register.md` 결정 38 이 소유하고 여기서 재서술하지 않음.
> 요지 하나만 옮김: **실물 관측을 쪼개 여러 세션으로 나누지 않음.**

## 착수 시점 기준선 — 이 턴에 직접 돌려 얻었음

| 무엇 | 값 |
|---|---|
| HEAD | `19192bc` · 추적 미커밋 0건 |
| 백엔드 게이트 (`app/backend` cwd) | **843 passed** 16.11s · `ruff check` exit 0 · `ruff format --check` 34 files · `ty check` exit 0 |
| 프론트 | `npx tsc --noEmit` exit 0 · `npx eslint app lib` exit 0 |
| 게이트 «밖» (착수 전) | `ruff check ../../tests ../../scripts` **6 errors** · `ruff format --check ../../tests` **4 files** · `../../scripts` 0 |
| DB | `schema_migrations` 9건(`001·003~007·009·010·011`) · `review_tasks` 10행(done 2 · pending 8) · `next_review_at` 8건 · `shadowing_items` 1행 · `utterances` 114행 · public 표 17개 |
| 스택 | 프론트 `:3000`(node pid 37650) · 백엔드 `:8002`(python pid 14879) 기동 중. ⛔ **정정 — 이 줄이 빠뜨린 것이 회차를 막았음**: 그 프로세스는 **2026-09-06 23:59:26** 에 떴고 `--reload` 가 없어 **`TASK-43`·`44`·`45` 코드를 실행하지 않았음**(소스 34건 중 16건이 프로세스보다 새로움). 「기동 중」은 참이었으나 **어느 코드를 돌리는지**를 적지 않은 것이 결함이었음. 아래 「브라우저 다리」가 그 경위를 가짐 |

---

## AC#6 — `tests/**` ruff·format 정리 ✅

**결과: 게이트 밖 기준선이 6 errors·4 files → `0`·`0` 으로 내려갔음.**

대상은 `tests/harness/` 의 네 파일이었음:

| 파일 | 무엇 | 어떻게 |
|---|---|---|
| `inject_errors.py` | I001 import 정렬 · E501 2건 | `--fix` + SQL 문자열을 줄로 쪼갬 |
| `measure_contrast.py` | E501 1건 | `print(...)` 안으로 넣어 f-string 을 쪼갬 |
| `spike_nova_protocol.py` | I001 import 정렬 | `--fix` |
| `ws_session.py` | E501 1건 · UP041 | 무음 프레임을 `silence` 지역변수로 뽑음 · `except (asyncio.TimeoutError, TimeoutError)` → `except TimeoutError` (`target-version = "py313"` 이라 동일 객체임) |

⛔ **`--fix` 를 디렉터리에 걸지 않았음** — 네 파일을 인자로 명시했음(2026-09-08 에 `--fix ../../tests/`
가 범위 밖 파일을 고친 사고가 있었음).

⚠️ **수치가 0 이 된 것은 스크립트가 도는 것을 증명하지 않음.** `inject_errors.py` 는 import 순서가
바뀌어 `psql_cli` 가 `app.*` **앞**으로 갔음 — 그 파일은 `sys.path.insert(0, BACKEND_DIR)` 뒤에
import 하는 구조라 순서가 런타임에 걸릴 수 있는 자리였음. 그래서 네 스크립트에 `--help` 를 돌려
**전부 exit 0** 을 직접 확인했음. 하네스 자체 게이트도 돌렸음: `test_c2_gates.py` +
`test_c3_gates.py` **71 passed**.

**이 정리가 낡게 만든 문장 넷을 같은 작업에서 고쳤음** (이 리포의 지배 실패 모드 대응):
`docs/ops/pitfalls.md` `H-L`·`H-AN` · `handoff/HANDOFF.md` 의 게이트 명령 주석·경고 상자·지표 3.
**완료 태스크의 노트는 고치지 않았음** — 그 수치는 그 턴에 참이었던 실측 기록임(`H-AL`).

### 곁가지로 나온 함정 2건 — 둘 다 등록했음

**`H-AR` 신설 — `ruff format --check` 의 「N files」는 `.py` 개수가 아님.** 정리 직후
`../../tests` 가 **64 files** 였는데 이 회차 기록을 만든 뒤 **65 files** 가 됐음. `tests/` 의
`.py` 는 39개 · `.md` 는 26개이고 39+26=65 로 정확히 맞았음. 단독 확인:
`ruff format --check tests/harness/runs/ROUNDS.md` → `1 file already formatted`. **ruff 0.16.4 는
마크다운 안의 파이썬 코드 블록을 포맷 대상으로 삼음.** 게이트 «안»(`app/backend`)에서는 `.md` 가
`.pytest_cache/README.md` 하나뿐이고 ruff 가 캐시를 기본 제외해 `34 files` = `.py` 34개로 **우연히
일치함** — 그래서 안쪽만 보면 틀린 규칙을 배움. 대응으로 handoff 지표 3 의 파일 수를 지우고
**「unformatted 0」** 으로 바꿨음.

**`H-AO` 번호 충돌 정리.** `pitfalls.md` 에 `H-AO` 가 두 항목(`[skip ci]` · 위임 리뷰)에 붙어
있었음 — 그 파일 자신의 규칙 위반임. 외부 인용 4건(`HANDOFF.md` 3건 · `TASK-45` 노트 1건)이
**전부 위임 리뷰 쪽**을 가리키는 것을 grep 으로 확인해 인용이 없는 `[skip ci]` 쪽을 **`H-AQ`** 로
옮겼음. 인용을 깨지 않는 방향을 근거로 골랐고 그 근거를 항목 안에 남겼음.

---

## AC#7 — A-3 tool 스키마 대조

### ① 정적 대조 ✅ — 스파이크가 앱 상수를 그대로 보내게 바꿨음

닫는 방법은 `docs/design/2026-09-08-tasks-md-archive.md` §A-3 이 지정해 뒀음:
*"`spike_nova_protocol.py` 의 `_PRONUNCIATION_TOOL_SCHEMA` 를 앱 상수로 바꿔 1회 왕복한다."*
그대로 했음 — 하드코딩 상수 2개를 지우고 `app.models.pronunciation` 의
`PRONUNCIATION_TOOL_NAME`·`PRONUNCIATION_TOOL_SCHEMA_JSON` 을 import 함.

두 봉투를 실제로 만들어 대조한 결과(실물 호출 0회 · `_tool_configuration()` 대 
`nova._pronunciation_tool_configuration()`):

| 항목 | 결과 |
|---|---|
| `toolSpec.name` | **동일** — `report_pronunciation_coaching` |
| `inputSchema.json` 문자열 | **동일** (바이트 비교 `True`) |
| `inputSchema.json` 이 문자열인가 | `True` — 객체가 아님(스파이크 F1 이 실측한 것) |
| `toolSpec.description` | **다름** — 앱에 `for later practice.` 가 붙음. Nova 수락 여부에 무관하므로 맞추지 않았음 |

앱이 보내는 모양(이제 스파이크도 같은 것을 보냄): 필드 **4개**
(`outcome`·`spoken_form`·`target_form`·`target_sound`) · required **2개**
(`target_form`·`outcome`) · `outcome` enum **4개**(`pending`·`correct`·`incorrect`·`unclear`).

⚠️ **F821 이 이 변경의 판별력을 증명했음.** 상수를 지운 뒤 `--help` 는 exit 0 이었는데
`ruff` 가 `_PRONUNCIATION_TOOL_NAME` 미정의 1건을 잡았음 — 그 줄이 `--help` 경로에 없었기
때문임. **`--help` 통과를 「스크립트가 성립한다」로 읽으면 안 되는 실측 사례임.**

**원자료 보존**: 재실행은 `.harness/evidence/P-tooluse-nova-protocol.json`(로컬 전용)만 덮음.
2026-08-27 실증본은 `runs/2026-08-27-P-tooluse-spike/P-tooluse-nova-protocol.json` 에 추적본으로
남아 있어 안전함.

### ② 실물 왕복 ✅ — **A-3 가 닫혔음** (2026-09-09 · 결정 38 · 구간 A1)

`spike_nova_protocol.py --wav p1m.wav --tools` **1회**. 아카이브 §A-3 이 지정한 닫는 방법 그대로임.

| 무엇 | 결과 |
|---|---|
| Nova 가 앱 스키마를 받아들였는가 | **예** — `toolUse` **1건**. 관측 이벤트: `toolUse` 1 · `textOutput` 3 · `audioOutput` 85 · `usageEvent` 19 |
| payload | `{"target_form":"I think I found three very useful videos.","outcome":"pending"}` · `toolName` = `report_pronunciation_coaching` · `role` = `TOOL` |
| **`pending` 이 실제로 쓰였는가** | **예** — 그 값은 스파이크의 **옛 3필드 스키마에 없던 것**임. 즉 수락에 그치지 않고 Nova 가 **골라 썼음** |
| required 2개만 왔는가 | **예** — `spoken_form`·`target_sound` 는 없음(둘 다 optional 이므로 계약대로임) |
| **앱 파서가 그것을 받는가** | **예** — `parse_tool_payload` 가 `PronunciationReport(target_form=…, outcome='pending', spoken_form=None, target_sound=None)` 를 냄. ⛔ **스키마 수락과 앱 파싱은 다른 일이라 따로 쟀음** |

**Nova 가 발음 코치 행동을 실제로 했음**: `Great! Let's work on the pronunciation of that sentence.`
+ `' Say this after me: "I think I found three very useful videos."'`
⚠️ **뒤 문장에 선행 공백이 있음** — `O-1`(agent 전사문 선행 개행)의 친척임. 4차수에서 관측한
선행 `\n` 과 같은 부류이고 여기서는 스파이크 경로라 저장 job 이 없어 무해함.

⚠️ **`p1m`(음소 치환)의 전사문이 `i think i found three very useful videos.` 임** — 정확 발음판과
사실상 같음. 4차수의 「약한 발음 오류는 ASR 이 원문으로 복원한다」가 재현됐음.

### ⛔ 이 회차가 답하지 않은 것 — 구분하지 않으면 틀린 결론이 남음

**`target_sound` 가 `None` 으로 온 것은 Nova 의 거동이 아님.** 이 회차는 **스파이크의 5문장
프롬프트**를 썼고 그것은 그 키를 요구하지 않음. **앱 프롬프트는 `nova.py:143` 에서
`Always include target_sound` 를 요구함**(직접 확인). 즉 앱 경로 관측이 따로 필요하고
그 소유자는 `TASK-13` 임.
⚠️ **이 구분 없이는 「Nova 가 그 키를 안 채운다」로 잘못 결론낼 자리였음** — 그리고 그 결론은
복습 주기(`refresh_review` 가 `target_sound` 로 찾는다)의 방아쇠가 원리적으로 안 걸린다는
말이 되어 `TASK-44` 의 구현 근거를 뒤집었을 것임. **아카이브 §A-3 표의 세 번째 행(시스템
프롬프트 차이)이 여전히 열려 있다**는 것이 정확한 서술임.

**낡아진 서술 둘을 같은 작업에서 고쳤음**: `nova.py` 의 `_pronunciation_tool_configuration`
docstring(*"실물 왕복으로 확인한 적이 없다"* + 아카이브된 `TASKS.md` 인용) ·
`docs/design/2026-09-08-tasks-md-archive.md` §A-3(*"남은 것은 이것 하나다"*).

---

## AC#5 — P9~P12 발음 복습 주기 시나리오 신설 ✅(작성) · 미실행

정본에 넣었음: `scenarios-P-pronunciation.md` §4.1. **규칙을 재서술하지 않고 「무엇을 관측하는가」만
담았음** — 규칙 계층은 `tests/unit/test_pronunciation_review.py` **19건**이 이미 덮음(직접 세어
확인했음). 그래서 하네스가 더하는 넷만 남겼음: **Nova 의 선택에서 시작하는 종단** · **실데이터** ·
**실물 계획 1회** · **화면**.

인용한 심볼 4개를 코드로 확인했음(`H-AL` 대응 — 설계서 서술을 근거로 쓸 때의 규약):
`build_plan_prompt`(`app/backend/app/services/plan.py:273`) ·
`allowed_pattern_ids`(`app/backend/app/models/plan.py:206`) ·
`"Due for review today"`(`plan.py:148`) · `refresh_review`(`pronunciation.py:523`).
⚠️ **첫 grep 이 `build_plan_prompt` 을 못 잡아 「없다」로 갈 자리였음** — 함수 목록을 다시 뽑아
실재를 확인했음. 도구가 한 번 못 잡은 것을 부재로 읽지 않음.

---

## 브라우저 다리 1차 시도 — `BLOCKED` 에서 배운 것

`frontend-verifier` 에 위임했고(등록부 §1 이 이 지시를 손으로 대체한 것을 실패로 기록해 둠)
**프리플라이트 P5 에서 막혀 단정 18건 전부 미평가**로 돌아왔음. 상세는
[2026-09-09-run-5-browser.md](2026-09-09-run-5-browser.md).

**막은 것**: `:8002` 를 듣던 pid **14879** 가 **2026-09-06 23:59:26** 에 떴고, 그 뒤 `app/backend/app/`
소스 **34건 중 16건**이 프로세스보다 새로웠음 — `--reload` 가 없으므로 그 프로세스는 HEAD 코드를
실행하지 않았음. **팀리드가 직접 다시 재서 같은 값을 얻어 확인했음**(위임 결과를 그대로 쓰지 않음).

✅ **위임의 판단이 옳았음.** 검증자 역할이 프로세스 재기동을 금지하므로 고치지 않고 멈췄음.
그리고 **P5 자신이 제 몫을 했음** — 통과가 아니라 실패를 냈고, 검사한 소스 개수 34건을 함께
인쇄해 공허 통과가 아님을 증명했음.

⛔ **위임이 문서 결함 1건을 찾았고 그것을 고쳤음**: §2 는 *"P5 가 실패하면 재기동한다"* 고 적고
§8-⑤ 예외는 *"백엔드를 호출자가 세웠으면 손대지 않고 「무변경」을 보고한다"* 고 적어, **호출자가
세운 백엔드가 낡았을 때 검증자가 할 수 있는 일이 문서 안에 없었음.** 거짓 통과가 아니라
**절차 불능**임. 규약을 넣었음 — 검증자는 `BLOCKED` 로 멈추고 낡은 소스 개수와 기동 시각을
보고하며, **재기동은 호출자의 몫**임.

⛔ **팀리드가 같은 문서에서 두 번째 어긋남을 찾았음**: 「기동 순서」의 명령이 `--log-level warning`
이었는데 **P5 의 신원 확인이 `Started server process [pid]` 를 찾고 그것은 uvicorn INFO 임.**
그대로 띄우면 P5 가 그 assert 에서 죽음. 명령을 `--log-level info` + `> /tmp/omy-backend.log` 로
고쳤음.

**재기동 실측 (팀리드가 직접 함)**:

| 무엇 | 값 |
|---|---|
| 종료 → 기동 | pid **14879** → pid **26253** |
| 명령 | `WORKER_ENABLED=false VOICE_ADAPTER=stub nohup .venv/bin/uvicorn app.api.main:app --port 8002 --log-level info > /tmp/omy-backend.log 2>&1 &` |
| 확인 | `/health` `{"status":"ok"}` · `ps eww` 에 두 플래그 확인 · 로그 `Started server process [26253]` 일치 |
| P5 재실행 | **통과** — `pid 26253 · 기동 00:27 전 · 소스 34건 → 통과` |
| 보존 세션 5개 | **재기동 전후 완전 동일** — C3a `completed`/`pending:3`/발화 6 · C3b `completed`/`done:1`/4 · C3c `completed`/`done:2,failed:1`/6 · C3d `failed`/job 0건/0 · C3e `completed`/**job 0건**/6 |
| 전체 집계 | 재기동 전후 동일 — `learning_sessions` 12 · `error_patterns` 8 · `utterances` 114 · `pending` job **7** · `review_tasks` 10 · `shadowing_items` 1 · `pronunciation_attempts` 4 |

⚠️ **`pending` job 이 7건인 것이 `H-AS` 의 위험을 수치로 만듦** — 워커를 켠 채 띄우면 그 7건이
전부 발화해 버드록 비용이 나가고 **C3a 의 `analyzing` 상태가 파괴됨.** 플래그를 붙인 재기동이
그것을 실제로 막은 것을 전후 대조로 확인했음.

## 브라우저 다리 2차 시도 — 회차 `8bc59725` · 단정 18건 평가

프리플라이트 P1~P9 **전건 통과**(P5 `pid 26253 · 소스 34건 → 통과`). 판정: **`PASS` 5건 ·
`BLOCKED` 12건 · C1 부분**. 상세는 [2026-09-09-run-5-browser.md](2026-09-09-run-5-browser.md).

✅ **`PASS` 5건 전부 음성 대조가 실제로 FAIL 을 냈음** — 이것이 판정을 신뢰하는 근거임:
A1-0(클릭 후 1 → 0 전이를 직접 관측) · A1-5(무력화 4종이 `APP_CONTENT_MISMATCH`·
`APP_EXCESS_RENDER`·이름 있는 throw 를 냈고 `verdict` 를 해석 없이 베낌) · A3-1(5상태 재방문에서
같은 요소가 매번 다른 라벨) · A3-2(실제 세션에서 오류 문구 부재) · A4-1(빈 세션 2건에서 접두 0개).
C3·C4 실행체는 단정 **57건 전건 통과**·exit 0.

⚠️ **`BLOCKED` 12건의 대부분이 원인 하나임 — 어댑터 모드.** 위임이 그것을 결함으로 보고했고
**규약으로 닫았음**: 한 회차는 어댑터 모드 하나만 판정하고, 호출자가 모드를 명시하며, 두 모드가
필요하면 회차를 두 번 연다(실물 0회라 비용은 시간뿐임). **평가하지 못한 것을 `PASS` 로 올리지
않은 것이 이 회차의 값어치임.**

⛔ **위임이 결함 2건을 찾았고 둘 다 고쳤음**:
① **한 회차로 두 어댑터 모드를 판정할 수 없음** — 재기동을 호출자 몫으로 정한 오늘 커밋의
**필연적 결과**이고 어느 쪽의 결함도 아님. 위 규약을 `browser_leg.md` §2 에 넣었음.
② **A1-6 의 유도 방식이 계측에 없었음** — §5 는 *"`session_started` 프레임의 `session_id` 를
계측이 기록해 대조"* 를 요구하는데 `instrument.js` 는 그 프레임을 **세기만** 했음(회차가 DB 세션
행으로 대신 대조했고 그것은 지정된 유도가 아님). 팀리드가 grep 으로 직접 확인한 뒤
**`startedSessionId`**(first-wins)를 계측에 넣었음. 백엔드가 실제로 그 키를 싣는 것을
`audio_gateway/session.py:130-133` 에서 확인했음 — **키 이름이 달랐으면 항상 `null` 이 되어
조용히 무의미해질 자리였음.** 하네스 자체 게이트 **71 passed** 로 무회귀를 확인했음.

### 팀리드가 화면을 직접 봤음 — 남의 `PASS` 는 증거가 아님

`.harness/evidence/` 7장의 md5 를 다시 계산해 보고값과 일치하고 **전부 서로 다름**을 확인했음.
그리고 `c3-final.png`(md5 `e91117203e7f9aaee5d5fa4c543535f6`)를 **이미지로 열어서 봤음**:

- 상태 라벨 **「확정」** 이 제자리에 렌더됨 (A3-1 판정과 일치)
- 교정 카드 1개 · `원문: go to gym` / `교정문: go to the gym` / **라벨 없는 셋째 줄이 비지 않음**
  (`gym처럼 늘 다니는 장소를 말할 때는 …`) — A4-1 판정과 일치
- 위계가 살아 있음 — 이유 줄이 라벨보다 흐림. **다크 모드에서 F-1 수정이 유지됨**
- **숫자 렌더 0건** — `TASK-34` 가 자동 게이트로 만들려는 계약이 이 화면에서 지켜짐

`drill` 줄이 없는 것은 회귀가 아님 — 그 세션(C3b)은 2026-09-06 생성이라 드릴 턴 마이그레이션
009(2026-09-07 적용)보다 앞섬.

⚠️ **위임이 이 회차 밖에서 바이트 동일 3건을 발견해 적어 뒀음**(`c2-dark.png` ·
`c2-selftest-failure.png` · `c2-selftest2-failure.png` 가 모두 md5 `b2c5daa5…`, 2026-09-06 생성).
**이 회차가 만든 파일이 아니므로 원인을 조사하지 않았음** — 4차수 교훈 5 와 같은 부류로 보임.
조사 주체가 정해지지 않았으므로 여기에 관측으로만 남김.

### ⚠️ 수치 불일치의 결말 — **팀리드의 정정이 틀렸음. 표가 달랐음**

경위를 그대로 남김. 위임이 baseline 복귀를 **`12·45·114·8·1·1`** 로 적었고, 팀리드가 두 번째
칸을 `error_occurrences` 로 읽어 **18** 을 재고 *"`45` 는 재현되지 않음"* 이라 적었음.
**그 정정이 틀렸음** — 위임이 되짚어 지적하고 팀리드가 직접 확인했음:

| 표 | 값 |
|---|---|
| `analysis_jobs` | **45** |
| `error_occurrences` | **18** |
| `learning_sessions` · `utterances` · `error_patterns` · `session_plans` · `learner_notes` | 12 · 114 · 8 · 1 · 1 |

즉 위임의 6수치는 `analysis_jobs` 를 담았고 **두 값 다 자기 표에서 맞음.** 팀리드가 표 대응을
잘못한 것임.

⛔ **일반화 — 이 리포의 지배 실패 모드의 새 변종임.** `H-AN` 이 *"수치에 **도구** 이름을 붙인다"*
를 정했는데, DB 수치에는 **표 이름**이 같은 자리임. **표 이름 없는 수치 묶음(`12·45·114·…`)은
읽는 사람이 엉뚱한 표에 대응시킨다** — 실제로 그렇게 됐음. 위임이 3차 보고에서 3열 표의 각 행에
쿼리를 붙여 그 경로를 닫았음. **앞으로 DB 수치는 표 이름이나 쿼리와 함께만 적음.**

✅ **양쪽이 놓치지 않은 것**: §8 이 정의하는 드리프트는 어느 총계도 아니라 **패턴 `frequency` 대
`harness_pattern_baseline`** 이고, 팀리드가 직접 조회해 두 회차 뒤 모두 **0**(8개 패턴 전건 일치 ·
baseline 밖 0건)을 얻었음. 회차도 `harness_runs` 에 정상 등록됨. 보존 세션 5개는 job 단위까지
온전함.

## 브라우저 다리 3차 시도 — `stub_unresponsive` · 회차 `bcafc8ba` · **11건 전부 `PASS`**

어댑터 모드 규약대로 호출자가 `stub_unresponsive` 로 재기동하고(pid 26253 → 56138) 그 모드가
뒷받침하는 것만 지시했음. **11건 전부 `PASS`** 이고 **음성 대조가 전부 실제로 FAIL 을 냈음**:

| 무엇 | 대조가 FAIL 을 낸 근거 |
|---|---|
| A1-1·A1-2·A1-3 대조 | `recv.final`·`partial`·`audio` 가 **전부 0** — 2차의 6·6·3 에서 떨어졌음 |
| A1-4 대조 ① | `started` = `{count:0, when:[], calls:[]}` — 2차의 3 에서 떨어졌음 |
| A1-6 대조 | URL 이 `/` 에 남고 `사유: voice_adapter_connect_timeout` — 2차의 `/results/<uuid>` 가 안 나왔음 |
| A1-8 대조 | 그 세션 `utterances` **0행** · 창 안 세션 5건 전부 0행 · 전체 `utterances` **114 불변** |
| A2-1·A2-2·A2-3 | 라이트 muted `rgb(89,89,89)` ≠ 다크 muted `rgb(154,154,154)` · fg 라이트 `rgb(23,23,23)` ≠ 다크 `rgb(237,237,237)`. **두 모드를 다 돌렸음** — 한 모드면 `A2-3 미평가`(FAIL) |
| A5-1·A5-2 | 4 outcome 이 같은 `p[aria-live="polite"]` 에서 매번 바뀌고 `PRONUNCIATION_BADGE` 와 등호 일치 · sentinel 4회 전부 0건이면서 같은 프레임의 outcome 문구는 있었음 |

C2 실행체는 단정 **56건 전건 통과**·exit 0.

✅ **팀리드가 넣은 계측 키가 실제로 동작했음** — `startedSessionId` 가
`db1e7af0-1146-4a0a-a8e8-84f98072e09a` 로 채워지고 `recv.session_started === 1` 이었음.
⚠️ **그 uuid 는 지금 DB 에 없음** — teardown 이 회차 세션을 지웠기 때문이고
(`learning_sessions` 12 = baseline) **정상 동작임.** 다만 그것은 **회차 안에서만 검증 가능한
증거**였으므로 4차 회차에 `startedSessionId` 와 URL 을 같은 `eval` 반환 JSON 에 담게 지시했음.

⚠️ **A1-6 은 아직 완결되지 않았음** — 본 단정이 2차(모드 `stub`)에 있고 대조가 3차에 있는데,
2차의 본 단정은 **DB 세션 행으로 유도**했음(§5 가 지정한 계측 유도가 그때는 없었음). 그래서
baseline 으로 되돌린 뒤 **4차 회차를 A1-6 본 단정 하나로** 열었음.

### 위임의 결함 ③·④ 처리

**③ fetch 캐시가 낡은 계측을 먹였음** — 계측을 고친 직후 첫 취득이 낡은 판을 받아 페이지 안
sha256 `846f130f…` 과 파일 해시 `cbc19fb6…` 가 어긋났고, 그날 넣은 `startedSessionId` **부재**로
교차 확인됐음. `{cache:'no-store'}` 로 해결. **`browser_leg.md` §10-3 에 규약으로 넣었음.**
⚠️ **sha256 대조가 없었으면 조용히 통과했을 자리임** — 낡은 계측으로 잰 값이 새 단정의 증거가
됐을 것임. 즉 그 항목의 값어치는 전사 드리프트 방어만이 아니라 **캐시 방어까지**임.

**④ C5 에 실행체가 없음** → **`TASK-49` 로 원장에 등록했음.** 이 환경 실측 라운드트립
**20,635 ms** 가 주입 창 **10,000 ms** 를 반드시 넘겨, CDP 클릭으로 activation 을 만든 뒤 같은
문서에서 재시도를 합성 클릭하는 우회로 성립시켰고 그 결과 **A5-1 의 대조 ①이 오염됐음**
(`PASS` 는 대조 ②를 근거로 냈고 한계를 기록했음). ⚠️ **`H-AE` 는 고치지 않았음** — 그 함정이
이미 CDP 클릭 대 합성 클릭의 갈림을 담고 있고 이 회차가 그것을 **재확인**한 것임.

## 브라우저 다리 4차 시도 — A1-6 을 지정된 유도로 닫음 · 회차 `3d7ddd94`

baseline(`stub`)으로 되돌린 뒤(pid 56138 → 72290) **A1-6 본 단정 하나만** 지시했음. `PASS`.
계측의 `startedSessionId` 가 `d08d470a-9347-407d-b2ca-68b7f73fd4ad` 로 채워지고
`recv.session_started` 가 1 이라 `null` 의 두 뜻이 발생하지 않았으며, `expectedPath`
(`/results/d08d470a-…`)와 `actualPath` 가 **등호로 일치**했음. **기대값·관측값·`recv` 를 같은
`eval` 반환 JSON 에 담아** 회차 밖에서도 대조되게 했고 **DB 를 유도에 쓰지 않았음.**
계측은 §10-3 새 규약대로 `{cache:'no-store'}` 로 받아 sha256 `cbc19fb6…` 가 파일 해시와 일치했음.

✅ **위임이 자기 대조 1건을 판별력 0 으로 스스로 낮춰 적었음.** `negControl_sentinelPathEquals` 는
본 단정이 등호를 요구하므로 sentinel 부등호가 **필연적으로 참**임 — §5 가 A4-2 대조 ①을 철회한
것과 같은 형태임. **그 판단을 그대로 받아들였음.** A1-6 을 받치는 대조는 3차의 모드 대조 하나임.

### A1 합산 — 여덟 건 `PASS` · A1-7 하나만 `BLOCKED`

`A1-0`·`A1-1`·`A1-2`·`A1-3`·`A1-4`·`A1-5`·`A1-6`·`A1-8` = **`PASS`**(본 단정과 대조를 얻은 회차가
칸마다 표시돼 있음). **`A1-7` 만 `BLOCKED`** 이고 사유는 **화면 결함이 아니라 표본 구성**임 —
스텁 세션이 32 ms 캡처 프레임 1건이 만들어지기도 전에 끝남(`elapsed 0.035s` 를 팀리드도 관측함).
3차의 `stub_unresponsive` 가 `sent.audio` **313** 을 낸 것이 그 진단을 뒷받침함.

**팀리드가 4차 보고를 전부 직접 대조했음**: §8 정의 drift **0** · 표별 수치 전건 baseline
(`learning_sessions` 12 · `analysis_jobs` 45 · `error_occurrences` 18 · `utterances` 114 ·
`error_patterns` 8 · `review_tasks` 10 · `shadowing_items` 1 · `pronunciation_attempts` 4) ·
보존 세션 **5개 생존** · 백엔드 pid 72290 플래그 동일 · 임시 CORS 서버 종료(`8899` 응답 `000`).

## A2 (깨진 프레임 내성) — 팀리드가 직접 돌림 · `PASS`

`ws_session.py --scenario A2-junk --junk --send-audio 3 --timeout 20`.
**17 프레임 완주** · `session_ended` 도달 · `order` 가 `session_started` → … → `session_ended`
로 온전함(발화 6건 · `audio` 3건 각 6444B `RIFFWAVE`).

⛔ **「죽지 않았다」로 끝내지 않았음** — 그것만으로는 정크가 **도달했는지** 알 수 없음. 백엔드
로그에서 정크 5종이 **각각 다른 이름 붙은 경로로 버려진 것**을 확인했음:
`base64로 해석할 수 없는 오디오 프레임을 무시했다` **2건**(비ASCII base64 · 잘못된 base64 알파벳) ·
`알 수 없는 클라이언트 메시지를 무시했다: '무엇'` · `JSON으로 해석할 수 없는 프레임을 무시했다` ·
`텍스트가 아닌 프레임을 무시했다`(바이너리 프레임). **다섯 갈래가 다섯 정크에 대응함.**

**teardown 을 직접 했음**: 그 세션이 발화 6건·job 3건(`pending`)을 만들었고 전부 지웠음 —
`learning_sessions` 13 → **12** · `utterances` 120 → **114** · `analysis_jobs` 49 → **45** ·
`pending` **7** 복귀 · drift **0**. ⚠️ **job 3건을 남기면 워커가 켜지는 순간 발화함** —
`H-AS` 의 위험에 내가 3건을 더할 자리였음.

## 브라우저 다리 5차 시도 — 라이트 모드 결과 화면 5상태 · 회차 `cb2f4e19` · **네 항목 `PASS`**

AC#2 문구는 *"B1~B4를 재확인하고 **라이트·다크** 5상태를 확인한다"* 임. 2차 C3 는 **다크** 한
모드에서 5상태를 냈고 C2 는 **세션 화면**을 두 모드에서 쟀음 — **결과 화면의 라이트 모드가
비어 있었음.** 1차수가 다크만 보고 2차수 R8 이 라이트를 본 그 항목임. 그것만 좁게 지시했음.

| 항목 | 판정 | 근거 |
|---|---|---|
| 5상태 라벨의 모드 무관성 | `PASS` | `분석 중`·`확정`·`부분 실패`·`연결 실패`·`분석 대상 없음` 이 라이트·다크에서 같음. **두 모드를 같은 실행에서 연달아** 재서 얻은 값이라 회차를 합친 것이 아님 |
| 교정 카드 위계 (라이트) | `PASS` | 라벨 줄 `rgb(23,23,23)` **17.93:1** 대 이유 줄 `rgb(89,89,89)` **7:1** — 덜 강조되면서 동시에 AA 를 넘음. 다크도 16.91:1 대 7.04:1 로 같은 위계 |
| 대비 AA (양쪽) | `PASS` | 라이트 15행 · 다크 15행 **전건 통과** · `[AA!]` 표시 0건. ⚠️ **F-1 회귀 확인** — 1차수 F-1 은 다크 확정 전사문 **1.05:1** 이었고 이번 다크 최저값이 **7.04:1** 이므로 회귀 없음 |
| 모드 전환의 음성 대조 | `PASS` | 10회 측정 전부 `page reports` 가 요청과 일치(어긋나면 회차를 죽일 자리) · 라이트 muted `#595959` ≠ 다크 `#9a9a9a` 로 C2 가 세션 화면에서 얻은 쌍과 같음 |

**세션을 하나도 만들지 않았음** — 보존 세션 5개 재방문임. teardown 이 **완전 무변경**
(`INSERT 0 0`·`DELETE 0`·`UPDATE 0`·`DELETE 0`).

✅ **스크린샷 규약이 이번에 제대로 지켜졌음.** 10장의 md5 가 전부 다르고(팀리드가 다시 계산해
확인 · 라이트·다크 쌍도 쌍마다 갈림) 프레임 하단에 `STAMP <pathname> | <ISO 시각>` 띠를 박아
**세션 id 가 화면 안에 남음.** 그 띠는 `main` **밖**에 붙여 측정을 오염시키지 않았음을
`stampInsideMain: false` 와 측정 행 수로 확인하고 회차 끝에 지웠음. 4차수 교훈 5 가 요구한 것이
처음으로 구조적으로 충족됐음.

**팀리드가 `r5-final-light.png` 를 직접 열어 봤음** — 흰 배경, 라벨이 다크와 같고, 이유 줄이
라벨보다 흐려 위계가 유지되며, 하단 노란 띠에
`STAMP /results/6225ddaf-90a8-43af-9aa8-e003921c75eb | 2026-09-08T19:40:54.326Z` 가 박혀 있음.
그리고 drift 0 · 표별 수치 전건 baseline · 보존 세션 5개 생존을 직접 조회해 확인했음.

**위임이 확인하지 못했다고 밝힌 것 둘**(통과로 적지 않음): 다크 png 을 눈으로 본 것(이번엔
수치로만 확인했고 다크 화면은 2차에서 팀리드가 봤음) · `danger` 토큰 렌더(이 5화면에서는
쓰이지 않음).

### ✅ AC#2 완료 — 근거

`B1`~`B4`(+`B5`)의 화면 상태를 C3 실행체가 **5상태 전건** 냈고(단정 57건 통과·exit 0),
**라이트·다크 양쪽**을 2차(다크)·5차(라이트, 그리고 같은 실행 안에서 다크 재측정)로 덮었음.
A3-1 의 음성 대조가 **같은 요소가 5상태 재방문에서 매번 다른 라벨**로 바뀌는 것을 보였으므로
상수 렌더 경로가 배제됐음.

---

## 실물 세션 1회의 순서 — 착수 전에 못박음

⛔ **즉흥으로 정하지 않음.** 결정 38 제약 1 이 *"실물 관측을 쪼개 여러 세션으로 나누지 않음"* 이라
빠뜨린 항목은 두 번째 세션의 비용이 됨. 아래가 그 한 자리의 순서임.

**실물 음성은 픽스처 브라우저 주입으로 얻음** — 사용자의 마이크를 요구하지 않음. 3차수 N5 가
`u1.wav` 브라우저 주입으로 실물 Nova 를 관통시킨 것과 같은 방법임(실물 마이크는 2026-09-01·09-03
에 이미 2회 완료돼 별도 관측 대상이 아님).

⛔ **재기동할 때 `WORKER_ENABLED=false` 를 반드시 붙임 — 기본값이 `True` 임**(`H-AS`, 이 차수에서
찾아 등록했음). 착수 전에 확인한 것: 지금 `:8002` 를 듣는 프로세스(pid 14879)의 환경에
`WORKER_ENABLED=false`·`VOICE_ADAPTER=stub` 이 들어 있음(`ps eww` 로 직접 읽었음). **문서에 적힌
기동 명령 셋은 그 플래그가 없었고** 그대로 따르면 보존 세션 C3a·C3e 가 파괴됨 — 세 문서를 고쳤음.
그 밖의 실효 설정: `nova_endpointing_sensitivity = LOW`(mic-1 이 내린 값 · N13 대조의 기준선) ·
`drill_count = 5` · `drill_turns_min = 4`. 픽스처 9개 전부 실재함
(`p1a`·`p1k`·`p1m`·`p2a`·`p2k`·`p2m`·`u1`·`u2`·`u3`).

### 구간 A — 워커 **꺼짐** (보존 세션이 살아 있는 구간)

| 순서 | 무엇 | 얻는 것 |
|--:|---|---|
| A1 | `spike_nova_protocol.py --tools` **1회** | **A-3 실물** — 앱 스키마(4필드·required 2·`pending`)가 `promptStart` 에서 수락되는가. `toolUse` 가 오면 `outcome` 값이 무엇인지 |
| A2 | 앱 경로 실물 세션 1개 — `u1.wav`+`u2.wav` 주입, 3턴 | **N7**(`sequence_no` 단조 · 사용자 발화당 job 1건) · **TASK-36**(exchange 수 + 소요 시간) · **O-1**(agent 전사문 선행 개행) |
| A3 | 같은 세션에서 agent 발화 중 오디오 주입 | **N6** barge-in — 1초 안에 출력 중단 · `stopReason=INTERRUPTED` · 클라이언트 큐 비움 |
| A4 | 잘못된 샘플레이트·코덱 프레임 주입 | **N12** 포맷 불일치 방어 — 조용히 죽지 않고 실패를 통보하는가 |
| A5 | `p2a`/`p2m`/`p2k` 주입(각 1세션) | **P계층 `p2` 쌍** — P1·P2·P3·P4 를 `p2` 로 재확인. **P7 재캡처**(⚠️ 세션 id 가 프레임에 들어가게 찍고 `md5` 로 서로 다른 파일임을 확인 — 4차수 교훈 5) |
| A6 | A2·A5 세션의 `pronunciation_attempts` 전수 조회 | **TASK-13**(`target_sound` 키 열거 + 음소 치환 대 비음소 분류 + 표본 수) · **TASK-24**(발음을 놓쳤는데 전사문이 한글도 아닌 턴 수 / 전체 발음 시도) · **P9**(Nova tool → `next_review_at` + `review_tasks` 종단) · **P12**(한글 전사 보조 신호가 시계를 만들지 않음) |
| A7 | 발음 개입이 없는 일반 대화 1턴 | **B-2 대조군** — 3차수 N5 와 같은가 |
| A8 | `endpointing` 을 `HIGH`/`MEDIUM`/`LOW` 로 바꿔 발화 종료 감지 시점 측정 | **N13** — mic-1 에서 `LOW` 로 내린 효과의 측정이 이 차수의 내용임 |

⛔ **A 구간에 들어가기 전에 `browser_leg.md` §9 보존 세션 5개(C3a~C3e)의 상태를 스냅샷함** —
결정 38 제약 2. 특히 **C3e 는 `flush_ended_sessions` 가 켜지면 되돌아감.**

### 구간 A2 실측 — 실물 Nova 앱 경로 세션 1건 (2026-09-09)

보존 세션: **`e0c5e580-dfc0-4793-b02d-54cf4346c3c5`** · `completed` ·
`started_at 19:54:29.256593+00` → `ended_at 19:56:15.624412+00` (**벽시계 1분 46.37초**).
⛔ **teardown 하지 않았음** — 구간 B 의 `N8` 이 그 job 을 씀. 증거는
`runs/2026-09-09-run-5-live/frames-e0c5e580.json` · `transcripts-repr-e0c5e580.txt`.

**① N7 `PASS`** — `sequence_no` **1~6 단조** · user/agent 교대 · **사용자 발화 1·3·5 에 job 각 1건**
(agent 발화 2·4·6 은 0건). 팀리드가 직접 조회해 확인했음. `recv`: final 6 · partial 7 ·
audio 341 · session_ended 1.
💡 **곁가지로 barge-in 이 실물에서 관측됐음** — 위임의 스케줄러가 agent 발화 종료 전에 다음 WAV 를
시작해 `interrupted` **2건**이 났고 **앱이 그것을 처리하고 3턴을 완주했음.** 의도한 관측이 아니므로
`N6` 을 이것으로 닫지 않되, 「끼어들면 앱이 죽는다」는 부류가 아님을 실물에서 봤음.

**② `TASK-36`** — exchange **3회** · 왕복별 **5,142 / 5,666 / 8,281 ms**(평균 **6,363 ms**) ·
`session_started` → 마지막 agent final **23,124 ms**.
⚠️ **벽시계 106초를 대화 소요로 읽으면 안 됨** — 마지막 final 이후 83초는 위임의 폴링 대기임.

⛔ **결정 20 의 모호함 중 한 갈래가 닫혔음. 다른 갈래는 열려 있음.**
- **「상한이 애초에 불가능하다」는 시간 근거로 배제됨**: 평균 6.36초/왕복이면 20 exchange 는
  **약 127초**이고 PRD 의 10분(600초) 예산의 **약 21%** 임. 즉 **시스템 지연이 구속 조건이 아님.**
  ⚠️ **결정 20 이 적은 「20 exchange 는 하나당 30초」는 600÷20 의 산수였고, 실측은 그 방향이
  아님** — 남는 473초가 학습자의 사고·발화에 쓸 수 있는 몫이고 왕복당 약 24초임.
- **「모델이 지시를 어겼다」는 갈래는 이 회차가 답하지 못함**: 3에서 멈춘 것은 **관측 설계가
  3턴이었기 때문**이고 모델의 선택이 아님. ⛔ **이것을 미달 로그로 쓰면 결정 20 이 경고한 바로
  그 오류임** — 불가능한 문턱을 향해 지시문을 고치게 됨.
- ⚠️ **실측값의 성격**: 하네스는 녹음된 WAV 를 흘리므로 **사고 시간이 0** 임. 6.36초는
  **하한**이고 실제 학습자는 더 걸림. 그래서 위 배제는 「시스템 지연 기준」으로만 성립함.

**③ `O-1` 부분 재현 — 문자가 다름.** **선행 개행(`\n`)은 0건**이고 DB 전사문의 선행 공백도 6행
전부 0임. 대신 **`partial` 프레임 4건에 선행 공백**(2·2·1·1자)이 있고 `final` 은 그 조각 병합으로
**agent 3행 전부 이중 공백**을 가짐. 팀리드가 DB 에서 직접 봤음:
`… what you did yesterday?  Try to say, "I ate a sandwich yesterday."` (`?` 뒤 공백 2개).
오늘 스파이크가 본 선행 공백과 같은 부류이고 **4차수가 적은 「선행 개행」과는 문자가 다름.**

**④ `TASK-13` `BLOCKED`** · **⑤ `TASK-24` `BLOCKED`** — 이 세션의 `pronunciation` 프레임 **0건** ·
`pronunciation_attempts` **0행 추가**(전체 4행이 실물 전과 동일 — 팀리드 확인:
`am_as_i_m`·`an_as_a`·`w_as_vw`, 4행 전부 `target_sound` 있음). 값역도 사각 비율의 분모도 얻지
못했음. ✅ **위임이 `0/0` 을 「0 %」로 적지 않았음.**

⛔ **원인은 관측 설계이고 그것은 팀리드의 지시 탓임.** 전사문이 그것을 그대로 보여 줌 — agent 가
3턴 **모두** `Try to say, "I ate a sandwich yesterday."` 로 시범 문장을 요청했는데, 팀리드가 지시한
픽스처가 **`u1`·`u2`·`u3`(깨끗한 발음)** 였음. **발음 오류 픽스처는 `p1m`(음소 치환)·`p1k`(강한
억양)인데 그것을 고르지 않았음.** 즉 앱 프롬프트가 `Always include target_sound` 를 요구해도
**고칠 발음이 없으면 tool 이 불릴 이유가 없음.** 위임은 원인으로 단정하지 않고 정황으로만 적었고,
두 번째 세션이 되므로 열지 않았음 — 지시를 지킨 것임.

**⑥ `refresh_review` 미발동** — `pronunciation_attempts` 4→4 · `review_tasks` 10→10 ·
`next_review_at` 8→8. **입력이 0 이라 발동할 것이 없었음**(고장이 아님).

**DB 증분을 남겨 뒀음**(구간 B 용): `learning_sessions` 12→**13** · `analysis_jobs` 45→**49** ·
`utterances` 114→**120** · `pending` job 7→**11**. drift **0**(읽기만) · 보존 세션 5개 생존 ·
백엔드 pid 96473 플래그 동일.

### 구간 B — 워커 **켜짐** (구간을 최대한 좁힘)

| 순서 | 무엇 | 얻는 것 |
|--:|---|---|
| B1 | A2 세션의 분석 job 을 돌림 | **N8** 실발화 → 실분석 병합(`u1`+`u2` → 같은 `article` 패턴, `frequency` **델타 +2**. ⛔ 절대값으로 단정하지 않음 — 4차수 교훈 4) |
| B2 | `p2k` 세션의 job | **P5 재확인** — 한글 전사문에 대해 `done`·findings 0 인가(4차수는 `p1k` 로 clean 이었음) |
| B3 | `inject_errors.py --scenario E4`·`E5`·`E6` | **AC#3 의 E계층 회귀** — E4 job 미생성 · E5 오류 0건 화면 · E6 재분석 멱등 |
| B4 | `inject_errors.py` 로 `pronunciation_intonation` 직접 주입 | **P8** 주입 내성 — 저장·조회·렌더가 깨지지 않는가 |
| B5 | 발음 패턴의 예정일을 지난 상태로 만들고 실물 계획 1회 | **P10 · AC11-6** — 「Due for review today」에 실리고 `allowed_pattern_ids` 에 들어가며 초점으로 고른 계획이 **거부되지 않는가** |

⛔ **B 구간이 끝나면 워커를 즉시 끄고 보존 세션 5개를 재조회해 A 구간 전 스냅샷과 대조함.**
어긋나면 그것을 **회귀로 보고함** — 조용히 넘기지 않음.

### 구간 C — 실물 0회

| 순서 | 무엇 | 얻는 것 |
|--:|---|---|
| C1 | `ws_session.py --junk` | **A2** 깨진 프레임 내성 |
| C2 | CDP 로 `getUserMedia` 거부 | **N11** `microphone_permission_denied` 화면 |
| C3 | 화면에서 발음 복습 과제를 찾음 | **P11** — 없으면 `FAIL` 이 아니라 **구조적 관측 불가**로 적고 부재를 기준선으로 고정함 |

### 일부러 빼는 것 — 근거를 함께 적음

| 무엇 | 왜 |
|---|---|
| **N9** 세션 롤오버 | 8분 이상 실음성이 필요함. 태스크 설명이 명시적으로 제외함 |
| **N10** 무응답형 자격증명 실패 | `.env` 편집 금지 제약에 걸림. 태스크 설명이 명시적으로 제외함 |
| **실물 마이크 세션** | 2026-09-01·09-03 에 2회 완료됨. 픽스처 주입이 같은 경로를 관통함 |
