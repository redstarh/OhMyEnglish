# 회차 — `TASK-86` 결정 60: 프롬프트 **구성**을 잰다 — 규칙 9·11 의 «문면»을 실제로 걷는다

세션 `ohmyenglish-7f` 후속 · 2026-09-11 · 브랜치 `design/first-vertical-slice`
회차 디렉터리: `tests/harness/runs/2026-09-11-task86-prompt-composition/`

> **결정 60**이 승인한 것은 **측정**이고 제품 경로 변경이 아니다. 실험은 하네스 팔에서 하고
> 제품 지시문(`nova.SYSTEM_PROMPT`)을 고치지 않는다.
>
> **왜 이 축인가**: 프롬프트 안에서 바꿔 본 것이 다섯인데(소리 줄 문구 · 초점 구성 · 놓친 소리
> 목록 · 질문 제거 · 질문 모양) **전부 tool 0** 이고, 같은 모델·같은 오디오·같은 tool 스키마에서
> **짧은 프롬프트만 15회에 14건**으로 tool 을 부른다.

---

## 0. ⛔ 상한과 해석 규칙 — 돌리기 전에 적는다

### 이 회차가 겨냥하는 것 — 프롬프트 안의 **모순** 하나

`_SOUND_INSTRUCTION` 은 *"take it up on that turn instead of the Grammar first rule 9"* 라고
**말하지만**, 규칙 9 의 본문은 프롬프트에 **그대로 남아 있다**:

```
9. Grammar first. On most turns, correct grammar and leave pronunciation alone. Take up
   pronunciation only when a sound is so far off ... — never for a mild accent.
11. A pronunciation correction is a correction. It counts against the one-per-turn limit
    in rule 4 — never add it on top of a grammar correction in the same turn.
```

⇒ 모델은 「대체한다는 선언」과 「대체 대상의 원문」을 **함께** 받는다. `TASK-75` 가 15회에서 얻은
것은 그 선언이 실린 판이고(코칭 2건 · tool 0) **문면을 걷어낸 판은 아직 재지 않았다.**

⚠️ **A7 회차의 발화가 이 가설을 가리킨다** — 코치는 무너진 발화를 **바르게 되말해 주고**
(`You finished the report and shared the results.`) 소리를 지목하지 않았다. 즉 그 턴의 한 교정을
**되말하기**에 썼고, 규칙 11 이 「발음 교정도 그 한 건을 쓴다」고 말하므로 발음이 들어갈 자리가
남지 않는다.

### 팔 A8(rule9cut) — 바꾸는 변수 **하나**: 규칙 9·11 의 문면

A7 프롬프트(`prompt_soundq.txt` · 5,078자)에서 **규칙 9·11 두 블록의 문장만** 갈아 넣는다.
번호는 그대로 둔다 — 소리 줄과 규칙 11 이 **번호로** 규칙 4·9 를 가리키므로 재번호하면 그 상호
참조가 깨진다(변수가 둘로 늘어난다).

| | 문면 |
|---|---|
| 뺀다 | `Grammar first. On most turns, correct grammar and leave pronunciation alone.` · `never for a mild accent.` · `never add it on top of a grammar correction in the same turn.` |
| 남긴다 | `name the sound that was off, say the whole sentence back ..., ask the learner to repeat it` · 규칙 10 전문(**한 자도 바꾸지 않는다**) · 계획 블록·질문 5개(A7 과 같음) |

⛔ **규칙 10 을 건드리지 않는다.** 그것을 같이 고치면 「모순 제거」와 「tool 지시 강화」가 섞여
어느 것이 효과를 냈는지 갈 수 없다. `MUST` 를 넣는 판은 **다음 팔**의 몫이다.

| 팔 | 프롬프트 | 회차 | wav |
|---|---|--:|---|
| **A8(rule9cut)** | A7 프롬프트 · 규칙 9·11 문면만 걷음 | **6** | `p2m.wav` → `p2a.wav` |
| **B(base)** | 스파이크 짧은 프롬프트 · 대조군 | **1** | 같음 |

**Nova 세션 상한 7회.** ⛔ 중간에 늘리지 않는다. 늘릴 것이면 **새 팔**을 열고 이유를 여기 적는다.
(오늘 누적 Nova: 앞 세션 70 + 이 세션 10 + 7 = **87**)

| 결과 | 읽는 법 |
|---|---|
| tool 이 **2회 이상** 온다 | 원인이 **프롬프트 안의 모순**으로 확정된다 → 제품 쪽 물음은 「소리 줄이 있는 세션에서 규칙 9·11 의 문면을 어떻게 뺄까」가 되고 그것은 지시문 변경이라 **사용자 결정 사안**이다 |
| tool 1건 | 방향은 관측됐으나 AC#2(같은 방향 최소 2회)는 닫히지 않는다 → 새 팔로 이어 잰다 |
| tool 0 인데 **소리 코칭이 난다** | 모순 제거가 **코칭까지는** 만든다 → 남은 축이 규칙 10 문면(`MUST` 부재)과 tool 경로로 좁혀진다 |
| tool 0 이고 소리 코칭도 0 | 모순도 원인이 아니다 → 남는 것은 **규칙 10 문면·역할 규정**(대조군은 자신을 *"pronunciation coach"* 로 규정하고 *"you MUST call"* 을 말한다)이고 그 팔을 새로 연다 |
| 대조군 tool 0/1 | 환경 고장이므로 **판정하지 않는다** |

⛔ **0/6 을 「모순 무효」로 단정하지 않는다** — 코칭 기준율 13% 가정에서 `P(0 in 6)=0.43` 이다.
배제되는 것은 높은 비율뿐이다(`P(0 in 6 | 0.4)=0.047`).

⚠️ **코칭 발생과 tool 도착을 갈라 센다.** 그리고 **되말하기는 코칭이 아니다**(결정 51 — 지목·재발화
요구만 코칭이다). A7 이 7/7 로 되말하기를 했으므로 이 구별이 이 회차의 판정을 가른다.

## 1. 팔 A8(rule9cut) — 실행과 결과: **모순을 걷어도 0 이다**

| 팔 | 세션 | tool 도착 | `target_sound` | 소리 코칭 | 프롬프트 |
|---|--:|--:|--:|--:|--:|
| **A8(rule9cut)** | **6** | **0/6** | 0/6 | **0/6** | 4,889자(A7 5,078 −189) |
| **B(base · 대조군)** | **1** | **1/1**(tool 2건) | 0/1 | 1/1 | 짧은 프롬프트 |

상한(§0) 7세션을 정확히 썼다. 죽은 회차 0.

⛔ **코치가 대신 «다른 초점»을 드릴했다.** 6회 전부 같은 방향이고, 발화 전문을 열어 확인했다
(내 추출기가 300자에서 자르므로 잘린 뒤에 코칭이 있었는지 직접 봤다 — 없었다):

- `Remember, we are focusing on using "the" before place names, like "the restaurant" or "the gym".`
- `Remember, we want to use "the" before place names, like "the park" or "the restaurant."`
- `Can you tell me where you usually go after work on a Thursday evening? Please use this sentence
  starter: I go to the ...`

즉 규칙 9 의 `Grammar first` 와 규칙 11 의 상호배제를 **문면째로 걷어냈는데도** 발음이 아니라
**계획의 문법 초점**(`article_missing_the_before_place_noun`)으로 갔다.

⚠️ **되말하기는 여전히 있었다** — `Great job sharing the report with your team.` ⛔ 결정 51 기준으로
코칭이 아니다(지목·재발화 요구가 없다).

**§0 이 미리 정한 읽는 법 그대로다**: *tool 0 이고 소리 코칭도 0 → 모순도 원인이 아니다 → 남는 것은
규칙 10 문면·역할 규정이고 그 팔을 새로 연다.*

⛔ **0/6 을 「모순 무효」로 단정하지 않는다**(`P(0 in 6 | 0.13)=0.43`). 배제되는 것은 높은 비율뿐이다
(`P(0 in 6 | 0.4)=0.047`).

## 2. 팔 A9(soundfirst) — ⛔ 상한 증설을 명시한다

**늘리는 이유**: §0 의 해석 규칙이 「소리 코칭도 0 이면 새 팔을 연다」로 미리 정했고 그 조건이
성립했다. ⛔ **A8 의 6회를 늘리는 것이 아니라 새 팔을 여는 것이다.**

| 팔 | 프롬프트 | 회차 | 바꾸는 변수 |
|---|---|--:|---|
| **A9(soundfirst)** | A7 프롬프트 · 소리 줄을 **맨 앞으로** | **6** | **위치 하나 — 문면은 한 자도 바꾸지 않는다** |
| **B(base)** | 대조군 | **1** | — |

**추가 상한 7세션** (오늘 누적 Nova: 앞 세션 70 + 이 세션 10 + 7 + 7 = **94**).

**기준을 A7 로 잡는다**(규칙 9·11 문면이 살아 있는 판) — A8 위에 얹으면 변수가 둘이 된다.
치환기가 `sorted(words)` 비교로 **문면 보존을 스스로 검사**하고 통과했다(`문면 보존: True` ·
5,078 → 5,079자, 늘어난 1자는 블록 경계 줄바꿈이다).

**왜 이 변수인가**: 결정 60 이 지목한 축이 *「규칙 10 을 앞으로 올리는」* 것이다. 대조군은 **첫
문장**에서 자신을 발음 코치로 규정하고 `you MUST call` 을 말하며 15회에 14건으로 tool 을 부르는데,
제품 프롬프트는 같은 내용을 **계획 블록 안쪽 46번째 줄**에 둔다. A7·A8 이 문면을 고쳐도 0 이었으므로
남은 후보가 위치다.

⛔ **`MUST` 를 함께 넣지 않는다.** A8·A9 가 전부 0 이면 그때 **코칭이 애초에 나지 않는 것**이
확정되고, 그러면 `MUST`(코칭 뒤에 오는 tool 지시)는 **선행 조건이 없어서** 애초에 작동할 수 없다 —
그 판정을 먼저 얻는 것이 순서다.

| 결과 | 읽는 법 |
|---|---|
| tool 2회 이상 | 원인이 **위치**로 확정된다 → 제품 물음은 「소리 줄을 어느 자리에 둘까」가 되고 지시문 변경이라 사용자 결정 사안이다 |
| tool 0 인데 소리 코칭이 난다 | 위치가 **코칭까지는** 만든다 → 남은 축이 규칙 10 문면(`MUST`)으로 좁혀지고 그때 그 팔을 연다 |
| tool 0 이고 소리 코칭도 0 | ⛔ **프롬프트 축이 소진된다** — 문면·구성·위치를 다 바꿔도 0 이므로 남는 것은 제품 경로 선택(전용 세션 · `toolChoice`)이고 **전부 사용자 결정 사안**이다 |
| 대조군 0/1 | 환경 고장이므로 판정하지 않는다 |

### 결과 — **위치를 맨 앞으로 올려도 0 이다**

| 팔 | 세션 | tool 도착 | `target_sound` | 소리 코칭 | 프롬프트 |
|---|--:|--:|--:|--:|--:|
| **A9(soundfirst)** | **6** | **0/6** | 0/6 | **0/6** | 5,079자(문면 보존) |
| **B(base · 대조군)** | **1** | **1/1**(tool 2건) | 0/1 | 1/1 | 짧은 프롬프트 |

상한(§2) 7세션을 정확히 썼다. 죽은 회차 0.

⛔ **소리 줄이 프롬프트의 «첫 줄»인데도 발화가 그것을 한 번도 집지 않았다.** 6회 발화 전문을
열어 확인했다(전문 길이 360·360·406·406·406·406자 — 잘림 없이 봤다):

- `I understand you finished a project with your team. Let's talk about where you go after work on
  Thursday evenings. ... where do you go after work on a Thursday evening? Please name two or three
  places.`
- `Let's focus on talking about where you go after work. Great, you shared the report with your
  team. ...`

즉 첫 줄이 *"Sound to coach today: th_as_s — this is a pronunciation focus, not a grammar one"* 인
프롬프트에서도 **코치는 계획의 질문·주제로 곧장 갔다.**

## 3. 판정 — **프롬프트 축이 소진됐다**

**§2 가 미리 정한 읽는 법 그대로다**: *tool 0 이고 소리 코칭도 0 → 프롬프트 축이 소진된다.*

| 이 회차가 잰 것 | 결과 |
|---|---|
| 프롬프트 안의 **모순**(규칙 9·11 문면 제거) | tool 0/6 · 소리 코칭 0/6 |
| 소리 지시의 **위치**(맨 앞으로 · 문면 보존) | tool 0/6 · 소리 코칭 0/6 |
| 같은 오디오의 **대조군** | 2회 전부 tool 도착 · 발음 지목 |

**A8+A9 합 0/12 대 대조군 2/2** — Fisher 양측 `p=0.011`.

**오늘까지 제품 규모 프롬프트 계열 누적 — `0/76`**:

| 회차 | 바꾼 변수 | 세션 | tool |
|---|---|--:|--:|
| D105 | 제품 계획(소리가 오디오에 없음) | 21 | 0 |
| D106 A2~A5 | 소리 일치 · 초점 구성 · 놓친 소리 목록 · 질문 제거 | 28 | 0 |
| D86-prio | 소리 줄에 우선순위 문장 | 8 | 0 |
| D86-soundq | **질문 모양**(형태 드릴 → 소리 연습) | 7 | 0 |
| D86-cut | **규칙 9·11 문면 제거** | 6 | 0 |
| D86-first | **소리 줄 위치를 맨 앞으로** | 6 | 0 |
| **합** | — | **76** | **0** |
| 대조군(짧은 프롬프트) | — | **17** | **16** |

Fisher 양측 `p < 1e-8`.

⛔ **여기서 처음으로 「낮은 비율」까지 배제된다.** 앞 회차들은 표본이 6~8 이어서 13% 기준율을
배제하지 못했다(`P(0 in 8 | 0.13)=0.33`). 누적 76 에서는 다르다 —
`P(0 in 76 | 0.05)=0.020` · `P(0 in 76 | 0.10)=0.0003`. 즉 **제품 규모 프롬프트에서 tool 도착률이
5% 이상이라는 가설이 배제된다.**
⚠️ **풀링의 한계를 적는다** — 여섯 회차의 프롬프트가 서로 다르므로 이 수치는 「어느 한 프롬프트의
비율」이 아니라 **「제품 규모·구성 계열의 비율」**이다. 그 계열이 이 회차들이 바꾼 여섯 변수 전부에
대해 0 이라는 것이 주장이다.

**⇒ 다음은 프롬프트가 아니다.** 문면·구성·위치·질문·초점·목록을 다 바꿨고 전부 0 이다. 남은 것은
**제품 경로 선택**이고 그 둘은 사용자 결정 사안이다(`TASK-106` §7 · 결정 60 이 고르지 않은 셋):

1. **발음 전용 세션**(`TASK-10` · PRD AC10-5) — 짧은 지시문이 실제로 작동하는 것이 17회에 16건으로
   실증됐으므로, 그 지시문 규모를 **제품의 한 모드로** 만드는 안이다.
2. **`toolChoice` 강제**(결정 49·50 이 승인 없이 금지) — ⚠️ 이 회차들이 그 위험을 **키웠다**:
   코칭 자체가 0 이므로 강제하면 **코칭 없는 턴에 빈 기록**이 남는다.
3. **보조 신호로 축소**(결정 50 의 ③ 철회) — 결정 59 가 그 우회로의 값어치를 이미 좁혔다
   (보조 신호는 복습 단계를 전진시키지 않는다).

⛔ **`TASK-86` AC#2 는 여전히 닫히지 않는다.** 이 회차는 「무엇이 원인이 아닌가」를 좁혔고
「tool 이 온다」를 만들지 못했다.

⚠️ **이 회차가 새로 얻은 사실 하나** — 코치는 **계획의 문법 초점을 충실히 수행한다.** A8 에서
규칙 9 를 걷어냈을 때 발음으로 가지 않고 **그 계획의 다른 초점**(`the` + 장소)으로 갔다. 즉
「발음이 밀린다」가 아니라 **「발음이 후보 목록에 들어가지 않는다」**에 가깝다. ⚠️ 단정하지 않는다 —
이것을 재는 팔은 없었다(초점을 발음 하나로 둔 D106 A3 은 8회 0 이었고 그때는 질문이 관사 드릴이었다).
