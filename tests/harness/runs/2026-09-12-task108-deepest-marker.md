# 회차 — `TASK-108`: 고친 프롬프트로 계획을 다시 생성해 `parse_plan` 이 통과하는지 본다

세션 `ohmyenglish-f4` · 2026-09-12 · 브랜치 `design/first-vertical-slice` · 기준 커밋 `bee48cc`
회차 디렉터리: `tests/harness/runs/2026-09-12-task108-deepest-marker/`

> **고친 것**(커밋 `bee48cc`): 만성 줄에 `[deepest recurrence]` 표식을 붙이고 조건부 블록
> `_DEEPEST_FOCUS_RULE` 로 초점 포함을 요구함. 순위는 `chronic.py` 의 `deepest_recurrence` 가 내고
> `parse_plan` 이 받는 값도 같은 함수의 결과임.
>
> **이 회차가 닫으려는 것**: `TASK-108` AC#2 — 같은 재료로 계획 생성을 다시 돌려 `parse_plan` 이
> 통과하는 것을 실물로 확인함.

---

## 0. ⛔ 상한과 해석 규칙 — 돌리기 전에 적는다

**Claude 상한 3회.** ⛔ 중간에 늘리지 않음. 늘릴 것이면 새 팔을 열고 이유를 여기 적음.
⛔ DB 는 SELECT 만 함 — `process_plan` 을 부르지 않고 `build_plan_prompt` → Claude → `parse_plan`
까지만 함. 계획 행·노트 행을 쓰지 않음.

### 재료 조건 — 이것을 먼저 기록하고 어긋나면 돌리지 않는다

⛔ **만성 목록이 2건 이상이어야 함.** 1건이면 후보가 하나라 모델이 틀릴 수 없어 **고치기 전에도
통과함**(태스크 노트의 앞 회차 관측 — 스모크 전용 DB 에서 `chronic=1` 로 통과했음). 판별력 0 인
재료로 얻은 통과를 근거로 쓰지 않음.

### ⛔ 판정 규칙 — 「통과」를 「결함이 사라졌음」으로 읽지 않는다

이 결함의 발생률이 낮음이 이미 실측됐음 — 내 관측 1건에 대해 동료 세션이 같은 공유 DB 에서
4회를 돌려 0건이었음(태스크 노트 · `runs/2026-09-12-task104-plan-drain/`). ⇒ **표본 3으로는
「거부가 사라졌음」을 주장할 수 없음.** 그래서 이 회차의 주 판정은 빈도가 아니라 아래 둘임.

| 관측 | 읽는 법 |
|---|---|
| 조립된 프롬프트에 `[deepest recurrence]` 표식과 규칙 블록이 실림 | 결정론적 관측임 — 단위 테스트가 이미 지키고 이 회차는 **실물 재료**에서 그것을 확인함 |
| 3회 모두 `parse_plan` 통과 · 초점에 표식 대상 `pattern_id` 포함 | **회귀 없음**으로 읽음. ⛔ 「결함이 사라졌음」으로 읽지 않음 |
| 1회라도 표식 대상이 초점에서 빠짐 | 문면이 약함 — 규칙 문장을 고쳐 이 회차를 다시 돔. ⛔ 검증 가드를 내리는 쪽으로 가지 않음(AC#3) |
| `parse_plan` 이 **다른 사유**로 거부함 | 이 태스크의 결함이 아님 — 사유를 그대로 적고 별도 태스크로 가름 |

⚠️ **대조군(구판 프롬프트)을 열지 않는 이유**: 무수정 판에서 4회 연속 통과가 이미 관측됐으므로
대조군 1~3회는 어느 방향으로도 판별력이 없음. 표본을 늘리는 대신 **결정론적 관측**에 판정을 걸음.

## 1. 재료 — 조건을 만족했다 (`DRY=1` 로 먼저 확인함)

실행체 `regen_plan.py.txt`. 공유 dev DB 에서 최신 계획이 붙은 세션의 소유자를 그대로 씀
(`user 00000000-…-0001`). ⛔ SELECT 만 했음 — `process_plan` 을 부르지 않았음.

- **만성 8건** — §0 의 재료 조건(2건 이상)을 만족함. 판별력 0 인 1건 재료가 아님.
- **`deepest` = `article_missing_before_noun` [`70ad1279-cfaf-4a57-b37b-c28f561baeaa`]**
  (frequency 7 · sessions 4 · days 3). ⚠️ **이 id 가 원래 거부 메시지의 그 id 임** —
  *"focus must include the deepest recurrence 70ad1279-…"*. 즉 같은 재료 위에 서 있음.
- 조립된 프롬프트 **14,538자**(고치기 전 14,030자) · **표식이 붙은 만성 줄 1건** · 규칙 블록 실림.
  산출물은 `plan_prompt.txt` 임.

## 2. 관측 — Claude 4회 (팔 A 1회 · 팔 B 3회)

⚠️ **팔 A 는 스파이크 결함으로 1회를 잃었음** — 내 실행체가 모델 원문을 `json.loads` 로 직접
읽었고 모델이 무인용 키 `reason: ""` 를 내 죽었음. 제품 경로(`parse_plan`)는 그 형태를 후보
탐색으로 다루므로 **제품 결함이 아니라 스파이크 결함임.** 판정은 제품 경로가 내도록 고치고
팔 B 를 열었음(§0 이 요구한 대로 이유를 적음 — 상한을 늘린 것이 아니라 도구 결함으로 잃은 회차를
회복한 것임). ⚠️ 팔 A 의 원문 파일은 팔 B 가 같은 경로에 덮어써 잃었음 — 관측한 문면만 남음.

| 회차 | 초점 | 표식 대상 포함 | `parse_plan` |
|--:|---|---|---|
| B-1 | `article_missing_before_noun` + `pronunciation_an_as_a` | **예** | 거부 — `level` 안에 **빈 키** `"": ""` (`extra_forbidden`) |
| B-2 | 같음 | **예** | **통과** |
| B-3 | 같음 | **예** | **통과** |

**집계: `parse_plan` 통과 2/3 · 표식 대상 포함 3/3 · 「deepest recurrence」 사유 거부 0/3.**

⚠️ **초점 조합이 3회 모두 같았음** — 표식이 붙은 만성 패턴 + 복습 예정 발음 패턴. 규칙 블록
셋째 줄이 지시한 배분과 같음.

## 3. 판정 — AC#2 를 닫는다. ⛔ 「결함이 사라졌음」으로는 읽지 않는다

- **AC#2 통과**: 같은 재료로 다시 돌려 `parse_plan` 통과를 실물로 확인했음(2/3). §0 의 주 판정인
  결정론적 관측 둘도 충족했음 — 표식이 실물 재료에서 그 id 에 붙었고 규칙 블록이 실렸음.
- ⛔ **표본 3으로 「거부가 사라졌음」을 주장하지 않음.** 이 결함의 발생률이 낮음이 이미 실측됐음
  (동료 세션 4/4 통과). §0 이 그 한계를 미리 적었고 여기서 뒤집지 않음.
- **B-1 의 거부는 이 태스크의 결함이 아님** — 사유가 「가장 깊은 재발」이 아니라 **여분 키**임.
  §0 의 판정 규칙대로 별도 태스크로 가름함(`TASK-119`). ⚠️ **생산 경로에도 같은 부류가 있었음** —
  `analysis_jobs` 의 2026-09-10 07:49 UTC 계획 job 이 `level.reason_en` 여분 키로 거부되고
  `attempts=2` 로 재시도해 통과했음(직접 조회). 세 형태가 전부 `level` 객체에 붙었음.
- ⚠️ **내 변경이 그 여분 키를 만들었다고 볼 근거는 없음** — 앞 회차에도 같은 부류가 관측됐음
  (`build_soundq_plan.py.txt` 주석의 `level.reason_note: null` · 태스크 노트의 「1회 없는 키」).
  ⛔ 다만 **표본 4로 배제하지도 못함** — 「근거가 없음」과 「배제했음」은 다름.
- ⚠️ **값싼 소급 집계는 판별력이 0 이었음**(AC#1 이 가리킨 경로). `analysis_jobs.last_error` 에
  「deepest recurrence」 문면이 **0건**인데, 원래 관측이 **스파이크**에서 나 job 행을 만들지
  않았기 때문임. ⛔ 그 0건을 「과거에 발생하지 않았음」으로 읽지 않음.
