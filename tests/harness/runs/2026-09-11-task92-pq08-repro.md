# TASK-92 — **스파이크에서는 3/3 재현되고 앱 경로에서는 3/3 재현되지 않는다**
# ⛔ 그런데 **두 팔이 변수 둘에서 갈리므로 원인을 프롬프트로 귀속하지 않는다**

> 회차 디렉터리: `runs/2026-09-11-task92-pq08-repro/`. 실물 Nova **6회**(스파이크 3 + 앱 경로 3) ·
> 워커 미기동 · 분석 job **0건 처리**.
> 착수 근거: handoff 가 다음 걸음으로 지목했고 사용자가 그 순서를 승인했다(2026-09-11).

---

## 1. 환경 — 이 턴에 직접 재서 얻음

| 항목 | 값 |
|---|---|
| 백엔드 | pid **25563** · `VOICE_ADAPTER=nova` · `WORKER_ENABLED=false` · 재기동 **0회** |
| ⛔ 프롬프트 통제 | **`app/backend/app/audio_gateway/nova.py` 가 `adf462c` 이후 «한 번도» 바뀌지 않았다** — `git log adf462c..HEAD -- <그 파일>` 이 **0건**이고 `SYSTEM_PROMPT` 가 두 리비전에서 **바이트 동일**(2347자). 즉 최초 관측과 **같은 프롬프트**로 다시 쟀다 |
| 스냅샷 | `error-patterns-before.json`(9행) · `review-tasks-before.json`(15행) · `sessions-before.txt`(17건) · 회차 전 drift **0** |
| 계측 | `instrument.js` sha256 `cbc19fb6…` · 파일 해시와 페이지 내 계산 해시 일치 · `sampleRate` 16000 |

⚠️ **스파이크는 DB 를 쓰지 않는다** — 3회 뒤 표 건수가 `17 | 128 | 9 | 7` 로 회차 전과 같았다(직접 확인).

---

## 2. 스파이크 팔 — **`pile` 이 그대로 들어간다. 3/3 · 문자 단위로 동일**

`spike_nova_protocol.py --wav pq08.wav --tools --app-prompt` 를 3회. **세 회차의 ASSISTANT 출력이
글자 하나까지 같았다**:

> ⑴ `It sounds like you are trying to say "Finish the first version of the pile."`
> ⑵ ` Did you mean to say "Finish the first version" or is there more you want to add?`
> ⑶ ` \nIf you meant "Finish the first version," could you please say that for me?`

전사문도 3/3 동일: `해피니시 the first version of the pile.` · `toolUse` **0건**(3/3).

### 2-1. ⚠️ 최초 관측과 **형태가 다르다** — 심각도가 내려갔다

| | 최초 관측(`TASK-90` 회차) | 이 회차 3/3 |
|---|---|---|
| 되불러 준 문장 | `Did you mean to say "I finished the first version of the pile"?` | `It sounds like you are trying to say "…of the pile."` |
| **다시 말하라는 요구** | `Let's try this sentence together. Say: "I finished the first version of the pile."` | ⛔ **`pile` 이 빠졌다** — `"Finish the first version"` 까지만 말하라고 한다 |

⇒ **「앱이 틀린 발음을 승인한다」는 절반만 재현된다.** 코치가 그 낱말을 **의도한 낱말로 확인해 주는
것**은 재현되고, **학습자에게 그 낱말을 따라 말하게 하는 것**은 재현되지 않는다.
⚠️ 프롬프트가 바이트 동일이므로 이 차이는 **모델 출력의 변동**이다 — 코드 변경이 아니다.

---

## 3. 앱 경로 팔 — **`pile` 이 agent 발화에 없다. 3/3**

`p_app_path.py --wav pq08.wav` 를 3회. agent 발화를 DB 에서 직접 읽었다.

| 회차 | 전사문 | agent 가 `pile` 을 말했는가 |
|--:|---|:--:|
| 1 | `he finished the first version of the pile.` | **아니다** — `"finishing the first version"` 으로 우회 |
| 2 | `he finished the first version of the pile.` | **아니다** — 회차 1과 문자 단위 동일 |
| 3 | `i finished the first version of the pile.` | **아니다** — `"finished the first version of something"` |

⇒ **앞 회차의 앱 경로 표본 1건과 합쳐 4/4 로 미재현이다.**

⚠️ **앱 경로는 문자 단위로 결정적이지 않다** — 회차 3의 전사문이 `i finished`(다른 둘은 `he finished`)이고
agent 발화가 7행으로 길어졌다. 스파이크 팔의 3/3 동일과 대조된다.

---

## 4. ⛔ 판정 — **제품 결함으로 단정하지 않는다. 그리고 원인을 프롬프트에 귀속하지도 않는다**

**AC#1 은 봉투에 따라 갈린다**: 스파이크 봉투에서는 `pile` 이 들어가고(미충족), 앱 봉투에서는
들어가지 않는다(충족). **이 태스크가 답해야 하는 것은 「제품에 결함이 있는가」이고 그 답은 아니다** —
학습자가 쓰는 경로에서 3/3(누적 4/4) 재현되지 않는다.

⛔ **그러나 두 팔의 차이를 프롬프트로 설명하지 않는다 — 변수가 둘 갈린다.**

| 변수 | 스파이크 팔 | 앱 경로 팔 |
|---|---|---|
| 시스템 프롬프트 | `SYSTEM_PROMPT` **기반만**(2347자 · `--app-prompt` 가 `build_system_prompt` 를 쓰지 않는다) | 계획·무대가 실린 조립 프롬프트 |
| **전사문** | `해피니시 the first version of the pile.`(혼재 문자) | `he/i finished the first version of the pile.`(라틴) |

⇒ **어느 쪽이 갈랐는지 이 회차는 배제하지 못한다.** 「계획 블록의 `Focus on: article_missing_before_noun`
이 관사 연습으로 유도해 그 낱말을 지나가게 한다」는 그럴듯하지만 **정황이고 측정이 아니다** —
전사문이 다르면 모델이 보는 입력 자체가 다르다.

⚠️ **판별력을 만드는 조건**: 같은 전사문을 만든 뒤 프롬프트만 갈아야 한다. `--prompt-file` 로
조립 프롬프트를 스파이크에 실으면 프롬프트는 통제되지만 **전사문은 여전히 스파이크 쪽**이다.
⇒ **이 축은 이 태스크의 범위를 넘는다** — 소유는 `TASK-67`·`TASK-86` 계열이다.

---

## 5. 남긴 것 · 함께 확정된 것

- ⚠️ **`toolUse` 가 스파이크 3/3 에서 0건**이다 — 이 갈래의 다른 관측(`TASK-75` 15회 · `TASK-86` 8회)과
  같은 방향이고 표본을 3 더한다. 판정 소유는 `TASK-86`·`TASK-87` 이다.
- ⚠️ **경로가 전사의 문자 체계를 바꾼다**는 앞 회차 관측이 **3/3 으로 굳었다**(스파이크는 혼재, 앱은 라틴).
  `TASK-65` 가 다룬 현상과 같은 방향이고 그 태스크가 소유한다.
- ⚠️ **라이트 모드·결과 화면을 재지 않았다** — 이 태스크의 단정이 agent 발화에만 걸린다.

## 6. teardown 대조 (전부 이 턴 출력)

내 세션 3건을 **명시 ID 로** 삭제(`DELETE 3`)했고 실행 전에 보존 목록과 겹치지 않는 것을 SQL 로
단정했다(**0건**). 삭제 후: `harness_pattern_baseline` drift **0** · `harness_review_task_baseline`
drift **0** · 표 건수 `learning_sessions` **17** · `utterances` **128** · `error_patterns` **9** ·
`review_tasks` **15** · `pronunciation_attempts` **7** · `analysis_jobs` **57** — 전부 회차 전과 같다.
내 세션 잔존 **0건**.
