# 실물 마이크 2회 — I-1 오탐 해소 대조 · 2026-09-03

> **차수를 소비하지 않았다.** 1회차와 같은 성격의 표적 검증이고 5차수 선언 범위는 그대로다.
> 절차의 정본은 `2026-09-01-mic-1.md` §1이며 이 문서는 **그 절차로 돌린 2회차의 결과**만 담는다.
> 세션 `182e7d49` · `completed` · **101초**(23:14:04 → 23:15:46 UTC).

---

## 0. 결론 — **I-1이 실물에서 오탐을 없앴다 (판정 3/3)**

| 볼 것 (handoff가 정한 3개) | 판정 | 근거 |
|---|:--:|---|
| 조각이 생겨도 **오탐 패턴이 안 생기는가** | ✅ | 조각 **2쌍**이 실제로 생겼고(아래) 둘 다 묶음으로 합쳐져 분석됐다. 이 세션의 occurrence **6건 중 조각산 0건** |
| 새 `error_patterns`에 `verb_form_missing_*` 계열이 **없어야** | ✅ | 새 패턴은 `preposition_time_expression_ago` 1개뿐 — 정상 문법 패턴이다("before some night" → "a few nights ago") |
| **job 수 < 사용자 learning 발화 수** | ✅ | 사용자 learning 발화 **7건** → job **5건**. 1회차는 18 → 14였다 |

**1회차와의 대조가 핵심이다.** 1회차에서는 occurrence 13건 중 **7건이 조각산**이고
`verb_form_missing_subject`(freq 4)·`verb_form_missing_object`(freq 2)가 **100% 오탐**이었다.
2회차에서는 같은 종류의 조각이 생겼는데 **오탐이 0건**이다.

## 1. 증가분 (baseline은 세션 전에 직접 찍었다)

| 표 | 세션 전 | 세션 후 | 증가 |
|---|--:|--:|--:|
| `learning_sessions` | 3 | 4 | +1 |
| `utterances` | 51 | 74 | **+23** (사용자 learning 7 · agent 16) |
| `analysis_jobs` | 21 | 26 | **+5** (전부 `done`) |
| `error_patterns` | 4 | 5 | +1 |
| `error_occurrences` | 8 | 14 | +6 |
| `pronunciation_attempts` | 3 | 3 | **+0** |

기동 확인(1회차 §1의 판별 3개 그대로): `/health` → `{"status":"ok"}`(`version` 키 없음 = 우리 것) ·
`ps eww`로 `VOICE_ADAPTER=nova` · `show server_version` → **17.9**(homebrew :5432) ·
사전 `spike_nova_protocol.py --wav p1a.wav` → **PASS**.

## 2. 조각이 어떻게 합쳐졌나 — 이것이 I-1의 증거다

`sequence_no` 순서로 본 사용자 발화와 job 위치:

| seq | 전사문 | job |
|--:|---|:--:|
| 1 | `she enjoyed joke before some night` | 1 |
| 6 | `i uh` | **0** |
| 7 | `she enjoyed a joke before some night` | 1 |
| 11 | `yes she enjoyed` | **0** |
| 12 | `a joke before some night` | 1 |
| 17 | `she enjoyed joke one night` | 1 |
| 21 | `she enjoyed a joke one night` | 1 |

**조각 2쌍**: `(6,7)`과 `(11,12)`. 둘 다 연속된 사용자 발화라 **한 묶음**이 되고 job은 묶음의
**마지막 발화**에만 걸렸다. `(11,12)`는 한 문장이 두 final로 쪼개진 전형적인 I-1 케이스다
(`yes she enjoyed` + `a joke before some night`).

I-1 **이전 코드라면** `i uh`와 `yes she enjoyed`가 각자 job을 받아 "주어/목적어 없음"으로
보고됐을 자리다. 실제 occurrence는 그 두 발화에 **하나도 붙지 않았다.**

## 3. 이 세션이 만든 교정 (occurrence 6건 — 전부 정상)

| seq | 패턴 | severity | conf | span → correction |
|--:|---|:--:|--:|---|
| 1 | `article_missing_before_noun` | medium | 0.90 | `joke` → `a joke` |
| 1 | `preposition_time_expression_ago` | high | 0.60 | `before some night` → `a few nights ago` |
| 7 | `preposition_time_expression_ago` | high | 0.85 | `before some night` → `a few nights ago` |
| 12 | `article_wrong_definite_article` | low | 0.45 | `a joke` → `the joke` |
| 12 | `preposition_time_expression_ago` | high | 0.85 | `before some night` → `a few nights ago` |
| 17 | `article_missing_before_noun` | medium | 0.90 | `enjoyed joke` → `enjoyed a joke` |

## 4. 재현되지 않은 것 · 새로 관측된 것

**재현되지 않음 — barge-in 묶음 분할(리뷰어의 미실측 추측).** 사용자 final 사이에 agent final이
끼어 한 문장이 두 묶음으로 갈리는 경우는 이 세션에 **없었다.** 사용자 발화는 항상 연속으로
붙었다(`1` / `6,7` / `11,12` / `17` / `21`). 추측이 틀렸다는 증거는 아니고 **이 세션에서는
관측되지 않았다**는 뜻이다.

**새 관측 ① — `{ "interrupted" : true }`가 agent 전사문으로 저장된다.** seq 9·15가 그 값이다.
Nova가 barge-in 시 그 마커를 ASSISTANT `textOutput`으로 보내고, `nova._on_text_output`이 그것을
그대로 `TranscriptEvent(final, agent)`로 만든다(`role`만 보고 내용은 보지 않는다). 피해는 제한적이다 —
agent 발화라 분석 대상이 아니고, 묶음 경계로는 오히려 올바르게 작동한다(사용자가 실제로 끼어든
지점이다). 다만 전사문에 기계 문자열이 남고, 규칙 6("Never read JSON... out loud")과 같은 계열의
누출이다. → `TASKS.md` **I-5**.

**새 관측 ② — 턴 간격이 짧다(캡틴 관측).** "내가 말하려고 하기 전에 다음 질문을 하거나 바로
튜터가 말을 한다. 내가 말하면 멈추기는 한다." 데이터가 이것을 뒷받침한다: 101초에 **agent 발화
16건 vs 사용자 7건**이고, agent가 `2,3,4,5` / `8,9,10` / `13,14,15,16` / `18,19,20`처럼 **연속
여러 행**을 만든다. `NOVA_ENDPOINTING_SENSITIVITY`는 **이미 `LOW`**(가장 덜 성급한 값, `.env`)라
그 노브에는 더 내릴 여지가 없다. 지시문에는 규칙 1("turns to one or two sentences")·규칙 2("Ask
one question at a time, then let the learner speak")가 있지만 지켜지지 않았다. → `TASKS.md` **I-6**
(캡틴 결정 대기 — 지시문 강화 / 저장 계층 분리 판정).

⚠️ **한계 2개를 명시한다.** ① 세션이 **101초**로 1회차(208초)보다 짧고 사용자 발화가 7건이라
표본이 작다. ② 사용자가 한 문장(`she enjoyed a joke one night`)을 반복 연습하는 흐름이어서
문장 다양성이 낮다 — 오탐 부재를 "모든 대화에서"로 일반화하지 않는다.
