# 회차 기록 — 결정 50 의 첫 걸음: 다중 턴에서 tool 이 오는 비율 (내 표본 6회)

> **판정: 확인되지 않았음. 단순화로 넘어가지 않음.** 결정 50 이 미리 못 박은 기준으로 재면
> 비율이 **절반 남짓**이고 **픽스처에 따라 갈림.** 표본을 늘리는 것보다 **오는 팔과 안 오는 팔을
> 가르는 것**이 다음 걸음임.
>
> ⛔ **기준은 돌리기 전에 적었음** — 대장의 결정 50 이 그것을 갖고 있음. 돌린 뒤에 기준을 옮기지
> 않았음.
>
> 조건은 전 회차 동일함: **앱 `SYSTEM_PROMPT`** · **`toolChoice` 강제 없음** · 다중 턴(발화 둘을
> 한 세션에 이어 흘림) · 스파이크 경로.

## 1. 내가 직접 돌린 것 — 6회

| 팔 | 세션에 심한 오류가 있나 | `toolUse` | `audioOutput` | `target_sound` |
|---|---|--:|--:|---|
| `p2a`+`p2k` (1차) | 있음 | **1** | 197 | `th_as_s` |
| `p2a`+`p2k` (2차) | 있음 | 0 | 148 | 없음 |
| `p2a`+`p2k` (3차) | 있음 | **1** | 140 | `th_as_s` |
| `p1a`+`p1k` | 있음 | 0 | 68 | 없음 |
| `p2m`+`p2k` | 있음 | 0 | 145 | 없음 |
| `p1a`+`p1m` | **없음**(mild 만) | 0 | 75 | 없음 |

⚠️ **마지막 줄을 실패로 세지 않음** — 규칙 9 가 *"never for a mild accent"* 를 지시하므로 심한
오류가 없는 세션에서 tool 이 오지 않는 것은 **규약대로**임. 그 줄은 오탐 대조로 값어치가 있음.

**심한 오류가 있는 세션 5회 중 2회에서 tool 이 왔음 (2/5).**

## 2. 다른 세션의 표본과 합침

다른 세션(`ohmyenglish-15`)이 같은 조건으로 4회를 돌려 3회에서 받았음(`p1k`→`p1m` 1건 ·
`p2a`→`p2k` 2건 · 같은 것 반복 1건 · `p1m`→`p1k` 0건). ⚠️ **그 4회는 내가 돌린 것이 아님** —
합산은 참고이고 내 증거는 §1 임.

**합치면 심한 오류 세션 9회 중 5회 (5/9).**

## 3. 왜 「확인」이 아닌가

- **비율이 절반 남짓임.** 우회로의 기준선(**0/51**)보다는 압도적으로 나으나, 「우회로를 버려도
  된다」를 뒷받침하려면 **오지 않는 절반**이 무엇인지 알아야 함. 지금 버리면 그 절반의 세션에서
  발음 기록이 **아무 데도** 남지 않음.
- **픽스처에 따라 갈림.** `p2a`+`p2k` 는 3회 중 2회 왔고 `p1a`+`p1k`·`p2m`+`p2k` 는 0회임. 즉
  무작위가 아니라 **조건이 있음** — 그 조건을 모르는 상태로 단순화하면 조용히 절반을 잃음.
- **앱 경로가 아님.** 전부 스파이크 직결임. `TASK-65` 가 **첫 발화가 이후 턴의 ASR 언어를
  고정한다**를 찾은 자리라 경로가 다르면 값이 다를 수 있음.

## 4. `target_sound` 는 오면 실림

tool 이 온 2회 **모두** `target_sound` 가 있었음(`th_as_s`). 즉 복습 시계의 SQL 게이트를 통과할
재료가 함께 옴 — **오는 것이 문제이고 실리는 것은 문제가 아님.**

## 4-1. 후속 — 「코칭 ⟺ tool」을 내 6회로 대조했음 (2026-09-10)

다른 세션이 표본을 늘리지 않고 **판별**로 닫았음(`runs/2026-09-10-task78-coaching-implies-tool.md` ·
15/15 · 반대 방향 0건). 하중을 지는 주장이라 **내 원자료로 같은 관계를 확인했음.**

⛔ **먼저 내 방법을 정정함** — 처음에 `sound` 키워드로 코칭 여부를 세려 했는데
*"That sounds very professional"* 이 걸려서 **판별력이 0이었음.** 그래서 `role=ASSISTANT` 인
`textOutput` 을 골라 **발화를 읽어서** 판정했음.

| 팔 | `toolUse` | agent 가 그 턴에 한 것 | 관계 |
|---|--:|---|---|
| `p2a`+`p2k` ① | 1 | *"Let's make it a little more natural … Try saying: …"* — **문장을 다시 말하게 함** | ⚠️ 소리를 **이름으로 부르지는 않았음** |
| `p2a`+`p2k` ② | 0 | *"Thank you for repeating. Your sentence is very clear."* — 문제 없다고 판정 | ✅ 맞음 |
| `p1a`+`p1k` | 0 | *"Good, you said that very clearly."* | ✅ 맞음 |
| `p2m`+`p2k` | 0 | *"I see you said 'la porte en chaille.' Did you mean …"* — 되물었으나 소리 지목 없음 | ✅ 맞음 |
| `p1a`+`p1m` | 0 | *"Thanks for repeating that. Can you tell me …"* | ✅ 맞음 |
| `p2a`+`p2k` ③ | 1 | *"Good. Let's focus on the th sound in the. Try saying …"* — **소리를 지목함** | ✅ 맞음 |

**판정: 내 6회에서도 같은 방향임.** 단 정의에 따라 한 칸이 갈림 — 「코칭」을 **문장을 다시 말하게
함**으로 읽으면 6/6 이 일치하고, **소리를 이름으로 부름**으로 좁게 읽으면 ①이 예외임(tool 은 왔고
발화는 자연스러움 교정이었음). ⚠️ **그 차이를 뭉개지 않음** — 좁은 정의에서는 반대 방향 사례가
내 표본에 **1건 있음.**

⚠️ 곁가지 관측 하나 — `p2m`+`p2k` 에서 전사문이 **`la porte en chaille`**(프랑스어풍)로 왔음.
ASR 언어 판별이 한글이 아닌 **제3 언어로도** 뒤집힘. `TASK-65` 가 찾은 기전의 범위가 더 넓을 수
있고, **보조 신호(한글 감지)가 그것을 못 잡음** — `note_transcript` 는 `[가-힣]` 만 봄.

## 5. 다음 걸음 — 표본 추가가 아니라 판별

⛔ **더 돌리는 것으로는 닫히지 않음.** 5/9 를 6/11 로 만들어도 「왜 안 오는가」는 그대로임.
갈라야 할 후보(어느 것도 배제하지 않았음):

1. **오류의 심함 정도** — `p2k`(sh/th) 대 `p1k`(th) 대 `p2m`(mild). 규칙 9 의 문턱이 어디인가.
2. **첫 발화가 만든 문맥** — `TASK-65` 가 찾은 기전이 tool 판단에도 걸리는가.
3. **턴 수** — 둘로는 부족하고 셋 이상에서 안정되는가.
4. **모델의 비결정성** — 같은 팔(`p2a`+`p2k`)이 3회 중 2회만 왔으므로 순수 잡음도 섞여 있음.

소유자는 `TASK-78` 임(앱 경로 확인 + 오는 조건·안 오는 조건 가르기).
