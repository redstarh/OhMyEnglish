# 하네스 2차수 기록 — 2026-08-26 (회귀)

- 대상 커밋: `3c62770` (1차수는 `57b2400`)
- run_id: `3ebf73eb-e9f8-4b72-90f7-6c53c28f9331`
- 입력: `claude_air_3-14` 수정 세션의 완료 보고 (F-1 수정 / F-2 미수정)
- 1차수: [2026-08-26-run-1.md](2026-08-26-run-1.md) · 원장: [ROUNDS.md](ROUNDS.md)

## 완료 여부

| 항목 | 판정 |
|---|---|
| **F-1 (HIGH) 다크모드 색상 위계 역전** | ✅ **수정 완료 — 검증됨** |
| **F-2 (MEDIUM) `target_form` 불일치** | ⏸ **미수정 — 캡틴 결정 대기** (스키마 변경 필요, 판단 근거 타당) |
| 2차수 신규 앱 결함 | **0건** |
| 회귀 | 전건 PASS |

**루프 판단: 3차수로 넘기지 않는다.** 남은 F-2는 수정 세션이 해결할 수 있는 항목이 아니라
**스키마 설계 결정(캡틴)**을 기다리는 상태다. 다시 전달해도 같은 답이 돌아온다.
10차수 상한 중 **2차수 사용**.

---

## 수정 보고 검증 — 보고를 믿지 않고 직접 확인

수정 세션이 보고한 내용을 항목별로 재현했다.

| 보고 내용 | 내 검증 | 결과 |
|---|---|---|
| `3c62770`으로 ff병합, 워크트리 제거 | `git log`·`git worktree list` | ✅ 워크트리 1개(메인)뿐, 커밋이 `design/first-vertical-slice`에 있음 |
| 변경 3파일 | `git show --stat` | ✅ `globals.css`(+15/-2) · `page.tsx`(+14/-4) · `results/page.tsx`(+11/-3) |
| `--foreground-muted`·`--danger` 토큰 추가 | `globals.css` 직접 읽음 | ✅ 두 모드 각각 정의. **다크에서 `--danger`를 `#cc0000`→`#ff8a8a`로 반전**(3.4:1→8.7:1) — 요청 범위를 넘어 올바르게 처리 |
| 인라인 하드코딩 전부 교체 | `grep 'color: "#' app/frontend/app/` | ✅ 잔존 0건(주석 1건만) |
| 게이트 4개 clean | 내가 직접 실행 | ✅ `206 passed` · ruff clean · format clean(24 files) · ty clean |
| 백엔드 소스 변경 없음 → 재기동 불필요 | `git show --stat` | ✅ 프론트 3파일만 |
| F-2는 스키마 변경 필요 | 아래 §F-2 검증 | ✅ 주장 정확 |

### F-2 "스키마 변경 필요" 주장 검증 — 타당하다

| 주장 | 검증 방법 | 결과 |
|---|---|---|
| `error_occurrences`에 `target_form` 컬럼이 없다 | `information_schema.columns` 조회 | ✅ 컬럼은 `id, utterance_id, pattern_id, original_span, correction, explanation, severity, confidence, created_at` — 없다 |
| occurrence insert가 그 값을 버린다 | `analysis.py` `_INSERT_OCCURRENCE_SQL` | ✅ 7컬럼 insert, `target_form` 미포함 |
| 패턴 upsert가 last-write-wins다 | `analysis.py` `_UPSERT_PATTERN_SQL` | ✅ `on conflict … do update set target_form = excluded.target_form`, 주석도 "가장 최근 분석이 제시한 목표 형태로 갱신한다"고 명시 |

따라서 occurrence 단위 일치는 마이그레이션 002(+백필+테스트 갱신) 없이는 불가능하다.
**"고치지 말고 근거를 보고하라"는 지시를 정확히 이행했다.**

---

## 회귀 결과

### 묶음 ① 이전 결함 재검증

| # | 대상 | 판정 | 실측 |
|---|---|:--:|---|
| **R1** | F-1 — **다크모드** | ✅ PASS | 확정 전사문 **16.91:1** / 부분 **7.04:1** — 둘 다 AA(4.5:1) 이상이고 **확정이 부분보다 높다**(1차수는 1.05 vs 6.95로 역전) |
| **R2** | F-1 — **라이트모드**(1차수 미수행) | ✅ PASS | 확정 **17.93:1** / 부분 **7.00:1** — 위계 동일. **라이트 부분이 원래 2.85:1로 AA 미달이던 것도 해소** |
| **R3** | F-2 잔존 확인 | ✅ 예상대로 잔존 | 아래 §R3 |
| **R4** | F-7 하네스 스윕 교정 | ✅ PASS | teardown 직전 스윕이 미등록 세션 **1건**(브라우저 B5 레그)을 잡았다. 복원 후 baseline 완전 일치 |

측정 수단: `tests/harness/measure_contrast.py` — CDP `Emulation.setEmulatedMedia`로
`prefers-color-scheme`을 강제한다(페이지 안에서는 바꿀 수 없고 플러그인에 그 액션이 없다).

측정 지점 3곳 모두 두 모드에서 AA 이상 + 위계 유지:

| 화면 | 강조 요소 | muted 요소 |
|---|---|---|
| 세션 화면 | 확정 전사문 16.91 / 17.93 | 부분 전사문 7.04 / 7.00 |
| 결과 화면(확정) | 라벨·원문·교정문 16.91 / 17.93 | 교정 이유 7.04 / 7.00 |
| 세션 실패 화면 | "연결에 실패했습니다." 16.91 / 17.93 | "사유: …" 7.04 / 7.00 |

증거: `2026-08-26-run-2/R1R2-session-{light,dark}.png` ·
`R8-results-{light,dark}.png` · `R1R2-failed-{light,dark}.png` · `contrast.json`

#### R3 — F-2 잔존, 그리고 조건이 선명해졌다

| pattern_key | occurrence 수 | `original_span` | `correction` | `target_form` | 일치 |
|---|--:|---|---|---|:--:|
| `verb_tense_past_simple_yesterday` | 2 | `Yesterday I go` | `Yesterday I went` | **`and presented`** | ❌ |
| `word_order_embedded_question` | 1 | `I know not why` | `I don't know why` | `I don't know why` | ✅ |

**occurrence가 1개인 패턴은 일치하고, 2개 이상이며 서로 다른 `target_form`을 가진 패턴에서만
불일치가 난다.** 1차수보다 조건이 좁혀졌다 — 재현 최소 입력은
`inject_errors.py --scenario E6`의 한 문장(`Yesterday I go to the office and present the
quarterly plan.`)이며 이 문장은 두 번 실행해서 두 번 모두 occurrence 2개를 만들었다.

### 묶음 ② 1차수 미수행 케이스

| # | 시나리오 | 판정 | 실측 |
|---|---|:--:|---|
| **E4** | `voice_command` 발화는 분석되지 않는다 (W6) | ✅ PASS | 명령 발화 2건 저장, **job 0건**, 패턴 0건 |
| **E5** | 오류 0건 세션 | ✅ PASS | 3문장 job 3/3 `done`, occurrence 0 → API `{"status":"final","corrections":[]}` — 키는 있고 배열이 빈 것이 정상 |
| **E6** | 재분석 멱등 (W3) | ✅ PASS | 1차 occurrences **2**·frequency **4** → 같은 발화에 job 재등록 성공 → 재분석 후 occurrences **2**·frequency **4** — **변하지 않았다** |
| **R8** | 라이트모드 화면 | ✅ PASS | 위 대비 표 참조 |

**E4의 부수 성과 — 1차수 판정을 정정한다.** 1차수는 `no_utterances`를 "앱 경로로 재현
불가(스텁 지연 0)"로 판정하고 DB 직접 삽입으로 대체했다. 그런데 `voice_command` 발화만 담긴
세션은 **job이 0건이므로 R2 규칙 2에 그대로 걸린다** — E4 세션의 결과 API가
`{"status":"no_utterances","partial_failure":false}`를 반환했다.
**`no_utterances`는 앱 경로로 재현 가능하다.**

### 묶음 ③ 회귀

| # | 대상 | 판정 | 실측 |
|---|---|:--:|---|
| A1 | 프레임 계약 | ✅ | 17프레임 계약 순서, `sequence_no` 1‥6, 0.129초 |
| A2 | 깨진 프레임 내성 | ✅ | 5종 섞어도 완주, 0.083초 |
| B1 | `no_utterances` | ✅ | `corrections` 키 없음 (E4 세션으로 앱 경로 확인) |
| B2 | `analyzing` | ✅ | 워커 OFF에서 키 없음 → 복구 후 **6초 내 `final` 수렴** |
| B3 | `final` | ✅ | corrections 2건(패턴 7개 세션) |
| B4 | `partial_failure` | ✅ | `partial_failure=true` + 성공분 교정 1건 병존 |
| B5 | `connection_failed` | ✅ | **10.017초** `voice_adapter_connect_timeout` |
| D1 | pytest | ✅ | `206 passed`, skip/xfail 0 |
| D2 | ruff·format·ty | ✅ | 전부 clean |
| F-3 | 마이크 실프레임(회귀) | ✅ | **33프레임 재현** — 1차수와 동일 수치 |

---

## 2차수에서 나온 하네스 자체 개선 사항 (앱 결함 아님)

| # | 내용 | 조치 |
|---|---|---|
| H-1 | **`eval` 안의 `element.click()`은 user activation을 만들지 않는다.** 그래서 `AudioContext.resume()`이 resolve되지 않고 화면이 `마이크 권한을 요청하는 중…`에서 멈춘다 | 클릭은 반드시 CDP 실제 입력(플러그인 `click` 액션)으로 한다. 실측으로 확인 |
| H-2 | `run_id.txt` 오염이 **재발**했다 — `psql -tAc "insert … returning id"`가 id와 `INSERT 0 1`을 함께 출력하고 `tr -d ' \n'`이 둘을 붙여버린다 | README의 경고만으로는 부족하다. UUID만 뽑는 형태로 절차를 고정해야 한다: `\| grep -oE '[0-9a-f-]{36}' \| head -1` |
| H-3 | 브라우저 레그의 세션 등록 누락(1차수 F-7)은 **teardown 직전 스윕**으로 해소됨을 확인 | R4에서 검증. 스윕을 절차의 필수 단계로 유지 |

---

## 격리·복원 (R4)

| 지표 | baseline | 실행 중 최대 | teardown 후 | 판정 |
|---|--:|--:|--:|:--:|
| `learning_sessions` | 2 | 15 | **2** | 일치 |
| `error_patterns` | 1 | 9 | **1** | 일치 |
| `frequency` | 2 | 8 | **2** | 일치 |
| `last_seen_at` | 원래 값 | 밀림 | **원래 값 복귀** | 일치 |

teardown에서 세션 13개 삭제, 고아 패턴 8개 삭제. 1·2차수 하네스 세션 **24개 전량**이
`harness_sessions`에 기록돼 있다.

프로세스: 백엔드 baseline 복구(`--log-level warning`), `WORKER_ENABLED`·`VOICE_ADAPTER`
환경변수 잔류 **0건**, `.env` 미편집, 프론트 미재기동(HTTP 200), Chrome 종료.

## 원자료

`2026-08-26-run-2/` — `contrast.json`(두 모드 전 측정값) ·
`R1R2-session-{light,dark}.png` · `R8-results-{light,dark}.png` ·
`R1R2-failed-{light,dark}.png` · `A1-r2-frames.json` · `B5-r2-frames.json` ·
`E4/E5/E6-injection.json`

## 한계

- 대비 측정은 CDP `Emulation.setEmulatedMedia`로 색상 스킴을 **강제**한 값이다. 실제 OS 설정
  변경으로 재확인하지는 않았다(계산식은 WCAG 표준 그대로).
- C2의 partial 전환 관측은 1차수와 같이 **이벤트 도착을 스로틀**한 상태에서 했다.
- 이 기록은 독립 critic 패스를 거치지 않았다(작성자=검증자).
