# 테스트↔수정 왕복 원장

**상한: 10차수.** 10차수에 닿으면 오류가 남아 있어도 중단하고 캡틴에게 보고한다
(캡틴 지시 2026-08-26). 무한 루프 방지가 목적이며, 수렴하면 그 전에 닫는다.

루프 구조:

```
테스트(이 세션 claude_air_5-49:0.0) ──오류 전달──▶ 수정(claude_air_3-14:0.0)
              ▲                                              │
              └────────────── 완료 보고(tmux send-keys) ──────┘
```

역할 분리 원칙: **테스트 세션은 앱 코드를 수정하지 않고, 수정 세션은 자기 수정을 스스로
합격 판정하지 않는다.**

## 차수별 진행

| 차수 | 테스트 | 발견(수정 대상) | 전달 | 수정 완료 보고 | 재테스트 | 상태 |
|:--:|---|---|:--:|:--:|:--:|---|
| 1 | [2026-08-26-run-1.md](2026-08-26-run-1.md) | **F-1** HIGH 다크모드 색상 위계 역전 · **F-2** MEDIUM `target_form` 불일치 | ✅ `claude_air_3-14`에 작업 생성 | ✅ tmux 콜백 도착 (F-1 수정 / F-2 미수정+근거) | ✅ 2차수 | **완료** |
| 2 | [2026-08-26-run-2.md](2026-08-26-run-2.md) | **신규 앱 결함 0건.** F-1 수정 검증 완료 · F-2 잔존(캡틴 결정 대기) | — (전달 안 함) | — | — | **완료 — 루프 일시 종료** |
| 3 | [2026-08-26-run-3.md](2026-08-26-run-3.md) | **F-2 수정 완료** · **Nova 실연동 동작 확인(AC1)** · 신규 앱결함 0건 | ✅ 지시 `dae398e` | ✅ 3커밋 보고 | ✅ Q1·Q2·Q3·N5·회귀 | **완료** |
| 4 | 미시작 | — | — | — | — | 테스트 세션 작업만 남음(N6~N13) |
| 5~10 | 미사용 | | | | | |

### 루프 상태: 3/10 사용, 다시 일시 종료

3차수에서 **F-2가 해소되고 Nova 실연동이 앱 경로로 관통**됐다(N5 PASS). 남은 것은
N6~N13과 B1~B4 전량 재확인인데 **전부 테스트 세션의 작업**이다 — 수정 세션에 되돌려 보낼
결함이 없다. 4차수는 수정 왕복이 아니라 **테스트 커버리지 확장**으로 진행한다.

관찰 1건(O-1 agent 전사문 선행 개행, LOW)은 4차수에서 다른 결함과 묶어 전달한다.

### 3차수를 연 근거

2차수 종료 시점에는 F-2가 캡틴 결정을 기다려 루프를 멈췄다. 캡틴이 결정을 내렸다(2026-08-26):

- **F-2 → 선택지 B**(패턴 수준 일반형). 근거: 문장별 교정형은 이미
  `error_occurrences.correction`에 있어 중복이다. `target_form`이 따로 존재할 값이 있으려면
  "이 패턴을 연습할 때 익힐 형태"여야 한다.
- **사용자 부분 전사문 UI → 선택지 ①**(`userSpeechStart`~`userSpeechEnd` 구간에 "듣고 있어요").
  근거: Nova는 사용자 부분 전사문을 주지 않는다(N-1 실측 — ASR은 `FINAL` 한 블록).
- **순서 → Nova 먼저.** 복습(L계층)은 오류가 쌓여야 의미가 있고, 실음성이 붙어야 쌓인다.

그리고 N-0·N-1이 완료돼 **프로토콜이 실증**됐으므로 어댑터 구현이 가능해졌다
(`scenarios-N-real-voice.md`).

### 3차수 회귀 계획 (확정)

**묶음 ① 이전 결함 재검증**

| # | 대상 | 단정 |
|---|---|---|
| Q1 | **F-2 수정** | `inject_errors.py --scenario E6`으로 occurrence 2개 패턴을 만들고, 결과 API의 `target_form`이 **어느 occurrence의 `correction`과도 같지 않은** 일반형인지 확인. `E1`(패턴 7개)로도 재확인 |
| Q2 | **F-1 회귀 + 신규 UI** | "듣고 있어요" 표시가 테마 토큰을 쓰는지 — `measure_contrast.py`로 다크·라이트 모두 AA(4.5:1) 이상 + 위계 유지 |
| Q3 | **스텁 모드 무손상** | 포트를 확장했으므로 `VOICE_ADAPTER=stub`이 그대로 동작해야 한다. A1 프레임 계약 17개·`sequence_no` 1‥6, C2 회색→검정 전환 유지 |

**묶음 ② 미수행 케이스 — Nova 실연동으로 새로 가능해진 것**
(정본: `scenarios-N-real-voice.md` §4)

| # | 시나리오 | 비고 |
|---|---|---|
| N5 | 실음성 1턴 왕복 — 전사문이 **실제 발화 내용**과 일치 | `u1.wav` 브라우저 주입. 스텁과 달리 픽스처가 결정하지 않는다 |
| N6 | barge-in — 1초 내 출력 중단 + `INTERRUPTED` + 큐 비움 | AC2 |
| N7 | 실음성 3턴 완주 | AC12 |
| N8 | 실발화 → 실분석 병합 (`u1`+`u2` → `frequency` +2) | E계층 단정을 실음성으로 재현 |
| N11 | 마이크 권한 거부 → `microphone_permission_denied` | 계측 없이 CDP로 거부 |
| N12 | 포맷 불일치 방어 — 조용히 죽지 않는가 | A2의 실물판 |
| N13 | endpointing 민감도 HIGH/MEDIUM/LOW | N-1 실측: MEDIUM에서 약 480ms |

**묶음 ③ 회귀**: A1 · A2 · B1~B5 · E4 · E5 · E6 · D1 · D2 (+프론트 `tsc --noEmit`)

**주의**: 백엔드 소스가 바뀌므로 **테스트 전 재기동이 필수**다(`--reload` 없음).
프론트도 `AudioWorklet`이 들어가면 dev 서버가 새로 빌드해야 한다 — 재기동 시 반드시
`NEXT_PUBLIC_API_BASE=http://localhost:8002`를 붙인다.

### 1차수 미정리 상태 — 의도적 보존

A·B·C·D계층(run_id `9c6fe697-…`)은 teardown 완료돼 baseline과 일치한다. **E계층(run_id
`ed121074-…`)은 일부러 남겼다** — 절차 문서 §10의 "결함이 나온 시나리오의 세션은 지우지
않는다(지우면 재현할 수 없다)" 예외다.

| 보존 세션 | 용도 |
|---|---|
| `478649fb-d7f4-4f7c-8f8f-1c99921d79fc` (E1, 발화 5) | **F-2 재검증(R3)의 재현 데이터.** `verb_tense_past_simple_yesterday`가 occurrence 2개를 가진 상태 — 실제 Claude 출력은 비결정적이라 지우면 같은 상태를 다시 만들 수 없다 |
| `9c780afc-ba30-4642-ba41-d03848e26951` (E3, 발화 3) | 패턴 축적(`frequency` 6) 상태 보존 |
| `acdb9fae-901d-4ca0-aeea-9fe68e4eb24b` (E1-aborted, 발화 0) | `run_id.txt` 오염 사고로 생긴 빈 세션 — 2차수 teardown에서 함께 삭제 |

현재 DB: 세션 **5**(baseline 2 + 보존 3) · 패턴 **9**(baseline 1 + E계층 8).
**2차수 R3 판정이 끝나면 위 3개를 teardown한다** — README의 3단계 복원을 그대로 적용하고
세션 2 · 패턴 1 · `frequency` 2로 돌아오는지 대조한다.

## 차수별 회귀 중점 규칙

새 차수를 시작할 때 **전건 재실행이 목적이 아니다.** 아래 두 묶음을 먼저 돌린다.

1. **이전 차수에서 실패했거나 결함이 나온 케이스** — 수정이 실제로 들었는지.
2. **아직 수행하지 않았던 케이스** — 커버리지를 넓힌다.

그다음 회귀 확인용으로 핵심 계약(A1 프레임·B계층 5상태·D계층 게이트)을 돌린다.

### 2차수 회귀 계획 (확정)

**묶음 ① 이전 결함 재검증**

| # | 대상 | 단정 |
|---|---|---|
| R1 | F-1 수정 확인 — **다크모드** | 확정 전사문 대비 ≥ 4.5:1, 부분 전사문이 확정보다 **덜** 강조됨(대비가 더 낮음). 결과 화면 이유/사유 줄도 ≥ 4.5:1 |
| R2 | F-1 수정 확인 — **라이트모드** (1차수 미수행) | 같은 위계가 라이트모드에서도 유지 — 확정이 부분보다 선명 |
| R3 | F-2 수정 확인 | 교정 카드의 `target_form`이 표시된 `original_span`/`correction`과 **같은 문장**을 가리킨다 (E1 세션 재현: `verb_tense_past_simple_yesterday`가 occurrence 2개인 상태에서 확인) |
| R4 | F-7 하네스 자체 교정 | 브라우저 세션 등록 스윕을 teardown 직전에 돌려 세션 수가 baseline과 일치 |

**묶음 ② 1차수 미수행 케이스**

| # | 대상 | 근거 |
|---|---|---|
| R5 | E4 — `voice_command` 발화 주입 시 job 미생성 | W6 종단 미검증 |
| R6 | E5 — 오류 0건 세션의 결과 화면("표시할 교정이 없습니다") | 1차수는 오류 있는 세션만 봤다 |
| R7 | E6 — 같은 문장 2회 주입 시 `frequency` 부풀지 않음 | W3 멱등을 종단에서 미확인 |
| R8 | 라이트모드 전체 화면 5상태 | 1차수는 다크모드만 |

**묶음 ③ 회귀**: A1 · B1~B5 · D1 · D2

`scenarios-E-agent-learning.md` E4~E6, `scenarios-N-real-voice.md`는 각각 시나리오 정본이다.

## 전달·보고 규약

- 전달: `tmux send-keys -t claude_air_3-14:0.0 '<한 줄>' Enter` — 줄바꿈은 조기 전송된다.
- 보고: 수정 세션이 `tmux send-keys -t claude_air_5-49:0.0 '<한 줄>' Enter`로 이 세션에 알린다.
- 전달 내용은 **파일 경로로 지목**한다(본문 붙여넣기 금지) — 무엇을 읽고 고쳤는지 추적된다.

## 수정 세션에게 — 보고 대상이 무엇인지

`claude_air_5-49:0.0`은 셸이 아니다. **tmux 페인 주소**이고 그 안에서 이 하네스를 운영하는
**또 다른 Claude Code 세션**(테스트 세션)이 대화형으로 돌고 있다. 그래서:

- `tmux send-keys`로 보낸 텍스트는 **그 세션의 사용자 프롬프트가 된다.** 셸 명령이 아니다.
- `Enter`를 **별도 인자로** 붙여야 실제로 전송된다. 빼면 타이핑만 되고 멈춘다.
- 텍스트는 **한 줄로** 보낸다 — 줄바꿈에서 조기 전송된다.
- 테스트 세션이 하는 일: 보고를 받으면 `README.md`의 절차로 **살아 있는 스택을 상대로 회귀
  테스트를 돌리고**(위 "2차수 회귀 계획") 결과를 이 원장에 기록한다. 수정이 들었는지는
  **테스트 세션이 판정한다** — 수정 세션이 자기 수정을 합격 처리하지 않는다.

### 수정 작업의 환경 제약 (반드시 지킬 것)

1. **워크트리를 새로 만들지 않는다.** 테스트 스택이 `/Users/redstar/MyProject/OhMyEnglish`를
   직접 서빙한다 — 프론트 dev 서버(`:3000`)와 백엔드(`:8002`)가 그 경로에서 떠 있다. 다른
   워크트리에서 고치면 **테스트가 수정 전 코드를 검증한다.** 부득이 워크트리를 썼다면 보고
   전에 `design/first-vertical-slice`로 병합하고 그 사실을 보고에 적는다.
2. **백엔드는 재기동하지 않아도 된다** — 프론트엔드만 고치는 경우. 백엔드 파이썬 소스를
   고쳤으면 `--reload`가 없으므로 **반영되지 않는다**: 보고에 "백엔드 소스 변경 있음"을
   명시하면 테스트 세션이 재기동한다. 직접 재기동한다면 baseline 명령을 그대로 쓴다 —
   `cd app/backend && .venv/bin/uvicorn app.api.main:app --port 8002 --log-level warning`.
3. **`.env`를 편집하지 않는다.** 자격증명이 들어 있고 플래그는 환경변수로만 넘긴다.
4. **DB를 건드리지 않는다.** `harness_*` 테이블과 보존된 E계층 세션은 다음 차수의 재현
   데이터다(위 "1차수 미정리 상태" 참조).
5. `tests/harness/**`와 `docs/design/**`은 수정 금지. `docs/ops/2026-08-26-test-harness*.html`도
   테스트 세션 소유다.
