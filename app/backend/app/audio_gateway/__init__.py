"""Audio Gateway — 음성 어댑터 포트와 세션 수명주기 (설계서 §5.3, AC G1~G4).

계층은 셋이다: `port`(계약) → `session`(수명주기) → `factory`(구현 선택).
`session`은 어떤 구현이 붙는지 모른다 — 주입만 받는다(G3). 그래서 픽스처 스텁을
Nova 어댑터로 갈아끼우는 일이 이 패키지 바깥에 파급되지 않는다.
"""
