"""이 도구의 **고정 사용자** — 단일 사용자 로컬 도구라 연결의 주인이 하나다(설계서 §2).

⚠️ **`api/ws.py` 에서 이리로 옮겼다** (`TASK-148` ④ · 결정 122). 그 파일은 「얇다」고 선언한 전송
계층인데 형제 라우터 둘(`api/results.py` · `api/daily.py`)과 하네스 스크립트 셋이 **소켓 모듈을
옆으로 참조해** 이 값을 얻고 있었다. 값이 프로토콜의 것이 아니라 도메인의 것이라 자리가 어긋났다.

⛔ **시드와 같은 값이고 그 사실은 단정이 지킨다.** `scripts/migrate.py` 의 `USER_ID` 가 이 UUID 로
사용자 행을 만든다. 그 스크립트는 앱 패키지를 import 하지 않는 독립 ops 스크립트라(`db_utils` 만
쓴다) 상수를 공유할 수 없다 — 그래서 값이 두 곳에 적히고,
`tests/unit/test_schema.py::test_the_fixed_user_matches_the_seed` 가 둘이 같은지 잰다.
⚠️ 그 단정이 없으면 어긋남이 **조용하다**: 앱은 없는 사용자로 세션을 만들려 하고 외래키 위반으로
떨어지는데, 그 실패가 「시드를 안 돌렸다」와 구별되지 않는다.

**인증이 붙으면 이 상수가 사라진다** — 그때는 연결이 주인을 들고 온다. 지금 없는 이유는 첫 슬라이스
범위가 localhost 전용·인증 없음으로 승인된 것이다.
"""

from __future__ import annotations

from uuid import UUID

FIXED_USER_ID = UUID("00000000-0000-0000-0000-000000000001")
