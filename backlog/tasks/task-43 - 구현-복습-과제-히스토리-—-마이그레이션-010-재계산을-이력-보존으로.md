---
id: TASK-43
title: '구현: 복습 과제 히스토리 — 마이그레이션 010 + 재계산을 이력 보존으로'
status: Done
assignee: []
created_date: '2026-09-07 22:18'
updated_date: '2026-09-08 07:21'
labels: []
dependencies:
  - TASK-12
ordinal: 46000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-12 설계(docs/design/2026-09-08-review-task-history-design.md)의 구현 소유자다. ⚠️ 이 태스크는 2026-09-08 팀리드 정리에서 신설했다 — TASK-12 가 Done 인데 마이그레이션 010 파일이 db/migrations/ 에 없고(009 까지만 있다) 구현을 소유한 태스크가 원장 어디에도 없었다. 설계만 Done 이 되고 코드가 따라오지 않는 것이 이 리포가 며칠째 마무리되지 않는 구조적 원인이다(CLAUDE.md work_continuity ② 가 사람 몫으로 지정한 「태스크 등록 누락」).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 마이그레이션 010 을 db/migrations/010_*.sql 로 작성한다 — 번호와 내용의 정본은 설계서 §5.1·§5.3 이다
- [x] #2 재계산 경로를 설계서 §3 의 파생 대 보존 구분대로 고친다 — 「지우고 다시 넣기」가 이력을 지우지 않게 한다
- [x] #3 설계서 §6 「함께 고쳐야 하는 것 — 이 설계가 깨는 자산」에 열거된 자산을 같은 커밋에서 함께 고친다
- [x] #4 red→green 증거를 남기고 게이트 무회귀를 확인한다 (직접 돌려 기존 732 passed 유지·증가 · ruff·ty exit 0)
- [x] #5 ⛔ 착수 전 설계서 §8 「캡틴에게 물을 것」이 닫혔는지 확인한다 — 열려 있으면 그것부터 올린다
- [x] #6 코드 리뷰를 받고 CRITICAL·HIGH 0건을 확인한다 — 캡틴 결정 13 이 코드 트랙의 Done 게이트다(결정 22 는 설계·문서만 풀었다). ⛔ 리뷰 전 Done 으로 올리지 않는다
- [x] #7 개발 DB 에 010 을 적용한다 — 캡틴 결정 33 이 정한 순서(리뷰 통과 후) + 결정 26 의 절차: pg_dump 백업 → 적용 → recompute_all → 행 수·내용 복원을 직접 확인해 출력을 노트에 남긴다 → 복원되지 않으면 멈추고 캡틴에게 올린다. TASK-44 가 남긴 실측 백필도 같은 실행에서 끝낸다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 착수 (팀리드).

**착수 전 확인 (AC#5)**: 설계서 §8 캡틴 질문 5건 중 Q1(008→011 이관)은 **결정 27로 닫혔다** — 008·002가 영구 결번이 됐으므로 010은 그대로 쓰고 순서 갈라짐 우려 자체가 사라졌다. Q4(기존 7행 삭제)는 **결정 26으로 승인**됐다(제약: pg_dump 백업 · 삭제 후 복원 직접 확인 · 복원 안 되면 멈추고 올린다). Q5는 **철회된 질문이라 올리지 않았다**(전제가 TASK-44/9df2369로 거짓이 됐다). Q2(학습자 손 표시)·Q3(표시 번호)는 답이 오지 않았으므로 설계서 명시 기본값으로 진행한다 — Q2는 학습자 행동 표를 TASK-3으로 연기, Q3은 표시 번호를 만들지 않고 「3단계 중 N단계」로 둔다.

**실측 확인**: `ls db/migrations/` → `001·003·004·005·006·007·009` (최대값 009) → 010이 맞다. `review_tasks` 제약 이름을 `pg_constraint` 직접 조회로 확보했다(추측 아님): `review_tasks_pattern_id_review_stage_key` · `review_tasks_status_check`.

**AC#1 산출**: `db/migrations/010_review_task_history.sql` 작성. 설계서 §5.3의 7단계 그대로 — 컬럼 2개 add · 기존 행 delete · not null 승격 · 유일키를 자연키로 교체 · status 값역에서 `skipped` 제거하고 `abandoned`·`superseded` 추가 · `done → completed_at not null` 함의 CHECK · `idx_review_tasks_due` 유지.

**red 1차 증거 (직접 돌렸다)**: 010 적용 후 전체 스위트 → **62 failed · 689 passed** (기준선 751 passed). 실패 원인이 정확히 010의 새 계약이다 — `asyncpg.exceptions.NotNullViolationError: null value in column "cycle_started_at" of relation "review_tasks" violates not-null constraint`. 즉 010이 마이그레이션 체인에 정상 적용됐고(테스트 DB는 매 세션 001~010을 새로 적용한다) 코드가 아직 따라오지 않았다. AC#2 구현이 이 62건을 green으로 되돌린다.

⚠️ **설계서 안에서 충돌하는 서술 1건 — 내가 이렇게 화해시켰다(뒤집은 것이 아니라 읽기를 확정했다).** §4 Contract는 `recompute`의 새 사후조건을 「현재 사이클 행은 사다리와 일치하고 **과거 사이클 행은 건드리지 않는다**」로 적는데, 같은 §4의 Failure는 「재발이 오면 **이전 사이클의 pending 행을 abandoned로 내린다**」고 적는다. 문자대로는 둘이 모순이다. 채택한 읽기: **과거 사이클 행은 재계산이 파생값으로 덮지 않는다(그 행의 done·due_at·completed_at은 그대로 산다). 다만 종료 상태를 못 받은 pending 행 하나만 abandoned로 내린다.** 근거는 Failure 렌즈가 그 전이의 목적을 명시한 것이다 — 「학습 히스토리 화면이 '여기서 끊겼다'를 그릴 근거이고, 지금은 그 행이 삭제되어 존재하지 않는다」. 이 전이가 없으면 이 설계가 고치려는 병(끊긴 사이클이 흔적 없이 사라진다)이 그대로 남는다. ⛔ 이 읽기를 뒤집으려면 Failure 렌즈의 그 줄과 §3.3의 `abandoned` 정의를 함께 지워야 한다 — 조용히 한쪽만 바꾸지 말 것.

**남은 것**: AC#2(재계산을 자연키 upsert + 사다리로) · AC#3(§6 자산 9건) · AC#4(green + 게이트). ⚠️ AC#2 는 `review.py` 를 고치는데 이 턴에 **TASK-44 재리뷰어가 그 파일을 읽고 있어** 착수를 미뤘다 — 리뷰가 돌아온 뒤 시작한다(H-AM: 같은 워킹트리 공유).

---

**2026-09-08 비정상 종료 후 재개 세션.** 이전 세션이 AC#2·AC#3 을 미커밋 상태로 남긴 채 끊겼다. 재개 시점 실측: HEAD `a981fa9` · 미커밋 8파일 + 미추적 `db/migrations/010_review_task_history.sql`.

**AC#2·AC#3 은 완료 상태로 발견됐고, 내가 직접 대조해 확인했다** (전달받은 주장이 아니라 파일을 열어 확인했다):
- AC#2 — `recompute` 가 자연키 upsert + 사다리로 바뀌었고 **756 passed**(기준선 751). 이전 세션이 기록한 red(62 failed · `NotNullViolationError: cycle_started_at`)가 green 으로 되돌아왔다.
- AC#3 — 설계서 §6 의 10행이 가리키는 **6개 파일 전부**가 diff 에 있고 내용을 직접 확인했다: `test_schema.py`(010 값역·유일키 테스트 2건 신설) · `test_review.py` · `test_pipeline.py` · `test_pronunciation_review.py`(`_task_rows` 를 `cycle_started_at` 우선 정렬로 · 발음 경로도 사다리를 남기는 것을 단정 — **자매 설계가 넘긴 요구가 여기서 충족됐다**) · `docs/database-schema.md`(규약 4개 · `done` 재정의 · `skipped` 제거 · 달력 날짜 컬럼 금지) · `docs/design/2026-08-25-...`(공백 1 을 취소선 + 뒤집힌 사실·근거·정본으로 보존).
- 플레이스홀더 검사: `TODO`·`FIXME`·`mark.skip`·`xfail`·`NotImplemented` **0건**.

**이 세션이 고친 것 — 게이트 「안」에 회귀 2건이 있었다** (이전 세션이 마감 전에 못 잡은 것):
① `review.py` 의 `_SUPERSEDE_TASKS_SQL` 호출이 `ruff format` 위반 → 포맷 적용(1지점).
② `test_review.py` 의 `_stages` 에 `list[dict]` 를 넘겨 `ty` 진단 1건 → **타입을 넓히지 않고** 그 한 곳이 스냅샷 대신 원본 `Record` 를 넘기게 했다(다른 17개 호출부는 전부 `Record` 를 넘긴다 — 넓히면 그 계약이 약해진다).
③ 게이트 **밖**이 6 → 10 errors 로 늘어 있었다(유지 대상인데 증가). 새 4건 전부 `test_review.py` 의 한글 주석 E501 → 줄바꿈만 조정해 **기준선 6 errors 로 복귀**.

**게이트 (이 턴에 직접 돌렸다)**: **756 passed** 13.88s · `ruff check .` exit 0 · `ruff format --check .` **33 files** · `ty check` exit 0 · 게이트 밖 `ruff check ../../tests ../../scripts` **6 errors** · `ruff format --check ../../tests` **4 files** · 프론트 `npx tsc --noEmit`·`npx eslint app lib` 둘 다 exit 0.

⚠️ **AC#4 를 아직 체크하지 않았다 — green 과 게이트는 내가 직접 돌렸지만 red 는 이전 세션의 출력이다.** 다른 세션의 「돌렸다」는 내 증거가 아니다(SVG 원칙 2). 리뷰가 돌아온 뒤(같은 트리를 공유하므로 `H-AM`) HEAD 의 `review.py` 를 임시로 되돌려 신규 6건이 실제로 red 인지 직접 관측하고 체크한다.

**개발 DB 실측 (직접 조회 · 읽기만)**: 적용된 마이그레이션 `001·003·004·005·006·007·009` → **010 미적용** · `review_tasks` **7행 전부 `status='pending'`** · `skipped` **0행** · `error_patterns.next_review_at` 있는 패턴 7건. **즉 010 이 무손실 근거로 든 항목 ②가 실측으로 참이다.** ⛔ 그리고 **지금 코드와 DB 가 어긋나 있다** — 코드가 `cycle_started_at` 을 쓰는데 컬럼이 없다.

**캡틴 결정 33 (이 세션에 받았다)**: 010 의 개발 DB 적용은 **코드 리뷰 통과 후**에 한다. 결정 26 이 삭제 자체를 이미 승인했으므로 이것은 순서 결정이다 — 리뷰가 마이그레이션에서 결함을 찾으면 지운 뒤에 고치게 되는 경로를 닫는다. → AC#7 로 등록했다. 정본은 `docs/ops/captain-instruction-register.md` 결정 33.

---

**AC#4 닫힘 — red 를 내가 직접 재관측했다** (이전 세션 출력을 내 증거로 쓰지 않았다 · SVG 원칙 2).
방법: `git show HEAD:app/backend/app/services/review.py` 로 010 이전 판을 임시 복원 → 전체 스위트 → 구현 복원(`diff` 로 바이트 동일 확인).
- **red: 67 failed · 691 passed** (39.56s). 원인이 정확히 010 의 새 계약이다 — `asyncpg.exceptions.NotNullViolationError: null value in column "cycle_started_at" of relation "review_tasks" violates not-null constraint`. 테스트 DB 는 매 세션 001~010 을 새로 적용하므로 010 이 체인에 정상 적용됐고 옛 코드가 따라오지 않은 것이다.
- **green: 758 passed** 13.60s.
⚠️ 이전 세션이 기록한 red 는 `62 failed · 689 passed` 였다. 내 값이 다른 이유는 **그 뒤 테스트가 늘었기 때문**이다(이 세션이 MEDIUM-3·LOW-3 로 2건 추가) — 회귀가 아니다.

**코드 리뷰 1차 결과: BLOCK** (CRITICAL 0 · HIGH 2 · MEDIUM 5 · LOW 4). 보고서 정본: `.superpowers/sdd/2026-09-07-scenario-and-drill-turns-design/review-task43-report.md` (397줄).
⚠️ **리뷰어가 보고서를 한 번에 쓰려다 출력 토큰 상한(16384)에 걸려 죽었다** — 파일이 생성조차 안 됐다. 새로 dispatch 하지 않고 `SendMessage` 로 「절마다 나눠 파일을 키워라 · 한 호출당 100줄 상한」을 지시해 살렸다(컨텍스트가 살아 있어 분석을 다시 하지 않았다).

**BLOCK 사유가 코드 로직이 아니다** — 재계산의 자연키 upsert·사다리·상태 전이는 리뷰가 「논리적으로 정확하고 멱등」으로 판정했다. HIGH 2건은 **이 리포의 지배 실패 모드**(본문을 고치고 그것을 설명하는 문장을 안 고쳤다)이고, **§6 목록 자신이 낡아서** 생겼다 — 그 목록은 TASK-44 가 발음 패턴을 review_tasks 로 들여보내기 전에 쓰였고 `scripts/`·하네스 자산을 애초에 열거하지 않았다. AC#3 위반이 아니다(§6 의 10행은 전부 처리됐다).

**11건 전부 닫았다** (직접 검증한 것을 함께 적는다):
- **HIGH-1** `scripts/smoke_analysis.py` — `Check("review_tasks가 패턴당 0~1행이다", len(review_rows) <= len(patterns))` 가 **실행되는 단정**인데 010 이 그 규약을 폐기했다 → 정상 동작에서 FAIL 로 뒤집힌다. **내가 그 줄을 직접 열어 확인했다.** 자연키 중복 0건 + 사이클당 열린 단계 ≤ 1(DB 가 아니라 `fold_stages` 가 보장하는 것)로 다시 썼다.
- **HIGH-2** `docs/design/2026-09-08-pronunciation-review-cycle-design.md` — §5.2 「review_tasks 0~1행 replace」 · §7 L1 Contract 「사후: review_tasks(0~1행)」 가 같은 함수의 사후조건을 반대로 적는다. **둘 다 직접 열어 확인했다.** 그 문서는 Done 이고 **살아 있는 정본**이라(review.py docstring 이 지목한다) 낡은 계약을 읽은 다음 사람이 사다리 행을 버그로 판정할 경로였다. 자매 문서와 **같은 형식**(원래 판정 · 왜 뒤집혔나 · 지금의 판정 · 정본)으로 고쳤다. §9 약점 3(정체성 문제를 상속한다)도 같은 부류라 함께 닫았다 — 리뷰가 열거하지 않았지만 010 이 해소한 것이다.
- **MEDIUM-1** `review.py` 의 「순서가 중요하다」 주석이 **거짓**이었다. **세 쿼리의 `where` 절과 `fold_stages` 를 직접 읽어 검증했다**: upsert=`cycle=C ∧ stage∈1..L` · supersede=`cycle>C ∨ (cycle=C ∧ stage>L)` · abandon=`cycle<C ∧ pending` → 쌍마다 서로소이므로 순서가 결과를 바꾸지 않는다(`fold_stages` 가 stage=1 부터 +1 로만 append 하므로 사다리가 정확히 1..L 연속인 것이 전제). **진짜 불변조건은 `$3` 이 「새」 사다리 길이라는 것**이고 그것을 주석에 박았다 — 옛 길이로 바꾸면 순서와 무관하게 깨진다.
- **MEDIUM-2** `tests/harness/scenarios-E-agent-learning.md` L2 가 **삭제된 제약**(`unique(pattern_id, review_stage)`)을 가드로 지목 → 자연키 + `id` 보존으로 다시 썼다. 같은 표 L3 의 `pronunciation_intonation → shadowing` 도 TASK-44 가 `rephrase` 로 확정해 거짓이 됐으므로 취소선 + 소유자(TASK-48) 표시만 붙였다(시나리오 재설계는 하지 않았다 — 범위 밖).
- **MEDIUM-3** `_SUPERSEDE_TASKS_SQL` 의 갈래 ①(`cycle_started_at > $2` — 재분석이 최신 재발을 지워 사이클 시작이 **뒤로** 움직인다)이 무테스트였다 → `test_reanalysis_that_drops_the_latest_relapse_retires_the_newer_cycle` 신설. **판별력을 뮤테이션으로 확인했다**: 갈래 ①을 제거하면 `AssertionError: (1,'pending') != (1,'superseded')` 로 red, 되돌리면 green(백업과 `diff` 바이트 동일 확인).
- **MEDIUM-4** 010 적용·백필·백업 절차가 파일 어디에도 없었다 → 머리주석에 5단계를 박았다(pg_dump → 적용 → **backfill 필수** → 복원 직접 확인 → 안 되면 멈추고 올린다) + 「코드와 마이그레이션은 같은 커밋으로 함께 나간다」. 태스크 소유는 AC#7 로 등록했다.
- **MEDIUM-5** 원장 노트가 산출물과 어긋났다 → 이 세션 노트와 AC 체크로 이미 해소했다.
- **LOW-1** `2026-08-25-learning-coach-agent-design.md` 의 옛 유일키 서술에 교체 표시. **LOW-2** 되살아난 행의 문맥이 한 라운드 늦게 갱신되는 것을 설계서 §7 약점 3 에 기록(고치지 않는 근거 포함). **LOW-3** `fold_stages` 의 새 출력(`ladder`·`cycle_started_at`)을 **DB 없이** 단정하는 테스트 신설 — 모듈 docstring 이 내세우는 「순수 함수라 DB 없이 검증된다」가 새 출력에는 적용되지 않고 있었다. **LOW-4** 스키마 표에 물리 컬럼 순서 한 줄.

**게이트 (이 턴에 직접 돌렸다)**: **758 passed** 13.40s · `ruff check .` exit 0 · `ruff format --check .` 33 files · `ty check` exit 0 · 게이트 밖 `ruff check ../../tests ../../scripts` **6 errors** · `ruff format --check ../../tests` **4 files** (둘 다 기준선 복귀) · 프론트 `tsc`·`eslint` exit 0 · `py_compile scripts/smoke_analysis.py` OK.
⚠️ **수정 중에 게이트 밖이 6 → 9 로 늘었던 것을 잡아 되돌렸다** — 내 HIGH-1 수정이 E501 3건과 포맷 2파일을 넣었다. 중복된 긴 표현식을 `review_detail` 지역 변수로 뽑아 닫았다.

**남은 것**: AC#6(재리뷰로 CRITICAL·HIGH 0건 확인) · AC#7(개발 DB 010 적용 · 결정 33 순서).

---

**AC#7 착수 전 — 이전 상태와 「복원」의 정의를 실행 전에 박는다** (직접 조회 · 읽기만).
⛔ 이것을 미리 적는 이유: 실행 후에 결과를 보고 「복원됐다」를 사후 합리화하지 않기 위해서다. 결정 26 이 요구한 「행 수와 내용 복원 확인」이 반증 가능해야 한다.

**이전 상태**: `review_tasks` **7행 전부 `stage=1 또는 2` · `pending` · `task_type=rephrase`**. 그런데 `error_patterns` 는 **8개**다 — 빠진 하나가 `pronunciation_an_as_a`(`pronunciation_intonation` · `next_review_at=None` · freq=2)이고 **그것이 TASK-44 가 남긴 실측 백필 대상**이다.
- stage=1 pending 5건: `article_missing_before_noun` · `article_missing_the_before_place_noun` · `article_wrong_definite_article` · `business_expression_verb_noun_collocation` · `preposition_time_expression_ago`
- stage=2 pending 2건: `preposition_missing_to_after_go` · `word_order_time_adverb_placement`

**⚠️ 「복원」은 7행으로 되돌아가는 것이 아니다 — 그보다 많아야 한다.** 010 의 요점이 이력 보존이므로:
- stage=2 인 2건은 **각각 2행**(1단계 `done` + 2단계 `pending`) = **4행** ← 이것이 010 이 새로 보존하는 접힌 단계다
- stage=1 인 5건은 각각 1행 = **5행**
- `pronunciation_an_as_a` 는 **1행 신설**(TASK-44 경로) = **1행**
- **예측 합계: 10행.** 사이클은 패턴당 1개여야 한다(재발이 하나뿐이므로).
- `error_patterns.next_review_at` **8건 전부 값이 있어야 한다**(지금은 7건 — 발음이 None). 문법 7건의 값은 **바뀌지 않아야 한다**(같은 이력에서 다시 계산하므로): `2026-09-04T13:56:38.186958+00` · `2026-09-07T14:36:53.838781+00` · `2026-09-03T23:15:01.903223+00` · `2026-09-01T22:21:03.123308+00` · `2026-09-09T14:36:53.838781+00` · `2026-09-03T23:15:01.903223+00` · `2026-09-09T14:36:53.838781+00`.
- 발음 예정일은 설계서 §4.4 가 **`2026-09-04T13:51:26+00`** 로 **예측**해 뒀다 — 그것이 실행 출력과 맞는지가 TASK-44 백필의 판정이다. ⚠️ 예측이므로 **틀릴 수 있다**; 틀리면 값을 고치지 말고 어긋남을 기록한다.

**⛔ 이 중 하나라도 어긋나면 멈추고 캡틴에게 올린다** (결정 26 제약). 특히 **행이 7행 이하면** 이력 보존이 동작하지 않은 것이다.

**절차 확인 (실행 아님)**: `pg_dump` 는 `/opt/homebrew/opt/postgresql@17/bin/pg_dump` 에 실재한다. 백필은 `app/backend/.venv/bin/python scripts/backfill_review_state.py` — 사용자별로 `recompute_all` 을 한 트랜잭션에 돌리고 `load_due_reviews` 결과까지 출력하므로 **복원 확인과 TASK-44 백필 증거가 같은 출력에서 나온다.** 멱등이다.

---

**AC#6 닫힘 — 재리뷰 판정 APPROVE (CRITICAL 0 · HIGH 0 · MEDIUM 0 · LOW 3 신규).** 보고서 정본: `.superpowers/sdd/2026-09-07-scenario-and-drill-turns-design/review-task43-rereview-report.md`. **11/11 CLOSED.**
💡 리뷰어가 한 가장 값어치 있는 대조: **`git diff 99d41f1..d0dca09` 에서 주석·docstring 을 걷어낸 결과가 1차 스냅샷과 동일하다** — 즉 이번 라운드는 서술·단정·테스트만 건드렸고 1차의 「재계산 세 문장은 논리적으로 정확하고 멱등」 판정이 그대로 유효하다. 그리고 **758 = 1차 756 + 신규 2** 정합도 스스로 확인했다.

**신규 LOW 3건도 닫았다** (⛔ **라운드 3을 돌리지 않았다** — 리뷰어 자신이 「새 라운드의 근거로 쓰지 마라」고 판정했고, 내 규약도 「N회차 수정이 N+1회차 findings를 만들면 라운드 대신 게이트를 바꾼다」다):
- **새-LOW-1** 스모크 Check A(자연키 중복 0건)는 **현재 스키마에서 실패할 수 없다** — 010 이 그 튜플에 UNIQUE 를 걸었고, 010 미적용에서는 `UndefinedColumn` 으로 단정에 도달하기 전에 죽는다. 지우지 않고 **tripwire 임을 주석과 단정 이름에 명시**했다(미래 마이그레이션이 제약을 떨어뜨리는 것을 잡는 유일한 감지기다).
- **새-LOW-2** Check B 가 `recompute` 가 보장하는 것보다 **약했다** — 묶음이 `(pattern_id, cycle_started_at)` 이라 「사이클마다 하나씩, 패턴당 둘」을 통과시키는데 그것이 정확히 `_ABANDON_TASKS_SQL` 이 빠졌을 때의 모양이다. **묶음을 `pattern_id` 로 좁혀** abandon 회귀 감지기를 하나 만들고, 기전 둘(`fold_stages` = 현재 사이클 · `_ABANDON_TASKS_SQL` = 과거 사이클)을 주석에 함께 적었다.
- **새-LOW-3 을 직접 검증해 리뷰어가 맞다고 확인했다.** 내 주석의 「옛 길이를 쓰면 **순서와 무관하게** 깨진다」가 붙은 시나리오와 어긋났다. 직접 따라간 결과: **성장**(L 1→2 · `$3`=1)은 supersede 를 먼저 돌리면 `(C,2)` 행이 아직 없어 안 걸리고 upsert 가 나중에 만들어 **깨지지 않는다** → 이 순서에서만 깨진다. **축소**(L 3→1 · `$3`=3)는 supersede 가 `stage > 3` 만 보아 은퇴시켜야 할 2·3단계를 놓쳐 **어느 순서에서도** 깨진다. 주석을 두 방향으로 갈라 적었다 — 「순서를 지키는 것으로는 첫째만 가려지고 둘째는 남는다」.

**게이트 (이 턴에 직접 돌렸다)**: **758 passed** 13.40s · `ruff check .` exit 0 · `ruff format --check .` 33 files · `ty check` exit 0 · 게이트 밖 `ruff check ../../tests ../../scripts` **6 errors** · `ruff format --check ../../tests` **4 files** · `--check ../../scripts` **0** · `py_compile scripts/smoke_analysis.py` OK.
⚠️ **또 게이트 밖이 6 → 7 로 늘었던 것을 잡아 되돌렸다** — 내 새-LOW 수정이 E501 1건과 포맷 1파일을 넣었다. **이번 세션에 이 형태가 두 번 나왔다**: 게이트 **안**만 보면 게이트 **밖** 회귀를 놓친다. `tests/`·`scripts/` 를 건드렸으면 밖도 반드시 다시 잰다.

**남은 것: AC#7 하나** — 개발 DB 010 적용 + 백필. 결정 33 의 선행(리뷰 통과)이 **충족됐다.**

---

**AC#7 닫힘 — 개발 DB 010 적용 + 백필 완료. 예측 전건 일치.** (결정 33 의 선행 「리뷰 통과」 충족 후 실행 · 결정 26 의 5단계 절차 그대로)

**① 백업**: `/opt/homebrew/opt/postgresql@17/bin/pg_dump --data-only --table=review_tasks --table=error_patterns` → `/tmp/ohmyenglish-review_tasks-before-010-20260908-161946.sql` (4.4k). **덤프 안의 `COPY public.review_tasks` 행 수를 세어 7행을 확인했다** — 파일 존재만 보지 않았다.

**② 적용**: `app/backend/.venv/bin/python scripts/migrate.py` → `schema_migrations` 에 **010 추가**(`001·003~007·009·010`). 직접 조회로 확인한 것 4개:
- 새 컬럼 2개가 **물리적으로 맨 뒤**다(`…created_at, cycle_started_at, completed_at`) → **LOW-4 의 서술이 맞았다.**
- 자연키 제약 **`review_tasks_cycle_stage_key` 신설**, 옛 `review_tasks_pattern_id_review_stage_key` **제거됨**.
- `review_tasks_done_has_completed_at` 신설.
- 7행 → **0행**(010 의 `delete` 가 돌았다).
⚠️ `migrate.py` 는 이미 적용된 파일에 대해 **아무 것도 출력하지 않는다** — 조용한 성공이라 출력만 보면 적용 여부를 알 수 없다. 반드시 `schema_migrations` 를 조회한다.

**③ 백필**: `app/backend/.venv/bin/python scripts/backfill_review_state.py` → 패턴 **8건** 재계산, 오늘 복습할 목록 **6건**.

**④ 복원 대조 — 실행 전에 박아둔 예측과 전건 일치** (사후 합리화가 아니다):
| 예측 | 실측 | |
|---|---|---|
| 10행 | **10행** | ✓ |
| 사이클 8개(패턴 8 × 1) | **8개** | ✓ |
| `next_review_at` 8건 | **8건** (이전 7건) | ✓ |
| 문법 7건 예정일 **불변** | 어긋남 **0건** | ✓ |
| 발음 예정일 = 설계서 §4.4 예측 `2026-09-04 13:51:26+00` | **`2026-09-04T13:51:26.680389+00`** | ✓ |
| status | `done` 2 · `pending` 8 | ✓ |

**⛔ 「복원」이 7행이 아니라 10행인 것이 이 설계의 전부다.** 늘어난 3행의 정체:
- `preposition_missing_to_after_go` · `word_order_time_adverb_placement` — 각각 **1단계 `done`(완주 `2026-09-06T14:36:53`) + 2단계 `pending`** 2행. **010 이전에는 이 접힌 1단계 행이 존재하지 않았다** — 「1단계는 했고 2단계가 열려 있다」를 화면이 그릴 근거가 실물 데이터에 처음 생겼다.
- `pronunciation_an_as_a` 1행 — **발음 패턴이 히스토리에 처음 들어왔다**(TASK-44 경로 · `task_type='rephrase'` · 자매 설계 §5.4 대로 새 컬럼 없이 `pattern_id → category` 로 유도).

**⑤ TASK-44 의 실측 백필도 이 실행에서 끝났다.** 설계서 §4.4 가 `an_as_a` 에 대해 예측해 둔 값이 **실행 출력과 일치했다** — 그것이 예측이었다는 사실이 이제 해소됐다. **두 번 쓰지 않았다**(결정 33 의 「같은 실행에서 함께」).

**⑥ 설계의 핵심 주장을 실물 DB에서 검증했다 — 이것이 가장 값어치 있는 증거다.**
백필을 **한 번 더** 돌린 뒤 `id`·`created_at` 10건을 `diff` 로 대조 → **전건 동일.** 즉 재계산이 delete+insert 가 아니라 같은 행에 다시 닿는다. 010 이 고치려던 병(`id` 가 매번 새로 발급되어 화면이 과제를 가리킬 손잡이가 사라진다)이 실제로 사라졌다.

⚠️ **복원되지 않은 것은 없어서 캡틴에게 올릴 것이 없다.** 결정 26 의 「복원 안 되면 멈추고 올린다」 조건은 발동하지 않았다.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-07 22:55
---
캡틴 결정 26·27(2026-09-08)로 착수 전 질문이 닫혔다. ① 결정 26 — 기존 review_tasks 7행 삭제 승인. 캡틴 원문이 「이말 자체를 모르겠어. 작업 진행간 필요한 작업이면 삭제하고 진행해」였으므로 무엇을 승인한 것인지 결정 26 본문에 못박았다. ⚠️ 팀리드가 붙인 제약: 삭제 전 pg_dump 백업 · 삭제 후 행 수와 내용 복원을 직접 확인해 출력을 노트에 남긴다 · 복원되지 않으면 멈추고 올린다. ② 결정 27 — 008 예약을 푼다(영구 결번). 이 태스크의 010 은 그대로 쓴다. ③ 설계서 §8 나머지 질문(Q2 학습자 손 표시 · Q3 표시 번호 · Q5 히스토리에 발음 복습)은 답이 오지 않았으므로 설계서가 명시한 기본값으로 진행한다.
---
<!-- COMMENTS:END -->
