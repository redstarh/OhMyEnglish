---
id: TASK-111
title: '정리: 전용 모드 지시문의 규칙 11 이 없는 규칙 4 를 가리킨다 — 고치려면 재측정이 필요하다'
status: Done
assignee: []
created_date: '2026-09-11 15:43'
updated_date: '2026-09-12 00:11'
labels: []
dependencies: []
ordinal: 116000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
2026-09-12 KST 관측. nova.PRONUNCIATION_MODE_PROMPT 의 규칙 11 이 「the one correction for that turn in rule 4」로 «이 프롬프트에 없는» 규칙 4 를 가리킴. ⛔ 일부러 그대로 뒀음 — 실물 왕복 4/4 를 얻은 문면이 정확히 그것이고, 문구를 다듬으면 그 근거가 다듬은 판에 붙지 않음(runs/2026-09-11-task86-dedicated-session.md). ⚠️ 그래서 이것은 「고치면 되는 오타」가 아니라 «고친 뒤 다시 재야 하는» 변경임. 표본 4 로는 열등을 배제하지 못하므로 회차 설계에 그 점을 적어야 함.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 문면을 자기완결로 고친 판을 만든다 — 규칙 번호 참조를 없애고 뜻은 유지한다
- [x] #2 고친 판을 실물 왕복으로 재고 4/4 판과 대조한다 — 상한과 해석 규칙을 돌리기 전에 적는다
- [x] #3 결과가 나빠지면 되돌리고 그 사실을 기록한다 — 「읽기 좋아졌다」를 근거로 유지하지 않는다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-12 KST — **TASK-116 과 같은 변경 묶음으로 다루는 것을 권고함.** 둘 다 전용 지시문(nova.PRONUNCIATION_MODE_PROMPT · _SOUND_INSTRUCTION)의 문면을 고치는 일이고 그 문면은 test_nova.py 의 바이트 대조 게이트로 실측 판(4/4)에 묶여 있음. 어느 쪽이든 고치면 그 게이트가 깨지고 회차를 다시 돌려야 하므로, 따로 고치면 회차를 두 번 쓰게 됨.

한 묶음의 내용: ① 이 태스크 — 규칙 11 이 없는 규칙 4 를 가리키는 참조를 자기완결로 고침 ② TASK-116 — 코칭한 소리와 target_sound 가 어긋나는 것을 지시문에서 막음(전용 세션은 이름 붙인 그 소리만 코칭하게 하거나, 키 규칙을 조건부로 바꿈).

⛔ 그 회차의 해석 규칙에 「고친 판이 4/4 판보다 나빠지면 되돌린다」를 미리 적어야 함 — 표본 4 로는 열등을 배제하지 못하므로 상한과 판정선을 먼저 정하는 것이 이 묶음의 선행임.

2026-09-12 KST 실측 — 회차 정본: tests/harness/runs/2026-09-12-task111-116-selfcontained-key.md (§0 에 상한·해석 규칙을 «돌리기 전에» 적었음).

AC#1 — 규칙 11 을 자기완결로 고쳤음. 이전: «A pronunciation correction is the one correction for that turn in rule 4.» 지금: «Make at most one correction per turn. When a sound is off, the pronunciation correction is that one - spend it on the sound…». 뜻(턴당 한 교정 · 그 한 자리를 발음이 씀)을 그대로 두고 번호 참조만 걷었음. 소리 줄의 «the one correction of rule 4» 도 같은 회차에서 «the one correction you make that turn» 으로 걷었음.
⇒ 전용 프롬프트의 번호 참조가 3개(rule 4 x2 · rule 9)에서 1개(rule 9)로 줄었음. 직접 잼: re.findall(r'rule \d+', build_pronunciation_prompt('th_as_s')) = ['rule 9'].

⛔ AC#1 을 문면 전체로 읽으면 «완전히» 닫히지 않았음 — 남은 rule 9 참조는 TASK-75 승인 문면이고 test_nova.py 가 문자열로 고정하므로 되돌리는 «결정» 이 필요함. 그 몫을 TASK-118 로 갈라 등록했고 그 태스크가 TASK-111 을 선행으로 가짐. 조용히 좁히지 않고 여기 적었음.

AC#2 — 실물 왕복으로 재고 4/4 판과 대조했음. REG 팔(pq06→pq12 · th_as_s · 4회 · 앱 경로 :8012 · 검증 전용 DB): 코칭 4/4. 기준선(runs/2026-09-12-task114-mild-band-dedicated.md §1 MILD 팔)도 4/4 ⇒ 판정선 통과. pronunciation_attempts 4행 · target_sound 전부 th_as_s · signal_source 전부 nova_tool · next_review_at 섰음.
⚠️ 숨기지 않은 차이: pronunciation 이벤트가 기준선 세션당 2건(합 8)인데 이 회차는 2·2·1·1(합 6). 보이는 기전은 둘째 오디오의 전사문(정답판이 오류판과 같은 문면으로 전사됨)이지만 표본 4 로 ASR 변동과 문면 효과를 가르지 못하므로 단정하지 않았음.
⚠️ 대조군이 «역사적» 임 — 같은 날 같은 스택이지만 동시 대조가 아님.
⛔ 표본 4 가 «동등» 을 증명하지 않음: P(0 in 4 | 0.1)=0.66.

AC#3 — 되돌리는 조건이 «발동하지 않았음» (4/4 유지). 그 사실을 회차 기록 §2 「남긴 변경과 그 근거」에 적었음. ⛔ 「읽기 좋아졌다」를 근거로 유지한 것이 아니라 미리 적은 판정선을 넘어서 유지했음.

바이트 게이트를 새 회차 산출물로 갱신했음: test_nova.py 가 이제 runs/2026-09-12-task111-116-selfcontained-key/prompt_dedicated_v2.txt (1,927자)를 가리킴. 문면 자수 1,702 → 1,927 (+225).
게이트: pytest 967 passed(16.39s) · ruff check 0 · ruff format --check 36 files · ty 0. 전부 파이프 없이 직접 돌렸음.
<!-- SECTION:NOTES:END -->
