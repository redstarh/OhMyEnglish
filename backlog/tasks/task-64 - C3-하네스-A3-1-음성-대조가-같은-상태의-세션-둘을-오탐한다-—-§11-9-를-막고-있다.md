---
id: TASK-64
title: 'C3 하네스: A3-1 음성 대조가 같은 상태의 세션 둘을 오탐한다 — §11-9 를 막고 있다'
status: Done
assignee: []
created_date: '2026-09-09 13:46'
updated_date: '2026-09-09 13:57'
labels: []
dependencies: []
ordinal: 67000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
2026-09-09 TASK-55 작업 중에 직접 관측했음. c3_results_screen.py 의 A3-1 음성 대조가 `len(set(texts)) != len(texts)` 로 「상태마다 문구가 바뀐다」를 재는데, 이 식은 「상태당 세션 1건」을 암묵 전제함. 교정 2건 이상 세션(e0c5e580)을 여섯째로 넣어 돌리면 `final` 이 둘이 되어 라벨이 중복되고 FAIL 이 남 — 앱 결함이 아니라 대조식의 오탐임.

직접 얻은 출력: FAIL A3-1 음성 대조: 같은 요소의 문구가 상태마다 바뀌지 않았다 — ['분석 중', '확정', '확정', '부분 실패', '연결 실패', '분석 대상 없음'] (단정 81건 검사 중 어긋남 1건).

왜 중요한가: 이것이 browser_leg.md §11-9 가 요구하는 「교정 2건 이상 세션 추가」를 막고 있음. 그 세션이 없으면 A4-2 기대값 교차 대조가 매 회차 「미확인」으로 남고(2026-09-09 회차에서도 그랬음), test_c3_gates.py:165 의 주석이 지목한 공허 통과 마스크가 벗겨지지 않음.

지금은 5건(상태당 1건)으로 돌려 픽스처 모양을 지켰음 — 범위를 넘지 않으려고 대조식을 고치지 않았음.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 상태가 아니라 「서로 다른 status 의 라벨이 서로 다르다」를 재도록 대조식을 고친다 — 같은 status 의 세션 둘이 같은 라벨을 갖는 것은 정상이다
- [x] #2 고친 대조식의 판별력을 test_c3_gates.py 의 무력화 입력으로 잰다 — 라벨이 상태마다 바뀌지 않는 입력에서 여전히 FAIL 이 나야 한다
- [x] #3 교정 2건 이상 세션(e0c5e580)을 포함해 C3 를 돌려 A4-2 미확인이 사라지는 것을 직접 확인한다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-09 완료. 판정 단위를 세션에서 status 로 옮겼음.

AC1 — `check_status_label_variation` 을 신설해 두 단정으로 갈랐음: ① 같은 status 는 같은 문구를 냄 ② 서로 다른 status 는 서로 다른 문구를 냄. 항진명제 방어(C3 검토 ③)는 그대로 계승해 status 가 2종 미만이면 미확인으로 냄 — 바뀐 것은 세는 대상뿐임.

⛔ 함수를 `main_async` 밖으로 뺐음. 판정이 코루틴 안에 있으면 무력화 입력으로 잴 수 없어서 AC2 를 만족할 수 없음.

AC2 — test_c3_gates.py 에 다섯 갈래를 넣었음: 실물 회차 통과(checked 2 · fails 0) · 같은 상태 셋을 오탐하지 않음 · 서로 다른 상태가 같은 문구를 내면 FAIL · 같은 상태가 두 문구를 내면 FAIL · 1종이면 미확인이고 검사 수 0. 앞의 둘이 오탐 회귀를 막고 뒤의 둘이 본래 목적을 지킴.

AC3 — 6세션 회차를 직접 돌렸음. 고치기 전: 단정 81건 중 어긋남 1건(FAIL — 라벨 목록 ['분석 중','확정','확정','부분 실패','연결 실패','분석 대상 없음']). 고친 뒤: 단정 82건 전건 통과이고 미확인 0건임 — 이전 회차의 「A4-2 판별력 미확인」이 사라졌음. final_two 가 교정 2건·카드 2장이라 카드별 관통 대조가 두 카드에서 평가됨.

범위를 하나 넓혔고 그 이유를 남김: 그 판독을 게이트 픽스처로 올렸음(runs/2026-09-09-c3-dom-read-post-task64.json · SESSION_IDS 에 final_two 추가). 브라우저 회차만 고치면 A4-2 는 사람이 회차를 돌릴 때만 지켜짐. ⛔ 픽스처가 그 표본을 잃는 것도 단정으로 막았음(test_fixture_keeps_a_two_correction_session) — 빼먹으면 A4-2 가 「카드 1장 대 교정 1건」으로 줄어 어긋남 0건으로 조용히 통과함.

본문을 고치고 그것을 설명하는 문장을 함께 고쳤음: browser_leg.md §5 A3-1 음성 대조 칸 · 같은 문서 §11-9 절(막고 있던 이유와 닫힌 근거) · c3_results_screen.py 모듈 docstring.

게이트: pytest 877 passed · ruff check exit 0 · format unformatted 0 · ty check exit 0 · 게이트 밖 ruff 0건 · format unformatted 0. 871 → 877 은 이 태스크의 판별력 6건임.
회차 기록은 tests/harness/runs/2026-09-09-task64-a3-1-status-unit.md 임.
<!-- SECTION:NOTES:END -->
