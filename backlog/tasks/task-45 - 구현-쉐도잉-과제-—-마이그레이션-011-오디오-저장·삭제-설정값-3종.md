---
id: TASK-45
title: '구현: 쉐도잉 과제 — 마이그레이션 011 + 오디오 저장·삭제 + 설정값 3종'
status: In Progress
assignee: []
created_date: '2026-09-07 22:19'
updated_date: '2026-09-08 12:38'
labels: []
dependencies:
  - TASK-27
ordinal: 48000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-27 설계(docs/design/2026-09-08-shadowing-task-design.md)의 구현 소유자다. ⚠️ 2026-09-08 팀리드 정리에서 신설했다 — TASK-27 의 AC#5(config.py Settings 에 필드를 더한다)는 설계가 아니라 구현이라 설계 태스크가 그것 때문에 닫히지 못했다. 그 AC 를 이리로 이관했다. 마이그레이션 011 파일은 아직 없다(db/migrations/ 는 009 까지).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 마이그레이션 011 을 db/migrations/011_shadowing_items.sql 로 작성한다 — 표 명세의 정본은 설계서 §3.3 이고 번호 근거는 §3.5 다
- [x] #2 재생 속도 0.5~2.0 과 반복 1~10 을 app/backend/app/config.py 의 Settings 에 필드로 더한다 — 새 설정 체계를 만들지 않는다. 범위 밖 값은 기동 시점에 거부한다 (TASK-27 AC#5 에서 이관)
- [ ] #3 학습자 오디오 저장을 설계서 §4.1~§4.5 대로 구현한다 — utterance_type 값 추가 · assets/audio/ 경로 · DB 포인터를 마지막에 쓴다
- [ ] #4 당일 경과 삭제를 설계서 §5.2·§5.3 대로 users.timezone 기준으로 구현한다 — current_date 를 쓰지 않는다. 진행 중 세션의 녹음은 삭제하지 않는다(§5.4)
- [ ] #5 삭제 주체를 설계서 §6.1 대로 워커 유휴 사이클 스윕으로 구현한다 — 새 job 종류를 만들지 않는다. 재시도는 §6.3 대로 상태를 만들지 않는다
- [ ] #6 red→green 증거를 남기고 게이트 무회귀를 확인한다 (직접 돌려 기존 732 passed 유지·증가 · ruff·ty exit 0)
- [x] #7 ⛔ 착수 전 설계서의 캡틴 질문(shadowing_items 를 누가 채우나)이 닫혔는지 확인한다 — 열려 있으면 그것부터 올린다
- [ ] #8 shadowing_items 를 0행으로 두지 않는다 — 학습 1회가 성립하는 최소 분량의 시드를 넣는다. ⛔ 외부 콘텐츠를 긁어오지 않는다(PRD 가 자동 수집을 비범위로 둔다): 전사문은 학습자 수준(A2)에 맞춘 자체 작성 문장으로 만든다 (캡틴 결정 25)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 착수 (재개 세션).

**AC#7 닫힘 (착수 전 확인)**: 설계서 §13 의 캡틴 질문(`shadowing_items` 를 누가 채우나)은 **결정 25 로 닫혔다** — 팀리드가 A2 수준 자체 문장으로 시드를 채운다(⛔ 외부 콘텐츠를 긁지 않는다). 이 태스크 코멘트 #1 이 그 근거를 갖는다.

**AC#2 완료 — 설정값 3종 + `.env.example`** (설계서 §7.1~§7.3).
산출: `app/backend/app/config.py` 의 `Settings` 에 `shadowing_playback_rate`(`ge=0.5, le=2.0`, 기본 1.0) · `shadowing_repeat_count`(`ge=1, le=10`, 기본 1) · `shadowing_audio_root`(`Path("../../assets/audio")`) 추가. `from pathlib import Path` 도 함께. **새 설정 체계를 만들지 않고 같은 클래스에 더했다.**
`.env.example` 도 갱신했다 — 설계서 §7.3 이 「작업세션이 정한다」로 열려 있던 질문에 **갱신한다**로 답했고 근거는 발명이 아니라 **선례**다(`DRILL_TURNS_MIN`·`DRILL_COUNT` 가 이미 근거 주석까지 달고 그 파일에 있다).

**red→green (직접 관측)**: 신규 테스트 **8건**(파라미터화 포함) → **8 failed** → 구현 후 **8 passed**. ⚠️ red 의 형태가 둘이었다: 기본값·경계값 테스트는 `AttributeError`(필드 부재), **거부 테스트는 `Failed: DID NOT RAISE`** — `model_config` 의 `extra="ignore"` 때문에 **미지의 필드가 조용히 무시되어** 값역 위반이 통과했다. 즉 이 값역 테스트는 「필드가 존재한다」까지 함께 잠근다.

**기본값을 항등원으로 둔 것이 유일한 판단이다** (설계서 유도 3). 캡틴 결정 6은 **값역만** 정했고 기본값을 말하지 않았다 — 관측 없이 중간값(예: 3회)을 고르면 그 숫자가 코드에 굳고 나중에 「누가 3을 정했나」에 답할 사람이 없어진다.

**내가 직접 확증한 사실 1건** — 설계서 §4.3 의 근거가 참이다. 패턴을 읽는 대신 `git check-ignore -v` 로 물어봤다: `assets/audio/x.wav` → `.gitignore:32` 에 걸려 무시됨 · **`app/backend/assets/audio/x.wav` → 무시되지 않음(= 추적 대상)**. 그래서 `shadowing_audio_root` 기본값이 `../../` 인 것은 편의가 아니라 **추적 회피**이고, 백엔드 트리 안으로 옮기면 학습자 녹음이 git 에 들어간다. 테스트가 그 성질을 단정한다.

**게이트 (이 턴에 직접 돌렸다)**: **766 passed**(기준선 758 + 신규 8 · 회귀 0) · `ruff check .` exit 0 · `ruff format --check .` 33 files · `ty check` exit 0 · 게이트 밖 `ruff` **6 errors** · `format --check ../../tests` **4 files**(기준선 유지).

---

⛔ **AC#1·#3·#4·#5·#8 은 착수하지 않았다 — 설계서 자신의 게이트가 막는다. 이것을 「안 한 것」이 아니라 「막힌 것」으로 읽어라.**

설계서 §9 의 **⛔「마이그레이션 파일을 쓰는 조건 — 지금은 쓰지 않는다」**가 `011_shadowing_items.sql` 을 **선행 2개 뒤로** 미룬다. 근거는 `data-first` §3 의 「소비자가 0곳이면 진행하지 않는다」이고, 설계서가 **이 리포는 같은 실패를 네 번 냈다**(값을 저장하는데 읽는 코드가 0곳)고 적었다. **선행 둘을 직접 확인했고 둘 다 미충족이다**:
1. **`TASK-10` 이 쉐도잉 진입점을 확정했다** → `TASK-10` 은 **`To Do`** 이고 그 자신이 `TASK-2` 를 기다린다(`backlog task view TASK-10` 실행 결과).
2. **`create_session` 이 `mode` 를 받는다** → 실측 시그니처가 **`create_session(pool: asyncpg.Pool, user_id: UUID)`** 다(`app/backend/app/services/sessions.py`). `_CREATE_SESSION_SQL` 이 `mode` 를 **하드코딩**한다.
→ 그래서 **`--dep TASK-10` 을 걸었다**(기존 `TASK-27` 과 함께). 파이프라인이 눈에 보이게 멈추는 것이 옳다.

**추가 실측 — 011 은 독립된 두 묶음이고 게이트가 걸리는 곳이 다르다:**
- **묶음 A** (§3.3): `shadowing_items` 표 + `learning_sessions.shadowing_item_id` + `check (shadowing_item_id is null or mode = 'shadowing')`. **소비자가 `TASK-10` 이라 게이트에 정면으로 걸린다.** ⚠️ `learning_sessions.mode` 값역에는 **이미 `'shadowing'` 이 있다**(001 실측) — 막는 것은 값역이 아니라 **`mode` 를 받는 진입점이 없다는 것**이다.
- **묶음 B** (§4.2): `utterance_type` 값역에 `shadowing_recording` 추가 + `audio_url` 을 그 값에만 허용하는 CHECK + 부분 인덱스. 소비자(서빙 엔드포인트·삭제 스윕)는 **이 태스크가 소유**한다(AC#3~#5). ⚠️ 그런데 **생산자**가 묶음 A 를 거쳐야 하므로 실물 데이터는 여전히 흐르지 않는다. 그리고 설계서 약점 1 이 이것을 **「되돌릴 수 없는 방향으로 굳는다」**로 지목한다(114행 든 표의 CHECK DROP→ADD · `shadowing_recording` 행이 하나라도 생기면 되돌리기 스크립트가 실패한다).
- 실측: `utterances.audio_url` **컬럼은 이미 있다**(nullable · 채워진 행 **0**) · `utterance_type` 값역은 `('learning','voice_command','command_confirmation')` · `utterances` 총 **114행**.

**⚠️ 캡틴에게 올릴 것 — 결정 25 를 문자대로 이행하면 그 목적이 달성되지 않는다.**
결정 25 는 *"너가 추정 클립을 수집해서 **간단한 학습이 가능하도록** 일단 시작해"* 다. 그런데 표를 만들고 시드를 채워도 **진입점(`TASK-10`)과 `create_session(mode)` 가 없으면 학습이 성립하지 않는다** — 아무도 그 클립을 고르지 않는다. 즉 011 을 지금 쓰는 것은 **게이트를 어기면서 결정 25 의 목적도 달성하지 못하는** 선택이다.
→ 물을 것: **`TASK-45` 를 여기서 멈추고 `TASK-10`·`TASK-2` 를 먼저 열까**, 아니면 **`create_session(mode)` 배선 + 최소 진입점을 이 태스크 범위로 넓혀 011 까지 한 번에 갈까.** 후자는 `TASK-10` 의 설계 몫을 구현이 선점하는 것이라 팀리드 단독으로 정하지 않는다.
⛔ **그 답이 오기 전에 011 을 쓰지 않는다.** 되돌릴 수 없는 CHECK 변경이 그 안에 있다.

**캡틴 결정 34 (2026-09-08) — 게이트가 풀렸다. `TASK-45` 가 최소 진입점까지 소유한다.**
위 「캡틴에게 올릴 것」의 두 갈래 중 **「지금 돌아가게 만들자 (입구까지)」**를 캡틴이 골랐다. → 설계서 §9 의 ⛔ 게이트가 요구한 선행 둘(진입점 · `create_session(mode)`)이 **이 태스크 범위**가 됐으므로 **011 을 쓸 수 있다.** `--dep TASK-10` 을 **뗐다**(그것이 선행이 아니게 됐다).
⚠️ **대가와 제약은 결정 34 본문이 소유한다** — 요지: 최소한만 만든다(추가 학습 5종 배치는 `TASK-10` 이 그대로 소유) · 되돌리기 쉬운 형태로 짜고 **무엇을 선점했는지 적는다** · 011 의 되돌릴 수 없는 부분은 010 의 적용 5단계를 그대로 쓴다.

**진입점 계약 1·2·3 구현 완료** (설계서 §12 가 진입점에 요구한 5개 중 셋).

⛔ **범위 경계를 지켰다 — 결정 34 의 「최소한만 만든다」.** 「추가 학습 5종을 어떻게 배치하나」는 `TASK-10` 이 그대로 소유한다. 그래서 만든 것은 **서비스 함수 하나**이고 **호출 표면(어느 화면·버튼·5종 중 어디)은 만들지 않았다** — `TASK-10` 설계가 나오면 이 함수의 **호출자만** 바뀐다.

산출:
- `create_session(pool, user_id, *, mode="speaking")` — **키워드 전용 + 기본값**이라 기존 호출부 4곳(`api/ws.py` · 하네스 · 테스트 2)이 그대로 산다. 결정 11 이 미뤄 둔 파라미터화가 여기서 발화했다. 값역은 001 의 `learning_sessions_mode_check` 가 가두므로 **목록을 복제하지 않았다.**
- `start_shadowing_session(pool, user_id)` — 세션을 `mode='shadowing'` 으로 열고 **한 트랜잭션 안에서** 클립을 붙인다.
- `_ATTACH_SHADOWING_CLIP_SQL` — **선택 규칙은 발명이 아니라 선례다**: 수준 일치 우선 → 없으면 가장 이른 행. 설계서 §3.3 이 *"learning_scenarios.level 과 같은 값역을 쓴다 — 선택의 기준이 같기 때문이다"* 로 그 근거를 이미 적어 뒀고, `_CREATE_SESSION_SQL` 의 시나리오 절과 **같은 모양**이다. ⛔ `mode` 를 조건에 넣지 않았다 — 011 의 CHECK 가 이미 가둔다(두 곳에 두면 갈라진다).

**red→green (직접 관측)**: 신규 4건 → `ImportError: cannot import name 'start_shadowing_session'` 로 **수집 단계 red** → 구현 후 **4 passed**.

**테스트 판별력을 선례에서 가져왔다** — `seed_scenarios_for_level` docstring 이 실측으로 정해 둔 배치를 그대로 썼다: **수준 우선**을 재려면 불일치 행을 가장 이른 행과 가장 최신 행 **양쪽에** 두고 일치 행을 가운데(3행) · **폴백의 「가장 이른 행」**을 재려면 불일치 행을 **2행 이상**(1행이면 어떤 정렬이든 통과한다). 신규 픽스처 `seed_shadowing_clips` 도 그 규약(커밋 + teardown + 누출 시끄러운 실패)을 따랐다.
⛔ **클립 0행에서도 세션이 열리는 것**을 따로 단정했다 — 011 의 CHECK 가 역방향을 강제하지 않는 것과 같은 판단이고, 예외를 던지면 시드가 빈 DB 에서 쉐도잉 진입이 **전부** 막힌다.
⚠️ **기존 호출부 무회귀도 단정했다**(`test_create_session_still_defaults_to_speaking`) — 이것이 없으면 `api/ws.py` 가 조용히 쉐도잉 세션을 열게 되는 변경이 통과한다.

**게이트 (직접 돌렸다)**: **775 passed**(771 + 4 · 회귀 0 — `_CREATE_SESSION_SQL` 시그니처가 바뀌었는데 기존 세션 테스트 전부 통과) · `ruff check .` exit 0 · format 33 files · `ty check` exit 0 · 게이트 밖 `ruff` **6 errors** · `format --check ../../tests` **4 files**(7 로 늘었던 import 정렬 1건을 잡아 기준선 복귀 · **harness 미변경 확인**).

**남은 것**: AC#8(시드) · AC#3(오디오 저장) · AC#4(당일 경과 삭제) · AC#5(스윕) · AC#6(게이트) · 설계서 §12 요구 4·5(낭독 턴 경계 · 설정값 전달 — 이 둘은 WS 표면에 닿아 `TASK-10` 선점 위험을 다시 재야 한다).

**세션 인계 지점 (2026-09-08).** 여기서 끊고 새 세션이 이어감.

**끝난 것**: AC#1(011 파일) · AC#2(설정값 3종 + `.env.example`) · AC#7(착수 전 확인) · 설계서 §12 진입점 계약 **1·2·3**(`create_session(mode)` · `start_shadowing_session` · 수준 기준 클립 선택).

**남은 것 넷과 권하는 순서** (근거를 함께 적음):
1. **AC#8 클립 시드** — 가장 안전하고, 넣으면 진입점이 실물로 동작함. 결정 25 가 형태를 정했음: 전사문은 **A2 수준 자체 작성 문장**. ⛔ 외부 콘텐츠를 긁지 않음. 자리는 `scripts/migrate.py` 의 `SEED_SCENARIOS` 가 선례임(멱등).
2. **AC#3 오디오 저장** — 설계서 §4.3~§4.5. **DB 포인터를 마지막에 씀**이 계약임. 뿌리는 `Settings.shadowing_audio_root`(이미 있음).
3. **AC#4 당일 경과 삭제** — 설계서 §5.2·§5.3. ⛔ **`current_date` 를 쓰지 않음** — `users.timezone` 으로 변환해 구함. 진행 중 세션의 녹음은 삭제하지 않음(§5.4).
4. **AC#5 워커 유휴 사이클 스윕** — 설계서 §6.1. **새 job 종류를 만들지 않음.**
AC#6(red→green + 게이트)은 나머지가 끝난 뒤 함께 닫음.

⛔ **011 은 개발 DB 에 적용하지 않았음.** 인계 시점 직접 조회: `schema_migrations` 가 `001·003~007·009·010` 이고 **`shadowing_items` 표가 없음.** **의도한 상태임** — 코드와 마이그레이션이 갈라지면 그 사이 경로가 깨지므로(010 에서 겪은 형태) 소비자와 같은 묶음에서 적용함. 절차는 011 머리주석의 5단계가 소유함(`pg_dump -n public` 백업부터 · ⛔ `-n public` 을 빼면 `permission denied for schema en_coach` 로 막힘).

⚠️ **판단이 필요한 자리가 하나 남아 있음** — 설계서 §12 의 요구 **4·5**(낭독 턴 경계 · 설정값 전달)는 WS 표면에 닿아서 `TASK-10` 선점 위험을 다시 재야 함. 결정 34 가 「최소한만 만든다」로 제약했으므로 **그 자리에 오면 멈추고 캡틴에게 올림.**

**인계 시점 게이트 (이 턴에 직접 돌렸음)**: **775 passed** 13.60s · `ruff check .` exit 0 · `ruff format --check .` 33 files · `ty check` exit 0 · 게이트 밖 `ruff` **6 errors** · `format --check ../../tests` **4 files** · `--check ../../scripts` **0** · 프론트 `tsc`·`eslint` 둘 다 exit 0.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-07 22:55
---
캡틴 결정 25(2026-09-08): 「너가 추정 클립을 수집해서 간단한 학습이 가능하도록 일단 시작해」 — 클립 시드를 팀리드가 채운다. 설계서 약점 2(표를 만들어도 0행이면 동작하지 않는다)가 닫힌다. 팀리드가 고른 형태와 근거는 결정 25 본문이 소유한다 — 외부 영상을 넣지 않고 A2 수준 자체 문장으로 만든다(PRD 비범위 준수 + 남의 저작물 전사문을 DB 에 담지 않는다). ⚠️ 이 태스크의 마지막 AC(착수 전 캡틴 질문 확인)는 이것으로 닫혔다.
---
<!-- COMMENTS:END -->
