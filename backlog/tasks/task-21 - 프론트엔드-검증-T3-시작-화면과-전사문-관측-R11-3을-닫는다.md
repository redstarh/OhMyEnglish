---
id: TASK-21
title: '프론트엔드 검증 T3: 시작 화면과 전사문 관측 (R11-3을 닫는다)'
status: In Progress
assignee: []
created_date: '2026-09-06 00:14'
updated_date: '2026-09-06 13:45'
labels:
  - caps-req
dependencies:
  - TASK-31
priority: high
ordinal: 21000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
계획서 T3(C1·C2). 캡틴 노트 항목 2가 지목한 핵심이다 — 시작 화면을 아무도 본 적이 없다. 지금 렌더 코드는 있으나 브라우저 확인 0회이고 자동 검사 0건이라 줄을 지워도 모든 검사가 통과한다(무력화 4건 중 4건 생존). 이 태스크가 R11-3 판정을 완료로 올릴 유일한 경로다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 추천 이유와 목표 수준이 시작 화면에 실제로 보이는 것을 관측하고 증거 파일을 남긴다
- [x] #2 부분 전사문은 muted 토큰, 확정 전사문은 foreground 토큰임을 라이트·다크 두 모드에서 확인
- [x] #3 기대값을 런타임에 토큰에서 유도한다 — 색값을 하드코딩하지 않는다
- [x] #4 주입을 생략한 회차에서 접두 단락이 0개임을 확인(음성 대조)
- [x] #5 R11-3 판정을 갱신하고 근거를 원장에 남긴다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
⚠️ 외부 감사가 순서 역전을 2주기 연속 지적했고 수용한다 — In Progress 였는데 선행이 미충족이었다. 착수 가능 상태가 아니므로 To Do 로 되돌린다. 의존 집합이 재작성된 것(TASK-20+TASK-17 → TASK-31)은 정당하지만 그것이 역전을 지우지 않는다는 지적이 맞다.

이미 끝난 부분: A1-0 관측 1건(R11-3 을 닫았다, runs/2026-09-06-browser-leg-1.md + browser-leg-2-agent.md). 남은 17건은 계측이 A1-5 를 재지 못하는 상태라 착수하면 거짓 판정이 나온다 → TASK-31 이 선행이다.

2026-09-06 T3 C2 회차 — AC #2·#3·#4 관측 완료. 판정 PASS (A2-1·A2-2·A2-3, 라이트·다크 두 모드, 2회 독립 회차 값 전건 일치).

증거의 소유자는 회차 기록 tests/harness/runs/2026-09-06-t3-c2-render-hierarchy.md 다 — 여기서 재서술하지 않는다. 원시 반환값은 .harness/evidence/c2-render-hierarchy{,-partial}.json, 스크린샷 4장은 .harness/evidence/c2-{light,dark}{,-partial}.png 이고 팀리드가 4장 전부 직접 열어 봤다.

핵심 수치: probe 유도값 light muted rgb(89,89,89) / fg rgb(23,23,23) · dark muted rgb(154,154,154) / fg rgb(237,237,237). partial 줄 색 == muted probe, 확정 줄 색 == fg probe (두 모드). 주입 전 active 도달 후 접두 p 0개, 사유 p 0개. 계측 sha256 846f130f2cf73365… 무변경.

실행체 신설: tests/harness/c2_render_hierarchy.py (browser_leg.md §6 의 실행체. measure_contrast.py 와 같은 CDP 9222 수단). 절차 정본 §6 에 이 회차가 확정한 셋을 박았다 — 실행체 지목 · Page.bringToFront 필수 · 탭 정확일치/신규생성.

새 함정 H-AH (pitfalls.md): 배경 탭에서는 CDP 클릭이 페이지에 닿지 않고 증상이 H-AE 와 구별되지 않는다. 판별값은 socketUrls 공백 여부와 resumeLog 길이.

미결로 남긴 것 (통과로 적지 않는다): 1차 회차 창 안에 세션이 6건 생겼고 그중 2건의 방아쇠가 미확정이다 (선행 next-plan 없는 WS 접속. 자동 재시도 경로는 없음을 grep 으로 확인). 2차에서는 정확히 2건이라 재현되지 않았다. 판정에는 영향 없음 — 각 회차 반환값이 recv.session_started==1 로 그 문서에 세션이 하나였음을 자기 증명한다.

2026-09-06 리뷰 1차 수신 — HIGH 1건·MEDIUM 1건, 둘 다 grep 으로 직접 대조해 수용했다.

HIGH-1: A2-2 의 음성 대조 ②(probe 두 값이 서로 다름)가 실행체 코드에 없었다 — 값을 인쇄만 하고 muted1 과 fg1 을 비교하는 자리가 없었다. 두 색 토큰이 같은 값으로 회귀하면 세 단정이 전부 항진명제가 되고 모드 간 차이는 그대로라 A2-3 도 못 잡는다.
MEDIUM-1: §6-0 의 '0이 아니면 ERROR' 와 주입 창의 '경쟁 프레임 0' 전제가 게이트가 아니라 출력이었다.

고친 것: 순수 함수 check_leg / check_cross 를 넣고 main_async 끝에서 호출한다. 어긋나면 비영 종료. 인쇄되던 모든 불리언이 게이트가 됐고 공허 통과 방어·초과 렌더·한 문서 한 세션이 더해졌다.

판별력을 관측했다 (게이트가 있다는 지표가 아니다): tests/harness/test_c2_gates.py 23 passed (실물 회차 반환값 위에 변이 19종) · 리뷰 지목 두 조건을 메모리에서 제거해 red→green 직접 관측 · 배선을 종단으로 확인 (판정 회차 56건 PASS exit 0 / 모드 하나면 A2-3 미평가 FAIL exit 1).

회차 3·4차 값이 1·2차와 동일 → 판정 회차 4회 독립 일치. 추적 증거 사본 6개를 게이트가 든 판의 회차로 갱신했다.

절차 문서 갱신: browser_leg.md §6 에 '인쇄하는 모든 불리언은 게이트여야 한다'와 게이트 테스트 실행 규약, §7 에 information_schema.schemata 가 거짓 「없다」를 낸다는 함정.

미결: 재검토를 요청했고 아직 수신하지 않았다 → APPROVE 전 Done 전환 금지. 그리고 스크립트 호출당 세션이 간헐적으로 1~2건 더 생기는 현상은 여전히 방아쇠 미확정이다 (1차 4회→6건 · 2차 2회→2건 · 3차 7회→8건). 새 게이트가 recv.session_started==1 을 문서마다 단정하므로 오염이 조용히 통과하는 경로는 닫혔다.

2026-09-06 리뷰 나머지 수신 — MEDIUM 3건·LOW 2건 추가. 5건 전부 grep/sed 로 직접 대조해 수용·수정했다 (누적 HIGH 1 · MEDIUM 4 · LOW 2).

MEDIUM-2: §6 이 금지한 부분일치 탭 폴백이 코드에 남아 있었다 (문서는 정확일치→새탭, 코드는 정확일치→부분일치→새탭). 이 회차가 함정으로 적은 하이재킹이 폴백으로 살아 있었다 → 경로 삭제, --url-substring 인자 제거.
MEDIUM-3: 실패한 회차가 H-AH 판별에 필요한 계측 상태를 버렸다 (active 시간초과는 __omy 를 읽기 전에 종료) → dump_failure 신설, .harness/evidence/c2-<모드>-failure.{json,png}.
MEDIUM-4: §5 가 A2-3 의 음성 대조로 지목한 '같은 모드 두 번 읽기'는 같은 tick 이라 판별력 0 이었다 → §5 대조 칸 정정 (실제 대조는 페이지 보고 스킴 == 요청, 그리고 단일 모드는 미평가). 두 번 읽기는 진단으로 강등.
LOW-1: '3규약' 은 어느 문서도 소유하지 않는 라벨이고 개수도 틀렸다 (H-AE 대응은 4단계) → 실제 소유자로 인용 교체.
LOW-2: '한 문서에 세션 하나' 의 인용이 §4-4 가 아니라 §5 A1-5 다 → 세 곳 수정.

⭐ 고치는 과정에서 공허 통과를 실물로 관측했다: 백엔드(:8002)를 완전히 내린 회차에서 색 단정 전건이 True 였다 (주입은 앱 핸들러를 직접 부르므로 소켓과 무관하고 active 도 백엔드 없이 도달한다). 게이트가 없던 판이라면 그 회차가 초록으로 통과했다. 새 게이트의 recv.session_started==1 이 잡았다.

⛔ 그 실험이 내 새 판별 문구의 반례도 만들었다: socketUrls=[] 인데 resumeLog 2건·appHandlerAttached=True (클릭은 닿았고 소켓만 안 열렸다). socketUrls 는 send 가 불릴 때만 채워진다 → 가장 상류 지표는 resumeLog 길이다. classify_failure 로 4갈래 구현하고 pitfalls H-AH 판별표와 browser_leg §6 을 함께 정정했다. 표본 1건에서 일반화한 판별표가 두 번째 관측에 반증된 사례다.

5차 회차 c50f335f: 바뀐 코드로 판정 56건 PASS exit 0. DB 3열 복원 확인(7/34/92/7, drift 0), 백엔드 stub 복원.
게이트: 618 passed · ruff/format/ty 통과 · 프론트 exit 0 · 게이트 밖 6 errors/4 files 유지.
미결: 재검토 미수신. 그리고 dump_failure 의 except 래퍼 3줄은 실제 앱 실패에서 발동하는 것을 관측하지 못했다 (백엔드를 내려도 active 에 도달해 그 경로를 밟지 않았다) — 코드 읽기로만 확인했다.
<!-- SECTION:NOTES:END -->

## 착수 전 확인 (TASK-16 실행으로 생긴 전제, 2026-09-06)

✅ **표제 단정의 전제가 생겼다.** 이 태스크는 "추천 이유와 목표 수준이 시작 화면에 실제로 보이는
것을 관측"하는데, **계획 행이 0개면 그 줄이 화면에 아예 없다**(조건 렌더다). TASK-16이 실물 모델로
계획 1행을 만들어 저장했고 API가 그것을 돌려주는 것까지 확인했다 → **관측이 가능해졌다.**

⚠️ **착수 직전에 계획 행이 여전히 있는지 확인한다**:
`select count(*) from session_plans;` 가 0이면 관측할 것이 없다. 다시 만드는 절차와 그때 나온 값은
`tests/harness/runs/2026-09-06-live-plan-1.md`가 소유한다 — **다시 만들면 실물 모델 비용이 또 든다.**

⚠️ **그 계획 행과 관찰 노트 1행은 보존 대상이다** — teardown에서 제외한다.

**관측할 기대값**(TASK-16이 실제로 저장·반환한 값): 추천 이유는 "명사 앞 a/an을 자주 빼시고 …"로
시작하는 한국어 문장, 목표 수준은 `A2`.
