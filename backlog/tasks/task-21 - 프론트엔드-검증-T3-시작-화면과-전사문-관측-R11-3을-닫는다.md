---
id: TASK-21
title: '프론트엔드 검증 T3: 시작 화면과 전사문 관측 (R11-3을 닫는다)'
status: In Progress
assignee: []
created_date: '2026-09-06 00:14'
updated_date: '2026-09-06 13:10'
labels:
  - caps-req
dependencies:
  - TASK-31
priority: high
ordinal: 21000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
계획서 T3(C1·C2). 캡틴 노트 항목 2가 지목한 핵심이다 — 시작 화면을 아무도 본 적이 없다. 지금 렌더 코드는 있으나 브라우저 확인 0회이고 자동 검사 0건이라 줄을 지워도 모든 검사가 통과한다(무력화 4건 중 4건 생존). 이 태스크가 R11-3 판정을 완료로 올릴 유일한 경로다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 추천 이유와 목표 수준이 시작 화면에 실제로 보이는 것을 관측하고 증거 파일을 남긴다
- [x] #2 부분 전사문은 muted 토큰, 확정 전사문은 foreground 토큰임을 라이트·다크 두 모드에서 확인
- [x] #3 기대값을 런타임에 토큰에서 유도한다 — 색값을 하드코딩하지 않는다
- [x] #4 주입을 생략한 회차에서 접두 단락이 0개임을 확인(음성 대조)
- [x] #5 R11-3 판정을 갱신하고 근거를 원장에 남긴다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
⚠️ 외부 감사가 순서 역전을 2주기 연속 지적했고 수용한다 — In Progress 였는데 선행이 미충족이었다. 착수 가능 상태가 아니므로 To Do 로 되돌린다. 의존 집합이 재작성된 것(TASK-20+TASK-17 → TASK-31)은 정당하지만 그것이 역전을 지우지 않는다는 지적이 맞다.

이미 끝난 부분: A1-0 관측 1건(R11-3 을 닫았다, runs/2026-09-06-browser-leg-1.md + browser-leg-2-agent.md). 남은 17건은 계측이 A1-5 를 재지 못하는 상태라 착수하면 거짓 판정이 나온다 → TASK-31 이 선행이다.

2026-09-06 T3 C2 회차 — AC #2·#3·#4 관측 완료. 판정 PASS (A2-1·A2-2·A2-3, 라이트·다크 두 모드, 2회 독립 회차 값 전건 일치).

증거의 소유자는 회차 기록 tests/harness/runs/2026-09-06-t3-c2-render-hierarchy.md 다 — 여기서 재서술하지 않는다. 원시 반환값은 .harness/evidence/c2-render-hierarchy{,-partial}.json, 스크린샷 4장은 .harness/evidence/c2-{light,dark}{,-partial}.png 이고 팀리드가 4장 전부 직접 열어 봤다.

핵심 수치: probe 유도값 light muted rgb(89,89,89) / fg rgb(23,23,23) · dark muted rgb(154,154,154) / fg rgb(237,237,237). partial 줄 색 == muted probe, 확정 줄 색 == fg probe (두 모드). 주입 전 active 도달 후 접두 p 0개, 사유 p 0개. 계측 sha256 846f130f2cf73365… 무변경.

실행체 신설: tests/harness/c2_render_hierarchy.py (browser_leg.md §6 의 실행체. measure_contrast.py 와 같은 CDP 9222 수단). 절차 정본 §6 에 이 회차가 확정한 셋을 박았다 — 실행체 지목 · Page.bringToFront 필수 · 탭 정확일치/신규생성.

새 함정 H-AH (pitfalls.md): 배경 탭에서는 CDP 클릭이 페이지에 닿지 않고 증상이 H-AE 와 구별되지 않는다. 판별값은 socketUrls 공백 여부와 resumeLog 길이.

미결로 남긴 것 (통과로 적지 않는다): 1차 회차 창 안에 세션이 6건 생겼고 그중 2건의 방아쇠가 미확정이다 (선행 next-plan 없는 WS 접속. 자동 재시도 경로는 없음을 grep 으로 확인). 2차에서는 정확히 2건이라 재현되지 않았다. 판정에는 영향 없음 — 각 회차 반환값이 recv.session_started==1 로 그 문서에 세션이 하나였음을 자기 증명한다.
<!-- SECTION:NOTES:END -->

## 착수 전 확인 (TASK-16 실행으로 생긴 전제, 2026-09-06)

✅ **표제 단정의 전제가 생겼다.** 이 태스크는 "추천 이유와 목표 수준이 시작 화면에 실제로 보이는
것을 관측"하는데, **계획 행이 0개면 그 줄이 화면에 아예 없다**(조건 렌더다). TASK-16이 실물 모델로
계획 1행을 만들어 저장했고 API가 그것을 돌려주는 것까지 확인했다 → **관측이 가능해졌다.**

⚠️ **착수 직전에 계획 행이 여전히 있는지 확인한다**:
`select count(*) from session_plans;` 가 0이면 관측할 것이 없다. 다시 만드는 절차와 그때 나온 값은
`tests/harness/runs/2026-09-06-live-plan-1.md`가 소유한다 — **다시 만들면 실물 모델 비용이 또 든다.**

⚠️ **그 계획 행과 관찰 노트 1행은 보존 대상이다** — teardown에서 제외한다.

**관측할 기대값**(TASK-16이 실제로 저장·반환한 값): 추천 이유는 "명사 앞 a/an을 자주 빼시고 …"로
시작하는 한국어 문장, 목표 수준은 `A2`.
