---
id: TASK-229
title: 전체 영역 통합테스트 회차 — 계획·수행·종합분석
status: Done
assignee: []
created_date: '2026-09-19 05:11'
updated_date: '2026-09-19 08:26'
labels: []
dependencies: []
ordinal: 290000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
사용자 지시(2026-09-19): 지금까지 개발된 모든 영역을 app-test-agent 로 통합테스트함. 테스트 계획의 정본은 테스트 원장(tests/agent/backlog · 접두사 ts)이고 결함은 이 작업 원장으로 옴. ⛔ 테스트 도중에 코드를 고치지 않음 — 결함만 등록하고 회차가 끝난 뒤 종합 분석으로 수정 태스크를 세움.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 테스트 원장에 영역별 시나리오가 전부 등록됨
- [x] #2 등록된 시나리오 전부가 통과·실패·차단 가운데 하나로 판정됨
- [x] #3 실패 전부가 이 작업 원장의 결함 태스크로 등록되고 시나리오 ID 와 양방향으로 이어짐
- [x] #4 회차 결과가 tests/agent/runs 아래 result.md 로 남음
- [x] #5 종합 분석으로 수정 태스크가 도출되고 심각도가 붙음
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
영역 18개로 확정 (TS-19~TS-36). 수행은 5파로 나눔 — ① TS-19·22·30 + TS-27·28(병행) ② TS-23·24·25·26 ③ TS-31·32·35 ④ TS-20·33·36(스텁 WS) ⑤ TS-21·29·34(실물 Nova 1회차로 묶음). 실물 비용을 마지막 한 회차에 몰아 싼 것부터 확인하는 순서임.

배치 보고를 전부 받은 뒤 정리한 것 다섯 (2026-09-19 · 호출 세션이 직접 확인).

1. ⛔ 넘겨진 관측 하나가 오탐이었음 — 등록하지 않았음. 배치 B1 이 「app/frontend/app/page.tsx 의 nextPlan 이 useState 초기값에서 움직이지 않고 그 화면에 fetch 가 0곳이라 NEXT_PLAN_PREFIX 가 렌더될 수 없다」를 B3 배치에 넘겼고 B3 은 그것을 다루지 않았음. 내가 직접 열어 보니 fetchNextPlan() 이 page.tsx:569 에서 useEffect 안에서 돌고 setNextPlan 으로 상태를 갱신함 — 렌더 경로가 실재함. 기전 추정: 「fetch(」 로만 찾아 api 층 래퍼(fetchNextPlan)를 놓쳤을 것임. ⇒ 교훈: 부재 관측을 넘길 때는 그 이름의 «래퍼»까지 찾아봐야 함. PRD R11-3 은 성립함.

2. ⚠️ 배치 B4 가 「백엔드 :8002 가 회차 끝에 내려가 있고 누가 내렸는지 가릴 수 없다」고 적었음 — 그것은 «내»가 내린 것임. 4파(b5-ws-stub-commands)에 어댑터 전환 권한을 넘기려고 리스너를 내렸고, 그 시점이 B4 의 마지막 관측과 겹쳤음. ⛔ 앱 결함이 아니므로 뒤에서 이것을 쫓지 말 것. ⇒ 교훈: 프로세스 소유권을 배치 사이에 넘길 때는 «아직 도는 배치»에 먼저 알려야 함. B4 가 그 시각에 아직 살아 있었음.

3. 배치 B2 의 관측 하나를 결함으로 올리지 않기로 정했음 — 「출처를 잃은 문장을 앱 경로로 지울 수 없음」(DELETE 엔드포인트 둘이 모두 video_id 를 요구하고 그 문장을 보여 주는 화면도 없음). 근거: 설계서 2026-09-18-video-learning-design.md §10-4 가 「출처를 잃은 문장의 화면 처리」를 명시로 «유보»했으므로 어긋남이 아니라 알려진 미결임. ⚠️ 그 회차가 남긴 고아 문장 408d6cc5-da54-4f82-b3a7-69b32404ecd4 은 그 유보 때문에 앱 경로로 지울 수 없어 남아 있음 — 다음에 그 유보를 닫는 작업이 함께 걷을 자리임.

4. 남은 미검증 경로 셋을 적어 둠 — 전건 통과가 이것들을 덮지 않음.
   - daily_error_summary 의 «쓰기» 경로: B3 이 정적 대조만 했고 측정이 아니라고 표시했음. 쓰기는 분석 워커에만 있음.
   - summarize_week job 의 «처리» 절반: 이 DB 에서 done 0건임(그 failed 10건은 TASK-191 의 사람 손 표시).
   - 실물 Claude 응답이 파서 계약(무대 제목 언어·category 값역)을 지키는가: B4 가 ClaudeClient 를 스텁으로 갈아 쟀으므로 Bedrock 만 지나가지 않았음.
   ⇒ 셋 다 «워커를 켜는 회차»의 몫이고 유료 호출이 전제임. 지금 태스크로 세우지 않았음 — 워커를 켜는 회차를 열 때 함께 세울 자리임.

5. TS-4 는 To Do 로 열려 있음(TASK-243 이 AC 를 규칙 단위로 다시 씀). 남은 둘은 규칙 4 의 「성공분 교정 포함」을 긍정으로 보이는 것과 규칙 1 대 3 우선순위의 앱 경로 관측임.
<!-- SECTION:NOTES:END -->
