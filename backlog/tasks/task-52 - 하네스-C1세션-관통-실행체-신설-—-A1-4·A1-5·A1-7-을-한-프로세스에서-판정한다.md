---
id: TASK-52
title: '하네스: C1(세션 관통) 실행체 신설 — A1-4·A1-5·A1-7 을 한 프로세스에서 판정한다'
status: In Progress
assignee: []
created_date: '2026-09-09 07:55'
updated_date: '2026-09-09 08:26'
labels: []
dependencies: []
ordinal: 55000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-30 회차 2 를 위임했다가 검증자가 산출물 직전에 API 오류로 죽어 관측값을 전부 잃었다(H-AO 재발 · 2026-09-09). 무결성은 지켜졌으나 A1-4 의 when 배열·A1-5 의 6줄 textContent·A1-7 의 sent 계수가 유실됐다. C5 는 TASK-49 로 실행체를 만들어 같은 위험을 없앴고 C1 도 같은 형태로 만든다. C1 은 단정이 9건(A1-0~A1-8)으로 가장 많아 유실 비용이 가장 크다. ⛔ 어댑터는 VOICE_ADAPTER=stub 이다(browser_leg.md §5 C1).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 판정을 순수 함수로 분리하고 test_c1_gates.py 가 브라우저 없이 변이를 잡는지 고정한다 — c3_results_screen.py·c5_pronunciation_badge.py 와 같은 형태
- [x] #2 A1-4 를 관측한다 — AudioBufferSourceNode.prototype.start 호출 3회와 when 배열. 판별력: 세 when 이 전부 같으면 FAIL 이 난다
- [x] #3 A1-5 를 관측한다 — 접두 <p> 6개이고 DOM 순서대로 FIXTURE_TURNS 와 등호. 기대값은 fixtures.py 에서 파싱해 유도하고 손 전사하지 않는다. 판별력: sentinel 변형에서 FAIL
- [ ] #4 실행체가 환경 실패와 판별력 실패를 구분한다 — hasBeenActive 단정과 앱 핸들러 부착 대기를 넣는다(TASK-49 가 셋 다 빼먹어 거짓 판별력을 만들 뻔했다)
- [ ] #5 browser_leg.md §5 C1 절에 실행체 경로와 사용법을 적는다
- [ ] #6 A1-7 을 관측한다 — sent.audio > 0 과 end_session 1건 동반 계수(후킹 고장 구별). ⛔ 무음에서 audio 계수가 0 이 되기를 기대하지 않는다 — §5 A1-7 행이 그 갈래를 폐기했다(원리적으로 불가능하고 무음·유음이 프레임 146건·바이트 199,728로 같다). 살아 있는 미결은 대체 대조가 세션 관통에서 성립하는가다: config.silentMic = true 에서 sent.audio > 0 이면서 sentAudioNonZeroFrames === 0. 계측 단위에서는 이미 확인됐다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-09 톤 회차 완료 — A1-4·A1-5 PASS. 회차 기록은 tests/harness/runs/2026-09-09-task30-c1-tone.md 다.

관측 시점 HEAD 07c9ab1 · 미커밋 0건 · VOICE_ADAPTER=stub · 실물 호출 0회 · 호출자가 직접 실행.
started={count:3, when:[0.016,0.216,0.416]} — 세 값이 서로 달라 대조 ② 성립.
final_lines.verdict=PASS · judgeable·reachedExpected·terminalMatches 전부 True · recv.final=6.

⛔ A1-7 은 이 어댑터에서 BLOCKED 다 — 앱 결함이 아니다. 픽스처가 정상 완주했고(partial 6·final 6·
session_ended 1) when 이 0.4초 안에 끝나 마이크 캡처 창이 없으며 ws.ts 가 소켓 닫힘 후 전송을
조용히 버린다. stub_unresponsive 에서 재야 하고 그 회차에서는 A1-4·A1-5 가 반대로 BLOCKED 가 된다.

⛔ 이 회차가 실행체 결함 1건을 잡아 고쳤다 — 종료 클릭 실패가 관측 전체를 버렸다(H-AO 와 같은
형태를 실행체가 재현). end_click 필드로 남기고 판정이 원인을 가르게 했다.

⚠️ DB 표 합계를 이 회차의 무변경 증거로 쓸 수 없다 — 정리 시점에 다른 세션의 여정 테스트가 같은
DB 에서 진행 중이어서 세션 3건·jobs 12·utterances 18 이 그쪽에서 늘었다. 내 세션 1건만 지웠고
그것이 정확히 사라진 것·drift 0·보존 세션 6개 유지가 이 회차의 증거다. 공유 DB 에서 두 회차가
겹치면 합계는 어느 쪽 지표도 되지 못한다 — 다음 회차는 시각창이나 run_id 로 범위를 좁힌다.
<!-- SECTION:NOTES:END -->
