---
id: TASK-31
title: 계측·단정 재작업 — T2 스파이크가 드러낸 결함 10건
status: To Do
assignee: []
created_date: '2026-09-06 03:12'
updated_date: '2026-09-06 04:00'
labels:
  - caps-req
dependencies: []
priority: high
ordinal: 31000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
T2 스파이크(tests/harness/runs/2026-09-06-t2-instrument-spike.md)가 instrument.js 결함 6건과 browser_leg.md 잔여 결함 4건을 드러냈다. 핵심: 스텁 세션이 23ms 에 자기종료해 A1-4·A1-7 이 이 구성에서 측정 불가이고, finalLines 스냅샷 두 경로가 서로 반대로 실패해 A1-5 에 쓸 경로가 보장되지 않는다. 무음 대조는 lib/audio.ts 워클렛에 무음 게이트가 없어 원리적으로 0 을 낼 수 없다(팀리드 코드로 확인). 이미 고친 것은 제외한다 — browser_leg.md 의 P5(로그 pid 대조)·§8-②(baseline 복원)·§8-③(발음 패턴 보호)는 2026-09-06 에 반영했고 pitfalls H-AB·H-AC 도 등록했다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 instrument.js: finalLines 스냅샷 시점을 회차 운에 걸리지 않는 형태로 재설계한다 (동기=React commit 전 · rAF=router.push 후, 둘 다 [] 를 낸다)
- [ ] #2 instrument.js: meta.audioContextState 를 resume 이후 값으로 기록한다 (현재는 async resume 직후 동기로 써서 항상 이전 값이다)
- [ ] #3 instrument.js: WebSocket 후킹에 URL 스코프를 건다 — 지금은 Next dev HMR 소켓까지 센다(sent.other 의 정체)
- [ ] #4 instrument.js: sentAudioBytes 를 목적에 맞게 고치거나 버린다 — 무음·유음 모두 199,728 로 동일해 아무것도 가르지 못한다
- [ ] #5 browser_leg.md §10: 'audio 0 + end_session 0 = 후킹 고장이므로 ERROR' 규칙이 오진을 낸다 — 실제 원인은 세션 길이였다. 판별 기준을 고친다
- [ ] #6 browser_leg.md A1-4: 기대값 3 과 when 대조가 이 구성에서 측정 불가다(관측 0·1). 측정 가능한 형태로 옮기거나 채택하지 않는다
- [ ] #7 browser_leg.md A1-7: 무음 대조를 버리고 PCM 바이트 내용을 보는 형태를 검토한다. 성립하지 않으면 A1-7 을 채택하지 않는다
- [ ] #8 browser_leg.md §11: '1회차에서 닫힌 것'(권한 대화상자)이 계측 경로에는 적용되지 않음을 반영한다 — 계측이 getUserMedia 를 교체하면 대화상자가 뜨지 않는다
- [ ] #9 회차 기록 규약을 반영한다: 하네스 회차는 baseline / 회차 후 / teardown 후 3열을 남긴다 (없으면 사후 대조가 원리적으로 불가능하다 — 팀리드가 실제로 오판했다)
- [ ] #10 instrument.js: start 호출에 프레임 태깅을 넣는다 — 지금은 어느 프레임이 통과했는지 알 수 없어 A1-4 기전 판정이 추론에 머문다
- [ ] #11 browser_leg.md §8-0: baseline 을 파일로도 유지하는 규약을 지킨다 — 값이 정당하게 바뀌면 새 날짜로 새 파일을 뜨고 문서의 참조 줄을 갱신한다
- [ ] #12 browser_leg.md §3: 두 명령의 cwd 가 어긋난다 — 앞 블록이 cd app/backend 로 끝나 뒤따르는 run_id 검증이 FileNotFoundError 로 죽는다 (T5a D-2)
- [ ] #13 finalLines 판별 실험을 돌린다: settle 없이 final 직후 종단 프레임을 같은 tick 에 연달아 주입해 줄을 잃는지 본다 — T5a 는 여유 9.7초 조건만 봤고 실제 session_ended 경로를 밟지 않았다
<!-- AC:END -->
