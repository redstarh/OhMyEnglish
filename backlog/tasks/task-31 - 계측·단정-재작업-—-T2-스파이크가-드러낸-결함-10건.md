---
id: TASK-31
title: 계측·단정 재작업 — T2 스파이크가 드러낸 결함 10건
status: In Progress
assignee: []
created_date: '2026-09-06 03:12'
updated_date: '2026-09-06 09:25'
labels:
  - caps-req
dependencies: []
priority: high
ordinal: 31000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
T2 스파이크(tests/harness/runs/2026-09-06-t2-instrument-spike.md)가 instrument.js 결함 6건과 browser_leg.md 잔여 결함 4건을 드러냈다. 핵심: 스텁 세션이 23ms 에 자기종료해 A1-4·A1-7 이 이 구성에서 측정 불가이고, finalLines 스냅샷 두 경로가 서로 반대로 실패해 A1-5 에 쓸 경로가 보장되지 않는다. 무음 대조는 lib/audio.ts 워클렛에 무음 게이트가 없어 원리적으로 0 을 낼 수 없다(팀리드 코드로 확인). 이미 고친 것은 제외한다 — browser_leg.md 의 P5(로그 pid 대조)·§8-②(baseline 복원)·§8-③(발음 패턴 보호)는 2026-09-06 에 반영했고 pitfalls H-AB·H-AC 도 등록했다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 instrument.js: finalLines 스냅샷 시점을 회차 운에 걸리지 않는 형태로 재설계한다 (동기=React commit 전 · rAF=router.push 후, 둘 다 [] 를 낸다)
- [x] #2 instrument.js: meta.audioContextState 를 resume 이후 값으로 기록한다 (현재는 async resume 직후 동기로 써서 항상 이전 값이다)
- [x] #3 instrument.js: WebSocket 후킹에 URL 스코프를 건다 — 지금은 Next dev HMR 소켓까지 센다(sent.other 의 정체)
- [x] #4 instrument.js: sentAudioBytes 를 목적에 맞게 고치거나 버린다 — 무음·유음 모두 199,728 로 동일해 아무것도 가르지 못한다
- [x] #5 browser_leg.md §10: 'audio 0 + end_session 0 = 후킹 고장이므로 ERROR' 규칙이 오진을 낸다 — 실제 원인은 세션 길이였다. 판별 기준을 고친다
- [x] #6 browser_leg.md A1-4: 기대값 3 과 when 대조가 이 구성에서 측정 불가다(관측 0·1). 측정 가능한 형태로 옮기거나 채택하지 않는다
- [x] #7 browser_leg.md A1-7: 무음 대조를 버리고 PCM 바이트 내용을 보는 형태를 검토한다. 성립하지 않으면 A1-7 을 채택하지 않는다
- [x] #8 browser_leg.md §11: '1회차에서 닫힌 것'(권한 대화상자)이 계측 경로에는 적용되지 않음을 반영한다 — 계측이 getUserMedia 를 교체하면 대화상자가 뜨지 않는다
- [x] #9 회차 기록 규약을 반영한다: 하네스 회차는 baseline / 회차 후 / teardown 후 3열을 남긴다 (없으면 사후 대조가 원리적으로 불가능하다 — 팀리드가 실제로 오판했다)
- [x] #10 instrument.js: start 호출에 프레임 태깅을 넣는다 — 지금은 어느 프레임이 통과했는지 알 수 없어 A1-4 기전 판정이 추론에 머문다
- [x] #11 browser_leg.md §8-0: baseline 을 파일로도 유지하는 규약을 지킨다 — 값이 정당하게 바뀌면 새 날짜로 새 파일을 뜨고 문서의 참조 줄을 갱신한다
- [x] #12 browser_leg.md §3: 두 명령의 cwd 가 어긋난다 — 앞 블록이 cd app/backend 로 끝나 뒤따르는 run_id 검증이 FileNotFoundError 로 죽는다 (T5a D-2)
- [x] #13 finalLines 판별 실험을 돌린다: settle 없이 final 직후 종단 프레임을 같은 tick 에 연달아 주입해 줄을 잃는지 본다 — T5a 는 여유 9.7초 조건만 봤고 실제 session_ended 경로를 밟지 않았다
- [x] #14 judgeFinalLines 의 atMax 선별을 고친다 — find() 가 첫 최대치를 골라 partial 섞인 과도 상태를 판정 대상으로 삼는다. L2r2 에서 거짓 FAIL 을 실측했다(snapshotCounts 에 6이 두 개, 첫 6의 마지막 줄이 partial 답변: I need to finish). AC #1 의 취지(회차 운 제거)가 미달임이 이것으로 증명됐다
- [x] #15 browser_leg.md §4-4 의 계약 키를 코드와 맞춘다 — 줄 169 가 finalLines 를 요구하는데 재설계된 계측에는 그 키가 없다(브라우저에서 keyState.finalLines = ABSENT 2회 관측). §5 A1-5 만 갱신되고 §4-4 가 낡았다
- [x] #16 instrument.js:264 의 NUL 2개를 없앤다 — join("\x00") 를 이스케이프 표기로 바꾼다(동작 동일). 이 셸의 grep 은 ugrep -I 래퍼라 NUL 이 든 파일을 통째로 건너뛰고 rc=1 을 낸다: 실측 셸 grep -c const 출력없음 대 command grep 37. '0은 고장과 구별되지 않는다' 를 축으로 삼는 리포에서 가장 중요한 파일이 grep 에 보이지 않는다. 함정 H-AG 가 소유한다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
계측 4건 + 절차 8건을 반영했다. AC #13 만 남았다 — 그것은 코드 수정으로 닫히지 않고 실제 세션 관통 회차가 필요하다.

instrument.js (292 → 437줄):
- finalLines 를 MutationObserver 적립으로 바꿨다. 동기 스냅샷은 진단용(finalLinesAtTerminal)으로 내렸다 — 동기는 React commit 전, rAF 는 router.push 후라 둘이 서로 반대로 실패했다. '언제 찍을까'를 고르지 않고 바뀔 때마다 전부 적립하는 것이 타이밍에서 벗어나는 유일한 방법이다
- judgeFinalLines(expected) 신설 — 최대개수==기대개수 · 최대지점 texts 순서일치 · count 비감소, 셋을 함께 본다. '기대값과 같은 스냅샷이 하나 있다'만으로는 골라내기다
- WebSocket 후킹에 URL 스코프(sent.foreign + meta.socketUrls). Next dev HMR 소켓이 섞여 sent.other 를 오염시켰다
- sentAudioNonZeroFrames 신설 — PCM 에 0 아닌 샘플이 있는 프레임 수. 무음·유음이 바이트로는 같아서 그것으로는 못 가른다
- start 프레임 태깅(started.calls: ts·when·afterRecvAudio·contextState)
- audioContextState 를 resume 의 then 에서 갱신 + contextState() 라이브 게터. resumeLog 에 before/afterSync/afterAwait 를 남긴다

판별력을 브라우저에서 직접 실측했다(세션 미개설 = DB 무접촉, 7·92·1·1 확인):
- 해시 대조: 페이지 안 sha256 0be00c20a99c4fa9 · 23917B = 디스크 일치 → 전사 드리프트 0
- eval 반환 'instrumented' · 계약 키 4개 존재
- resumeLog[0] = {before: suspended, afterSync: suspended, afterAwait: null} → 동기 값이 낡은 값임을 실증
- 송신 후킹: audio 2 · end_session 1 · foreign 1 · nonZeroFrames 1 · bytes 2736(각 1368 동일) → 바이트로는 못 가르고 내용으로는 갈린다
- judgeFinalLines 합성 5시나리오: 정상만 pass. 본문삭제→texts 실패 · 초과렌더→count 실패 · 순서뒤바뀜→texts 실패 · 개수감소→nonDecreasing 실패

browser_leg.md: A1-4(stub 전용·측정 불가 조건 명시) · A1-5(judgeFinalLines 기반으로 교체) · A1-7(무음 계수 대조 폐기 → PCM 내용 대조) · §10 오진 규칙을 3갈래 판별로 · §10 3열 규약 · §11 권한 대화상자 범위 축소 · §3 cwd · §8-0 baseline 파일 규약.

── 세션 마감 (2026-09-06) ──
AC 13건 중 12건 닫혔고 #13 만 남았다. #13 은 코드 수정으로 닫히지 않는다 — settle 없이 final 직후 종단 프레임을 같은 tick 에 연달아 주입하는 실험이고 실제 세션 관통 회차가 필요하다.

⚠️ 착수 전 필수: 백엔드가 지금 VOICE_ADAPTER=stub_unresponsive 로 떠 있다(pid 30860). 그 모드는 audio·final·partial 을 0건 보내므로 A1-1~A1-5·A1-8 을 평가할 수 없다. #13 을 하려면 VOICE_ADAPTER=stub 으로 재기동해야 하고, 로그를 /tmp/omy-backend.log 로 리다이렉트해야 P5 가 통과한다(P5 가 로그 pid 와 실행 pid 를 대조한다).

게이트 실측(마감 시점 직접 실행): 595 passed · ruff check 통과 · ruff format 32 files · ty 통과 · 프론트 tsc·eslint 둘 다 exit 0.

── AC #13 회차 (2026-09-06, 팀리드 직접 실행) ──
산출물: tests/harness/runs/2026-09-06-t13-finallines-discrimination.md · 회차 c9745e3b-cfb8-4982-bc2d-cc827ead6b41 · 계측 sha256 0be00c20a99c4fa9…(23917B, 페이지 안 계산으로 디스크와 3회 대조).

실험을 돌렸고 두 조건이 서로 다른 답을 냈다.
- 같은 tick 주입(await 0개, span 1.1ms): 동기 스냅샷 4곳 전부 []. MutationObserver 적립은 ≈3ms 뒤 count:2 정확 일치로 살아남았고 judgeFinalLines pass, 음성대조 3/3 FAIL.
- 실제 session_ended 경로(VOICE_ADAPTER=stub 재기동, 주입 0건): finalLinesAtTerminal 이 2회 모두 픽스처 6줄과 정확히 일치했다. 즉 '동기는 commit 전이라 [] 가 된다'는 T2 기제 서술을 이 회차가 지지하지 않는다 — 실제 프레임은 각자 자기 task 로 와서 사이에 commit 이 난다(적립 12·13건이 partial 이 자라는 것까지 남겼다).

⛔ 그 실험이 결함 2건을 찾았고 그래서 이 태스크를 Done 으로 올리지 않는다:
- AC #14 (결함 A): judgeFinalLines 의 atMax 가 find() 로 첫 최대치를 골라, partial 이 섞인 과도 상태를 판정 대상으로 삼는다. L2r2 에서 거짓 FAIL 실측(1회차 pass / 2회차 fail, 같은 코드·픽스처·어댑터). AC #1 이 약속한 '회차 운 제거'는 달성되지 않았고 운의 자리만 옮겼다.
- AC #15: browser_leg.md §4-4 가 계약 키로 finalLines 를 요구하는데 코드에 그 키가 없다(keyState.finalLines = ABSENT, 2회 관측).
- 미확인 위험(AC 로 올리지 않는다): nonDecreasing 의 || c === 0 예외가 언마운트를 통과시키는데, 그 예외가 진짜 이상도 통과시키는 회차는 관측하지 못했다.

덤: A1-4 계수·프레임 태깅이 stub 에서 측정됐다(started.count 3 == recv.audio 3, 2회 일치, afterRecvAudio 1·2·3). §10 의 3갈래 판별이 2회차에서 실제로 작동했다(sent.audio 0 + foreign 1 → 세션 22ms → BLOCKED 갈래). A1-7 무음 대조는 돌리지 않았으므로 여전히 판별력 미확인이다.

절차 발견: 하네스의 합성 클릭은 user activation 을 만들지 않아 lib/audio.ts:143 await context.resume() 가 매달리고 setState('active') 에 도달하지 못한다. 무해한 요소를 CDP 로 한 번 클릭해 sticky activation 을 얻은 뒤 전 과정을 한 eval 에 넣으면 §6 와 충돌 없이 해결된다(양쪽 조건 실측: hasBeenActive false → resume 2500ms 미해결 / true → 0ms).

DB 3열: 7·34·92·7·1·1 → 11·44·104·7·1·1 → 7·34·92·7·1·1. teardown ② UPDATE 0(복원식) · drift 0 · 캡틴 패턴 무손상. 백엔드는 stub_unresponsive(30860) → stub(38202) 로 바뀌어 있다(앱 기본값과 일치).
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-06 05:38
---
AC #1 의 체크를 되돌린다. 근거: AC #1 은 'finalLines 스냅샷 시점을 회차 운에 걸리지 않는 형태로 재설계한다' 인데, T13 회차가 같은 입력으로 2회 돌려 판정이 pass/fail 로 뒤집히는 것을 실측했다(snapshotCounts 최대치 6이 1회차 1개·2회차 2개). 운이 없어진 것이 아니라 '언제 찍을까' 에서 '어느 최대치를 고를까' 로 옮겼다 — 독립 리뷰도 같은 판정을 냈고 뒤집을 근거를 찾지 못했다. 체크를 살려 두면 task list 만 보는 쪽이 13/16 으로 읽어 미달을 못 본다(원장 원칙 4: 실행 상태는 인수기준 체크박스로 표현한다). AC #14 가 닫히면 #1 을 다시 체크한다.

⚠️ AC #14 의 수정 방향에 독립 리뷰의 반박을 반영했다. 'atMax 를 최대치 전부 후보로 두고 하나라도 일치하면 통과' 로 고치면 거짓 PASS 가 생긴다 — page.tsx:123-124 의 I-8 병합은 개수를 바꾸지 않고 텍스트만 바꾸므로, 병합이 망가져 뒤쪽이 오염돼도 앞쪽의 올바른 최대치 스냅샷 하나로 통과한다(docstring 이 금지한 골라내기를 최대치 부분집합 안에서 되살린다). 뿌리는 find 가 아니라 선별이 partial 을 계수에 넣는 것이다 — partial 줄과 확정 줄이 같은 접두를 쓴다(page.tsx:313-317). 그래서 방향은 셋이다: ① 확정/partial 을 구조로 가른다 ② 판정 대상을 종단 상태로 옮긴다 ③ 적립은 초과 검출(max <= 기대)에만 쓴다. 그리고 nonDecreasing 의 || c === 0 (instrument.js:285) 도 #14 에서 함께 판정한다 — 미확인 위험이므로 '고친다' 가 아니라 '판정하고 근거를 남긴다' 다.
---

created: 2026-09-06 08:40
---
AC #16 완료. instrument.js:264 의 리터럴 NUL 2개를 유니코드 이스케이프 표기로 바꿨다(런타임 동작 동일) + 되돌리지 못하게 주석 3줄을 붙였다. 새 sha256 190e659cce9deb63ffa8715f5b6feea3393c854bb64a80ee3fb4280f3c8be20b · 24280 바이트.

판별쌍을 양쪽 다 봤다(둘 다 그 턴에 직접 실행):
- 셸 grep -c const: 수정 전 출력없음·rc=1 → 수정 후 37·rc=0
- command grep -n 'texts.join': 수정 전 'Binary file … matches'(줄 없음·rc=0) → 수정 후 267줄 인용 정상

브라우저 런타임 동등성(계측을 CORS 서버로 그대로 받아 페이지 안에서 해시 대조):
- eval 반환 'instrumented' 정확 · 계약 키 7개 전건 존재 · 로드된 소스의 리터럴 NUL 0개 · 이스케이프 2개
- 그 이스케이프가 실제로 NUL 1문자로 평가됨(길이 1 · charCode 0)
- 중복 억제가 그대로 동작: 접두 없는 변이 2회는 억제(1→1, 2→2), 접두 줄 추가는 적립(1→2), 제거도 적립(2→3)
- 구분자 판별: ['a b','c'] 대 ['a','b c'] 가 공백 join 에서는 충돌(true), NUL join 에서는 충돌하지 않는다(false) → 구분자 선택이 실제로 일을 한다. 공백으로 되돌리면 경계가 모호해진다

게이트: 595 passed · ruff check·format exit 0 · ty exit 0. 게이트 밖 기준선 유지(../../tests+scripts 6 errors · format 4 files).

⚠️ 회차 기록의 계측 해시는 그 회차 시점의 값이므로 이 수정으로 낡는다 — browser_leg.md §10-3 대로 해시는 회차 기록에만 적고 절차 문서에 박지 않는다.

⚠️ 이 회차에 같은 함정을 두 번 더 밟았다: pitfalls.md 에 리터럴 NUL 을 써 넣었고(스캔해 제거), 커밋 메시지에도 넣어 툴이 거부했다. 문서·메시지에서 그 구분자를 언급할 때는 표기를 조립하거나 말로 쓴다.
---

created: 2026-09-06 08:41
---
AC #15 완료. browser_leg.md §4-4 의 계약 키 목록을 코드와 맞췄다.

무엇이 틀렸나: 그 절이 'finalLines' 를 요구하면서 '하나라도 빠져 있으면 PASS 를 낼 수 없다' 고 적었는데, 2026-09-06 재설계가 그 키를 finalLinesAtTerminal(동기 진단) + snapshots(적립) 둘로 갈랐다. 즉 문자 그대로 따르면 모든 브라우저 회차가 §4 에서 ERROR 로 끝나야 했다. §5 의 A1-5 는 재설계를 따라갔는데 §4-4 만 낡아 있었다 — 한 문서 안에서도 두 곳에 적으면 한쪽이 조용히 낡는다.

새 목록(그 턴에 브라우저에서 직접 재서 확인한 7개): recv · sent · started · snapshots · finalLinesAtTerminal · judgeFinalLines · inject. 018-eval 의 contractKeys 가 그 7개 전건 존재를 냈다.

그리고 두 줄을 더 박았다: ① 'finalLines 키는 없다 — 요구하지 마라' 와 그 이유 ② 이 목록을 셸 grep 으로 계측과 대조하지 마라(함정 H-AG). 가장 강한 확인은 페이지 안에서 k in window.__omy 를 재는 것이다.

⚠️ §5 A1-5(줄 232)의 '동기 스냅샷은 React commit 전이라 [] 가 되고' 는 이 AC 에서 고치지 않았다 — 그것은 A1-5 의 판정 방법 자체이고 AC #14 가 소유한다. 조건 없는 일반문이라 T13 이 실제 서버 경로에서 반증한 바로 그 문장이다.
---

created: 2026-09-06 08:49
---
AC #14 완료 + AC #1 재체크. 산출물 tests/harness/runs/2026-09-06-ac14-judge-rerun.md · 회차 700164a3-8657-4e16-8daa-617e6f12f734 · 계측 sha256 face761b53f6b3e9e9a026b0b678e54c1ad70b99418f1aa05147890670cfb446 (27735B).

무엇을 고쳤나: 내용·순서 판정을 적립된 최대치 스냅샷에서 종단 동기 스냅샷(finalLinesAtTerminal)으로 옮겼다. 적립은 초과 검출과 단조성만 담당한다. 판정 4항목 — terminalPresent · terminalMatches · noExcess · nonDecreasing. nonDecreasing 의 || c === 0 도 함께 고쳤다(마지막에 오는 0만 허용 — 중간의 0은 언마운트·재마운트라 이상이다). 이것이 결함 B 의 판정이다.

채택하지 않은 대안 2개와 이유: ① '최대치 전부를 후보로 두고 하나라도 일치' 는 거짓 PASS 를 만든다(I-8 병합은 개수 불변·텍스트만 변경이라 뒤쪽이 오염돼도 앞쪽 하나로 통과) ② 색으로 확정/partial 가르기는 §6 이 금지한다(둘의 유일한 구조적 차이가 색이다 — page.tsx:308 대 :314 를 직접 읽어 확인).

red→green(합성 9시나리오, 이전 판 로직을 참조 구현으로 나란히 돌렸다): 전건 기대와 일치. 정상 2회차 실측 계열이 이전 fail → 새 pass. 음성 7건 전부 새 판에서 fail. '중간에 0 낙하 후 상승' 은 이전 판이 pass 를 냈고(|| c === 0 의 구멍) 새 판이 fail 을 낸다.

실물 회차 2건 — H-AF 가 요구한 '같은 입력 2회 동일 판정' 충족: A·B 둘 다 judge.pass true. 두 회차 모두 atMaxDiagnostic 의 마지막 줄이 partial('답변: I need')이었다 = 이전 판이면 정상 회차 두 건이 다 FAIL 이다. 회차 B 에서 이전 로직을 같은 런타임 데이터에 직접 돌려 oldJudgeOnThisRound=false 를 관측했다. 음성 대조 4건 양 회차 4/4 FAIL. 덤으로 started.count 3 == recv.audio 3 · afterRecvAudio [1,2,3] 이 2회 일치.

AC #1 을 다시 체크하는 근거: 그 AC 는 '회차 운에 걸리지 않는 형태로 재설계한다' 였다. 이제 내용·순서는 고정 지점(종단)에서 재고, 어느 스냅샷을 고르는지에 판정이 걸리지 않는다. 실물 2회가 같은 판정을 냈고 그 둘은 이전 판이라면 갈렸을 데이터다.

⚠️ 남은 한계(미확인, 통과로 적지 않는다): 이 판정은 종단 시점에 partial 이 없다는 것에 의존한다. 픽스처는 턴마다 user final 로 끝나 구조적으로 보장되지만 실물 Nova 는 미확인이다. 그 경우 terminalMatches 가 FAIL 을 내므로 거짓 PASS 는 아니고 거짓 FAIL 이 될 수 있다 — browser_leg.md §5 A1-5 에 한계로 적었다. A1-7 무음 대조는 이 회차에서도 돌리지 않았다(판별력 미확인 유지, TASK-30 소유).

DB 3열: 7·34·92·7·1·1 → 9·42·104·7·1·1 → 7·34·92·7·1·1. teardown ② UPDATE 0 · drift 0 · 캡틴 패턴 무손상. 게이트 595 passed · ruff·ty exit 0 · 게이트 밖 기준선 6 errors·4 files 유지.

⛔ 16/16 체크지만 Done 으로 올리지 않는다 — instrument.js 수정에 대한 독립 코드리뷰가 아직 안 왔다(9단계: Approve 전 Done 전환 금지). 미수신이면 미결로 남긴다.
---

created: 2026-09-06 09:03
---
⛔ 코드리뷰 수신 — CHANGES REQUESTED (HIGH 3 · MEDIUM 4 · LOW 1). AC #14 와 #1 의 체크를 되돌린다. common/code-review.md 기준 'Approve: No CRITICAL or HIGH issues' 이므로 HIGH 가 남은 상태로 Approve 가 아니고, 따라서 Done 도 아니다.

리뷰 주장을 형식적으로 수용하지 않고 전건 재현했다(node 로 파일에서 함수를 그대로 뽑아 돌린 출력):
- H2 공허 통과: expected=[] · terminal=[] → pass=true, 4항목 전부 true. 백지 화면이 통과한다. 리포에 judgeFinalLines 호출부가 0건이고 기대 배열을 회차마다 손으로 적으므로 실제로 밟을 수 있는 경로다. 이 파일이 두 번 못 박은 '0은 고장과 구별되지 않는다' 를 정면 위반한다.
- M1: noExcess 가 === 라서 max=5 + 종단 6줄 정확이 pass=false 다(거짓 FAIL). 그런데 설계 합의·pitfalls H-AF·코드 주석은 전부 max <= 기대 라고 적는다 — 문서 3곳과 코드가 어긋난다.
- M2: terminalPresent 와 terminalMatches 가 늘 같이 false 다(terminalMatches 가 terminalPresent && 로 시작한다). pass 의 독립 조건은 3개이고, 회차 기록·browser_leg.md·handoff 가 '4항목' 이라 적은 것이 틀렸다.
- M3: 종단 프레임 없이 소켓이 닫히는 경로가 앱의 정상 지원 경로다(page.tsx:187-198 onClose → goToResults). 그 회차는 화면이 정상인데 pass=false 가 되고 'FAIL' 로 적힌다 — browser_leg.md A1-4 가 이미 '측정 불가(FAIL 아님)' 어휘를 정해 뒀다.
- M4: 한 페이지에서 두 세션을 돌리면 nonDecreasing 이 false 다(중간 0 뒤 상승). 그리고 finalLinesAtTerminal 에 first-wins 가드가 없어 두 번째 종단 프레임이 덮는다 — 앱은 같은 위험을 terminalHandledRef 로 막는데 계측이 미러링하지 않았다. 새 전제(한 페이지 1세션)가 어디에도 적혀 있지 않다.
- L1: expected=null 이 익명 TypeError 로 죽고, expected='abcdef'(문자열)가 pass=true 를 낸다. 이 파일은 같은 부류를 need() 로 처리하는데 그 컨벤션이 이 함수에만 적용되지 않았다.

⛔ H1 이 가장 나쁘고 변명할 게 없다 — 같은 파일 docstring 4곳이 새 판정과 정반대를 말한다. :138('finalLinesAtTerminal 은 진단용이다, 단정은 snapshots 로 한다') · :141('snapshots: A1-5 단정의 정본') · :150-153(내가 결함 원인으로 제거한 그 ② 를 계약으로 처방) · :368-372(그 값을 만드는 줄 바로 위에서 '진단용' 이라 적는다). 본문은 고쳤는데 그 본문을 설명하는 블록 4개가 이전 설계를 서술한다. 이것이 이 리포의 지배 실패 모드이고, 같은 날 H-AF·§4-4 에서 그 형태를 등재한 파일에서 내가 다시 밟았다.

⛔ H3 은 반박할 수 없다 — 근거를 낮춘다. '실제 경로는 프레임마다 별개 task 라 커밋 개입이 구조적이다' 는 단정인데, React 18 의 default-lane flush 도 MessageChannel 매크로태스크이고 WS message 도 매크로태스크라 프레임이 몰리면 flush 가 뒤로 밀린다. 그리고 이 픽스처에는 간격이 없다 — stub.py 의 events() 에 await asyncio.sleep 이 한 줄도 없다(내가 직접 읽어 확인). 내가 가진 것은 4회 관측이고 기제 증명이 아니다. 게다가 내용 단정의 폴백이 없어져서, 프레임 합병으로 종단이 짧아진 회차는 atMaxDiagnostic 이 정상 6줄을 보여줘 '앱이 틀렸다' 와 구별되지 않는다 — 판정 실패가 조용하다.

⚠️ H3 을 근거로 이전 판을 되살리지 않는다 — 이전 판이 거짓 FAIL 을 낸 것은 실측이다. 고칠 것은 새 판의 근거 서술과 관측 수단이다(exactStateSeen 진단 필드).

착지 순서: H1 → H2·L1(방어) → M1(<=) → M2·M3(어휘와 주장 정정 + judgeable) → M4(first-wins 가드 + 한 페이지 1세션 명시) → H3(주장 하향 + exactStateSeen). 고친 뒤 합성 재실행 + 실물 2회를 다시 돌린다.
---

created: 2026-09-06 09:11
---
코드리뷰 지적 8건을 전건 수용해 고치고 재검증했다. 산출물: runs/2026-09-06-ac14-judge-rerun.md §9 · 회차 d590d8dc-26f9-4a3e-8287-a1debf7bf383 · 계측 sha256 6dd2a6b3979616e6186be44610a5b07a0eed7c4147ebc6abaae22eed7568712d (34392B).

고친 것: H1 docstring 4곳 재작성(옛 3항목 처방을 삭제 — 남겨 두면 다음 세션이 현재 코드를 회귀로 오판해 되살린다) · H2+L1 need() 가드(빈/비배열 expected 를 이름 있는 오류로 죽인다) · H3 주장 하향(구조적 → 관측 6회·기제 미확정) + exactStateSeen 진단 필드 신설 · M1 noExcess 를 <= 로 · M2 pass 에서 terminalPresent 제거(독립 조건은 셋) · M3 judgeable 필드 신설(측정 불가 ≠ FAIL) · M4 종단 스냅샷 first-wins 가드 + 한 페이지 1세션 명시.

재검증: 합성 가드 4건 전부 THROW · 정상 2계열 무회귀 pass · max=5+종단정확이 pass 로 전환 · 음성 7건 전부 fail · exactStateSeen 의미 판별(적립·종단 함께 오염=false 앱결함 / 종단만 짧음=true 하네스 놓침). 실물 2회 A·B 둘 다 pass, firstWinsHolds_viaInject=true(종단 프레임 재주입이 값을 덮지 않는다), oldJudgeOnThisRound=false.

⚠️ 실물 6회 연속 atMaxDiagnostic 마지막 줄이 partial 이다 — 이전 판의 거짓 FAIL 은 드문 경우가 아니라 기본값에 가까웠다.

DB 3열 7·34·92·7·1·1 → 9·42·104·7·1·1 → 7·34·92·7·1·1 · teardown ② UPDATE 0 · drift 0 · 캡틴 패턴 무손상. 게이트 595 passed · ruff·ty exit 0 · 게이트 밖 6 errors·4 files 기준선 유지.

⛔ 여전히 Done 이 아니다 — 리뷰어에게 재검증을 요청했고 미수신이다. 남은 커버리지 공백 2건도 명시했다: 종단 스냅샷 자리를 실패 방향으로 밟는 합성 케이스 부재(실물 6회가 통과 방향으로만 밟았다) · A1-7 무음 대조 미실행(TASK-30 소유).
---

created: 2026-09-06 09:16
---
2차 코드리뷰 수신 — 코드 축 Approve(CRITICAL 0 · HIGH 0), 전체는 NEW-1(HIGH)로 보류. 둘 다 반영했고 하나는 리뷰어가 낡은 트리를 본 것이었다.

NEW-1(browser_leg.md §5 A1-5 가 코드와 반대를 지시한다) — **절반이 낡은 관측이다.** 직접 확인: 내 커밋 c90f32f 가 2026-09-06T18:11:19+09:00(=09:11:19Z)이고 리뷰 보고가 09:08:27Z 다 — 3분 전 트리를 봤다. 지금 문서에 '4항목 전부 참이어야'는 0건이고 judgeable 1건·exactStateSeen 2건이 들어 있다(command grep -c -a). 다만 절반은 진짜였다: :169 의 snapshots 계약 키가 texts 의 역할이 단정→진단으로 바뀐 것을 안 적었다 → 그 절을 붙였고, 앱 코드 결합(확정 줄은 치환되지 않는다)이 전제임과 그 전제가 깨지는 경로(교정 표시·재전사·undo)를 함께 못 박았다.

NEW-2(<= 변경의 근거로 리뷰어가 철회한 도달 불가 입력이 코드에 박혔다) — **전부 진짜다. 고쳤다.** 근거를 사실로 갈았다: 종단 스냅샷은 message 핸들러 맨 위에서 찍히므로 앞선 태스크의 커밋은 이미 MutationObserver 를 통과해 snapshots 에 있다 → |종단| <= maxCount 가 항상 성립한다. 리뷰가 12,440 조합 전수 비교로 pass 가 갈린 60건 전부 maxCount < |종단|(도달 불가)임을 확인했다. 즉 이 변경은 동작이 아니라 이름과 합의를 맞춘 것이다. 이 파일 머리말이 정한 규칙이 정확히 그것이다 — 바뀐 것이 근거면 근거를 갈아라.

⚠️ 리뷰어가 잡지 못한 잔여 블록 1건을 내가 찾아 함께 고쳤다: snapshots docstring 에 '타이밍에 걸리지 않는 유일한 방법은 바뀔 때마다 전부 적립하는 것' 이 남아 있었다 — 그것이 틀렸다는 것이 H-AF 다. 지운 이유와 남겨 둘 사실(rAF 는 router.push 이후라 [] 가 된다)만 남겼다.

⚠️ 브라우저 재실행을 하지 않았고 그 근거를 적는다: c90f32f 이후 instrument.js 변경에 **코드 줄이 하나도 없다**(주석·docstring 만 +19/-8, browser_leg.md +7). git diff 로 코드 줄만 걸러 확인했고 결과가 비었다. 그래서 c90f32f 의 실물 회차 A·B 검증이 실행 로직에 그대로 적용된다. 안전 확인으로 판정 함수를 파일에서 다시 뽑아 계산했다: 회차A·B 계열 pass=true · 빈 기대값 THREW · 종단 짧음 pass=false·exactSeen=true·judgeable=true.

채택하지 않은 제안 1건(유실 방지로 기록한다) — 리뷰어의 '접두 불변' 진단: S[k].texts 에서 마지막 줄을 뺀 것이 S[k+1].texts 의 접두여야 한다. 마지막 줄만 제외하면 partial 과 I-8 병합이 둘 다 자동 배제되어 확정 줄 오염만 정확히 본다. 채택하지 않은 이유: 리뷰어 스스로 '판별력 미측정'이라 밝혔고(회차 기록에 texts 전문이 없어 실물로 못 돌렸다), 판별력을 재지 않은 대조를 넣으면 이 리포가 반복해 밟은 '대조 있음 = 판별력 있음' 오독을 새로 만든다. 실물 회차에서 texts 전문을 남기게 된 뒤에 측정하고 그때 채택 여부를 정한다.

⛔ 여전히 Done 이 아니다 — NEW-1·NEW-2 반영에 대한 3차 확인이 미수신이다.
---

created: 2026-09-06 09:25
---
3차 코드리뷰 수신 — 코드 축 Approve(CRITICAL 0 · HIGH 0), 8건+NEW-1 닫힘 확인. 남은 3건 중 코드는 하나였고 그것은 내가 재검증 요청에서 스스로 물은 것이다(Q_B). 산출물: runs/2026-09-06-ac14-judge-rerun.md §10 · 회차 a43a54a3-626b-446e-a8ac-00778c874609 · 계측 sha256 378372be0ac5f7f1… (39449B · 612줄).

Q_B — 내가 물었고 리뷰가 변이 테스트로 답했고 내가 재현했다: noExcess 를 <= 로 바꾼 뒤 terminalMatches 의 길이 검사만 잃는 변이에서 '앱이 5줄만 렌더'가 거짓 PASS 가 된다(옛 === 는 noExcess 가 막았다). 그리고 '적립 max5 · 종단 6줄'(적립 잘림)을 현재 코드가 조용히 통과시키고 있었다. → 되돌리지 않고 갈랐다: noExcess(<=, 초과 검출) + reachedExpected(>=, 도달 검출) 둘 다 pass 에. 강도는 옛 === 와 같고(<= ∧ >= ⟺ ==) 사유가 갈린다. H-AB 의 '배선 줄만 무력화해 확인한다'를 이 함수에 적용한 결과다.

verdict 필드 신설(리뷰 권고 채택) — 진단 필드가 안 읽히는 위험을 규약에서 구조로 내렸다. 회차는 pass 를 해석하지 말고 verdict 를 베껴 적는다. ⚠️ 리뷰가 제안한 라벨을 그대로 쓰지 않고 한 갈래를 더 쪼갰다: maxCount < 기대 를 NEVER_REACHED_EXPECTED 하나로 묶으면 앱 결함과 하네스 사고가 섞인다. maxCount < |종단| 이면 적립이 잘린 것(HARNESS_TRUNCATED_ACCRUAL), 같으면 앱이 덜 그린 것(APP_UNDER_RENDER)으로 갈랐고 실측으로 확인했다. 라벨이 앱 결함을 하네스 사고로 오분류하면 그 자체가 새 함정이다.

정정 3건: ① 리뷰가 M1 을 MEDIUM→LOW 로 내렸다(자기 failure_scenario 가 도달 불가 입력임을 스스로 밝혔다 — 12,440 조합 전수 비교로 갈린 60건 전부 M < |T|) ② 그 철회된 입력이 내 코드 주석과 browser_leg.md 두 곳에 근거로 박혀 있었다(NEW-2) → 사실로 갈았다 ③ exactStateSeen 을 pass 에 넣지 않은 근거가 부정확했다 — 연접으로 넣으면 골라내기가 되살아나지 않는다(그건 이접이어야 한다). 진짜 이유는 pass 밖에 있어야 원인을 지목할 수 있다는 것.

⚠️ 리뷰가 낡은 트리를 두 번 봤다(NEW-1 의 절반, instrument.js 두 차례) — 커밋 시각을 대조해 구별했다. 낡은 관측을 근거 없이 기각하지도, 그대로 받지도 않았다. 남은 절반(:169 의 snapshots 계약 키)은 진짜였고 고쳤다.

⚠️ 리뷰가 잡지 못한 잔여 낡은 블록 1건을 내가 찾아 고쳤다: snapshots docstring 의 '타이밍에 걸리지 않는 유일한 방법은 바뀔 때마다 전부 적립하는 것' — 그것이 틀렸다는 것이 H-AF 다.

보류 1건(기각 아님): 리뷰의 접두 불변 진단. 판별력 미측정이라 지금 넣지 않고, 실물 회차가 texts 전문을 남기게 된 뒤 측정해 정한다.

재검증 실물 2회(회차 a43a54a3): A·B 둘 다 verdict=PASS · 4항목+진단 전부 참 · 가드 3/3 THREW · firstWins true · 음성 대조 verdict 가 의미대로 갈린다. 합성: 가드 4건 THROW · verdict 8종 분류 전건 일치 · 변이 내구성 복구 · 실물 계열 무회귀. 실물 8회 연속 atMaxDiagnostic 마지막 줄이 partial 이다.

DB 3열 7·34·92·7·1·1 → 9·42·104·7·1·1 → 7·34·92·7·1·1 · teardown ② UPDATE 0 · drift 0 · 캡틴 패턴 무손상. 게이트 595 passed · ruff·ty exit 0 · 게이트 밖 6 errors·4 files.

⛔ 여전히 Done 이 아니다 — Q_B 반영에 대한 4차 확인이 미수신이다. 못 메운 것 3건을 명시했다: 종단 스냅샷 자리를 실패 방향으로 밟는 표본 0건(리뷰가 방법을 제시했고 다음 회차 몫으로 동의했다 — 래퍼를 통과시키는 같은-tick 주입) · A1-7 무음 대조 미실행 · 실물 Nova 종단 partial 미확인.
---
<!-- COMMENTS:END -->
