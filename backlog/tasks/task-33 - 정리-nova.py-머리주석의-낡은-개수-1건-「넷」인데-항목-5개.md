---
id: TASK-33
title: '정리: nova.py 머리주석의 낡은 개수 1건 (「넷」인데 항목 5개)'
status: Done
assignee: []
created_date: '2026-09-07 15:34'
updated_date: '2026-09-08 09:27'
labels: []
dependencies: []
ordinal: 36000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
app/backend/app/audio_gateway/nova.py:5 가 '문서만 읽고는 알 수 없었던 것 넷:' 인데 아래 번호 목록이 5개다(5번 'tool use가 동작한다'). Batch B 구현자가 발견해 판정을 요청했고, 팀리드가 BASE(a1d6ce8) 에서 5번 항목의 존재와 이 배치가 머리주석을 건드리지 않은 것(첫 hunk 가 :51 부터)을 직접 확인했다 — 이 배치가 만든 것이 아니라 b31227a(Nova 발음 tool 연결) 가 항목을 더하면서 개수를 안 고친 것이다. 규약(Surgical Changes: 요청 범위 밖은 언급만) 에 따라 Batch B 에서 고치지 않고 등록한다. 같은 부류(본문 고치고 그것을 설명하는 개수 문장 안 고침)이고 이 리포의 지배 실패 모드다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 nova.py 머리주석의 개수 서술을 고친다 — 숫자를 세지 않는 서술로 바꾸는 것도 정당한 선택이다(Batch B 의 F-5 픽스가 그 선례이고 근거를 함께 남겼다)
- [x] #2 구현자가 이미 훑어 정확함을 확인한 4건(factory.py '넷 다' · ws.py '네 개' · PreparedPlan '둘' · 축 목록 '둘')은 다시 세지 않는다 — 보고서 batch-B-report.md 의 표가 그 근거다
- [x] #3 nova.py 의 F-5 수정문 안의 낡은 개수도 함께 고친다 — '이 배치가 축을 셋 더하면서(무대·질문↔규칙2·질문↔규칙3·드릴반복·우선순위)' 에서 괄호가 5개를 열거하고 git diff a1d6ce8..HEAD 로 확인한 실제 추가 축도 5개다. 개수가 왜 낡는지 설명하는 문장 안에 낡은 개수가 있다(라운드 2 재리뷰가 부수로 지적 · Important 아님 — 역사 서술이라 현재 동작을 오인시키지 않는다)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 완료 (재개 세션). 문서 정리라 코드 동작 변경 0.

**AC#1** — 머리주석의 「문서만 읽고는 알 수 없었던 것 **넷**:」을 **세지 않는 서술**로 바꿨다(항목은 다섯이었다). 개수를 5로 고치는 대신 없앤 근거를 그 자리에 적었다: `b31227a`가 5번(tool use)을 더하면서 「넷」을 남긴 것이고, **세지 않는 서술은 낡지 않는다.** Batch B의 F-5 픽스가 같은 선례다.

**AC#3** — F-5 수정문 안의 낡은 개수도 고쳤다. *"이 배치가 축을 **셋** 더하면서(무대 · 질문↔규칙2 · 질문↔규칙3 · 드릴 반복 · 우선순위)"* 에서 **괄호가 다섯을 열거한다.** 「여럿」으로 바꾸고, ⛔ **그 정정문 자신이 같은 병에 걸려 있었다는 사실을 명시적으로 남겼다** — 개수가 왜 낡는지 설명하는 문장 안에서 개수가 낡았으므로 **세지 않는 서술만이 이 부류를 닫는다**는 증거다. 지우지 않고 남긴 이유가 그것이다.

**AC#2** — 구현자가 이미 훑은 4건(`factory.py` 「넷 다」 · `ws.py` 「네 개」 · `PreparedPlan` 「둘」 · 축 목록 「둘」)은 **다시 세지 않았다.** 근거는 `batch-B-report.md`의 표다. ⚠️ 그중 「축 목록 둘」은 위 문단이 *"대체하는 쪽은 세어도 안전하다"*로 그 예외를 명시한 자리이므로 정당하다 — 늘어나려면 캡틴 결정이 필요하고, 두 이름이 명시돼 읽는 사람이 직접 검산할 수 있다.

**게이트 (직접 돌렸다)**: **767 passed**(변동 0 — 문서 변경이다) · `ruff check .` exit 0 · `ruff format --check .` 33 files · `ty check` exit 0 · 게이트 밖 `ruff` **6 errors** · `format --check ../../tests` **4 files**.
⚠️ 첫 수정에서 `ruff` E501(161 > 100)이 났다 — 내가 넣은 문단이 다음 문장과 한 줄로 붙었다. 줄바꿈으로 닫았다. **이 세션에서 이 형태가 세 번째다**(게이트 밖 2회 + 여기). 한글 산문을 넣을 때마다 길이를 다시 잰다.
<!-- SECTION:NOTES:END -->
