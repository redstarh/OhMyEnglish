---
id: TASK-154
title: 전용 모드 규칙 9 에도 인용 형태 요구를 넣는다 — 실측 산출물 재핀이 필요하다 (결정 124 의 나머지 절반)
status: Done
assignee: []
created_date: '2026-09-17 02:57'
updated_date: '2026-09-17 03:43'
labels: []
dependencies: []
ordinal: 215000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
결정 124 가 SYSTEM_PROMPT 규칙 9 에 「소리를 따옴표로 따로 인용하라」를 넣었으나 PRONUNCIATION_MODE_PROMPT 에는 넣지 못했다. 이유는 그 문면이 test_the_pronunciation_prompt_is_byte_identical_to_the_measured_one 으로 실측 산출물(tests/harness/runs/2026-09-12-task128-sound-as-candidate/prompt_dedicated_v4.txt · ARM-A 4/4)에 바이트로 묶여 있고, 그 테스트가 「문면을 고치면 되돌리거나 회차를 다시 돌려 새 수치를 얻어라」로 선택지를 둘로 못박았기 때문이다. ⛔ 그 게이트를 우회하지 않는다 — 실측 수치가 고친 판에 붙지 않는 것이 그 테스트가 막는 실패다. 측정된 구멍 2/59 중 T123B1 이 전용 모드 계열 세션이라 이 절반이 남아 있으면 구멍도 절반 남는다. ⚠️ 규칙 8·10 은 두 프롬프트가 글자 그대로 같아야 하고 규칙 9 는 이미 다르다(전용 모드에 Grammar first 절이 없다) — 그 비대칭은 의도다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 전용 모드 규칙 9 에 SYSTEM_PROMPT 와 같은 인용 형태 요구를 넣는다
- [x] #2 실물 회차를 돌려 새 문면의 수치를 얻고 prompt_dedicated_v5.txt 로 남긴다 — 이전 판을 덮지 않는다
- [x] #3 byte 동일성 테스트와 그 docstring 의 이력 목록을 새 파일로 갱신한다
- [x] #4 코치가 실제로 소리를 따로 인용했는지 회차 증거로 센다 — 「지시가 있다」로 대체하지 않는다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## 결과 — ⛔ **결정 124 가 반증됐고 문면을 되돌렸음** (2026-09-17)

정본은 `tests/harness/runs/2026-09-17-task154-dedicated-quote/README.md` 임. Nova 실물 **7세션**
(계획 4 → 실제 7 · 늘린 이유는 아래 AC#4). 공유 dev DB·`:8002` 를 건드리지 않았음.

**AC#1** — 전용 모드 규칙 9 에 일반 세션과 같은 요구를 넣었음. TDD 로 먼저 red 를 봤음
(`test_the_dedicated_prompt_also_makes_the_coach_quote_the_off_sound` 가
`AssertionError: 전용 모드에 인용 형태 요구가 없다` 로 떨어짐). 길이 1,959자로 앞 판 셋
(1,660·1,825·1,927 — 전부 4/4)과 같은 대역이라 회차가 길이가 아니라 문면을 잼을 먼저 확인했음.

**AC#2** — 실물 회차를 돌렸고 `prompt_dedicated_v5.txt` 로 남겼음(이전 판을 덮지 않았음).
스택: 검증 DB `ohmyenglish_t154`(마이그레이션 24 · `ohmyenglish` 스키마 17표) · 후보 `th_as_s` 1행을
제품 writer 경로로 심음 · 백엔드 `:8014` `VOICE_ADAPTER=nova WORKER_ENABLED=false`.

**AC#4** — 셌고, **셈이 판정을 뒤집었음**:

| 팔 | 문면 | 날짜 | tool 프레임 | 코치가 집은 소리 | `sound_check` |
|---|---|---|--:|---|---|
| 기준선 | v4 | 09-12 | 2 ×4 | `"th"` 4/4 | matched 4/4 |
| ARM-A | **v5** | 09-17 | **1** ×4 | **`"are"` 4/4** | mismatched 4/4 |
| ARM-C | v4 | **09-17** | 2 ×3 | `"th"` 3/3 | matched 3/3 |

⛔ 깨진 것 둘 — ⑴ 오디오 오류는 /θ/→/s/ 인데 코치가 **오류가 아닌 낱말 `"are"`** 를 집었음 ⇒
학습자가 틀리지 않은 소리를 연습함 ⑵ tool 호출이 **2 → 1** 로 줄어 규칙 10 의 재발화 판정 호출이
사라졌음. ⚠️ **`sound_check` 가 `mismatched` 로 켜진 것을 성과로 읽으면 정반대로 읽는 것임** —
검사가 좋아진 게 아니라 코치가 틀린 소리를 코칭해서 어긋난 것임.

⛔ **ARM-C(짝 대조)를 회차 중간에 더했고 그것이 상한을 4 → 7 로 늘린 이유임.** 기준선과 5일 차이가
있어 「문면인가 모델인가」를 가를 수단이 없었음. 같은 날 앞 문면이 3/3 으로 제 오류를 집었으므로
**모델 변화가 아님.** 그 팔이 없었으면 「모델이 바뀐 듯하다」로 잘못 닫았음.

**AC#3** — ⛔ **「새 파일로 갱신한다」 형태로 닫히지 않았음.** 회차가 되돌리라고 말했으므로 바이트
게이트를 **v4 에 그대로 두었고** 지금 초록임(= 트리의 문면이 v4 와 바이트로 같음). AC 문면이
예상하지 못한 갈래(게이트 docstring 이 준 선택지 둘 중 「되돌린다」)로 닫혔음을 여기 적음.
그 대신 게이트 docstring 의 이력 목록에 **5번 항목을 더해 v5 를 「시도했고 되돌렸다」로** 남겼음 —
그 파일이 갱신 후보가 아니라 반증의 증거임을 그 자리가 말함.

## 되돌린 범위 — 일반 세션까지

전용 모드와 **`SYSTEM_PROMPT` 둘 다** 되돌렸음. ⚠️ 일반 세션은 이 회차가 직접 재지 않았음 — 같은
문면·같은 기전이고, 겨냥한 이득이 세션 **2/59** 인데 관측된 해가 「틀린 소리를 연습시킴」이라
비대칭이 커서 **안전한 쪽으로** 되돌렸음. 재려면 일반 세션 회차가 따로 필요함.

단정도 뒤집었음 — 「요구가 있는지」를 재던 테스트를 **「요구가 들어오지 못하게」** 막는 테스트로
바꿨음(`test_neither_prompt_asks_the_coach_to_quote_the_sound_on_its_own` · 두 프롬프트를 함께 봄).
검사 쪽 단정 둘은 그대로 유효함 — 순수 함수의 거동을 재고 프롬프트에 걸려 있지 않음.

⛔ **겨냥한 구멍은 열린 채로 남음**(`test_a_whole_sentence_quote_is_still_not_judged` 가 이름으로 가짐).
검사 쪽 대안은 이득이 구조적으로 0, 프롬프트 쪽은 이 회차가 반증 ⇒ 남은 후보는 오디오를 듣는 판정이고
설계서 §6 이 범위 밖에 뒀음.

등재: 지시 대장 결정 124 절에 반증을 이어 붙였음 · 함정 `H-BZ` 신설.
게이트 일곱: 수집 1286 · pytest 1286 passed · ruff 0 · format 273 · ty 0 · tsc 0 · eslint 0.
<!-- SECTION:NOTES:END -->
