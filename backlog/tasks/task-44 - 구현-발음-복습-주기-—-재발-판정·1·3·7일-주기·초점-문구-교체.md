---
id: TASK-44
title: '구현: 발음 복습 주기 — 재발 판정·1·3·7일 주기·초점 문구 교체'
status: In Progress
assignee: []
created_date: '2026-09-07 22:19'
updated_date: '2026-09-07 23:10'
labels: []
dependencies:
  - TASK-9
ordinal: 47000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-9 설계(docs/design/2026-09-08-pronunciation-review-cycle-design.md)의 구현 소유자다. ⚠️ 2026-09-08 팀리드 정리에서 신설했다 — TASK-9 의 원래 AC#3 이 「설계에 반영」과 「계획 프롬프트 문구를 고친다」를 한 칸에 묶어서, 설계 태스크가 구현을 기다리며 In Progress 로 영구 정박했다. 설계 AC 는 TASK-9 에 남기고 구현을 이리로 갈랐다. 설계서 §6 판정: 마이그레이션은 발급하지 않는다(컬럼·표·CHECK 값역을 늘리지 않는다).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 발음 재발 판정을 설계서 §3.2 확정안대로 구현한다 — 시각 축은 resolved_at 이다(§3.3). utterances.created_at 을 쓰지 않는다
- [x] #2 복습 주기를 설계서 §4.1 결론대로 1·3·7일 그대로 쓴다 — 다른 값을 발명하지 않는다(§4.2)
- [x] #3 초점 허용 집합 배선을 설계서 §5.2·§5.3·§5.5 대로 구현한다 — 상태 계산 카테고리 분기 · target_sound 이력 쿼리 · 재계산 트리거
- [x] #4 계획 프롬프트의 「발음은 초점 원천이 아니다」 문구를 설계서 §5.6 의 새 문구 전문으로 교체한다 — 문구를 새로 발명하지 않고, §5.6 이 「고치지 않는다」고 판정한 자리는 그대로 둔다
- [x] #5 red→green 증거를 남기고 게이트 무회귀를 확인한다 (직접 돌려 기존 732 passed 유지·증가 · ruff·ty exit 0)
- [x] #6 ⛔ 착수 전 설계서 §10 캡틴 질문 4건이 닫혔는지 확인한다 — 열려 있으면 그것부터 올린다
- [ ] #7 코드 리뷰를 받고 CRITICAL·HIGH 0건을 확인한다 — 캡틴 결정 13 이 코드 트랙의 Done 게이트로 살아 있다(결정 22 는 설계·문서만 풀었다). ⛔ 리뷰 전 Done 으로 올리지 않는다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-08 구현 완료 (팀리드). 산출: tests/unit/test_pronunciation_review.py 신설(17건) · review.py 에 _PRONUNCIATION_HISTORY_SQL + 카테고리 분기 · pronunciation.py 에 refresh_review + 두 진입점 배선 · plan.py 문구 교체(§5.6 ①·③·④) · models/analysis.py 에 PRONUNCIATION_CATEGORY 신설.

red→green 증거(직접 돌렸다): ① 첫 실행은 ImportError 로 전체 수집 실패 → refresh_review 실물 구현 후 재실행에 **14건 중 8건 red**(통과 6건은 부재 단정이라 red 로 세지 않았다) ② 이력 쿼리 분기 후 13/14 → 프롬프트 문구 교체 후 14/14 ③ 배선 테스트 2건 추가로 다시 **2건 red** → 두 진입점 호출 후 16/16 ④ §5.6 ④ 유도 테스트 1건 red → 분기 후 17/17.

게이트(직접 돌렸다): **749 passed**(기준선 732 + 신규 17, 회귀 0) · ruff check exit 0 · ruff format --check 33 files · ty check exit 0 · 게이트 밖 ruff **6건**·format **4건** 유지 · 프론트 tsc·eslint 둘 다 exit 0.

설계서와 다르게 한 것 2건(둘 다 단어를 바꾸지 않았다): ① _PRONUNCIATION_NOTE 의 **줄바꿈 위치** — 설계서 판이 인용 제목을 'Due for review\ntoday' 로 쪼갰는데 다른 절 제목을 이름으로 인용하는 규약(_OUTPUT_SPEC + test_plan.py 의 f'"{bare}"' 단정)이 인용을 한 덩어리로 두는 것을 전제한다. ② 카테고리 상수를 models/analysis.py 로 올렸다 — pronunciation.py 가 review.py 를 import 하므로(§5.5) 반대 방향으로 공유할 수 없고, 서비스 각자 갖게 두면 리터럴이 세 번째로 복사된다.

⚠️ 범위로 **넣은** 것: 설계서 §5.6 ④(복습 줄을 발음일 때 'target sound' 로). 설계서가 유도(§9-4)로 남긴 제안이지만 **이 태스크로 살아 있는 문제가 됐다** — 그전에는 발음 패턴이 이 목록에 못 들어왔다. 이제 들어오는데 'target form "an_as_a"' 로 나가면 모델이 소리 키를 연습 문장으로 읽을 여지가 생기고 그것이 1차수 F-2 부류다. 뒤집으려면 그 분기와 테스트 1건만 지운다.

⚠️ 아직 하지 않은 것: **실측 데이터 백필**(recompute_all). 설계서 §4.4 가 an_as_a 에 대해 stage=1 · next_review_at=2026-09-04 13:51:26+00 · review_tasks 1행을 **예측**해 뒀고 그것은 실행 출력이 아니다. 개발 DB 에 돌려 확인하는 것이 남았다 — 공유 DB 쓰기라 캡틴 확인 후에 한다.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
created: 2026-09-07 22:57
---
착수(2026-09-08 팀리드). AC#6 닫는다 — 설계서 §10 캡틴 질문 4건은 결정 25~32 회신에 포함되지 않았다. §10 이 네 질문 전부에 기본값을 명시해 뒀으므로 그 기본값으로 진행한다: Q1 전부 싣는다(범위 밖) · Q2 1·3·7일 · Q3 task_type='rephrase' 겸용(마이그레이션 없음) · Q4 만성 목록에 넣지 않는다. ⚠️ Q2·Q3·Q4 를 뒤집으면 마이그레이션과 발명값이 생기므로 기본값 유지가 되돌리기 쉬운 쪽이다.
---
<!-- COMMENTS:END -->
