---
id: TASK-226
title: '적용: 정리 회차의 동작변경 없는 지적 (재사용 7 · 단순화 6 · 고도 3)'
status: Done
assignee: []
created_date: '2026-09-19 01:02'
updated_date: '2026-09-19 02:58'
labels: []
dependencies: []
ordinal: 287000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
정리 회차(TASK-222)가 받은 지적 가운데 동작변경=아니오 인 것 전부. 지적 전문과 파일·줄 번호는 TASK-222 노트가 정본이다. ⛔ 묶음 배정은 이 태스크 착수 «뒤에» 확정한다 — 스킬이 그렇게 요구하고(미리 만들면 리뷰가 그 경계 밖을 지목할 때마다 묶음을 새로 만들어야 함), 파일이 겹치지 않게만 가른다. ⛔ 적용 전에 codex 의 회귀 위험 8건을 함께 읽는다(TASK-222 노트) — 그 목록이 「정리하다 조용히 깨질 자리」이고 넷은 테스트가 못 잡는다. ⛔ 묶은 자리마다 변이를 한 번 걸어 그 자리를 덮는 테스트가 있는지 잰다. ⚠️ 문면 정본 검사가 SYSTEM_PROMPT 부분 문자열 12건 등으로 많아 프롬프트·주석·예외 메시지를 건드리면 FAIL 하고, 그때 검사를 고치지 않고 내 변경을 고친다.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 동작변경 없는 지적을 파일이 겹치지 않는 묶음으로 적용한다
- [x] #2 묶음마다 변이로 테스트 커버리지를 확인하고 구멍이면 메운다
- [x] #3 수집 개수가 1400 이상이고 게이트 여덟이 통과한다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## TASK-224 가 넘긴 관찰 1건 (2026-09-19)

`session_summary._store` 와 `weekly_report._store` 의 `bool` 반환을 **양쪽 호출부가 모두 버림**(`await _store(...)`). `complete` 가 `LeaseLost` 를 올리게 된 뒤로 그 `bool` 이 알리는 것이 없으므로 `None` 으로 줄일 수 있음. 동작변경 없음.

## 적용 결과 (2026-09-19) — 지적 16건 가운데 14건 적용 · 2건 사용자 승인으로 보류

묶음 일곱으로 갈라 각각 게이트를 돌리고 커밋했음(파일이 겹치지 않게 갈랐음).

| 묶음 | 적용한 것 |
|---|---|
| 1 | JSON 로더 3벌 → `models/plan.loaded_object`(재사용 1) |
| 2 | 실패 문면 15자리 → `jobs.report_exception` · 전사문 부질의 2벌 → `utterances.SESSION_TRANSCRIPT_SUBQUERY`(재사용 2·3) |
| 3 | 「학습자의 오늘」 + naive 거부 4자리 → `models/learner_time`(재사용 4·5) |
| 4 | 발음 UPDATE 2벌 + insert 호출 2벌 → 한 틀(단순화 1) · `partial_failure` → property(단순화 3) |
| 5 | 소켓 조회 4벌 → `_read_or_fallback`(단순화 6) · 세션 삽입 2벌 → `_insert_session`(단순화 7) · 워커 유휴 블록 추출(단순화 5) |
| 6 | RIFF 조립 2벌 → `models/recording.wav_from_pcm`(재사용 6) |
| 7 | `contentEnd` 봉투 3벌(재사용 7) · `toolUseId` 덧칠 제거(고도 5) · `is_openai_model` 선언 좁힘(고도 6) · `scenario_progress` 낡은 주석 |

## 보류 2건 — 사용자 승인(2026-09-19)

- **고도 4**(`daily_summary` 의 `timezone` 을 필수 키워드로): 리뷰는 「호출부 변경 0」이라 적었으나 실측은 다름 — 테스트 여섯 자리가 인자 없이 부르고 그중 `test_daily_summary.py:242` 는 **함수가 스스로 타임존을 조회해 없는 사용자에게 `LookupError` 를 내는 경로**를 잼. 필수로 바꾸면 그 경로와 단정이 사라지고 얻는 것은 조건 네 줄임.
- **단순화 8**(`session._send` 한 홉 제거): 리뷰 자신이 「값이 가장 낮음」으로 적었고, 그 파일은 codex 가 지목한 회귀 위험 넷 가운데 셋이 모인 자리임(pending 경쟁 · `live_sessions.add` 순서 · final 저장 순서).

## 변이로 찾은 구멍 여섯 — 전부 메웠음

1. 「JSON 최상위가 객체가 아니다」 갈래를 **세 파서 모두** 재지 않았음 → 단정 3건
2. 전사문 순서(`order by ut.sequence_no`) → 단정 1건. ⚠️ 첫 판(행 2개)은 판별력이 없었음 — 두 행으로는 `created_at` 오름·내림 가운데 하나가 반드시 겹침. 행 셋과 어긋난 시각으로 고쳤음
3. 소켓의 클립 조회 실패 갈래(소리·계획·무대 셋만 있었음) → 단정 1건
4. 「걷은 묶음을 주기를 기다리지 않고 곧바로 처리한다」 → 단정 1건(주기 5초로 재야 보임)
5. `wav_from_pcm` 이 넘긴 규격을 존중하는지 → 단정 1건. ⛔ **반증 불가였던 변이의 실측**: 두 규격의 값이 같아(16kHz·16bit·mono) 인자를 무시하는 변이가 1414건을 통과했음 — 8kHz 로 굳히니 2건이 실패했음
6. `contentEnd` 의 `promptName` → 단정 1건(빠지면 세션이 응답을 기다린 채 매달리는 조용한 실패)

## 게이트 여덟 (직접 돌림)

`pytest` **수집·통과 1416**(착수 시 1400 · 새 단정 16건 · 줄지 않았음) · `ruff` 0 · `ruff format` **306 files** · `ty` 0 · `tsc` 0 · `eslint` 0 errors/경고 1(기준선) · `next build` `/`=`○` Static.

## 실수 하나를 기록함

변이를 되돌리며 `git checkout <파일>` 을 썼다가 **그 파일의 미커밋 변경(A5·R7)을 지웠음.** 다시 적용해 복구했고 잃은 것은 없음. ⛔ 변이는 스크립트로 넣고 스크립트로 되돌릴 것 — `git checkout` 은 커밋되지 않은 작업까지 함께 지움.
<!-- SECTION:NOTES:END -->
