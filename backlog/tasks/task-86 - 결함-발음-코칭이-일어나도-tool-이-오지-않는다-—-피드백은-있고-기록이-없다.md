---
id: TASK-86
title: '결함: 발음 코칭이 일어나도 tool 이 오지 않는다 — 피드백은 있고 기록이 없다'
status: In Progress
assignee: []
created_date: '2026-09-10 04:29'
updated_date: '2026-09-11 15:57'
labels: []
dependencies: []
ordinal: 89000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
2026-09-10 TASK-75 판정에서 확정했음. 근거는 tests/harness/runs/2026-09-10-task75-rule9-rule11-replacement.md §8.6 임.

규칙 9·4 대체가 실린 판 누적 15회에서 발음 코칭 2건이 났는데 둘 다 toolUse 가 0 임. 즉 학습자는 발음 피드백을 받았는데 pronunciation_attempts 행도 error_patterns 갱신도 없고 복습 시계가 돌지 않음.

⚠️ TASK-78 의 「문장 되말하기 ⟺ tool」 11/11 을 무효로 보지 않음 — 그 회차는 기반 프롬프트 범위였고 지금은 계획 블록과 규칙 9·4 대체가 실린 조건임. 관계의 범위가 좁혀진 것으로 읽음.

⛔ 결정 50 의 순서 ③(우회로 제거)을 이것이 막음. tool 만 믿으면 그 두 코칭마저 사라짐.

첫 확인 지점: 규칙 10 이 「Call report_pronunciation_coaching twice: once with outcome pending right after you have modeled the sentence」로 «문장 시범 직후»를 조건으로 거는데, 관측된 두 코칭이 그 조건을 만족하는지임. 하나는 문장을 시범했고(schwa 회차) 하나는 낱말만 격리했음 — 후자가 조건을 못 만족할 수 있음.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 코칭이 난 회차에서 tool 이 오지 않는 이유를 규칙 10 의 조건으로 설명하거나 반증한다
- [ ] #2 고친 뒤 코칭 회차에서 tool 이 오는 것을 실물 왕복으로 확인한다 — 최소 2회 같은 방향
- [ ] #3 ⛔ 우회로를 걷어내지 않는다 — 이 태스크가 닫히기 전에는 보조 신호가 유일한 기록 경로다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-10 ⛔ 앞 노트의 「살아남는 재료」도 좁힘 — 세션 ohmyenglish-40 이 그것에서 순환을 찾았고 옳음.

내가 「앱 프롬프트 팔의 toolUse 0건이 픽스처를 12종으로 통제해도 유지됨」을 경계 해석에 의존하지 않는 새 표본으로 적었는데, 그 12종(pq*)은 결정 52 가 「규칙 9 가 글자로 배제한 구간」으로 정한 자리임. 즉 그 구간의 0건은 프롬프트와 무관하게 예상되는 값이고, 그러면 프롬프트 탓인지 픽스처 탓인지 구별하지 못함.

⛔ 판별력이 생기는 조건: 같은 픽스처로 기반(스파이크) 프롬프트 팔에서 tool 이 나야 함. 그런데 pq* 로는 그 대조군을 만들 수 없음 — 결정 52 대로면 양쪽 다 0 이 예상됨.

⛔ 그래서 이 회차가 이 태스크에 줄 수 있는 문면은 한 줄뿐임. 인용할 때 그대로 씀:
「pq* 구간(규칙 9 가 배제한 자리)에서 앱 프롬프트 팔의 toolUse 가 0/12 임.」
이것을 「앱 프롬프트가 tool 을 막는다」의 근거로 쓰지 않음 — 순환임.

⇒ 결국 AC#2 를 잴 수 있는 자리는 *k/p2m 구간뿐임. 결정 52 가 이미 그렇게 정했고 이 절이 그 이유를 하나 더 보탬.

⚠️ 이 회차에서 자기 반증이 두 번 났고 기전이 같음 — 결과를 이미 예측하는 조건에서 얻은 관측을 다른 원인의 근거로 쓴 것임. 정본은 tests/harness/runs/2026-09-10-task90-pq-phoneme.md §12.1·§12.3 임.

⚠️ 착수 방법 (2026-09-11 · 팀리드) — 다른 세션이 「프롬프트 파일이 리포에 남아 있지 않아 build_system_prompt 로 조립해야 하고 「제품이 실제로 보내는 것과 같은지」를 정하는 것이 설계 결정」이라며 판단을 넘겼음. ⛔ 그것은 설계 결정이 아니라 구현 방법이고 아래로 닫힌다.

같음을 «보장»하는 방법: 앱과 «같은 함수»를 부른다 — nova.build_system_prompt(인자 6개). 그 함수가 조립 규칙을 소유하므로 같은 인자를 주면 산출물이 같다. 즉 정해야 하는 것은 조립 규칙이 아니라 «인자» 뿐이다.

인자를 제품과 같게 만드는 법: 실제 세션이 쓰는 값을 DB 에서 읽는다. session_plans 가 계획(SessionInstruction)을 담고 SessionScenario·PlanQuestion·known_sounds·drill_count·drill_turns_min 이 그 조립의 나머지다. ⛔ 손으로 지어낸 계획을 넣으면 그때부터 「제품이 보내는 것」이 아니게 되므로 값의 출처를 회차 기록에 반드시 적는다.

⚠️ 그리고 이미 실측된 사실 둘을 전제로 둔다. ⑴ nova.py:245~246 에 따라 인자 넷이 전부 비면 결과가 SYSTEM_PROMPT «그 자체» 이므로 AS4 경로는 스파이크의 --app-prompt 팔과 글자 그대로 같다(TASK-67 정정). 즉 이 태스크가 새로 재야 하는 것은 «계획이 실린» 조건 하나다. ⑵ 다른 세션이 D50/D50k 16회에서 팔 A·B 의 전사문이 문자 단위로 같은 것을 관측했으므로 프롬프트는 ASR 에 영향을 주지 않는다 — 그 축의 대조군은 면제되고, 이 태스크의 대조군은 «계획 유무» 축이다.

⛔ 우회로 제거(결정 50 의 ③) 판정에는 「내용의 질」이 들어가야 한다 — 다른 세션이 tool payload 에서 target_form 이 무너진 전사 그대로이고 target_sound 가 r as r 인 것을 관측했고, 내 P7 이 교정 카드가 청취 문제에 어휘 처방을 내는 것을 관측했다. 즉 tool 이 오게 고쳐도 지금 오는 tool 의 내용이 쓸 수 없다. 그 판정 기준을 이 태스크가 정의해야 한다.

2026-09-11 판정 재료 — **세 갈래 지도와 판별력 계산.** 내가 이 턴에 직접 재서 얻었고 동료 세션(`TASK-98`)에 전달했다.

⛔ **제품 프롬프트에 갈래가 셋이다.** ⑴ AS4(계획 없음 · `build_system_prompt((), None, [], None, …) == SYSTEM_PROMPT`) · ⑵ 계획 있음 + 발음 초점 **없음**(소리 줄이 아예 없어 고정부 규칙 9·11 이 온전히 산다) · ⑶ 계획 있음 + 발음 초점 **있음**(`_SOUND_INSTRUCTION` 이 규칙 9·4 를 대체한다).

⛔ **⑵ 에서 tool 0 을 재는 것은 판별력이 0 이다.** 대체 범위가 소리 줄이 있는 세션으로 한정되므로(`nova.py` `_SOUND_INSTRUCTION` 위 주석 · TASK-75 사용자 승인) ⑵ 는 0 을 예측한다. `runs/2026-09-10-task91-p2-p3-p7.md` §6-3 이 `pq*` 에 대해 적은 순환과 **같은 형태**다.

**⑶ 은 이미 측정됐고 대조군도 있다** — 정본은 `runs/2026-09-10-task75-rule9-rule11-replacement.md` 다(교차 3쌍이 §3·§6·§8.1 에 셋 · 대체 판 누적 15회에 코칭 2건 약 13% · 코칭 2건 모두 `toolUse` 0 · §10 이 이 태스크 첫 시도 8회를 더해 코칭 0·tool 0). ⚠️ **그 파일 머리말은 「AC#1 미충족」에서 멈춰 있고 §8.5 가 「문면상 충족」으로 닫았다** — 머리말만 읽으면 반대로 읽힌다.

**판별력 계산(이 턴 · 기준율 2/15 = 0.133)**: 4회 전부 0 이 될 확률 **0.564** · 8회 **0.318** · P(≥1회 코칭) ≥ 0.95 에 **21회**. ⇒ **팔당 4~8회로는 AC#2 를 판정할 수 없다.**

⛔ **AC#3 의 종단 조건은 ⑶ 을 요구하고 그것은 지금 재생성 가능하다.** 최신 `session_plans` 행 `87a51db9` 는 **2026-09-08 22:58 UTC** 이고 발음에 초점 한 자리를 준 커밋 `4563f15` 는 **2026-09-09 23:35 UTC** 다 — **계획이 그 커밋보다 낡았다.** 발음 패턴 `99fc908f` `pronunciation_an_as_a` 의 `next_review_at` 은 **2026-09-04 13:51 UTC** 로 예정일이 지났고 그 경로는 `9df2369`(TASK-44 · 2026-09-07 23:11 UTC)가 열었다. ⇒ **지금 코드로 계획을 다시 생성하면 ⑶ 이 제품 데이터로 성립한다** — 손으로 지어내지 않아도 된다. 그 회차의 소유는 **`TASK-81` AC#4** 이고 선행 `TASK-75` 가 `Done` 이라 **의존이 풀렸다.**

⚠️ **AC#4 로 사용자에게 올릴 때 두 결함을 갈라 올린다**: 코칭 자체가 13% 라는 것과 **코칭이 나도 tool 이 0** 이라는 것은 다른 층이다. 후자만 고쳐도 학습자는 여섯 번에 한 번 피드백을 받는다.

⚠️ 계획 재생성은 **공유 학습 데이터에 행을 더한다** — 스냅샷(`browser_leg.md` §8-0)이 선행해야 하고, 그 순간부터 다른 세션이 측정한 제품 프롬프트 길이가 낡는다.

2026-09-11 (세션 `ohmyenglish-7f`) — `TASK-105` 회차의 판정 재료를 넘겨받는 자리임. 정본은
`tests/harness/runs/2026-09-11-task105-sound-line.md` §2·§3 임.

측정값: 소리 줄이 실린 제품 프롬프트(5,223자) 팔 **21회** — tool **0/21** · `target_sound` **0/21**.
대조군(스파이크 짧은 프롬프트) **5회** — tool **5/5**. Fisher 양측 `p=0.0000`.
코칭 발화는 21회 «전부» 났으나 **전부 문법 지시**였고 발음 코칭은 0회임
(`Use "an update" or "an action plan"` 형태 · 원문은 회차 기록 §2).

⛔ **그 수치로 AC#2 를 판정하지 않음 — 판별력이 없음.** 계획이 지목한 소리는 `an_as_a` 인데
`p2m.wav` 의 오류는 f→p · r→l · θ→s 이고 두 문장에 «an» 이 없음. 즉 tool 0 은 프롬프트가 아니라
픽스처가 예측한 값임. 그리고 초점이 둘(`pronunciation_an_as_a` · `article_missing_before_noun`)이라
같은 낱말에서 만나 「소리 키를 관사 지시로 읽었는가」도 갈리지 않음.

⇒ AC#2(「고친 뒤 코칭 회차에서 tool 이 오는 것을 실물 왕복으로 확인 — 최소 2회」)는 **여전히
미충족이고 「측정 불가」임**. 목표 소리가 든 픽스처가 선행이고 `TASK-106` 이 그것을 소유함.

⚠️ 결정 54 가 payload 질(`TASK-97`·`TASK-103`)을 결정 50 의 ③ 기준에 넣었으므로, tool 도착률이
좋아져도 ③ 은 `TASK-103` 이 닫히기 전에 열리지 않음 — 동료 세션이 교차 확인해 준 사실임.

2026-09-11 추가 (세션 `ohmyenglish-7f`) — `TASK-106` 이 네 팔로 후보 셋을 반증했음. 정본은
`tests/harness/runs/2026-09-11-task106-matched-sound.md` §7 임.

집계(같은 픽스처 `p2m`→`p2a`): 제품 계열 4변형 **49회 tool 0** · 대조군(스파이크 짧은 프롬프트)
**11회 tool 10**.

확정 하나: 대화의 `an`(관사) 지시는 «계획의 질문 5개»에서 왔음 — 질문을 빼면 사라졌고 초점·목록을
빼도 남았음.
반증 셋: 「소리가 오디오와 안 맞아서」 · 「문법 초점이 이겨서」 · 「놓친 소리 목록의 an_as_a 때문에」.
남은 후보 둘(구별 관측 없음): 규칙 전문 2,339자가 규칙 10 을 묻음 · Nova 가 이 오디오의 θ→s 를
오류로 인지하지 않음(전사가 `with my team` 으로 정상임).

⚠️ PRD §10.1 의 설명(「지시문에 발음 규칙이 없기 때문」)이 이 픽스처에서 반증됐음 — 규칙 8~11 ·
발음 초점 · 소리 줄이 전부 있는데도 코칭이 0 임.

⇒ AC#2 는 여전히 미충족이고 다음 갈래는 **제품 요구사항 판단**임(팀리드가 정하지 않음).
후보 넷을 회차 기록 §7 이 가짐.

2026-09-11 ⛔ 위 2026-09-10 노트의 마지막 문단 「의존 방향을 뒤집었음 — TASK-87(빈도)이 먼저임」은 무효임. 정정을 여기 남김.

무효가 된 이유: 그 노트를 쓴 세션이 18시간 전 원장 상태(TASK-87 = To Do · dep TASK-86)로 판단했음. 그 사이 결정 52 가 TASK-87 을 닫았고(「빈도를 숫자로 정하지 않음 · 낮은 코칭률은 설계값」) 결정 55 가 다시 열 시점을 정했음. 그 세션이 TASK-87 을 Done 에서 In Progress 로 내렸다가 원복했고 이 태스크에 걸었던 dep TASK-87 도 제거했음.

유효하게 남는 것: 회차 기록 tests/harness/runs/2026-09-10-task86-reminder-control.md 의 관측임. 규칙 10 리마인더가 코칭을 줄인다는 가설을 같은 시점 대조군으로 반증했고(계획 통제 시 Fisher 양측 p=1.0), 팔이 넷인 것을 원자료 stem 으로 확정했음. 그 두 가지는 시점과 무관한 관측임.

⚠️ 무효가 된 것과 유효한 것을 갈라 읽어야 함 — 「코칭률이 낮다」는 관측은 맞고 「그래서 빈도를 먼저 올려야 한다」는 추론이 결정 52 로 기각됐음.

2026-09-11 셋째 시도 (세션 `ohmyenglish-7f`) — 결정 56 을 프롬프트에 넣고 재봤고 **바뀌지 않았음**.
정본은 `tests/harness/runs/2026-09-11-task86-priority-line.md` 임.

바꾼 것은 문장 하나임: 소리 줄에 *「This outranks the question list and the sentence-shape target
below: pause the current question, coach the sound, then come back to the question you paused.」* 를
넣었음(결정 56 의 문면을 그대로 옮김 · 수치 없음).

결과: 팔 A6 8회 **tool 0/8** · 코칭 문구도 앞 회차와 그대로(전부 관사 드릴) · 같은 시각 대조군
2회 **tool 2/2**. ⛔ 그 대조는 **한 문장만** 다름(같은 픽스처·소리·초점·놓친 소리 목록·질문 5개)
이므로 성립함 — `D106-hsound` 0/8 대 `D86-prio` 0/8.
⚠️ 「문구가 무효」로 단정하지 않음 — 8회는 낮은 비율을 배제하지 못함(`P(0 in 8 | 0.13)=0.33`).
말할 수 있는 것은 「문구 하나로 높은 비율(≥40%)이 되지는 않았음」임.

⛔ 세 회차가 같은 것을 가리킴: **모델은 계획의 질문 5개를 충실히 수행함.** 그래서 지렛대는 프롬프트
문구가 아니라 **계획 생성**임 — 발음 초점이 선택됐을 때 그 소리를 유발하는 질문을 만들게 하는 것.
그것을 후보 ⑸ 로 사용자에게 올렸음(Slack · 후보 ⑴ 은 위 실측으로 사실상 배제됐음).

⇒ AC#2 는 여전히 미충족이고 다음 갈래는 **사용자 결정**임. 그래서 다시 `Awaiting Decision` 임.

2026-09-11 결정 57 을 받아 Awaiting Decision 을 닫음 (세션 ohmyenglish-7f 후속 · AskUserQuestion 으로 직접 물었음). 사용자가 후보 넷 중 ⑸ 를 골랐음 — 계획 생성이 발음 초점일 때 그 소리를 유발하는 질문을 만들게 함.

정본은 docs/ops/captain-instruction-register.md 의 결정 57 임 — 근거·배제된 후보·열리지 않는 것 셋을 그 자리가 소유하고 여기 옮기지 않음.

이 태스크에 들어오는 제약 셋만 적음.
1. ⛔ toolChoice 강제(후보 ⑶)를 쓰지 않음 — 사용자가 고르지 않았으므로 결정 49·50 의 금지가 그대로임.
2. ⛔ 보조 신호로 축소(후보 ⑷)도 승인되지 않았음 — AC#3 의 「우회로를 걷어내지 않는다」는 그대로 유지임.
3. ⚠️ 후보 ⑵(발음 전용 세션)는 폐기가 아니고 TASK-10 의 자리임 — 이 태스크의 경로가 아닐 뿐임.

⇒ AC#2(코칭 회차에서 tool 이 오는 것을 실물 왕복 최소 2회로 확인)를 계획 생성 경로의 변경으로 만들어야 함. 계획 블록 축은 TASK-81 이 이미 열려 있으므로 그쪽과 범위를 먼저 가름.

2026-09-11 결정 57 의 경로를 실물로 재고 **AC#2 를 닫지 못했음**. 정본은 tests/harness/runs/2026-09-11-task86-sound-shaped-questions.md 임 — 상한·해석 규칙을 돌리기 전에 적었고 그 표대로 읽었음.

구현: plan.py 의 _PRONUNCIATION_FOCUS_RULE 에 두 줄을 더했음(커밋 47c94d1) — 질문을 형태 사용 지시·낭독 과제로 쓰지 말 것과, _OUTPUT_SPEC 의 «Same target form» 을 발음 초점에서는 소리가 고정으로 읽을 것.

⛔ 먼저 확정한 사실 하나: 앞 규칙(「그 소리를 담은 낱말로 질문을 만들어라」 · 00c7d39)은 이미 지켜지고 있었음. 그 뒤 생성된 계획의 질문 다섯이 전부 an 낱말이었고 그런데도 코치가 관사 드릴을 했음. 즉 결함은 낱말이 아니라 질문의 «모양» 이었음.

1단계(Claude 1회) — 통과. 질문이 형태 사용 지시에서 소리 연습으로 바뀌었음(Thursday·Think·three 를 학습자가 스스로 말하게 함). 모델이 문법 초점도 article_missing_the_before_place_noun 로 바꿔 골라 한 문장이 둘을 담게 만들었음.

2단계(Nova) — 팔 A7 유효 7회 · tool 0/7 · 소리 코칭 0/7 · **관사 드릴 0/7(사라졌음)** 대 대조군 2/2. Fisher 양측 p=0.0278. ⚠️ 8회 중 1회는 Bedrock 55초 타임아웃으로 죽어 분모에서 뺐음(H-BF).

⇒ 결정 57 의 «기전» 은 확인됐고 «목적» 에는 닿지 않았음: 질문을 고치면 대화가 바뀌지만 tool 은 오지 않음. ⛔ 0/7 을 「질문 모양 무효」로 단정하지 않음 — P(0 in 7 | 0.13)=0.377 이고 배제되는 것은 높은 비율뿐임(P(0 in 7 | 0.4)=0.028).

누적: 제품·하네스 계열 64회 tool 0 대 대조군 15회 14건. 프롬프트 안에서 바꿔 본 것이 다섯(소리 줄 문구·초점 구성·놓친 소리 목록·질문 제거·질문 모양)이고 전부 0 임.

⛔ 남은 축은 프롬프트 «규모·구성» 하나로 좁혀졌고(TASK-106 §7 후보 ⑴ · 아직 재지 않았음) 그 선택은 사용자 결정 사안임. ⚠️ 후보 ⑵(Nova 가 θ→s 를 인지하지 못한다)는 이 회차가 약화시켰음 — 대조군이 같은 오디오에서 소리를 지목했음.

부산물: 계획 생성이 두 번 다 parse_plan 에서 거부됐고 사유가 서로 달랐음 → TASK-108(deepest recurrence 가 프롬프트에 0건) · TASK-109(모델이 없는 키를 붙임)로 등록했음.

2026-09-11 결정 60 의 프롬프트 구성 실험을 끝냈고 **프롬프트 축이 소진됐음**. 정본은 tests/harness/runs/2026-09-11-task86-prompt-composition.md 임 — 두 팔의 상한과 해석 규칙을 각각 돌리기 전에 적었음.

팔 A8(rule9cut · 규칙 9·11 문면을 실제로 걷음) 6회 → tool 0/6 · 소리 코칭 0/6.
팔 A9(soundfirst · 소리 줄을 맨 앞으로 · 문면 보존) 6회 → tool 0/6 · 소리 코칭 0/6.
대조군 2회 → 2/2 tool 도착 · 발음 지목. A8+A9 합 0/12 대 2/2 Fisher 양측 p=0.011.

⛔ 여기서 처음으로 낮은 비율까지 배제됨 — 제품 규모 계열 누적 **0/76** 이고 P(0 in 76 | 0.05)=0.020 · P(0 in 76 | 0.10)=0.0003 임. 즉 「제품 규모 프롬프트에서 tool 도착률이 5% 이상」이 배제됨. ⚠️ 풀링의 한계: 여섯 회차의 프롬프트가 서로 달라 이 수치는 어느 한 프롬프트의 비율이 아니라 «계열» 의 비율임.

바꿔 본 변수 여섯이 전부 0 임 — 소리 일치 · 초점 구성 · 놓친 소리 목록 · 질문 제거 · 질문 모양 · 규칙 문면 · 위치. 대조군은 같은 오디오·같은 모델·같은 tool 스키마에서 17회에 16건임.

⚠️ 새 사실 하나: A8 에서 규칙 9(Grammar first)를 걷어내자 코치가 발음으로 가지 않고 **계획의 다른 문법 초점**(the + 장소)으로 갔음. 즉 「발음이 밀린다」보다 「발음이 후보 목록에 들어가지 않는다」에 가까움. ⛔ 단정하지 않음 — 그것을 겨냥해 잰 팔이 없음.

⛔ AC#2 는 여전히 닫히지 않음. 남은 것은 제품 경로 선택 셋이고 전부 사용자 결정 사안임(전용 세션 · toolChoice 강제 · 보조 신호로 축소). ⚠️ toolChoice 의 위험은 이 회차가 «키웠음» — 코칭 자체가 0 이므로 강제하면 코칭 없는 턴에 빈 기록이 남음.

2026-09-11 결정 61 의 전용 세션 프로토타입에서 **tool 이 왔음**. 정본은 tests/harness/runs/2026-09-11-task86-dedicated-session.md 임.

팔 P1(제품 문면 그대로 · 1,891자) 4회 → tool 2/4 · 소리 코칭 4/4.
팔 P2(규칙 9 유보를 걷음 · 1,702자) 4회 → tool 4/4 · 소리 코칭 4/4.
대조군 1회 → 1/1.

⛔ 원인은 프롬프트 «규모» 임. 같은 문면이 5,078자에서 0/76 이고 1,891자에서 6/8 임 — Fisher 양측 p=6.9e-8. 짧은 프롬프트가 작동하는 것이 스파이크 «문구» 때문이라는 설명은 배제됐음(제품 문면 그대로도 짧으면 옴).

⛔ payload 가 쓸 수 있음 — target_sound 가 계획이 준 th_as_s 로 정확히 실려 왔고 규칙 10 의 2회 호출 규약(pending → 판정)이 6회 중 5회에서 지켜졌음. ⚠️ 대조군(스파이크 문면)은 5회 중 4회에서 target_sound 가 아예 없고 1회는 지어낸 키(ch_as_sh · t_as_ch)였음 — 즉 제품 문면이 대조군보다 낫고, 없던 것은 규모뿐이었음.

⚠️ 내 판정 도구가 틀렸던 자리를 고쳐 적음 — 앞 두 회차 기록의 target_sound 열은 추출기 정규식이 이스케이프된 따옴표를 놓쳐 만든 값임. 이 회차 값은 payload 를 직접 열어 얻었고, 대조군의 0 은 다시 확인해 맞았음(결론은 안 바뀌지만 근거가 약했음).

⛔ AC#2 를 이 회차로도 닫지 않음 — 기준이 「고친 뒤」이고 이것은 제품이 아니라 프로토타입임. 제품에 짧은 지시문 모드를 만드는 것은 TASK-10(AC10-5)의 자리이고 무엇을 만들지는 결정 61 대로 사용자에게 다시 물음.
⚠️ target_form 에 무너진 전사가 실리는 결함은 그대로임 — TASK-97 이 소유함.
⚠️ 아직 재지 않은 것: 「짧다」의 경계임. 1,891자는 오고 5,078자는 오지 않는데 그 사이를 재지 않았음 — 계획 블록을 얼마까지 실을 수 있는지가 전용 세션 설계의 값임.

2026-09-11 결정 63 의 경계 회차 — **앞 판정을 정정함**. 정본은 tests/harness/runs/2026-09-11-task86-length-boundary.md 임.

되돌려 붙이기 결과: S1(P2 + 규칙 1~7 · 2,911자) tool 0/4 · 소리 코칭 0/4. S2(P2 + 놓친 소리·무대·계획 블록·질문 5개 · 3,681자) tool 1/4 · 코칭 2/4. 대조군 1/1.

⛔ 더 긴 판이 더 잘했음 — 자수와 단조가 아님. 그래서 앞 회차에 적은 「원인은 프롬프트 규모다」를 정정함. 정확한 서술: **발음 코칭을 덮는 것은 일반 대화 규칙 1~7 이고 계획 블록은 덜 덮음. 자수는 그 두 블록의 부수 효과임.**

⚠️ 어느 규칙인지는 가르지 않았음(1~7 을 한 덩어리로 되돌렸음). 정황은 규칙 4(교정 1건/턴 · quote-correct-repeat)를 가리킴 — 앞 회차들에서 코치가 그 한 건을 되말하기에 썼음. 단정하지 않음.

제품 선택에 주는 값 셋:
① 일반 세션 지시문을 줄이는 방향으로는 안 됨 — 규칙 1~7 은 그 세션의 핵심 장치임(한 번에 한 질문 · 교정 예산 · 65% 발화 목표).
② 계획 블록은 발음 세션에 실을 수 있음 — 3,681자 팔에서 코칭 2/4 · tool 1/4 이고 target_sound 가 정확했음.
③ ⇒ 전용 모드가 필요하고 그 모드는 규칙 1~7 을 대체하는 «짧은 대화 규칙» 을 따로 가져야 함. TASK-10 이 그 자리임.

다음에 재야 할 것: 규칙 1~7 을 하나씩 되돌려 어느 규칙이 덮는지 가름(규칙 4 가 첫 후보). ⛔ 이 회차 상한으로는 하지 않았음.

2026-09-11 팔 S3(규칙 4 만 뺌 · 2,644자) 4회 → tool 0/4 · 코칭 1/4. 대조군 1/1.

⛔ 규칙 4 하나가 아님. 그리고 **부분 제거가 새 고장을 만들었음** — 한 회차에서 모델이 자기 절차를 소리 내어 낭독했음(발화 1,175자 · 다른 회차는 246~298자). 원문: 「Wait for the learner to respond, then: If they said ... that's the target sound th_ as_s. After they speak, ... issue the tool call for pending ...」. ⚠️ 규칙 6(메타데이터를 읽지 말라)이 살아 있는데도 났음 — 규칙을 부분적으로 걷으면 남은 규칙이 지키던 경계도 흔들림. ⚠️ 동시에 모델이 tool 규약과 target_sound 를 정확히 이해하고 있다는 증거임(실행하지 않고 설명했음).

덮는 순위(전부 표본 4 · 절대 비율이 아님): 없음 4/4 > 계획 블록 1/4 > 규칙 4 만 뺌 0/4 > 규칙 1~7 전부 0/4.

⇒ 전용 모드 설계 결론 셋(TASK-10 이 받음):
① 규칙 1~7 을 «통째로 대체» 함. 골라서 빼지 않음 — 부분 제거는 tool 을 살리지 못하고 낭독 고장을 더함. 작동이 확인된 형태는 역할 문단 + 규칙 8~11 + 소리 줄(1,702자 · 4/4)임.
② 계획 블록·질문은 실을 수 있음(코칭 2/4 · tool 1/4 · target_sound 정확). 다만 없는 쪽이 더 잘 작동함(4/4 대 1/4).
③ 일반 세션 지시문 축약 방향은 닫힘 — 두 목적을 한 지시문에 담을 수 없음.

오늘 이 세션이 쓴 Nova 47세션(누적 117).

2026-09-12 KST 증거 사슬을 세 조각으로 이었음 — 그런데 **AC#2 는 여전히 열려 있고 남은 고리가 하나임**.

① 제품 함수가 조립한 지시문이 4/4 를 얻은 문면과 **바이트 단위로 같음**: build_pronunciation_prompt('th_as_s') == runs/2026-09-11-task86-dedicated-session/prompt_dedicated_cut.txt (1,702자 · 직접 대조했음). 그 사슬이 조용히 끊기지 않게 **테스트로 고정했음**(test_the_pronunciation_prompt_is_byte_identical_to_the_measured_one) — 변이 검사로 확인했음: 규칙 11 의 「rule 4」 참조를 지우자 그 테스트만 깨졌고 되돌리니 통과했음.
② 통합 테스트가 ?mode=pronunciation → 팩토리 → 어댑터 생성자까지 그 지시문이 도달하는 것을 잼.
③ Nova 실물 왕복이 그 문면에서 tool 6/8 을 냈고 같은 방향 2회 이상임(P2 4/4 · P1 2/4).

⛔ **그래도 AC#2 를 체크하지 않음.** 셋을 이어 붙인 것이고 «한 경로로 끝까지» 돌린 것이 아님 — 실물 Nova 호출이 NovaVoiceAdapter 가 아니라 스파이크 클라이언트로 났음. 결정 50 이 「스파이크만으로 닫지 않는다」를 명시했고 TASK-81 AC#4 도 앱 레그를 요구함.

⚠️ **남은 고리 하나가 지금 막혀 있음**: 앱 레그(브라우저)는 H-BD 로 막힘 — CDP 에서 getUserMedia 가 응답하지 않아 음성 세션이 시작되지 않음. 그 함정의 우회로(서비스 함수로 전사문을 남기는 것)는 «음성 경로 자체를 재는 회차에는 쓸 수 없음» 이라 이 자리에 쓸 수 없음. ⇒ 마이크가 열리는 경로를 확보하는 것이 AC#2 의 선행이고, 그것은 사람이 실제 마이크로 한 세션을 여는 것으로도 됨(결정 38·39 가 그런 세션을 승인한 선례임).

2026-09-12 KST 막힌 고리를 여는 «도구» 를 만들었음 — 아직 돌리지는 않았음.

tests/harness/ws_session.py 에 --mode 와 --wav 를 더했음. 근거: 앱 레그의 유일한 길이던 브라우저가 H-BD(CDP 에서 마이크가 열리지 않음)로 막혀 있는데 /ws/session 프로토콜 자체가 base64 오디오 프레임을 받으므로 **마이크·브라우저 없이** 제품 경로(ws → factory → NovaVoiceAdapter → 실물 Nova)를 지나갈 수 있음.

⛔ 프레임 크기·박자를 스파이크와 같게 뒀음(32ms · 1024B) — 다르게 두면 오디오 도착 속도가 달라져 두 팔의 비교가 깨짐.

**부분 검증만 했음(정직하게 적음)**: 서버 없이 되는 부분을 직접 돌려 확인했음 — slice_frames(['p2m.wav','p2a.wav'], 640) 이 **208프레임 · 212,310B · 재생 6.66초**를 냈고 p2m 단독이 3.04초임(스파이크가 쓰는 픽스처 길이와 맞음). ⛔ **살아 있는 서버에 붙여 돌린 적은 없음** — 그것이 이 도구의 첫 사용이 될 것이고, 그때 확인해야 하는 것 셋: ① ?mode=pronunciation 이 서버 로그에서 전용 지시문을 고르는지 ② tool 이 오는지 ③ pronunciation_attempts·error_patterns·next_review_at 이 실제로 생기는지(TASK-97 AC#3).

⚠️ **공유 dev DB 에 쓰지 않도록** 검증 전용 DB + 별 포트로 띄워야 함(H-BC 의 방법) — 그래서 --no-register 를 함께 넣었음(그 DB 에는 harness_sessions 표가 없음).

게이트: pytest 957 passed · ruff·format·ty exit 0.
<!-- SECTION:NOTES:END -->
