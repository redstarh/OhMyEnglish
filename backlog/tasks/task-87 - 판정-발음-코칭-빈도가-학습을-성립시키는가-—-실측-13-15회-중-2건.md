---
id: TASK-87
title: '판정: 발음 코칭 빈도가 학습을 성립시키는가 — 실측 13% (15회 중 2건)'
status: Done
assignee: []
created_date: '2026-09-10 04:30'
updated_date: '2026-09-11 02:01'
labels: []
dependencies: []
ordinal: 90000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
2026-09-10 TASK-75 판정에서 나온 수치임. 근거는 tests/harness/runs/2026-09-10-task75-rule9-rule11-replacement.md §8.5 임.

규칙 9·4 대체가 실린 판 누적 15회에서 코칭 2건임. 여섯 번 말해서 한 번 발음 피드백을 받는 빈도임.

⛔ TASK-75 의 AC#1 은 「최소 2회 같은 방향」으로 하한만 걸었고 비율을 걸지 않아서 이 문제를 문면이 잡지 못함. 즉 AC 는 충족됐으나 기능은 성립하지 않음 — 그 구별을 이 태스크가 소유함.

⚠️ 이것은 제품 요구사항 판단임. 「발음 코칭이 세션에서 몇 번 일어나야 하는가」에 문서 근거가 없음. 캡틴 결정 없이 목표치를 발명하지 않음.

⚠️ 선행 관계: TASK-86(tool 미도착)이 먼저 닫히는 것이 나음. 기록이 안 되는 상태에서 빈도를 올리면 관측만 늘고 학습 데이터는 여전히 없음.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 목표 빈도를 캡틴 결정으로 받는다 — 발명하지 않는다
- [x] #2 그 목표에 도달하는 방법을 실물 왕복으로 판정한다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-10 사용자 결정으로 닫음 — 결정 52. 정본은 docs/ops/captain-instruction-register.md 임.

물은 것: 「앱 프롬프트 규칙 9 가 「알아들을 만한 발음은 코칭하지 않는다(never for a mild accent)」를 명시하는데 이 방침을 유지할까요?」
답: 「유지 — 낮은 코칭률은 설계값」.

⛔ AC#1(목표 빈도를 결정으로 받는다 · 발명하지 않는다) 충족 — 사용자가 빈도를 숫자로 정하지 않는 쪽을 골랐음. 즉 목표치는 없고 방침이 판정을 대신함. 내가 숫자를 발명하지 않았음.

⛔ AC#2(목표에 도달하는 방법을 실물 왕복으로 판정한다) 충족 — 판정 결과는 「도달할 목표가 없음」임. TASK-90 회차가 「알아들을 만한데 특정 음소가 틀린」 픽스처 12종으로 실물 왕복 14회를 돌려 코칭 0/12 · toolUse 0/12 를 얻었고, 그 12케이스가 전부 규칙 9 가 글자로 배제한 구간이므로 0건이 방침대로임. 앱 경로 1회에서도 pronunciation_attempts 0행임. 정본은 tests/harness/runs/2026-09-10-task90-pq-phoneme.md 임.

⛔ 이 결정이 결정 50 의 순서 ①을 제한함 — 「코칭을 늘린다」로 읽으면 결정 52 를 어김. 결정 50 의 ②(tool 도착률 재측정)를 재는 조건이 좁혀졌음: 다중 턴 + 규칙 9 의 문턱을 넘는 픽스처(*k · p2m)임. 중간 지대 픽스처(pq*)로는 재지 않음.

⚠️ 이 결정이 정당화하지 않는 것: 「전사에 남은 발음 오류를 앱이 다루지 않는 것」. TASK-90 §13 이 앱 경로에서 pile(← file)이 전사에 남았는데 agent 가 회피한 것을 관측했음 — 그 판정의 소유는 TASK-67 쪽임.

2026-09-11 — **사용자 결정 55 를 받았다: 「지금은 보류하고 기록부터」.** 등록부 정본은 `docs/ops/captain-instruction-register.md` 결정 55 다.

물은 것: 「발음 코칭이 15회 중 2건(약 13%) 나는 것이 학습을 성립시키는가」. 함께 올린 사실: 그 **코칭 2건 모두 toolUse 가 0** 이었다(`runs/2026-09-10-task75-rule9-rule11-replacement.md` §8.3·§8.6) — 학습자는 피드백을 받았는데 복습 시계는 돌지 않는다.

⇒ **빈도를 올리는 것보다 「코칭이 났을 때 기록이 남게 하는 것」이 먼저다.**

⛔ **정정 — 이 태스크는 결정 52 로 이미 `Done` 이고 다시 열지 않는다.** 처음에 이 노트를 「열어 두되 뒤로 미룬다」로 적고 `TASK-103` 의존까지 걸었는데, **`Done` 인 태스크가 열린 태스크에 의존하는 것은 그래프 이상**이라 그 의존을 되돌렸다. 결정 55 가 정한 것은 **그 질문을 다시 열 「시점」**이고 그 시점은 기록 축(**TASK-86** · **TASK-103**)이 닫힌 뒤다 — 다시 열 때는 **새 태스크**로 등록한다.
⚠️ **결정 52 를 뒤집지 않는다** — 「빈도를 숫자로 정하지 않는다」는 그대로이고 이 결정은 **순서**만 정했다.
⚠️ **지금 빈도를 다시 재는 회차는 해석 불가다** — 결정 53 이 승인한 21회가 **TASK-100** 에 막혀 있어 빈도 측정 자체가 성립하지 않는다.

2026-09-11 01:59 UTC — **다른 세션이 이 태스크를 `Done` → `In Progress` 로 돌렸고 팀리드가 `Done` 으로 되돌렸다.** 경위를 남긴다.

**관측**: `git diff f8e250a -- <이 파일>` 이 **딱 두 줄**이었다 — `status` 와 `updated_date` 뿐이고 설명·AC 문면은 그대로였다. AC 는 `[x]` **2건 · 미체크 0건**이었다. 같은 시각에 `task-86` 도 수정 중이었다.

**되돌린 근거 둘**: ⑴ **사용자 결정 55 가 「이 태스크를 다시 열지 않는다 · 다시 열 때는 새 태스크로 등록한다」로 정했다** — `Done` 이 그 결정이 정한 상태다. ⑵ **게이트 G2**(In Progress 인데 AC 전부 체크)가 발동해 이 리포의 세션이 턴을 못 닫았다. 훅이 지목한 수정이 `-s Done` 이었고 그것이 ⑴과 같은 방향이다.

⛔ **먼저 알리고 나서 되돌렸다** — 조작한 세션에 선택지 셋(되돌리기 · 미충족 AC 추가 · 새 태스크로 옮기기)을 보내고, 게이트가 차단한 뒤에 그중 결정 55 와 같은 것을 골랐다. **조용히 덮지 않았다.**

⇒ **21회 회차의 빈도 측정을 담을 자리가 필요하면 새 태스크를 만든다.** 이 태스크를 다시 열면 결정 55 를 뒤집는 것이므로 그 사실과 근거를 함께 적어야 한다.

2026-09-11 ⛔ 다른 세션(ohmyenglish-e4)이 이 태스크의 상태를 잘못 건드렸다가 원복했음. 기록으로 남김 — 조용히 덮으면 다음 세션이 경위를 못 봄.

무엇을 했나: status 를 Done → In Progress 로 내리고 AC#1 을 다시 체크했음(이미 [x] 였으므로 무해). dependencies 는 이미 비어 있어 --clear-deps 가 상태를 바꾸지 않았음.

원인: 그 세션이 18시간 전(2026-09-10 16:0x KST) 원장을 조회한 컨텍스트로 일했음. 그 시점 이 태스크는 To Do · dep TASK-86 · AC 0/2 였고, 그 사이 결정 52 와 결정 55 가 쌓여 Done 이 됐음. 그 세션의 커밋(7e3b2a3) 이후 60커밋이 쌓인 것을 git rev-list 로 확인했음.

원복: status 를 Done 으로 되돌렸음. 결정 52(빈도를 숫자로 정하지 않음 · 낮은 코칭률은 설계값)와 결정 55(다시 열 시점은 기록 축이 닫힌 뒤 · 새 태스크로 등록)는 그대로 유효함. 이 태스크를 다시 열지 않음.

⚠️ 그 세션이 사용자에게 목표 빈도를 다시 물었고 답을 받았음 — 결정 52 와 방향이 다를 수 있어 그 세션이 사용자에게 관계를 확인함. 이 노트는 그 확인 결과를 담지 않음.
<!-- SECTION:NOTES:END -->
