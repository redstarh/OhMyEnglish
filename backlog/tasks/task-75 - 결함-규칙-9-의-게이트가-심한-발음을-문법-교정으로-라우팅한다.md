---
id: TASK-75
title: '결함: 규칙 9 의 게이트가 심한 발음을 문법 교정으로 라우팅한다'
status: In Progress
assignee: []
created_date: '2026-09-09 16:11'
updated_date: '2026-09-10 02:44'
labels: []
dependencies: []
ordinal: 78000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
2026-09-09 TASK-67 조사에서 양성 대조 기준 단일 변수로 확정했음(n=2). 근거는 tests/harness/runs/2026-09-09-task67-tool-call-investigation.md §2 C1 임.

작동하는 프롬프트에 규칙 9 의 게이트 문장만(+198자) 더하니 발화가 「Let's make sure the grammar is correct. The sentence should be ...」로 바뀌었음. 두 회차의 발화 문구가 동일해 간헐이 아님. 쓴 픽스처는 p1k.wav 로 ASR 이 한글로 전사할 만큼 심한 발음임 — 규칙 9 자신이 「take it up」 하라고 지정한 구간인데 모델이 문법으로 라우팅함.

⛔ 이것은 tool 호출과 별개 문제임. 결정 49 가 tool 강제를 버렸으므로 tool 은 이 태스크의 관심이 아님. 관심은 학습자가 듣는 것임 — 심하게 잘못 발음했는데 문법 교정을 받음.

⚠️ 확률적임. 기준선·H1·H3 회차는 코칭을 발화했고 H4 두 회차는 문법으로 갔음. 즉 게이트가 라우팅을 「항상」 뒤집는 것이 아니라 뒤집을 수 있음.

⛔ 게이트를 그냥 느슨하게 하지 않음 — 조사 중에 시도했고 더 나빠졌음. 규칙 9·10 을 함께 재작성한 수정 후보 회차에서 모델이 「That was a clear and natural sentence.」로 발음에 문제가 없다고 판정했음. 즉 「never for a mild accent」를 앞세우면 분기 진입이 더 줄어듦.

문구 변경은 실물 왕복으로만 판정됨 — 단위 테스트로는 이 거동을 잴 수 없음.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 심한 발음(p1k 류)에서 문법이 아니라 발음을 다루는 것을 실물 왕복으로 확인한다 — 같은 픽스처로 최소 2회 같은 방향이어야 한다
- [x] #2 느슨하게 하는 방향으로 가지 않는다 — 분기 진입이 줄어드는 것을 함께 잰다(문제 없다고 판정하는 회차가 늘지 않는지)
- [x] #3 규칙 4·11 의 one-per-turn 계약을 깨지 않는다 — 발음 교정이 그 턴의 한 교정임을 유지한다
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
2026-09-10 착수 — ⛔ 이 태스크의 범위가 제목보다 넓다.

제목이 규칙 9 만 지목하는데 **규칙 9 와 11 이 겹치지 않는 두 경로로 발음을 막는다**. TASK-81 이 그것을 실물로 확정했다(왕복 28회 · 조건 넷 전부 0 · 계획 블록이 없는 기준 팔도 0 — runs/2026-09-10-task81-pronunciation-focus.md).
- 규칙 9: 문법 교정이 «없는» 턴에서도 leave pronunciation alone 으로 막는다. 내 층①+② 팔 8회와 탐침 2회가 교정 0건인 턴이었고 예산이 남아 있었는데도 다루지 않았다.
- 규칙 11: 문법 교정이 «있는» 턴에서 상호배제로 막는다. 기준 팔에서 시제 교정을 하나 한 회차가 그것이다.
⛔ 하나만 풀면 다른 하나가 남는다.

사용자 승인 안(2026-09-10): **계획이 발음 초점을 지정한 세션의 어긋난 턴에서만 완화하고 one-per-turn 예산은 유지한 채 발음이 그 자리를 차지하게 순서만 바꾼다.**

구현: 고정부를 건드리지 않고 계획 블록의 소리 줄(_SOUND_INSTRUCTION)이 규칙 9 와 규칙 4 를 명시로 대체한다. 이 리포의 기존 관례를 그대로 쓴 것이다 — 힌트 시점과 목표 수준이 이미 같은 방식으로 고정 규칙을 대체하고, build_system_prompt docstring 이 「같은 축을 말하는 줄은 대체를 문장으로 적는다」를 규약으로 갖는다.
⚠️ 그 docstring 의 「대체하는 축은 둘뿐이다」를 「셋이다」로 고쳤다. 그 문단이 「새 축이 대체 쪽이면 위 두 이름을 고쳐야 한다」고 스스로 적어 뒀고 그대로 따랐다. 그 문단이 「늘어나려면 캡틴 결정을 요구한다」고 한 것도 이번에 실제로 그 순서를 지켜 검증됐다.
⛔ 대체 범위가 소리 줄이 있는 세션으로 한정된다 — 계획이 발음 초점을 지정하지 않으면 줄이 아예 없고 규칙 9·11 이 그대로다. 조건을 프롬프트가 아니라 계획 데이터가 정한다.

AC#3(one-per-turn 계약 유지)을 문면과 테스트로 지켰다 — 예산을 늘리는 문구(two corrections 류)가 없어야 한다는 음성 단정을 테스트에 넣었다.

게이트: pytest 898 passed · ruff check exit 0 · format 34 files already formatted · ty check 통과.

2026-09-10 실물 판정 — ⛔ AC#1 미충족이다. 태스크를 열어 둔다.

정본은 tests/harness/runs/2026-09-10-task75-rule9-rule11-replacement.md 다.

단일 변수 대조를 만들었다 — 두 프롬프트의 diff 가 소리 줄 한 줄이고 그 줄에 「instead of the Grammar first rule 9」와 「spend the one correction of rule 4」가 더해진 것이 전부다. 나머지는 바이트 동일하다. 픽스처는 p2k 를 첫 발화로 줘 심한 발음 조건을 만들었다(AC#1 이 요구한 p1k 류).

결과: 교차 3쌍에서 **양쪽 0**이다. 이전 판도 새 판도 발음을 다루지 않고 toolUse 도 0이다. 양쪽이 같은 방향으로 갔다 — 한글 전사를 받고 「더 짧고 쉬운 문장으로」로 라우팅한다.

✅ AC#2 충족 — 「문제 없다」 판정이 늘지 않았다. 이전 판은 Great! 로 시작해 단축을 제안한 회차가 3이고, 새 판은 Great! 1 에 「전달이 안 됐다는 인식」이 2 다. 즉 느슨해져 분기 진입이 줄어드는 전례를 반복하지 않았다.
✅ AC#3 충족 — 예산을 늘리지 않았다. 규칙 11 의 「같은 턴에 얹지 마라」가 그대로 살고 발음이 그 한 자리를 차지할 뿐이다. 예산을 늘리는 문구가 없어야 한다는 음성 단정을 테스트에 넣었다.

⛔ 층이 하나 더 있다 — 문장 단순화 라우팅이다. 규칙 9 의 예외 조건이 성립할 조건인데도 발음을 다루는 대신 과제를 낮춘다. 후보 셋을 적었고 어느 것도 재지 않았다: 규칙 1(A2-B1 수준과 턴 길이) · 규칙 5(막히면 시작 힌트 — 한글 전사를 「막혔다」로 읽으면 정확히 그 행동을 명령한다) · 계획의 sentence_length. 2번이 가장 그럴듯해 보이지만 정황이다.

⛔ 이 변경을 되돌리지 않는 근거 셋: AC#2 를 위반하지 않았다 · 규칙 9·11 이 막는다는 것은 TASK-81 이 왕복 28회로 실측한 사실이라 대체 문장은 어느 안을 고르든 필요하다 · 지금 판이 단일 변수 대조의 기준선이 되므로 다음 층을 잴 때 대조군으로 쓸 수 있다.
⚠️ 그러나 AC#1 은 체크하지 않는다 — 「구현했다」가 「충족했다」가 아니다.

다음 세션이 할 것: 후보 셋 가운데 규칙 5 를 먼저 잰다. 같은 단일 변수 방식으로 그 규칙 문장만 빼거나 조건을 좁혀 3쌍을 돌리면 된다. 지금 판(prompt-after-75)이 대조군이다.
<!-- SECTION:NOTES:END -->
