# TASKS — 절 제목과 포인터만 남았다

> ⛔ **이 파일은 더 이상 원장이 아니다.** 무엇이 남았는지·누가 소유하는지는 **Backlog.md**가
> 정본이다 — `backlog task list --plain` / `backlog board`로 읽는다. 조작 규약은
> `~/.claude/rules/task-management.md`.
>
> **그런데 왜 남아 있나**: 코드 주석·테스트·설계서가 이 파일의 **절 제목**으로 근거를 인용한다
> (예: `TASKS.md I-1` · `TASKS.md A-2 후단 ①` · `TASKS.md C절`). 파일을 지우면 그 인용이 전부
> 끊긴다. 그래서 **절 제목과 「내용이 어디로 갔다」는 포인터만** 남겼다 —
> `TASK-28`(원장 마이그레이션 마감, 2026-09-08).
>
> **내용의 정본**: **`docs/design/2026-09-08-tasks-md-archive.md`**(아래에서 **보관본**이라 부른다).
> 사후 기록·판정 근거·캡틴 결정 원문을 절 구조 그대로 담고 있다. 여기서 한 칸 더 들어가면 거기다.
>
> **「상태 기호」 절은 지웠다** — `✅`·`⏭`·`⏸` 같은 표기는 도구의 `statuses` 설정이 소유한다
> (`task-management.md` §2). 브랜치 `design/first-vertical-slice`

---

## A. 요구사항 v1.1 §10 — 발음 시범·재발화

계획: `docs/design/2026-08-27-pronunciation-echo-plan.md`(⚠️ 머리말의 「구현 후 정정」 표를 먼저
읽어라) · 설계: `docs/design/2026-08-27-pronunciation-echo-design.md`(계약의 정본 §3.1a·§3.2·§6.1)

**Task 1~8은 완료됐다** — 커밋·검증 기록은 보관본 A절이 소유한다. **Task 9**(하네스 5차수
P9~P12)는 **`TASK-37`**이다.

### A-1. Task 7 착수 전 필수 3건 — ✅ 전건 해소 (`21ebf80` + `43ab5f7`)

→ 보관본 A절. 요지: ① `record_attempt`가 `async with conn.transaction():`을 직접 연다(`resolve_dangling`도
같다) ② 적용 조건은 `link_pattern`의 SQL `where`에 둔다 ③ `target_form`은 **정규화한 `target_sound`**다
— ③은 뒤집힌 결정이고 **문장으로 되돌리지 마라**(근거: `docs/database-schema.md:120`).

### A-2. Task 8 착수 전 캡틴 결정 — 판정↔시범 짝짓기에 상관키가 없다

→ 보관본 A절. 캡틴 결정 2건: **(b) 최신순 수용**(2026-08-29, 근거의 정본은 설계서 §8 D5-5) ·
**발음 카드는 기계 키를 표시하지 않는다**(2026-08-30, 설계서 §10 미결 4).
**후단 ①** = 응답에 `signal_source`를 실어 신호 행을 따로 렌더한다(신호 행의 `target_form`은
문장이 아니라 설명 문구다) · **후단 ②** = `incorrect` + `spoken_form` null은 대답 없이 끝난 시도다.
둘 다 `9b7985f`로 해소.

### A-3. 실물 Nova 왕복 — ✅ 1회 완료 (2026-09-01), tool 스키마 대조만 남았다

→ 보관본 A절이 스파이크↔앱 스키마 차이 표를 소유한다. 회차 기록은
`tests/harness/runs/2026-09-01-mic-1.md`. **남은 대조는 `TASK-37`**(AC 7).

### A-4. 의도적 미충족 (요구사항 대비)

→ 보관본 A절. R10-4 `agent_reprompt`는 캡틴 결정으로 만들지 않는다 → 사각의 크기 판정은
**`TASK-24`** · R10-5·R10-8 교정 예산은 프롬프트로만(설계서 §8 D5-1, 결정된 축소).

### A-5. 세션 화면 발음 배지 — ✅ 완료 (2026-09-01, 실물 마이크로 확인)

→ 보관본 A절. ⚠️ **닫힌 태스크의 접힌 역사 서술이다** — 배지 미구현 시절의 착수 안내와 검증
경로 기각 근거를 상시 정책으로 확장하지 마라(`docs/design/2026-09-05-frontend-test-agent-plan.md` §4).

---

## B. 캡틴 결정 — **전건 종결 (2026-08-30)**

→ 전문(B-1~B-10, 근거 원문 보존)은 **`docs/design/2026-08-30-captain-decisions-phase1.md`**가
소유한다. 코드가 인용하는 **B-4**(발음 키 값역)·**B-10**(`frequency` 이중 writer)도 그 문서에 있다.

---

## G. 결정에서 생긴 후속 작업 (2026-08-30 신설)

→ 보관본 G절이 G-1~G-8 표와 **G-8 해석의 근거**를 소유한다(캡틴 문구는 세는 방식을 구속하고
저장 구조를 지정하지 않는다 → **(나) 경로를 나눈다**). **G-4**는 실물 마이크 1회이고 완료됐다
(2026-09-01, 기록은 A-3·A-5·I절). 열려 있는 것은 **G-6 대조 하나 = `TASK-47`**이다.
G-5는 소진됐다 — 근거는 보관본의 「원장에 등록하지 않은 이연」 표.

---

## C. 요구사항 v1.1 §11 — 이력 기반 추천 학습 루틴

설계: `docs/design/2026-08-25-learning-coach-agent-design.md`(정본 승격) · 계획: 슬라이스 1
`docs/design/2026-09-03-learning-coach-slice1-plan.md` · 슬라이스 2
`docs/design/2026-09-04-learning-coach-slice2-plan.md` · 실행 판단은
`docs/design/2026-09-05-slice2-execution-log.md`.

→ 보관본 C절이 아래 전부를 소유한다: **슬라이스 2 태스크 원장**(S2-1~S2-12 · S2-L1 · S2-L2) ·
**S2-6 고침 4라운드의 계단 표와 교훈 3개** · **S2-11 결과와 재검증 3통** · **슬라이스 1 코드 리뷰
결과와 이연 4건** · **슬라이스 2 착수 전 확인 4건** · **캡틴 결정 2건(2026-09-03)**.

**열려 있는 것은 S2-12 하나 = `TASK-48`**(문서 마감 + AS11 신설). S2-L1·S2-L2는 `TASK-15`·`TASK-16`
으로 닫혔다. **이 슬라이스의 지배 실패 모드는 「통과하지만 이유가 틀린 테스트」**이고 판별법은
`tests/unit/test_plan.py` 머리말이 소유한다.

### ✅ S2-8 결과 (2026-09-05) — APPROVE · 고침 2라운드

→ 보관본 C절. ⚠️ **의도적으로 막지 않은 경계 2개를 버그로 재조사하지 말 것**(근거: 설계서 §3.3이
여럿일 때 어느 것인지 정하지 않는다). 상세는 `tests/unit/test_sessions.py`의 두 시나리오 테스트 주석.

### ✅ S2-9 결과 (2026-09-05) — APPROVE · 고침 1라운드

→ 보관본 C절. ⚠️ **M-6**(안 재는 범위)과 **M-2**(폐기된 서술 — 위치 인자 2개를 넘기는 호출은 0건)를
재조사하지 말 것.

### ✅ S2-10 결과 (2026-09-05) — APPROVE · 고침 2라운드

→ 보관본 C절. ⚠️ **그대로 열려 있는 6건은 리뷰어가 「차단 아님」으로 판정했다 — 버그로 재조사하지
말 것.** 지시문 축 모순의 축별 처분은 `docs/design/2026-09-05-slice2-execution-log.md` §5 판단 41이
소유한다. **규칙 번호가 밀렸다**: 발음 규칙은 7~10 → **8~11**(규칙 4는 밀리지 않았다).

---

## D. 테스트 하네스 — 차수 원장

→ 차수의 정본은 **`tests/harness/runs/ROUNDS.md`**다. 1~4차수 기록과 5차수 범위는 보관본 D절이
소유하고, **5차수 실행은 `TASK-37`**이다(반드시 포함할 관측 3건 A-3·A-4·B-2와 일부러 빼는
N9·N10을 그 태스크가 담고 있다).

---

## E. 비차단 후속

→ 보관본 E절이 원문을 소유한다. 열린 것은 원장으로 갔다: **`TASK-37`**(`tests/**` ruff·format ·
음성 픽스처 · O-1 선행 개행) · **`TASK-38`**(AWS 키 로테이션) · **`TASK-39`**(botocore 재시도) ·
**`TASK-40`**(추적 체크리스트 HTML) · **`TASK-41`**(`public` → 전용 스키마). 등록하지 않은 것과
그 이유(WS origin 미검증 수용 · 이연 minor triage 완료)는 보관본의 「원장에 등록하지 않은 이연」 표.

---

## H. En-Coach와 DB 공유 — ✅ 구성 완료 (2026-08-30)

→ 규칙·접속의 정본은 **`docs/ops/shared-database-naming-rules.md`** · 배경·구축·실측은
**`docs/ops/shared-database-guide.md`**(§4.2-1이 이 절의 실측값을 가졌다). 비대칭 해소는
**`TASK-41`**, 평문 비밀번호로 GitHub 공개 push가 막힌 건은 **`TASK-42`**(캡틴 결정 대기).
F절(전역 규약 정리)은 범위가 `~/.claude/**`라 삭제했다 — 조치는 그쪽에 완료돼 있다.

---

## I. 실물 마이크 1회(G-4)에서 나온 결함 — 2026-09-01 신설

세션 `bbfc3908-639c-4f1e-a565-fdbd74e0a420`(3분 28초, `completed`). 값은 전부 그 세션의 DB를
직접 조회해 얻었다. **I-1~I-8의 구현·리뷰·실측 기록은 보관본 I절이 소유한다.**

### I-1. 발화 종료 감지가 이르면 문장이 쪼개지고, 그 조각이 오탐 패턴을 만든다 — ✅ 구현 완료 (2026-09-01)

→ 보관본 I절. 캡틴 결정 **(가) 턴 경계까지 모아서 분석한다**(화면 표시는 그대로, 분석 입력만 합친다).
함정은 `docs/ops/pitfalls.md` **H-U**. ⚠️ **철회된 엣지 1건을 되살리지 마라** — `DRAIN_TIMEOUT`
초과 시 저장이 비행 중이라는 전제가 틀렸고, 함께 기각한 완화안(**스윕에 시간 창을 넣는 것**)은
없는 위험을 막으려고 실재하는 버그를 들여온다. `partition by u.session_id`·`order by u.sequence_no`를
지우지 마라(경고는 코드 옆에 있다).

### I-2. `target_sound` 값역이 계획과 다르다 — 관측 3건 (설계 재검토 필요)

→ 보관본 I절. 관측된 키는 `am_as_i_m`·`w_as_vw`·`an_as_a`이고 **음소 치환이 하나도 없다.**
표본을 늘려 판정하는 것은 **`TASK-13`**이다.

### I-3. `awscrt` teardown Traceback — 무해, 기록만

→ **`docs/ops/pitfalls.md` H-AK**가 소유한다.

### I-4. `active` 고아 세션 리퍼 — ✅ 완료 (2026-09-02 신설, 2026-09-03 구현)

→ 보관본 I절. 캡틴 결정 2건: **1분 이상 무활동이면 `failed` + live 가드 병행**(2026-09-03) ·
**`end_session`은 `active`인 세션만 닫는다**(리퍼 판정을 덮지 못하게). ⚠️ 리퍼가 닫은 세션의
결과 API는 `connection_failed`가 된다 — 회귀로 오인하지 마라(`services/sessions.py`가 이 경고를 갖는다).

### I-5. `{ "interrupted" : true }`가 agent 전사문으로 저장된다 — ✅ 완료 (2026-09-03)

→ 보관본 I절. 조치 `adb9e8d` — `_is_control_payload`가 **JSON 객체로 파싱되는지**만 본다(문자열
상수로 박지 않았고 포함 검사도 하지 않았다).

### I-6. agent 한 턴이 여러 `utterances` 행으로 저장된다 — ✅ 완료 (2026-09-03)

→ 보관본 I절. 증거는 `tests/harness/runs/2026-09-03-nova-turn-events.log`. **(가)로 구현**(`adb9e8d`) —
`contentEnd(END_TURN)`에서 flush하고 뒤따르는 중복 FINAL을 버린다. `completionEnd` 경로는 남겼다.
⚠️ **`sequence_no`를 재계산하지 마라**(전사문 인용이 깨진다).

### I-7. 튜터가 기다리지 않는다 — ✅ 조치 + 마이크 1회 PASS (2026-09-03)

→ 보관본 I절. 조치 2단(`d07a7c7`) — 규칙 2에 대기 지시, 규칙 4에 "새 질문과 교정을 한 턴에 묶지
마라". ⚠️ **「1회 관측에서 지켜졌다」로만 적는다** — LLM 준수는 확률적이라 해결로 단정하지 않는다.

### I-8. 조각 발화가 화면에 두 줄로 보인다 — ✅ 완료 (2026-09-03)

→ 보관본 I절. 결함이 아니라 I-1이 명시적으로 남긴 선택이었다. 캡틴 결정 **(가) 표시 계층에서
합친다**(`21e9d1b`) — 공백 하나로 잇는 규약을 백엔드 `string_agg`와 같게 해서 화면이 분석 묶음과
일치한다. 시간 간격 임계는 두지 않았다.

---

## 관련 문서 지도

→ **`README.md`** §관련 문서 지도가 최신판을 소유한다. 「무엇이 남았나」 행은 이제 이 파일이 아니라
`backlog board` · `backlog task list --plain`을 가리킨다.

---

## J. 프론트엔드 검증 자산 — 계획 완료 (2026-09-05 신설)

계획서: **`docs/design/2026-09-05-frontend-test-agent-plan.md`**(⚠️ 머리말의 「리뷰 상태」를 먼저
읽어라) · 절차의 정본: **`tests/harness/browser_leg.md`**.

→ **critic 지적 원문(N-1~N-4 · M-a~M-c)과 캡틴 결정 5건은 보관본 J절이 소유한다.** 계획서 §15가
어디를 어떻게 고쳤는지의 대응 표를 갖는다. T1~T6은 **`TASK-17`~`TASK-22`**로 닫혔다.

⚠️ **가장 값어치 있는 발견 — 검증 도구가 통과한 뒤에 썩었다.** 1차수(`tests/harness/runs/2026-08-26-run-1.md`)가
프론트 C1~C4를 전건 PASS로 기록했지만 그 뒤 앱이 두 번 리팩터돼 근거 토큰이 주석에만 남았다.
**신호는 있었고**(같은 run-1의 `F-1`) 안 된 것은 **그 수정에 맞춰 계측을 갱신하는 일**이다 →
처방은 계측을 게이트에 넣는 것이 아니라 **프론트 결함 수정의 완료 조건에 「계측 대조」를 붙이는 것**이다.

---

_2026-09-08 — `TASK-28`로 분류를 마감했다. 이 파일에서 옮긴 내용의 정본은
`docs/design/2026-09-08-tasks-md-archive.md`이고, 상태의 정본은 Backlog.md다._
